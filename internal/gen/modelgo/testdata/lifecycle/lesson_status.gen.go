// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"strings"
)

// Lifecycle state of a lesson.
type LessonStatus string

// The values of LessonStatus.
const (
	// Created but not yet started.
	LessonStatusPlanned LessonStatus = "planned"
	// Currently being taught.
	LessonStatusInProgress LessonStatus = "in_progress"
	// Finished; grades may now be entered.
	LessonStatusCompleted LessonStatus = "completed"
)

// AllLessonStatus is every value, in declaration order.
var AllLessonStatus = []LessonStatus{LessonStatusPlanned, LessonStatusInProgress, LessonStatusCompleted}

// Valid reports whether the value is one the database will accept.
func (v LessonStatus) Valid() bool {
	switch v {
	case LessonStatusPlanned, LessonStatusInProgress, LessonStatusCompleted:
		return true
	default:
		return false
	}
}

// ParseLessonStatus reads a value, accepting any casing and surrounding space.
//
// Normalization uses it, so "IN_PROGRESS" from one client and "in_progress"
// from another mean the same thing rather than one of them being a validation
// failure nobody can explain. The spelling still has to be the label’s: a
// value is a name the database knows, not a phrase.
func ParseLessonStatus(s string) (LessonStatus, bool) {
	switch strings.ToLower(strings.TrimSpace(s)) {
	case "planned":
		return LessonStatusPlanned, true
	case "in_progress":
		return LessonStatusInProgress, true
	case "completed":
		return LessonStatusCompleted, true
	default:
		return "", false
	}
}

// String implements fmt.Stringer.
func (v LessonStatus) String() string { return string(v) }
