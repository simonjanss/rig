// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { RigFile, RigFileListResponse } from "./rig_file.gen.js";
import type {
    RigFileCreateFields,
    RigFileCreateInput,
    RigFileListDeletedQuery,
    RigFileListQuery,
    RigFileSearchQuery,
    RigFileUpdateFields,
    RigFileUpdateInput,
} from "./rig_file_input.gen.js";
import type { RigFileFilter } from "./rig_file_query.gen.js";
import type { CallOptions, RigError, Runtime } from "@rig/client";

import {
    METHOD_QUERY,
    isInvalid,
    pathValue,
    send,
    sendNoContent,
    setParam,
} from "@rig/client";

/**
 * RigFileClient is what `client.rigFiles` is, and what a test writes to stand
 * in for it.
 *
 * The documentation for each call is here rather than on the object below,
 * because this is the type the property resolves to and so this is what an
 * editor shows.
 */
export interface RigFileClient {
    /**
     * List RigFiles.
     *
     * GET /api/v1/rig-files
     *
     * Operation listRigFiles.
     */
    list(query?: RigFileListQuery, options?: CallOptions): Promise<RigFileListResponse>;

    /**
     * Create a RigFile.
     *
     * POST /api/v1/rig-files
     *
     * Operation createRigFile.
     *
     * A refusal is read back with `isRigFileCreateError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    create(input: RigFileCreateInput, options?: CallOptions): Promise<RigFile>;

    /**
     * Search RigFiles with filters.
     *
     * QUERY /api/v1/rig-files, falling back to POST /api/v1/rig-files/_search
     * when something between here and the server refuses the method. The
     * fallback is remembered, so it is tried once.
     *
     * Operation searchRigFiles.
     */
    search(filter: RigFileFilter, query?: RigFileSearchQuery, options?: CallOptions): Promise<RigFileListResponse>;

    /**
     * A deletion stamps the row rather than removing it, so this is what was
     * deleted and can still be brought back. Only rows inside the 7-day restore
     * window appear: past it a row is gone as far as anyone is concerned, so it
     * is not in the trash either.
     *
     * GET /api/v1/rig-files/_deleted
     *
     * Operation listDeletedRigFiles.
     */
    listDeleted(query?: RigFileListDeletedQuery, options?: CallOptions): Promise<RigFileListResponse>;

    /**
     * The row is retired by stamping a deletion time; it stops appearing in
     * reads but can be restored within the retention window.
     *
     * DELETE /api/v1/rig-files/{id}
     *
     * Operation deleteRigFile.
     */
    delete(iD: string, options?: CallOptions): Promise<void>;

    /**
     * Fetch one RigFile by identifier.
     *
     * GET /api/v1/rig-files/{id}
     *
     * Operation getRigFile.
     */
    get(iD: string, options?: CallOptions): Promise<RigFile>;

    /**
     * Only the fields present in the body are changed. A field set to null is
     * cleared; a field left out is left alone.
     *
     * PATCH /api/v1/rig-files/{id}
     *
     * Operation updateRigFile.
     *
     * A refusal is read back with `isRigFileUpdateError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    update(iD: string, input: RigFileUpdateInput, options?: CallOptions): Promise<RigFile>;

    /**
     * The deletion stamp is cleared and the row appears in reads again. It
     * works for 7 days after the deletion; past that the row is no longer
     * eligible and this answers 409.
     *
     * Restoring a row that was never deleted is not an error. It is already in
     * the state the caller asked for, and answering otherwise would make a
     * retry of a request whose response went missing look like a failure.
     *
     * The request carries no fields. What to do when the world has moved on
     * while the row was retired — a unique value taken by something created
     * since — is the application's decision, and it is made in the restore
     * hook: refuse, or change the row so that it fits.
     *
     * Every rule then runs against whatever the hook settled on, not only the
     * ones for fields it touched. The row was not live, so nothing about it has
     * been checked against the world it is returning to.
     *
     * POST /api/v1/rig-files/{id}/_restore
     *
     * Operation restoreRigFile.
     */
    restore(iD: string, options?: CallOptions): Promise<RigFile>;
}

/**
 * Builds the RigFile half of a client.
 *
 * Called by `createClient` and not usually by anything else: a resource reached
 * through the client it belongs to shares that client's credential, and one
 * built here would not.
 */
export function createRigFileClient(rt: Runtime): RigFileClient {
    return {
        list(query: RigFileListQuery = {}, options?: CallOptions): Promise<RigFileListResponse> {
            const search = new URLSearchParams();
            setParam(search, "limit", query.limit);
            setParam(search, "offset", query.offset);

            return send<RigFileListResponse>(rt, {
                name: "listRigFiles",
                method: "GET",
                path: "/rig-files",
                query: search,
            }, options);
        },

        create(input: RigFileCreateInput, options?: CallOptions): Promise<RigFile> {
            return send<RigFile>(rt, {
                name: "createRigFile",
                method: "POST",
                path: "/rig-files",
                body: input,
            }, options);
        },

        search(filter: RigFileFilter, query: RigFileSearchQuery = {}, options?: CallOptions): Promise<RigFileListResponse> {
            const search = new URLSearchParams();
            setParam(search, "limit", query.limit);
            setParam(search, "offset", query.offset);

            return send<RigFileListResponse>(rt, {
                name: "searchRigFiles",
                method: METHOD_QUERY,
                path: "/rig-files",
                query: search,
                body: { filter: filter },
                fallback: "/rig-files/_search",
            }, options);
        },

        listDeleted(query: RigFileListDeletedQuery = {}, options?: CallOptions): Promise<RigFileListResponse> {
            const search = new URLSearchParams();
            setParam(search, "limit", query.limit);
            setParam(search, "offset", query.offset);

            return send<RigFileListResponse>(rt, {
                name: "listDeletedRigFiles",
                method: "GET",
                path: "/rig-files/_deleted",
                query: search,
            }, options);
        },

        delete(iD: string, options?: CallOptions): Promise<void> {
            return sendNoContent(rt, {
                name: "deleteRigFile",
                method: "DELETE",
                path: `/rig-files/${pathValue(iD)}`,
            }, options);
        },

        get(iD: string, options?: CallOptions): Promise<RigFile> {
            return send<RigFile>(rt, {
                name: "getRigFile",
                method: "GET",
                path: `/rig-files/${pathValue(iD)}`,
            }, options);
        },

        update(iD: string, input: RigFileUpdateInput, options?: CallOptions): Promise<RigFile> {
            return send<RigFile>(rt, {
                name: "updateRigFile",
                method: "PATCH",
                path: `/rig-files/${pathValue(iD)}`,
                body: input,
            }, options);
        },

        restore(iD: string, options?: CallOptions): Promise<RigFile> {
            return send<RigFile>(rt, {
                name: "restoreRigFile",
                method: "POST",
                path: `/rig-files/${pathValue(iD)}/_restore`,
            }, options);
        },
    };
}

/**
 * isRigFileCreateError reads back what createRigFile refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isRigFileCreateError(err: unknown): err is RigError<RigFileCreateFields> {
    return isInvalid(err);
}

/**
 * isRigFileUpdateError reads back what updateRigFile refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isRigFileUpdateError(err: unknown): err is RigError<RigFileUpdateFields> {
    return isInvalid(err);
}
