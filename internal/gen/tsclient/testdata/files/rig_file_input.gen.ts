// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { RigFileFilter } from "./rig_file_query.gen.js";
import type { FieldError } from "@rig/client";

/**
 * RigFileListQuery is the query string listRigFiles takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type RigFileListQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * RigFileCreateInput is what creating a RigFile takes.
 *
 * A field the server fills in — the identifier, the tenant, the audit columns
 * — is not here: those are not a caller's to send.
 */
export type RigFileCreateInput = {
    /** Where the bytes are, derived from the identifier and never from a name. */
    storageKey: string;
    /**
     * Where the file is served from. It is a column so a synced row carries it
     * and a client can render the file without asking anything first.
     */
    url: string;
    /** What the file was called when it arrived. */
    fileName: string;
    /** What the bytes were sniffed to be, which is not what the client claimed. */
    contentType: string;
    /** How large the object is. */
    sizeBytes: number;
    /**
     * When the bytes landed. Null means the row was inserted and the upload has
     * not finished, which is what makes an abandoned upload reapable.
     */
    uploadedAt?: string | null;
};

/**
 * RigFileCreateFields is what a validation failure says, shaped like the body
 * it is about — one member per member, so each message can be put beside the
 * control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type RigFileCreateFields = {
    storageKey?: FieldError;
    url?: FieldError;
    fileName?: FieldError;
    contentType?: FieldError;
    sizeBytes?: FieldError;
    uploadedAt?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

/** The body searchRigFiles sends. */
export type RigFileSearchBody = {
    /** Conditions rows must satisfy. */
    filter: RigFileFilter;
};

/**
 * RigFileSearchQuery is the query string searchRigFiles takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type RigFileSearchQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * RigFileListDeletedQuery is the query string listDeletedRigFiles takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type RigFileListDeletedQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * RigFileUpdateInput is what changing a RigFile takes.
 *
 * A field left out is left alone. A nullable field set to `null` is cleared —
 * which is why only some members accept one: a column that cannot hold null has
 * no way to be given one.
 *
 * `undefined` reads as left out, because JSON.stringify drops the key either
 * way. It is `null` that is a request to clear.
 */
export type RigFileUpdateInput = {
    /** Where the bytes are, derived from the identifier and never from a name. */
    storageKey?: string;
    /**
     * Where the file is served from. It is a column so a synced row carries it
     * and a client can render the file without asking anything first.
     */
    url?: string;
    /** What the file was called when it arrived. */
    fileName?: string;
    /** What the bytes were sniffed to be, which is not what the client claimed. */
    contentType?: string;
    /** How large the object is. */
    sizeBytes?: number;
    /**
     * When the bytes landed. Null means the row was inserted and the upload has
     * not finished, which is what makes an abandoned upload reapable.
     */
    uploadedAt?: string | null;
};

/**
 * RigFileUpdateFields is what a validation failure says, shaped like the body
 * it is about — one member per member, so each message can be put beside the
 * control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type RigFileUpdateFields = {
    storageKey?: FieldError;
    url?: FieldError;
    fileName?: FieldError;
    contentType?: FieldError;
    sizeBytes?: FieldError;
    uploadedAt?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

