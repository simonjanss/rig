// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type {
    ProfileAttachmentFilter,
    ProfileAttachmentFilterContains,
    ProfileAttachmentFilterEquals,
    ProfileAttachmentFilterLike,
    ProfileAttachmentFilterNull,
    ProfileAttachmentFilterRange,
} from "./profile_attachment_query.gen.js";
import type {
    ProfileFilter,
    ProfileFilterContains,
    ProfileFilterEquals,
    ProfileFilterLike,
    ProfileFilterNull,
    ProfileFilterRange,
} from "./profile_query.gen.js";

/** Conditions a RigFile must satisfy to match a search. */
export type RigFileFilter = {
    /** Fields that must equal the given value. */
    equals?: RigFileFilterEquals | null;
    /** Fields that must not equal the given value. */
    notEquals?: RigFileFilterEquals | null;
    /** Fields that must be greater than the given value. */
    greaterThan?: RigFileFilterRange | null;
    /** Fields that must be less than the given value. */
    smallerThan?: RigFileFilterRange | null;
    /** Fields that must be greater than or equal to the given value. */
    greaterOrEqual?: RigFileFilterRange | null;
    /** Fields that must be less than or equal to the given value. */
    smallerOrEqual?: RigFileFilterRange | null;
    /** Fields that must be one of the given values. */
    contains?: RigFileFilterContains | null;
    /** Fields that must not be one of the given values. */
    notContains?: RigFileFilterContains | null;
    /** Text fields that must match the given pattern. */
    like?: RigFileFilterLike | null;
    /** Text fields that must not match the given pattern. */
    notLike?: RigFileFilterLike | null;
    /** Fields that must be null. */
    null?: RigFileFilterNull | null;
    /** Fields that must not be null. */
    notNull?: RigFileFilterNull | null;
    /** Combine this filter's conditions with OR instead of AND. */
    orCondition: boolean;
    /**
     * Sub-filters combined with this one, so that AND and OR can be mixed to
     * any depth.
     */
    nestedFilters: RigFileFilter[];
    /**
     * Relations with no row matching the given conditions. An empty one asks
     * for no related row at all.
     *
     * This is where a negation belongs when what you mean is "not": it negates
     * whether a matching related row exists, so a row with no related row at
     * all satisfies it. The negative operators above negate the comparison
     * instead and still require the related row to be there — "no related row
     * called X" is here, "a related row not called X" is NotEquals.
     */
    without?: RigFileFilterWithout | null;
};

/** Set-membership conditions on RigFile fields. */
export type RigFileFilterContains = {
    /** Unique identifier for this row. */
    id: string[];
    /** Where the bytes are, derived from the identifier and never from a name. */
    storageKey: string[];
    /**
     * Where the file is served from. It is a column so a synced row carries it
     * and a client can render the file without asking anything first.
     */
    url: string[];
    /** What the file was called when it arrived. */
    fileName: string[];
    /** What the bytes were sniffed to be, which is not what the client claimed. */
    contentType: string[];
    /** How large the object is. */
    sizeBytes: number[];
    /**
     * When the bytes landed. Null means the row was inserted and the upload has
     * not finished, which is what makes an abandoned upload reapable.
     */
    uploadedAt: string[];
    /** When this row was created. Set automatically and never changes. */
    createdAt: string[];
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt: string[];
    /**
     * Conditions on one of this row's BannerFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.BannerFileProfiles asks the second.
     */
    bannerFileProfiles?: ProfileFilterContains | null;
    /**
     * Conditions on one of this row's DocumentFileProfileAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.DocumentFileProfileAttachments asks the second.
     */
    documentFileProfileAttachments?: ProfileAttachmentFilterContains | null;
    /**
     * Conditions on one of this row's ProfileImageFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.ProfileImageFileProfiles asks the second.
     */
    profileImageFileProfiles?: ProfileFilterContains | null;
};

/** Exact-match conditions on RigFile fields. */
export type RigFileFilterEquals = {
    /** Unique identifier for this row. */
    id?: string | null;
    /** Where the bytes are, derived from the identifier and never from a name. */
    storageKey?: string | null;
    /**
     * Where the file is served from. It is a column so a synced row carries it
     * and a client can render the file without asking anything first.
     */
    url?: string | null;
    /** What the file was called when it arrived. */
    fileName?: string | null;
    /** What the bytes were sniffed to be, which is not what the client claimed. */
    contentType?: string | null;
    /** How large the object is. */
    sizeBytes?: number | null;
    /**
     * When the bytes landed. Null means the row was inserted and the upload has
     * not finished, which is what makes an abandoned upload reapable.
     */
    uploadedAt?: string | null;
    /** When this row was created. Set automatically and never changes. */
    createdAt?: string | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: string | null;
    /**
     * Conditions on one of this row's BannerFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.BannerFileProfiles asks the second.
     */
    bannerFileProfiles?: ProfileFilterEquals | null;
    /**
     * Conditions on one of this row's DocumentFileProfileAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.DocumentFileProfileAttachments asks the second.
     */
    documentFileProfileAttachments?: ProfileAttachmentFilterEquals | null;
    /**
     * Conditions on one of this row's ProfileImageFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.ProfileImageFileProfiles asks the second.
     */
    profileImageFileProfiles?: ProfileFilterEquals | null;
};

/** Pattern conditions on RigFile text fields. */
export type RigFileFilterLike = {
    /** Where the bytes are, derived from the identifier and never from a name. */
    storageKey?: string | null;
    /**
     * Where the file is served from. It is a column so a synced row carries it
     * and a client can render the file without asking anything first.
     */
    url?: string | null;
    /** What the file was called when it arrived. */
    fileName?: string | null;
    /** What the bytes were sniffed to be, which is not what the client claimed. */
    contentType?: string | null;
    /**
     * Conditions on one of this row's BannerFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.BannerFileProfiles asks the second.
     */
    bannerFileProfiles?: ProfileFilterLike | null;
    /**
     * Conditions on one of this row's DocumentFileProfileAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.DocumentFileProfileAttachments asks the second.
     */
    documentFileProfileAttachments?: ProfileAttachmentFilterLike | null;
    /**
     * Conditions on one of this row's ProfileImageFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.ProfileImageFileProfiles asks the second.
     */
    profileImageFileProfiles?: ProfileFilterLike | null;
};

/** Presence conditions on nullable RigFile fields. */
export type RigFileFilterNull = {
    /**
     * When the bytes landed. Null means the row was inserted and the upload has
     * not finished, which is what makes an abandoned upload reapable.
     */
    uploadedAt?: boolean | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: boolean | null;
    /**
     * Conditions on one of this row's BannerFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.BannerFileProfiles asks the second.
     */
    bannerFileProfiles?: ProfileFilterNull | null;
    /**
     * Conditions on one of this row's DocumentFileProfileAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.DocumentFileProfileAttachments asks the second.
     */
    documentFileProfileAttachments?: ProfileAttachmentFilterNull | null;
    /**
     * Conditions on one of this row's ProfileImageFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.ProfileImageFileProfiles asks the second.
     */
    profileImageFileProfiles?: ProfileFilterNull | null;
};

/** Ordering conditions on RigFile fields that can be compared. */
export type RigFileFilterRange = {
    /** How large the object is. */
    sizeBytes?: number | null;
    /**
     * When the bytes landed. Null means the row was inserted and the upload has
     * not finished, which is what makes an abandoned upload reapable.
     */
    uploadedAt?: string | null;
    /** When this row was created. Set automatically and never changes. */
    createdAt?: string | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: string | null;
    /**
     * Conditions on one of this row's BannerFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.BannerFileProfiles asks the second.
     */
    bannerFileProfiles?: ProfileFilterRange | null;
    /**
     * Conditions on one of this row's DocumentFileProfileAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.DocumentFileProfileAttachments asks the second.
     */
    documentFileProfileAttachments?: ProfileAttachmentFilterRange | null;
    /**
     * Conditions on one of this row's ProfileImageFileProfiles.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.ProfileImageFileProfiles asks the second.
     */
    profileImageFileProfiles?: ProfileFilterRange | null;
};

/** Relations a RigFile must have no matching row for. */
export type RigFileFilterWithout = {
    /**
     * Match rows with no BannerFileProfiles satisfying these conditions,
     * including rows with none at all.
     *
     * This negates the existence of a matching row rather than the comparison,
     * which is the whole difference from NotEquals.BannerFileProfiles: that one
     * is satisfied by one of them differing, and a row can have one that
     * differs and one that matches at the same time. An empty object asks for
     * rows with no BannerFileProfiles at all.
     */
    bannerFileProfiles?: ProfileFilter | null;
    /**
     * Match rows with no DocumentFileProfileAttachments satisfying these
     * conditions, including rows with none at all.
     *
     * This negates the existence of a matching row rather than the comparison,
     * which is the whole difference from
     * NotEquals.DocumentFileProfileAttachments: that one is satisfied by one of
     * them differing, and a row can have one that differs and one that matches
     * at the same time. An empty object asks for rows with no
     * DocumentFileProfileAttachments at all.
     */
    documentFileProfileAttachments?: ProfileAttachmentFilter | null;
    /**
     * Match rows with no ProfileImageFileProfiles satisfying these conditions,
     * including rows with none at all.
     *
     * This negates the existence of a matching row rather than the comparison,
     * which is the whole difference from NotEquals.ProfileImageFileProfiles:
     * that one is satisfied by one of them differing, and a row can have one
     * that differs and one that matches at the same time. An empty object asks
     * for rows with no ProfileImageFileProfiles at all.
     */
    profileImageFileProfiles?: ProfileFilter | null;
};

