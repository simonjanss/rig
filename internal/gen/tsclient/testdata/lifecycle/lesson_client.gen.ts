// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { Lesson, LessonListResponse } from "./lesson.gen.js";
import type {
    LessonCreateFields,
    LessonCreateInput,
    LessonListDeletedQuery,
    LessonListQuery,
    LessonPublishBody,
    LessonPublishFields,
    LessonRevertBody,
    LessonSearchQuery,
    LessonUpdateFields,
    LessonUpdateInput,
} from "./lesson_input.gen.js";
import type { LessonFilter } from "./lesson_query.gen.js";
import type { CallOptions, RigError, Runtime } from "@rig/client";

import {
    METHOD_QUERY,
    isInvalid,
    pathValue,
    send,
    sendNoContent,
    setParam,
} from "@rig/client";

/**
 * LessonClient calls the Lesson endpoints. It is reached as `client.lessons`
 * rather than built directly.
 */
export class LessonClient {
    readonly #rt: Runtime;

    constructor(rt: Runtime) {
        this.#rt = rt;
    }

    /**
     * List Lessons.
     *
     * GET /api/v1/lessons
     *
     * Operation listLessons.
     */
    list(query: LessonListQuery = {}, options?: CallOptions): Promise<LessonListResponse> {
        const search = new URLSearchParams();
        setParam(search, "limit", query.limit);
        setParam(search, "offset", query.offset);

        return send<LessonListResponse>(this.#rt, {
            name: "listLessons",
            method: "GET",
            path: "/lessons",
            query: search,
        }, options);
    }

    /**
     * Create a Lesson.
     *
     * POST /api/v1/lessons
     *
     * Operation createLesson.
     *
     * A refusal is read back with `isLessonCreateError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    create(input: LessonCreateInput, options?: CallOptions): Promise<Lesson> {
        return send<Lesson>(this.#rt, {
            name: "createLesson",
            method: "POST",
            path: "/lessons",
            body: input,
        }, options);
    }

    /**
     * Search Lessons with filters.
     *
     * QUERY /api/v1/lessons, falling back to POST /api/v1/lessons/_search when
     * something between here and the server refuses the method. The fallback is
     * remembered, so it is tried once.
     *
     * Operation searchLessons.
     */
    search(filter: LessonFilter, query: LessonSearchQuery = {}, options?: CallOptions): Promise<LessonListResponse> {
        const search = new URLSearchParams();
        setParam(search, "limit", query.limit);
        setParam(search, "offset", query.offset);

        return send<LessonListResponse>(this.#rt, {
            name: "searchLessons",
            method: METHOD_QUERY,
            path: "/lessons",
            query: search,
            body: { filter: filter },
            fallback: "/lessons/_search",
        }, options);
    }

    /**
     * A deletion stamps the row rather than removing it, so this is what was
     * deleted and can still be brought back. Only rows inside the 30-day
     * restore window appear: past it a row is gone as far as anyone is
     * concerned, so it is not in the trash either.
     *
     * GET /api/v1/lessons/_deleted
     *
     * Operation listDeletedLessons.
     */
    listDeleted(query: LessonListDeletedQuery = {}, options?: CallOptions): Promise<LessonListResponse> {
        const search = new URLSearchParams();
        setParam(search, "limit", query.limit);
        setParam(search, "offset", query.offset);

        return send<LessonListResponse>(this.#rt, {
            name: "listDeletedLessons",
            method: "GET",
            path: "/lessons/_deleted",
            query: search,
        }, options);
    }

    /**
     * The row is retired by stamping a deletion time; it stops appearing in
     * reads but can be restored within the retention window.
     *
     * DELETE /api/v1/lessons/{id}
     *
     * Operation deleteLesson.
     */
    delete(iD: string, options?: CallOptions): Promise<void> {
        return sendNoContent(this.#rt, {
            name: "deleteLesson",
            method: "DELETE",
            path: `/lessons/${pathValue(iD)}`,
        }, options);
    }

    /**
     * Fetch one Lesson by identifier.
     *
     * GET /api/v1/lessons/{id}
     *
     * Operation getLesson.
     */
    get(iD: string, options?: CallOptions): Promise<Lesson> {
        return send<Lesson>(this.#rt, {
            name: "getLesson",
            method: "GET",
            path: `/lessons/${pathValue(iD)}`,
        }, options);
    }

    /**
     * Only the fields present in the body are changed. A field set to null is
     * cleared; a field left out is left alone.
     *
     * PATCH /api/v1/lessons/{id}
     *
     * Operation updateLesson.
     *
     * A refusal is read back with `isLessonUpdateError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    update(iD: string, input: LessonUpdateInput, options?: CallOptions): Promise<Lesson> {
        return send<Lesson>(this.#rt, {
            name: "updateLesson",
            method: "PATCH",
            path: `/lessons/${pathValue(iD)}`,
            body: input,
        }, options);
    }

    /**
     * Publish the lesson to its participants.
     *
     * POST /api/v1/lessons/{id}/_publish
     *
     * Operation publishLesson.
     *
     * A refusal is read back with `isLessonPublishError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    publish(iD: string, input: LessonPublishBody, options?: CallOptions): Promise<Lesson> {
        return send<Lesson>(this.#rt, {
            name: "publishLesson",
            method: "POST",
            path: `/lessons/${pathValue(iD)}/_publish`,
            body: input,
        }, options);
    }

    /**
     * The deletion stamp is cleared and the row appears in reads again. It
     * works for 30 days after the deletion; past that the row is no longer
     * eligible and this answers 409.
     *
     * Restoring a row that was never deleted is not an error. It is already in
     * the state the caller asked for, and answering otherwise would make a
     * retry of a request whose response went missing look like a failure.
     *
     * The request carries no fields. What to do when the world has moved on
     * while the row was retired — a unique value taken by something created
     * since — is the application's decision, and it is made in the restore
     * hook: refuse, or change the row so that it fits.
     *
     * Every rule then runs against whatever the hook settled on, not only the
     * ones for fields it touched. The row was not live, so nothing about it has
     * been checked against the world it is returning to.
     *
     * POST /api/v1/lessons/{id}/_restore
     *
     * Operation restoreLesson.
     */
    restore(iD: string, options?: CallOptions): Promise<Lesson> {
        return send<Lesson>(this.#rt, {
            name: "restoreLesson",
            method: "POST",
            path: `/lessons/${pathValue(iD)}/_restore`,
        }, options);
    }

    /**
     * The version is replayed through the ordinary update path rather than
     * written over the row, so the state being replaced is itself snapshotted
     * first and every rule an update runs still runs. Reverting is undoable,
     * and a value that would be refused now is still refused.
     *
     * POST /api/v1/lessons/{id}/_revert
     *
     * Operation revertLesson.
     */
    revert(iD: string, input: LessonRevertBody, options?: CallOptions): Promise<Lesson> {
        return send<Lesson>(this.#rt, {
            name: "revertLesson",
            method: "POST",
            path: `/lessons/${pathValue(iD)}/_revert`,
            body: input,
        }, options);
    }

    /**
     * Every update copies the row as it was before writing the change, so this
     * is the row's history: newest first, and never the live row itself.
     *
     * These do not appear in ordinary reads. A version answers Get by its own
     * identifier, but it is filtered out of List and Search, so history does
     * not turn up in a listing.
     *
     * GET /api/v1/lessons/{id}/_versions
     *
     * Operation versionsOfLesson.
     */
    versions(iD: string, options?: CallOptions): Promise<LessonListResponse> {
        return send<LessonListResponse>(this.#rt, {
            name: "versionsOfLesson",
            method: "GET",
            path: `/lessons/${pathValue(iD)}/_versions`,
        }, options);
    }
}

/**
 * isLessonCreateError reads back what createLesson refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isLessonCreateError(err: unknown): err is RigError<LessonCreateFields> {
    return isInvalid(err);
}

/**
 * isLessonUpdateError reads back what updateLesson refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isLessonUpdateError(err: unknown): err is RigError<LessonUpdateFields> {
    return isInvalid(err);
}

/**
 * isLessonPublishError reads back what publishLesson refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isLessonPublishError(err: unknown): err is RigError<LessonPublishFields> {
    return isInvalid(err);
}
