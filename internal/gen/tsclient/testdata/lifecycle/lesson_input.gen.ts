// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { LessonFilter } from "./lesson_query.gen.js";
import type { LessonStatus } from "./lesson_status.gen.js";
import type { FieldError } from "@rig/client";

/**
 * LessonListQuery is the query string listLessons takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type LessonListQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * LessonCreateInput is what creating a Lesson takes.
 *
 * A field the server fills in — the identifier, the tenant, the audit columns
 * — is not here: those are not a caller's to send.
 */
export type LessonCreateInput = {
    /** Human-readable name shown in the timetable. */
    title: string;
    /** Free-text notes kept alongside the lesson. */
    notes?: string | null;
    /** Address that notifications about this lesson are sent to. */
    managerEmailAddress: string;
    /** When the lesson begins. */
    startsAt: string;
    /** Maximum number of participants, or null for no limit. */
    capacity?: number | null;
    /** Amount charged per participant. */
    price?: string | null;
    /** Free-form labels used for grouping. */
    tags?: string[] | null;
};

/**
 * LessonCreateFields is what a validation failure says, shaped like the body it
 * is about — one member per member, so each message can be put beside the
 * control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type LessonCreateFields = {
    title?: FieldError;
    notes?: FieldError;
    managerEmailAddress?: FieldError;
    startsAt?: FieldError;
    capacity?: FieldError;
    price?: FieldError;
    tags?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

/** The body searchLessons sends. */
export type LessonSearchBody = {
    /** Conditions rows must satisfy. */
    filter: LessonFilter;
};

/**
 * LessonSearchQuery is the query string searchLessons takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type LessonSearchQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * LessonListDeletedQuery is the query string listDeletedLessons takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type LessonListDeletedQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * LessonUpdateInput is what changing a Lesson takes.
 *
 * A field left out is left alone. A nullable field set to `null` is cleared —
 * which is why only some members accept one: a column that cannot hold null has
 * no way to be given one.
 *
 * `undefined` reads as left out, because JSON.stringify drops the key either
 * way. It is `null` that is a request to clear.
 */
export type LessonUpdateInput = {
    /** Human-readable name shown in the timetable. */
    title?: string;
    /** Free-text notes kept alongside the lesson. */
    notes?: string | null;
    /** Lifecycle state of the lesson. */
    status?: LessonStatus;
    /** Address that notifications about this lesson are sent to. */
    managerEmailAddress?: string;
    /** Maximum number of participants, or null for no limit. */
    capacity?: number | null;
    /** Amount charged per participant. */
    price?: string | null;
    /** Free-form labels used for grouping. */
    tags?: string[] | null;
};

/**
 * LessonUpdateFields is what a validation failure says, shaped like the body it
 * is about — one member per member, so each message can be put beside the
 * control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type LessonUpdateFields = {
    title?: FieldError;
    notes?: FieldError;
    status?: FieldError;
    managerEmailAddress?: FieldError;
    capacity?: FieldError;
    price?: FieldError;
    tags?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

/** The body publishLesson sends. */
export type LessonPublishBody = {
    /** Whether guardians are notified as well. */
    notifyGuardians: boolean;
};

/**
 * LessonPublishFields is what a validation failure says, shaped like the body
 * it is about — one member per member, so each message can be put beside the
 * control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type LessonPublishFields = {
    notifyGuardians?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

/** The body revertLesson sends. */
export type LessonRevertBody = {
    /** The version to put back, from the Lesson's history. */
    versionId: string;
};

