// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/** Lifecycle state of a lesson. */
export const LessonStatus = {
    /** Created but not yet started. */
    Planned: "planned",
    /** Currently being taught. */
    InProgress: "in_progress",
    /** Finished; grades may now be entered. */
    Completed: "completed",
} as const;

/**
 * One of the values of LessonStatus.
 *
 * The type and the object share a name on purpose: TypeScript keeps types and
 * values in separate namespaces, so `LessonStatus.Planned` and `: LessonStatus`
 * both read the way somebody would write them.
 */
export type LessonStatus = (typeof LessonStatus)[keyof typeof LessonStatus];

/**
 * Every value of LessonStatus, in declaration order. It is what a picker is
 * drawn from.
 */
export const allLessonStatus: readonly LessonStatus[] = [LessonStatus.Planned, LessonStatus.InProgress, LessonStatus.Completed];
