// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { BlogPostFilter } from "./blog_post_query.gen.js";
import type { FieldError } from "@rig-ts/client";

/**
 * BlogPostListQuery is the query string listBlogPosts takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type BlogPostListQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * BlogPostCreateInput is what creating a BlogPost takes.
 *
 * A field the server fills in — the identifier, the tenant, the audit columns
 * — is not here: those are not a caller's to send.
 */
export type BlogPostCreateInput = {
    /** What the post is called. */
    title: string;
    /** When the post goes live, or null while it is a draft. */
    publishAt?: string | null;
};

/**
 * BlogPostCreateFields is what a validation failure says, shaped like the body
 * it is about — one member per member, so each message can be put beside the
 * control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type BlogPostCreateFields = {
    title?: FieldError;
    publishAt?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

/** The body searchBlogPosts sends. */
export type BlogPostSearchBody = {
    /** Conditions rows must satisfy. */
    filter: BlogPostFilter;
};

/**
 * BlogPostSearchQuery is the query string searchBlogPosts takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type BlogPostSearchQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * BlogPostListDeletedQuery is the query string listDeletedBlogPosts takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type BlogPostListDeletedQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * BlogPostUpdateInput is what changing a BlogPost takes.
 *
 * A field left out is left alone. A nullable field set to `null` is cleared —
 * which is why only some members accept one: a column that cannot hold null has
 * no way to be given one.
 *
 * `undefined` reads as left out, because JSON.stringify drops the key either
 * way. It is `null` that is a request to clear.
 */
export type BlogPostUpdateInput = {
    /** What the post is called. */
    title?: string;
    /** When the post goes live, or null while it is a draft. */
    publishAt?: string | null;
};

/**
 * BlogPostUpdateFields is what a validation failure says, shaped like the body
 * it is about — one member per member, so each message can be put beside the
 * control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type BlogPostUpdateFields = {
    title?: FieldError;
    publishAt?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

