// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { BlogPost, BlogPostListResponse } from "./blog_post.gen.js";
import type {
    BlogPostCreateFields,
    BlogPostCreateInput,
    BlogPostListDeletedQuery,
    BlogPostListQuery,
    BlogPostSearchQuery,
    BlogPostUpdateFields,
    BlogPostUpdateInput,
} from "./blog_post_input.gen.js";
import type { BlogPostFilter } from "./blog_post_query.gen.js";
import type { CallOptions, RigError, Runtime } from "@rig-ts/client";

import {
    METHOD_QUERY,
    isInvalid,
    pathValue,
    send,
    sendNoContent,
    setParam,
} from "@rig-ts/client";

/**
 * BlogPostClient is what `client.blogPosts` is, and what a test writes to stand
 * in for it.
 *
 * The documentation for each call is here rather than on the object below,
 * because this is the type the property resolves to and so this is what an
 * editor shows.
 */
export interface BlogPostClient {
    /**
     * List BlogPosts.
     *
     * GET /api/v1/blog-posts
     *
     * Operation listBlogPosts.
     */
    list(query?: BlogPostListQuery, options?: CallOptions): Promise<BlogPostListResponse>;

    /**
     * Create a BlogPost.
     *
     * POST /api/v1/blog-posts
     *
     * Operation createBlogPost.
     *
     * A refusal is read back with `isBlogPostCreateError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    create(input: BlogPostCreateInput, options?: CallOptions): Promise<BlogPost>;

    /**
     * Search BlogPosts with filters.
     *
     * QUERY /api/v1/blog-posts, falling back to POST /api/v1/blog-posts/_search
     * when something between here and the server refuses the method. The
     * fallback is remembered, so it is tried once.
     *
     * Operation searchBlogPosts.
     */
    search(filter: BlogPostFilter, query?: BlogPostSearchQuery, options?: CallOptions): Promise<BlogPostListResponse>;

    /**
     * A deletion stamps the row rather than removing it, so this is what was
     * deleted and can still be brought back. Only rows inside the 30-day
     * restore window appear: past it a row is gone as far as anyone is
     * concerned, so it is not in the trash either.
     *
     * GET /api/v1/blog-posts/_deleted
     *
     * Operation listDeletedBlogPosts.
     */
    listDeleted(query?: BlogPostListDeletedQuery, options?: CallOptions): Promise<BlogPostListResponse>;

    /**
     * The row is retired by stamping a deletion time; it stops appearing in
     * reads but can be restored within the retention window.
     *
     * DELETE /api/v1/blog-posts/{id}
     *
     * Operation deleteBlogPost.
     */
    delete(iD: string, options?: CallOptions): Promise<void>;

    /**
     * Fetch one BlogPost by identifier.
     *
     * GET /api/v1/blog-posts/{id}
     *
     * Operation getBlogPost.
     */
    get(iD: string, options?: CallOptions): Promise<BlogPost>;

    /**
     * Only the fields present in the body are changed. A field set to null is
     * cleared; a field left out is left alone.
     *
     * PATCH /api/v1/blog-posts/{id}
     *
     * Operation updateBlogPost.
     *
     * A refusal is read back with `isBlogPostUpdateError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    update(iD: string, input: BlogPostUpdateInput, options?: CallOptions): Promise<BlogPost>;

    /**
     * The deletion stamp is cleared and the row appears in reads again. It
     * works for 30 days after the deletion; past that the row is no longer
     * eligible and this answers 409.
     *
     * Restoring a row that was never deleted is not an error. It is already in
     * the state the caller asked for, and answering otherwise would make a
     * retry of a request whose response went missing look like a failure.
     *
     * The request carries no fields. What to do when the world has moved on
     * while the row was retired — a unique value taken by something created
     * since — is the application's decision, and it is made in the restore
     * hook: refuse, or change the row so that it fits.
     *
     * Every rule then runs against whatever the hook settled on, not only the
     * ones for fields it touched. The row was not live, so nothing about it has
     * been checked against the world it is returning to.
     *
     * POST /api/v1/blog-posts/{id}/_restore
     *
     * Operation restoreBlogPost.
     */
    restore(iD: string, options?: CallOptions): Promise<BlogPost>;
}

/**
 * Builds the BlogPost half of a client.
 *
 * Called by `createClient` and not usually by anything else: a resource reached
 * through the client it belongs to shares that client's credential, and one
 * built here would not.
 */
export function createBlogPostClient(rt: Runtime): BlogPostClient {
    return {
        list(query: BlogPostListQuery = {}, options?: CallOptions): Promise<BlogPostListResponse> {
            const search = new URLSearchParams();
            setParam(search, "limit", query.limit);
            setParam(search, "offset", query.offset);

            return send<BlogPostListResponse>(rt, {
                name: "listBlogPosts",
                method: "GET",
                path: "/blog-posts",
                query: search,
            }, options);
        },

        create(input: BlogPostCreateInput, options?: CallOptions): Promise<BlogPost> {
            return send<BlogPost>(rt, {
                name: "createBlogPost",
                method: "POST",
                path: "/blog-posts",
                body: input,
            }, options);
        },

        search(filter: BlogPostFilter, query: BlogPostSearchQuery = {}, options?: CallOptions): Promise<BlogPostListResponse> {
            const search = new URLSearchParams();
            setParam(search, "limit", query.limit);
            setParam(search, "offset", query.offset);

            return send<BlogPostListResponse>(rt, {
                name: "searchBlogPosts",
                method: METHOD_QUERY,
                path: "/blog-posts",
                query: search,
                body: { filter: filter },
                fallback: "/blog-posts/_search",
            }, options);
        },

        listDeleted(query: BlogPostListDeletedQuery = {}, options?: CallOptions): Promise<BlogPostListResponse> {
            const search = new URLSearchParams();
            setParam(search, "limit", query.limit);
            setParam(search, "offset", query.offset);

            return send<BlogPostListResponse>(rt, {
                name: "listDeletedBlogPosts",
                method: "GET",
                path: "/blog-posts/_deleted",
                query: search,
            }, options);
        },

        delete(iD: string, options?: CallOptions): Promise<void> {
            return sendNoContent(rt, {
                name: "deleteBlogPost",
                method: "DELETE",
                path: `/blog-posts/${pathValue(iD)}`,
            }, options);
        },

        get(iD: string, options?: CallOptions): Promise<BlogPost> {
            return send<BlogPost>(rt, {
                name: "getBlogPost",
                method: "GET",
                path: `/blog-posts/${pathValue(iD)}`,
            }, options);
        },

        update(iD: string, input: BlogPostUpdateInput, options?: CallOptions): Promise<BlogPost> {
            return send<BlogPost>(rt, {
                name: "updateBlogPost",
                method: "PATCH",
                path: `/blog-posts/${pathValue(iD)}`,
                body: input,
            }, options);
        },

        restore(iD: string, options?: CallOptions): Promise<BlogPost> {
            return send<BlogPost>(rt, {
                name: "restoreBlogPost",
                method: "POST",
                path: `/blog-posts/${pathValue(iD)}/_restore`,
            }, options);
        },
    };
}

/**
 * isBlogPostCreateError reads back what createBlogPost refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isBlogPostCreateError(err: unknown): err is RigError<BlogPostCreateFields> {
    return isInvalid(err);
}

/**
 * isBlogPostUpdateError reads back what updateBlogPost refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isBlogPostUpdateError(err: unknown): err is RigError<BlogPostUpdateFields> {
    return isInvalid(err);
}
