// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { RigAccountFilter } from "./rig_account_query.gen.js";
import type { FieldError } from "@rig-ts/client";

/**
 * RigAccountListQuery is the query string listRigAccounts takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type RigAccountListQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * RigAccountCreateInput is what creating a RigAccount takes.
 *
 * A field the server fills in — the identifier, the tenant, the audit columns
 * — is not here: those are not a caller's to send.
 */
export type RigAccountCreateInput = {
    displayName: string;
};

/**
 * RigAccountCreateFields is what a validation failure says, shaped like the
 * body it is about — one member per member, so each message can be put beside
 * the control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type RigAccountCreateFields = {
    displayName?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

/** The body searchRigAccounts sends. */
export type RigAccountSearchBody = {
    /** Conditions rows must satisfy. */
    filter: RigAccountFilter;
};

/**
 * RigAccountSearchQuery is the query string searchRigAccounts takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type RigAccountSearchQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * RigAccountUpdateInput is what changing a RigAccount takes.
 *
 * A field left out is left alone. A nullable field set to `null` is cleared —
 * which is why only some members accept one: a column that cannot hold null has
 * no way to be given one.
 *
 * `undefined` reads as left out, because JSON.stringify drops the key either
 * way. It is `null` that is a request to clear.
 */
export type RigAccountUpdateInput = {
    displayName?: string;
};

/**
 * RigAccountUpdateFields is what a validation failure says, shaped like the
 * body it is about — one member per member, so each message can be put beside
 * the control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type RigAccountUpdateFields = {
    displayName?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

