// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { RigAccount, RigAccountListResponse } from "./rig_account.gen.js";
import type {
    RigAccountCreateFields,
    RigAccountCreateInput,
    RigAccountListQuery,
    RigAccountSearchQuery,
    RigAccountUpdateFields,
    RigAccountUpdateInput,
} from "./rig_account_input.gen.js";
import type { RigAccountFilter } from "./rig_account_query.gen.js";
import type { CallOptions, RigError, Runtime } from "@rig-ts/client";

import {
    METHOD_QUERY,
    isInvalid,
    pathValue,
    send,
    sendNoContent,
    setParam,
} from "@rig-ts/client";

/**
 * RigAccountClient is what `client.rigAccounts` is, and what a test writes to
 * stand in for it.
 *
 * The documentation for each call is here rather than on the object below,
 * because this is the type the property resolves to and so this is what an
 * editor shows.
 */
export interface RigAccountClient {
    /**
     * List RigAccounts.
     *
     * GET /api/v1/rig-accounts
     *
     * Operation listRigAccounts.
     */
    list(query?: RigAccountListQuery, options?: CallOptions): Promise<RigAccountListResponse>;

    /**
     * Create a RigAccount.
     *
     * POST /api/v1/rig-accounts
     *
     * Operation createRigAccount.
     *
     * A refusal is read back with `isRigAccountCreateError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    create(input: RigAccountCreateInput, options?: CallOptions): Promise<RigAccount>;

    /**
     * Search RigAccounts with filters.
     *
     * QUERY /api/v1/rig-accounts, falling back to POST
     * /api/v1/rig-accounts/_search when something between here and the server
     * refuses the method. The fallback is remembered, so it is tried once.
     *
     * Operation searchRigAccounts.
     */
    search(filter: RigAccountFilter, query?: RigAccountSearchQuery, options?: CallOptions): Promise<RigAccountListResponse>;

    /**
     * Delete a RigAccount.
     *
     * DELETE /api/v1/rig-accounts/{id}
     *
     * Operation deleteRigAccount.
     */
    delete(iD: string, options?: CallOptions): Promise<void>;

    /**
     * Fetch one RigAccount by identifier.
     *
     * GET /api/v1/rig-accounts/{id}
     *
     * Operation getRigAccount.
     */
    get(iD: string, options?: CallOptions): Promise<RigAccount>;

    /**
     * Only the fields present in the body are changed. A field set to null is
     * cleared; a field left out is left alone.
     *
     * PATCH /api/v1/rig-accounts/{id}
     *
     * Operation updateRigAccount.
     *
     * A refusal is read back with `isRigAccountUpdateError`, whose `fields` say
     * what was wrong with each member of the body.
     */
    update(iD: string, input: RigAccountUpdateInput, options?: CallOptions): Promise<RigAccount>;
}

/**
 * Builds the RigAccount half of a client.
 *
 * Called by `createClient` and not usually by anything else: a resource reached
 * through the client it belongs to shares that client's credential, and one
 * built here would not.
 */
export function createRigAccountClient(rt: Runtime): RigAccountClient {
    return {
        list(query: RigAccountListQuery = {}, options?: CallOptions): Promise<RigAccountListResponse> {
            const search = new URLSearchParams();
            setParam(search, "limit", query.limit);
            setParam(search, "offset", query.offset);

            return send<RigAccountListResponse>(rt, {
                name: "listRigAccounts",
                method: "GET",
                path: "/rig-accounts",
                query: search,
            }, options);
        },

        create(input: RigAccountCreateInput, options?: CallOptions): Promise<RigAccount> {
            return send<RigAccount>(rt, {
                name: "createRigAccount",
                method: "POST",
                path: "/rig-accounts",
                body: input,
            }, options);
        },

        search(filter: RigAccountFilter, query: RigAccountSearchQuery = {}, options?: CallOptions): Promise<RigAccountListResponse> {
            const search = new URLSearchParams();
            setParam(search, "limit", query.limit);
            setParam(search, "offset", query.offset);

            return send<RigAccountListResponse>(rt, {
                name: "searchRigAccounts",
                method: METHOD_QUERY,
                path: "/rig-accounts",
                query: search,
                body: { filter: filter },
                fallback: "/rig-accounts/_search",
            }, options);
        },

        delete(iD: string, options?: CallOptions): Promise<void> {
            return sendNoContent(rt, {
                name: "deleteRigAccount",
                method: "DELETE",
                path: `/rig-accounts/${pathValue(iD)}`,
            }, options);
        },

        get(iD: string, options?: CallOptions): Promise<RigAccount> {
            return send<RigAccount>(rt, {
                name: "getRigAccount",
                method: "GET",
                path: `/rig-accounts/${pathValue(iD)}`,
            }, options);
        },

        update(iD: string, input: RigAccountUpdateInput, options?: CallOptions): Promise<RigAccount> {
            return send<RigAccount>(rt, {
                name: "updateRigAccount",
                method: "PATCH",
                path: `/rig-accounts/${pathValue(iD)}`,
                body: input,
            }, options);
        },
    };
}

/**
 * isRigAccountCreateError reads back what createRigAccount refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isRigAccountCreateError(err: unknown): err is RigError<RigAccountCreateFields> {
    return isInvalid(err);
}

/**
 * isRigAccountUpdateError reads back what updateRigAccount refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isRigAccountUpdateError(err: unknown): err is RigError<RigAccountUpdateFields> {
    return isInvalid(err);
}
