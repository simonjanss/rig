// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"cmp"
	"os"
	"strings"
)

// WebOriginEnv is where the front end's origin comes from, for a deployment
// whose front end differs from environment to environment. [WebOrigin] reads
// it.
//
// A constant rather than a string inside that function, so what a deployment
// has to set is readable off the package — and so the refusal for having not
// set it can name it.
const WebOriginEnv = "APP_ORIGIN"

// WebCallbackPath is the route on the front end that receives a finished
// provider sign-in.
//
// It is the front end's own route, so rig cannot check that it exists — what
// rig does with it is resolve the redirect and scope the handoff cookie to
// that one path, which is why the two have to be the same string and why it
// comes from rig.yaml rather than from each end separately.
const WebCallbackPath = "/auth/finish"

// WebOrigin is where this API's browser front end is served.
//
// It is a function rather than a constant for the reason [BaseURL] is, where a
// project has both: the same binary serves more than one deployment and the
// configuration cannot know which.
//
// What the configuration and the environment say, which is not the whole
// answer: [Hooks.OAuth] carries a WebOrigin of its own and [Config] prefers
// it. This is what a project that supplied none gets.
func WebOrigin() (string, error) {
	return strings.TrimRight(cmp.Or(os.Getenv(WebOriginEnv), "https://console.example.com"), "/"), nil
}
