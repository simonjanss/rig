// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"github.com/simonjanss/rig/runtime/tenancy"
)

// Permissions is every permission this API's endpoints require.
//
// Hand it to the authentication foundation at startup and it reconciles the
// permission table against it: a key this API checks and the database does not
// have is one no role can hold, which denies every caller — so the missing
// ones are written. A row the database has and this API does not check is
// reported and never deleted: it grants nothing, and removing it would take
// live grants with it.
//
// The names and descriptions come from the tables' own comments, so the
// sentence somebody reads while handing out a permission is the one that was
// already written about the table.
func Permissions() []tenancy.Permission {
	return []tenancy.Permission{
		{Key: "fixture.delete", Name: "Delete fixtures", Description: "Delete fixtures — A scheduled contest between two teams."},
		{Key: "fixture.read", Name: "Read fixtures", Description: "Read fixtures — A scheduled contest between two teams."},
		{Key: "fixture.write", Name: "Write fixtures", Description: "Create and change fixtures — A scheduled contest between two teams."},
		{Key: "player.delete", Name: "Delete players", Description: "Delete players — A person who plays for one or more teams."},
		{Key: "player.read", Name: "Read players", Description: "Read players — A person who plays for one or more teams."},
		{Key: "player.write", Name: "Write players", Description: "Create and change players — A person who plays for one or more teams."},
		{Key: "team.delete", Name: "Delete teams", Description: "Delete teams — A group of players that competes as a unit."},
		{Key: "team.read", Name: "Read teams", Description: "Read teams — A group of players that competes as a unit."},
		{Key: "team.write", Name: "Write teams", Description: "Create and change teams — A group of players that competes as a unit."},
	}
}

// PermissionKeys is [Permissions] as the keys alone.
//
// It is what a role grant wants: the set an owner should hold is every
// permission this API checks, and deriving it beats a list somebody maintains
// — a hand-written one goes stale the moment a table is added, and the owner
// of a brand new tenant silently cannot touch it.
func PermissionKeys() []string {
	out := make([]string, 0, 9)
	for _, p := range Permissions() {
		out = append(out, p.Key)
	}
	return out
}
