// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"rigtest/model"

	"github.com/google/uuid"
)

// A page of BlogPosts.
type BlogPostListResponse struct {
	// The rows in this page.
	//
	// Pointers, because that is what the repository returns and copying every row
	// to change that would be work nobody asked for.
	Data []*model.BlogPost `json:"data"`
	// Where this page sits in the full result set.
	Pagination Pagination `json:"pagination"`
}

// Query parameters for BlogPost.List.
type BlogPostListQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// Query parameters for BlogPost.Search.
type BlogPostSearchQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// The request body for BlogPost.Search.
type BlogPostSearchBody struct {
	// Conditions rows must satisfy.
	Filter model.BlogPostFilter `json:"filter"`
}

// Query parameters for BlogPost.ListDeleted.
type BlogPostListDeletedQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// Path parameters for BlogPost.Delete.
type BlogPostDeletePath struct {
	// Identifier of the BlogPost.
	ID uuid.UUID `json:"id"`
}

// Path parameters for BlogPost.Get.
type BlogPostGetPath struct {
	// Identifier of the BlogPost.
	ID uuid.UUID `json:"id"`
}

// Path parameters for BlogPost.Update.
type BlogPostUpdatePath struct {
	// Identifier of the BlogPost.
	ID uuid.UUID `json:"id"`
}

// Path parameters for BlogPost.Restore.
type BlogPostRestorePath struct {
	// Identifier of the BlogPost.
	ID uuid.UUID `json:"id"`
}
