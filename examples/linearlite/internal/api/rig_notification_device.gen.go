// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"github.com/google/uuid"
	"github.com/simonjanss/rig/examples/linearlite/internal/model"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// A page of RigNotificationDevices.
type RigNotificationDeviceListResponse struct {
	// The rows in this page.
	//
	// Pointers, because that is what the repository returns and copying every row
	// to change that would be work nobody asked for.
	Data []*model.RigNotificationDevice `json:"data"`
	// Where this page sits in the full result set.
	Pagination Pagination `json:"pagination"`
}

// Query parameters for RigNotificationDevice.List.
type RigNotificationDeviceListQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
	// How wide to read: own for the rignotificationdevices the caller created,
	// which is the default, or all for every one in the tenant. Asking for all
	// requires the rig_notification_device.read.all permission and is refused
	// without it, rather than quietly answering with fewer rows.
	Scope tenancy.Scope `json:"scope"`
}

// Path parameters for RigNotificationDevice.Delete.
type RigNotificationDeviceDeletePath struct {
	// Identifier of the RigNotificationDevice.
	ID uuid.UUID `json:"id"`
}
