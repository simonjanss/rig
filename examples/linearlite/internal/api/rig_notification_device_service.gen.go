// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"context"

	"github.com/google/uuid"
	"github.com/simonjanss/rig/examples/linearlite/internal/model"
	"github.com/simonjanss/rig/examples/linearlite/internal/store"
	"github.com/simonjanss/rig/runtime/dbhook"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// RigNotificationDeviceService is the interface your service layer implements.
//
// Embedding DefaultRigNotificationDeviceService satisfies all of it, so a
// resource with no business logic needs nothing but a constructor. Override a
// method to add a rule and delegate to the embedded default for the rest.
type RigNotificationDeviceService interface {
	// List RigNotificationDevices.
	List(ctx context.Context, r Request[struct{}, RigNotificationDeviceListQuery, struct{}]) (*RigNotificationDeviceListResponse, error)

	// Create a RigNotificationDevice.
	Create(ctx context.Context, r Request[struct{}, struct{}, model.RigNotificationDeviceCreateInput]) (*model.RigNotificationDevice, error)

	// Delete a RigNotificationDevice.
	Delete(ctx context.Context, r Request[RigNotificationDeviceDeletePath, struct{}, struct{}]) error
}

// RigNotificationDeviceWriter writes a RigNotificationDevice with the
// service's rules attached.
//
// Every write rig generates goes through it, and so should every write a
// custom endpoint makes: reaching for the repository directly means passing
// the hooks by hand, and forgetting once is a second way into the table where
// the rules do not run.
type RigNotificationDeviceWriter struct {
	repo  store.RigNotificationDeviceRepository
	hooks RigNotificationDeviceHooks
}

// NewRigNotificationDeviceWriter pairs a repository with the rules that apply
// to it.
func NewRigNotificationDeviceWriter(repo store.RigNotificationDeviceRepository, hooks RigNotificationDeviceHooks) RigNotificationDeviceWriter {
	return RigNotificationDeviceWriter{repo: repo, hooks: hooks}
}

// Create inserts a row, running the create rules and hooks.
func (w RigNotificationDeviceWriter) Create(ctx context.Context, in model.RigNotificationDeviceCreateInput) (*model.RigNotificationDevice, error) {
	return w.repo.Create(ctx, dbhook.Create[model.RigNotificationDeviceCreateInput, model.RigNotificationDevice]{Input: in, Hooks: w.hooks.Create})
}

// Update changes a row, running the update rules and hooks.
func (w RigNotificationDeviceWriter) Update(ctx context.Context, id uuid.UUID, in model.RigNotificationDeviceUpdateInput) (*model.RigNotificationDevice, error) {
	return w.repo.Update(ctx, id, dbhook.Update[model.RigNotificationDeviceUpdateInput, model.RigNotificationDevice]{Input: in, Hooks: w.hooks.Update})
}

// Delete removes a row, running the delete hooks.
func (w RigNotificationDeviceWriter) Delete(ctx context.Context, in model.RigNotificationDeviceDeleteInput) error {
	return w.repo.Delete(ctx, dbhook.Delete[model.RigNotificationDeviceDeleteInput, model.RigNotificationDevice]{Input: in, Hooks: w.hooks.Delete})
}

// DefaultRigNotificationDeviceService implements every operation.
//
// The generated ones it answers itself, by calling the repository. A custom
// endpoint it hands to the contract, which is where the only implementation
// there could be lives.
type DefaultRigNotificationDeviceService struct {
	repo     store.RigNotificationDeviceRepository
	contract RigNotificationDeviceContract
	// write is the same writer a custom endpoint gets through Writer, so the
	// generated operations and the hand-written ones take one path.
	write RigNotificationDeviceWriter
}

// RigNotificationDeviceContract is what the service layer owes
// RigNotificationDevice: everything about it the schema cannot describe.
//
// The members are optional in the sense that an empty one runs nothing. None
// of them is optional in the sense of being skippable: the constructor takes
// this value, so a resource with no rules has an empty literal somebody can
// read rather than an absence nobody can see.
type RigNotificationDeviceContract struct {
	// Hooks is everything that happens around a write: the rules it is checked
	// against, and the callbacks that run with it. One set per operation, because
	// the rules for creating a row and for changing one are different questions
	// asked about different fields.
	Hooks RigNotificationDeviceHooks
}

// RigNotificationDeviceHooks are the callbacks that run around each generated
// write.
//
// Before and After run inside the write's transaction, so returning an error
// from either undoes the write. AfterCommit runs once it has landed, which is
// where anything outside the database belongs.
type RigNotificationDeviceHooks struct {
	// Read shapes what a read answers with. It is the one set that is not about a
	// write, and the only one that runs outside the repository — a write reads
	// the row it is about to change, and narrowing that read would have the update
	// judge a row that is not the stored one.
	Read dbhook.ReadHooks[model.RigNotificationDeviceFilter, model.RigNotificationDevice]

	Create dbhook.CreateHooks[model.RigNotificationDeviceCreateInput, model.RigNotificationDevice]
	Update dbhook.UpdateHooks[model.RigNotificationDeviceUpdateInput, model.RigNotificationDevice]
	Delete dbhook.DeleteHooks[model.RigNotificationDeviceDeleteInput, model.RigNotificationDevice]
}

// RigNotificationDeviceRules is everything the service layer supplies about
// RigNotificationDevice.
//
// Implement it on a type of your own and hand it to
// NewRigNotificationDeviceService. It is an interface rather than a struct so
// that the rules and the endpoints are one value: a resource whose
// configuration declares an endpoint cannot be wired up without an
// implementation of it, and the failure is at the call to the constructor
// rather than on the route.
type RigNotificationDeviceRules interface {
	// Hooks is everything that happens around a write, plus what a read answers
	// with. It is called once, during construction.
	Hooks() RigNotificationDeviceHooks

	// Bind receives the writer built from those hooks, so a custom endpoint can
	// write through the same path a generated operation does.
	//
	// It is in the interface rather than looked for, so a service that never
	// writes says so with an empty body instead of finding out at runtime that a
	// misspelled method left it without one. rig calls it once, before anything
	// can reach a hook.
	Bind(RigNotificationDeviceWriter)
}

// NewRigNotificationDeviceService is the front door.
//
// It asks the rules what they are, builds the writer from that, and hands the
// writer back. The service layer never has to hold a half-built value or name
// the type it is part of.
func NewRigNotificationDeviceService(repo store.RigNotificationDeviceRepository, rules RigNotificationDeviceRules) DefaultRigNotificationDeviceService {
	svc := NewDefaultRigNotificationDeviceService(repo, RigNotificationDeviceContract{Hooks: rules.Hooks()})
	rules.Bind(svc.Writer())
	return svc
}

// NewDefaultRigNotificationDeviceService builds the default implementation.
//
// The contract is a parameter rather than a field left to default, because a
// rule nobody attached is a rule that does not run, and nothing at the call
// site would have said so. An empty RigNotificationDeviceContract is still
// allowed — it is just a thing somebody wrote down.
func NewDefaultRigNotificationDeviceService(repo store.RigNotificationDeviceRepository, contract RigNotificationDeviceContract) DefaultRigNotificationDeviceService {
	return DefaultRigNotificationDeviceService{repo: repo, contract: contract, write: NewRigNotificationDeviceWriter(repo, contract.Hooks)}
}

// Writer is how a custom endpoint writes.
//
// It is the repository with this service's own rules already attached, so a
// hand-written endpoint takes the path a generated one takes. Reaching for the
// repository instead means passing the hooks by hand, and forgetting once is a
// second way into the table where the rules do not run.
//
// It is a method rather than something the service layer holds because there
// is then nothing to wire, and nothing to wire wrongly.
func (s DefaultRigNotificationDeviceService) Writer() RigNotificationDeviceWriter { return s.write }

// readFilter combines the caller's filter with whatever the read hook narrows
// to.
//
// Nested rather than merged, because the two are combined with AND: a caller
// whose own filter is an OR cannot widen its way out of the one the service
// added.
func (s DefaultRigNotificationDeviceService) readFilter(ctx context.Context, claims tenancy.Claims, asked model.RigNotificationDeviceFilter) (model.RigNotificationDeviceFilter, error) {
	if s.contract.Hooks.Read.Narrow == nil {
		return asked, nil
	}

	narrowed, err := s.contract.Hooks.Read.Narrow(ctx, caller(claims))
	if err != nil {
		return asked, err
	}
	if narrowed == nil {
		return asked, nil
	}

	return model.RigNotificationDeviceFilter{NestedFilters: []model.RigNotificationDeviceFilter{*narrowed, asked}}, nil
}

// readRows runs the read hook over what a read is about to answer with.
func (s DefaultRigNotificationDeviceService) readRows(ctx context.Context, claims tenancy.Claims, rows []*model.RigNotificationDevice) error {
	if s.contract.Hooks.Read.Rows == nil {
		return nil
	}
	return s.contract.Hooks.Read.Rows(ctx, caller(claims), rows)
}

// List implements RigNotificationDeviceService.
func (s DefaultRigNotificationDeviceService) List(ctx context.Context, r Request[struct{}, RigNotificationDeviceListQuery, struct{}]) (*RigNotificationDeviceListResponse, error) {
	page := model.RigNotificationDevicePage{Limit: r.Query.Limit, Offset: r.Query.Offset}
	asked := model.NewRigNotificationDeviceFilter()
	filter, err := s.readFilter(ctx, r.Claims, asked)
	if err != nil {
		return nil, err
	}

	rows, total, err := s.repo.List(ctx, filter, page, readScope(r.Query.Scope)...)
	if err != nil {
		return nil, err
	}
	if err := s.readRows(ctx, r.Claims, rows); err != nil {
		return nil, err
	}

	return &RigNotificationDeviceListResponse{
		Data:       rows,
		Pagination: Pagination{Offset: page.Offset, Limit: page.Limit, Total: total},
	}, nil
}

// Create implements RigNotificationDeviceService.
func (s DefaultRigNotificationDeviceService) Create(ctx context.Context, r Request[struct{}, struct{}, model.RigNotificationDeviceCreateInput]) (*model.RigNotificationDevice, error) {
	return s.write.Create(ctx, r.Body)
}

// Delete implements RigNotificationDeviceService.
func (s DefaultRigNotificationDeviceService) Delete(ctx context.Context, r Request[RigNotificationDeviceDeletePath, struct{}, struct{}]) error {
	return s.write.Delete(ctx, model.RigNotificationDeviceDeleteInput{ID: r.Path.ID})
}
