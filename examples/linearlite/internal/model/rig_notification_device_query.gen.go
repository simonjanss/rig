// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on RigNotificationDevice fields.
type RigNotificationDeviceFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// Whose device it is.
	AccountID *uuid.UUID `json:"accountId,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel *RigNotificationChannel `json:"channel,omitempty"`
	// What the provider was given to address this device. Opaque to rig.
	Token *string `json:"token,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *string `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *time.Time `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *time.Time `json:"revokedAt,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterEquals `json:"account,omitempty"`
}

// Ordering conditions on RigNotificationDevice fields that can be compared.
type RigNotificationDeviceFilterRange struct {
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *time.Time `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *time.Time `json:"revokedAt,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterRange `json:"account,omitempty"`
}

// Set-membership conditions on RigNotificationDevice fields.
type RigNotificationDeviceFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// Whose device it is.
	AccountID []uuid.UUID `json:"accountId,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel []RigNotificationChannel `json:"channel,omitempty"`
	// What the provider was given to address this device. Opaque to rig.
	Token []string `json:"token,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label []string `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt []time.Time `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt []time.Time `json:"revokedAt,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterContains `json:"account,omitempty"`
}

// Pattern conditions on RigNotificationDevice text fields.
type RigNotificationDeviceFilterLike struct {
	// What the provider was given to address this device. Opaque to rig.
	Token *string `json:"token,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *string `json:"label,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterLike `json:"account,omitempty"`
}

// Presence conditions on nullable RigNotificationDevice fields.
type RigNotificationDeviceFilterNull struct {
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *bool `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *bool `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *bool `json:"revokedAt,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterNull `json:"account,omitempty"`
}

// Relations a RigNotificationDevice must have no matching row for.
type RigNotificationDeviceFilterWithout struct {
	// Match rows with no Account satisfying these conditions, including rows that
	// have none at all.
	//
	// This negates the existence of the related row rather than the comparison,
	// which is the whole difference from NotEquals.Account: that one needs the
	// Account to be there and to differ, so a row without one fails it and
	// satisfies this. An empty object asks for rows with no Account at all.
	Account *RigAccountFilter `json:"account,omitempty"`
}

// Conditions a RigNotificationDevice must satisfy to match a search.
type RigNotificationDeviceFilter struct {
	// Fields that must equal the given value.
	Equals *RigNotificationDeviceFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *RigNotificationDeviceFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *RigNotificationDeviceFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *RigNotificationDeviceFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *RigNotificationDeviceFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *RigNotificationDeviceFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *RigNotificationDeviceFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *RigNotificationDeviceFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *RigNotificationDeviceFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *RigNotificationDeviceFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *RigNotificationDeviceFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *RigNotificationDeviceFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []RigNotificationDeviceFilter `json:"nestedFilters,omitempty"`
	// Relations with no row matching the given conditions. An empty one asks for
	// no related row at all.
	//
	// This is where a negation belongs when what you mean is "not": it negates
	// whether a matching related row exists, so a row with no related row at all
	// satisfies it. The negative operators above negate the comparison instead and
	// still require the related row to be there — "no related row called X" is
	// here, "a related row not called X" is NotEquals.
	Without *RigNotificationDeviceFilterWithout `json:"without,omitempty"`
}

// RigNotificationDevicePage says which slice of a result to return, and in
// what order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type RigNotificationDevicePage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []RigNotificationDeviceOrder
}

// RigNotificationDeviceOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type RigNotificationDeviceOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a RigNotificationDevice supports.
var (
	RigNotificationDeviceOrderIDAsc          = RigNotificationDeviceOrder{Column: "id"}
	RigNotificationDeviceOrderIDDesc         = RigNotificationDeviceOrder{Column: "id", Desc: true}
	RigNotificationDeviceOrderTenantIDAsc    = RigNotificationDeviceOrder{Column: "tenant_id"}
	RigNotificationDeviceOrderTenantIDDesc   = RigNotificationDeviceOrder{Column: "tenant_id", Desc: true}
	RigNotificationDeviceOrderAccountIDAsc   = RigNotificationDeviceOrder{Column: "account_id"}
	RigNotificationDeviceOrderAccountIDDesc  = RigNotificationDeviceOrder{Column: "account_id", Desc: true}
	RigNotificationDeviceOrderCreatedAtAsc   = RigNotificationDeviceOrder{Column: "created_at"}
	RigNotificationDeviceOrderCreatedAtDesc  = RigNotificationDeviceOrder{Column: "created_at", Desc: true}
	RigNotificationDeviceOrderUpdatedAtAsc   = RigNotificationDeviceOrder{Column: "updated_at"}
	RigNotificationDeviceOrderUpdatedAtDesc  = RigNotificationDeviceOrder{Column: "updated_at", Desc: true}
	RigNotificationDeviceOrderChannelAsc     = RigNotificationDeviceOrder{Column: "channel"}
	RigNotificationDeviceOrderChannelDesc    = RigNotificationDeviceOrder{Column: "channel", Desc: true}
	RigNotificationDeviceOrderTokenAsc       = RigNotificationDeviceOrder{Column: "token"}
	RigNotificationDeviceOrderTokenDesc      = RigNotificationDeviceOrder{Column: "token", Desc: true}
	RigNotificationDeviceOrderLabelAsc       = RigNotificationDeviceOrder{Column: "label"}
	RigNotificationDeviceOrderLabelDesc      = RigNotificationDeviceOrder{Column: "label", Desc: true}
	RigNotificationDeviceOrderLastSeenAtAsc  = RigNotificationDeviceOrder{Column: "last_seen_at"}
	RigNotificationDeviceOrderLastSeenAtDesc = RigNotificationDeviceOrder{Column: "last_seen_at", Desc: true}
	RigNotificationDeviceOrderRevokedAtAsc   = RigNotificationDeviceOrder{Column: "revoked_at"}
	RigNotificationDeviceOrderRevokedAtDesc  = RigNotificationDeviceOrder{Column: "revoked_at", Desc: true}
)

// RigNotificationDeviceOrderAccount orders a RigNotificationDevice by a column
// of its Account.
//
// Take the term from RigAccount's own orderings:
//
//	RigNotificationDeviceOrderAccount(RigAccountOrderIDDesc)
//
// The repository reaches it with a left join, so a RigNotificationDevice with
// no Account sorts to one end rather than vanishing. Only RigAccount's own
// columns can be used; an ordering that itself reaches across a relation is
// refused, because one join is where this stops.
func RigNotificationDeviceOrderAccount(o RigAccountOrder) RigNotificationDeviceOrder {
	return RigNotificationDeviceOrder{Relation: "Account", Column: o.Column, Desc: o.Desc}
}

// NewRigNotificationDeviceFilterEquals builds an empty
// RigNotificationDeviceFilterEquals to fill in.
func NewRigNotificationDeviceFilterEquals() *RigNotificationDeviceFilterEquals {
	return &RigNotificationDeviceFilterEquals{}
}

// NewRigNotificationDeviceFilterRange builds an empty
// RigNotificationDeviceFilterRange to fill in.
func NewRigNotificationDeviceFilterRange() *RigNotificationDeviceFilterRange {
	return &RigNotificationDeviceFilterRange{}
}

// NewRigNotificationDeviceFilterContains builds an empty
// RigNotificationDeviceFilterContains to fill in.
func NewRigNotificationDeviceFilterContains() *RigNotificationDeviceFilterContains {
	return &RigNotificationDeviceFilterContains{}
}

// NewRigNotificationDeviceFilterLike builds an empty
// RigNotificationDeviceFilterLike to fill in.
func NewRigNotificationDeviceFilterLike() *RigNotificationDeviceFilterLike {
	return &RigNotificationDeviceFilterLike{}
}

// NewRigNotificationDeviceFilterNull builds an empty
// RigNotificationDeviceFilterNull to fill in.
func NewRigNotificationDeviceFilterNull() *RigNotificationDeviceFilterNull {
	return &RigNotificationDeviceFilterNull{}
}

// NewRigNotificationDeviceFilterWithout builds an empty
// RigNotificationDeviceFilterWithout to fill in.
func NewRigNotificationDeviceFilterWithout() *RigNotificationDeviceFilterWithout {
	return &RigNotificationDeviceFilterWithout{}
}

// NewRigNotificationDeviceFilter builds an empty filter, which matches every
// row the caller is allowed to see.
func NewRigNotificationDeviceFilter() RigNotificationDeviceFilter {
	return RigNotificationDeviceFilter{}
}
