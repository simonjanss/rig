// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"strings"
)

// What somebody is doing with the thing they are on, as far as their own
// client can tell.
type PresenceActivity string

// The values of PresenceActivity.
const (
	// Looking at it. The default, and what a heartbeat says when nothing else was
	// claimed.
	PresenceActivityViewing PresenceActivity = "viewing"
	// Typing into it. What draws the ring on a control somebody else has open, and
	// the only reason this column exists rather than presence being a row per
	// person.
	PresenceActivityEditing PresenceActivity = "editing"
)

// AllPresenceActivity is every value, in declaration order.
var AllPresenceActivity = []PresenceActivity{PresenceActivityViewing, PresenceActivityEditing}

// Valid reports whether the value is one the database will accept.
func (v PresenceActivity) Valid() bool {
	switch v {
	case PresenceActivityViewing, PresenceActivityEditing:
		return true
	default:
		return false
	}
}

// ParsePresenceActivity reads a value, accepting any casing and surrounding
// space.
//
// Normalization uses it, so "IN_PROGRESS" from one client and "in_progress"
// from another mean the same thing rather than one of them being a validation
// failure nobody can explain. The spelling still has to be the label’s: a
// value is a name the database knows, not a phrase.
func ParsePresenceActivity(s string) (PresenceActivity, bool) {
	switch strings.ToLower(strings.TrimSpace(s)) {
	case "viewing":
		return PresenceActivityViewing, true
	case "editing":
		return PresenceActivityEditing, true
	default:
		return "", false
	}
}

// String implements fmt.Stringer.
func (v PresenceActivity) String() string { return string(v) }
