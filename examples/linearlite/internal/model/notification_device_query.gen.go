// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on NotificationDevice fields.
type NotificationDeviceFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// Whose device it is.
	AccountID *uuid.UUID `json:"accountId,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel *NotificationChannel `json:"channel,omitempty"`
	// What the provider was given to address this device. Opaque to rig.
	Token *string `json:"token,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *string `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *time.Time `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *time.Time `json:"revokedAt,omitempty"`
}

// Ordering conditions on NotificationDevice fields that can be compared.
type NotificationDeviceFilterRange struct {
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *time.Time `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *time.Time `json:"revokedAt,omitempty"`
}

// Set-membership conditions on NotificationDevice fields.
type NotificationDeviceFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// Whose device it is.
	AccountID []uuid.UUID `json:"accountId,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel []NotificationChannel `json:"channel,omitempty"`
	// What the provider was given to address this device. Opaque to rig.
	Token []string `json:"token,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label []string `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt []time.Time `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt []time.Time `json:"revokedAt,omitempty"`
}

// Pattern conditions on NotificationDevice text fields.
type NotificationDeviceFilterLike struct {
	// What the provider was given to address this device. Opaque to rig.
	Token *string `json:"token,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *string `json:"label,omitempty"`
}

// Presence conditions on nullable NotificationDevice fields.
type NotificationDeviceFilterNull struct {
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *bool `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *bool `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *bool `json:"revokedAt,omitempty"`
}

// Conditions a NotificationDevice must satisfy to match a search.
type NotificationDeviceFilter struct {
	// Fields that must equal the given value.
	Equals *NotificationDeviceFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *NotificationDeviceFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *NotificationDeviceFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *NotificationDeviceFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *NotificationDeviceFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *NotificationDeviceFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *NotificationDeviceFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *NotificationDeviceFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *NotificationDeviceFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *NotificationDeviceFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *NotificationDeviceFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *NotificationDeviceFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []NotificationDeviceFilter `json:"nestedFilters,omitempty"`
}

// NotificationDevicePage says which slice of a result to return, and in what
// order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type NotificationDevicePage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []NotificationDeviceOrder
}

// NotificationDeviceOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type NotificationDeviceOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a NotificationDevice supports.
var (
	NotificationDeviceOrderIDAsc          = NotificationDeviceOrder{Column: "id"}
	NotificationDeviceOrderIDDesc         = NotificationDeviceOrder{Column: "id", Desc: true}
	NotificationDeviceOrderTenantIDAsc    = NotificationDeviceOrder{Column: "tenant_id"}
	NotificationDeviceOrderTenantIDDesc   = NotificationDeviceOrder{Column: "tenant_id", Desc: true}
	NotificationDeviceOrderAccountIDAsc   = NotificationDeviceOrder{Column: "account_id"}
	NotificationDeviceOrderAccountIDDesc  = NotificationDeviceOrder{Column: "account_id", Desc: true}
	NotificationDeviceOrderCreatedAtAsc   = NotificationDeviceOrder{Column: "created_at"}
	NotificationDeviceOrderCreatedAtDesc  = NotificationDeviceOrder{Column: "created_at", Desc: true}
	NotificationDeviceOrderUpdatedAtAsc   = NotificationDeviceOrder{Column: "updated_at"}
	NotificationDeviceOrderUpdatedAtDesc  = NotificationDeviceOrder{Column: "updated_at", Desc: true}
	NotificationDeviceOrderChannelAsc     = NotificationDeviceOrder{Column: "channel"}
	NotificationDeviceOrderChannelDesc    = NotificationDeviceOrder{Column: "channel", Desc: true}
	NotificationDeviceOrderTokenAsc       = NotificationDeviceOrder{Column: "token"}
	NotificationDeviceOrderTokenDesc      = NotificationDeviceOrder{Column: "token", Desc: true}
	NotificationDeviceOrderLabelAsc       = NotificationDeviceOrder{Column: "label"}
	NotificationDeviceOrderLabelDesc      = NotificationDeviceOrder{Column: "label", Desc: true}
	NotificationDeviceOrderLastSeenAtAsc  = NotificationDeviceOrder{Column: "last_seen_at"}
	NotificationDeviceOrderLastSeenAtDesc = NotificationDeviceOrder{Column: "last_seen_at", Desc: true}
	NotificationDeviceOrderRevokedAtAsc   = NotificationDeviceOrder{Column: "revoked_at"}
	NotificationDeviceOrderRevokedAtDesc  = NotificationDeviceOrder{Column: "revoked_at", Desc: true}
)

// NewNotificationDeviceFilterEquals builds an empty
// NotificationDeviceFilterEquals to fill in.
func NewNotificationDeviceFilterEquals() *NotificationDeviceFilterEquals {
	return &NotificationDeviceFilterEquals{}
}

// NewNotificationDeviceFilterRange builds an empty
// NotificationDeviceFilterRange to fill in.
func NewNotificationDeviceFilterRange() *NotificationDeviceFilterRange {
	return &NotificationDeviceFilterRange{}
}

// NewNotificationDeviceFilterContains builds an empty
// NotificationDeviceFilterContains to fill in.
func NewNotificationDeviceFilterContains() *NotificationDeviceFilterContains {
	return &NotificationDeviceFilterContains{}
}

// NewNotificationDeviceFilterLike builds an empty NotificationDeviceFilterLike
// to fill in.
func NewNotificationDeviceFilterLike() *NotificationDeviceFilterLike {
	return &NotificationDeviceFilterLike{}
}

// NewNotificationDeviceFilterNull builds an empty NotificationDeviceFilterNull
// to fill in.
func NewNotificationDeviceFilterNull() *NotificationDeviceFilterNull {
	return &NotificationDeviceFilterNull{}
}

// NewNotificationDeviceFilter builds an empty filter, which matches every row
// the caller is allowed to see.
func NewNotificationDeviceFilter() NotificationDeviceFilter { return NotificationDeviceFilter{} }
