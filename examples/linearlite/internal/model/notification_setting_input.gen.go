// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"context"
	"strings"
	"time"

	"github.com/google/uuid"
	"github.com/simonjanss/rig/runtime/patch"
	"github.com/simonjanss/rig/runtime/rigerr"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// NotificationSettingCreateInput is what creating a NotificationSetting takes.
//
// The identifier, the tenant, and the audit columns are absent: those are
// stamped by the repository from the request's claims.
type NotificationSettingCreateInput struct {
	// Whose preference it is.
	AccountID uuid.UUID `json:"accountId"`
	// Null is the default for the channel, which is the second step of the
	// resolution.
	Kind *string `json:"kind"`
	// Which channel it answers for.
	Channel NotificationChannel `json:"channel"`
	// False writes no delivery row at all. Not the same as digest Off, which is
	// about preferring to look rather than be told.
	IsEnabled bool `json:"isEnabled"`
	// Immediate sends each on its own; anything else waits for the window and goes
	// out as one message.
	Digest NotificationDigest `json:"digest"`
	// When to start delivering, in the account time zone. Null means all day. A
	// window that wraps midnight is ordinary: 22:00 to 06:00 is how somebody says
	// "not overnight".
	ActiveFrom *time.Time `json:"activeFrom"`
	// When to stop. Outside the window a delivery is held, not dropped: the inbox
	// line exists either way, and a discarded copy makes the badge and the mailbox
	// disagree.
	ActiveUntil *time.Time `json:"activeUntil"`
	// ISO weekdays, 1 being Monday. Empty means every day. An array rather than a
	// bitmask because [1,2,3,4,5] is legible in a settings payload and 62 is not.
	ActiveDays []int16 `json:"activeDays"`
}

// Normalize tidies what was given before anything checks it.
//
// It runs first so that validation sees the value that will actually be
// stored: a title with a trailing space and one without are the same title,
// and rejecting the second for a length rule the first passes would be
// indefensible.
func (i *NotificationSettingCreateInput) Normalize() {
	if i.Kind != nil {
		*i.Kind = strings.TrimSpace(*i.Kind)
	}
	if v, ok := ParseNotificationChannel(string(i.Channel)); ok {
		i.Channel = v
	}
	if i.IsEnabled == false {
		i.IsEnabled = true
	}
	if v, ok := ParseNotificationDigest(string(i.Digest)); ok {
		i.Digest = v
	}
	if i.Digest == "" {
		i.Digest = NotificationDigestImmediate
	}
}

// NotificationSettingCreateInputError says what was wrong with each field of a
// NotificationSettingCreateInput.
//
// Its shape is the input's shape, so a client can attach every message to the
// field it is about without matching on strings. A member is nil when that
// field was fine, and the whole value is nil when the input was. It is what
// the 422 carries.
type NotificationSettingCreateInputError struct {
	// Whose preference it is.
	AccountID *rigerr.FieldError `json:"accountId,omitempty"`
	// Null is the default for the channel, which is the second step of the
	// resolution.
	Kind *rigerr.FieldError `json:"kind,omitempty"`
	// Which channel it answers for.
	Channel *rigerr.FieldError `json:"channel,omitempty"`
	// False writes no delivery row at all. Not the same as digest Off, which is
	// about preferring to look rather than be told.
	IsEnabled *rigerr.FieldError `json:"isEnabled,omitempty"`
	// Immediate sends each on its own; anything else waits for the window and goes
	// out as one message.
	Digest *rigerr.FieldError `json:"digest,omitempty"`
	// When to start delivering, in the account time zone. Null means all day. A
	// window that wraps midnight is ordinary: 22:00 to 06:00 is how somebody says
	// "not overnight".
	ActiveFrom *rigerr.FieldError `json:"activeFrom,omitempty"`
	// When to stop. Outside the window a delivery is held, not dropped: the inbox
	// line exists either way, and a discarded copy makes the badge and the mailbox
	// disagree.
	ActiveUntil *rigerr.FieldError `json:"activeUntil,omitempty"`
	// ISO weekdays, 1 being Monday. Empty means every day. An array rather than a
	// bitmask because [1,2,3,4,5] is legible in a settings payload and 62 is not.
	ActiveDays *rigerr.FieldError `json:"activeDays,omitempty"`

	// Entity is a problem with the row as a whole rather than with one field: what
	// the Entity rule said.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// Empty reports whether anything went wrong. A validator that found nothing
// returns nil rather than one of these.
func (e *NotificationSettingCreateInputError) Empty() bool {
	if e == nil {
		return true
	}

	return e.AccountID == nil && e.Kind == nil && e.Channel == nil && e.IsEnabled == nil && e.Digest == nil && e.ActiveFrom == nil && e.ActiveUntil == nil && e.ActiveDays == nil && e.Entity == nil
}

// Error implements error. The sentence is for logs and for a person; the
// structure above is what a client acts on.
func (e *NotificationSettingCreateInputError) Error() string {
	var parts []string
	if e.AccountID != nil {
		parts = append(parts, "accountId "+e.AccountID.Error())
	}
	if e.Kind != nil {
		parts = append(parts, "kind "+e.Kind.Error())
	}
	if e.Channel != nil {
		parts = append(parts, "channel "+e.Channel.Error())
	}
	if e.IsEnabled != nil {
		parts = append(parts, "isEnabled "+e.IsEnabled.Error())
	}
	if e.Digest != nil {
		parts = append(parts, "digest "+e.Digest.Error())
	}
	if e.ActiveFrom != nil {
		parts = append(parts, "activeFrom "+e.ActiveFrom.Error())
	}
	if e.ActiveUntil != nil {
		parts = append(parts, "activeUntil "+e.ActiveUntil.Error())
	}
	if e.ActiveDays != nil {
		parts = append(parts, "activeDays "+e.ActiveDays.Error())
	}
	if e.Entity != nil {
		parts = append(parts, e.Entity.Error())
	}

	return "rig_notification_setting is not valid: " + strings.Join(parts, "; ")
}

// ErrorCode implements [rigerr.Coder]: the request was understood and its
// content is what is wrong, which is 422 and not 400.
func (e *NotificationSettingCreateInputError) ErrorCode() rigerr.Code {
	return rigerr.CodeUnprocessableEntity
}

// ErrorFields implements [rigerr.FieldReporter], which is how the HTTP layer
// finds this and answers with it rather than with prose.
func (e *NotificationSettingCreateInputError) ErrorFields() any { return e }

// Validate checks what the schema can decide on its own.
//
// Everything a column declares — NOT NULL, a length, an enumeration's values
// — is checked here, so a service only writes the rules that are actually
// about the business. Every field is checked before returning, because a form
// that reports one problem per round trip is a form people give up on.
//
// What comes back is a *NotificationSettingCreateInputError, shaped like the
// input itself.
func (i *NotificationSettingCreateInput) Validate() error {
	var failed NotificationSettingCreateInputError

	if !i.Channel.Valid() {
		failed.Channel = rigerr.NewFieldError(rigerr.FieldCodeInvalidValue, "%q is not one of the allowed values", i.Channel)
	}
	if !i.Digest.Valid() {
		failed.Digest = rigerr.NewFieldError(rigerr.FieldCodeInvalidValue, "%q is not one of the allowed values", i.Digest)
	}

	if failed.Empty() {
		return nil
	}
	return &failed
}

// NotificationSettingUpdateInput is what changing a NotificationSetting takes.
//
// A field left out is untouched. A nullable field set to null is cleared —
// which is why the two wrappers differ: a column that cannot hold null has no
// way to be given one, so clearing it is a compile error rather than a
// rejection at runtime. Immutable fields are not here at all.
type NotificationSettingUpdateInput struct {
	// Whose preference it is.
	AccountID patch.Optional[uuid.UUID] `json:"accountId"`
	// Null is the default for the channel, which is the second step of the
	// resolution.
	Kind patch.Nullable[string] `json:"kind"`
	// Which channel it answers for.
	Channel patch.Optional[NotificationChannel] `json:"channel"`
	// False writes no delivery row at all. Not the same as digest Off, which is
	// about preferring to look rather than be told.
	IsEnabled patch.Optional[bool] `json:"isEnabled"`
	// Immediate sends each on its own; anything else waits for the window and goes
	// out as one message.
	Digest patch.Optional[NotificationDigest] `json:"digest"`
	// When to start delivering, in the account time zone. Null means all day. A
	// window that wraps midnight is ordinary: 22:00 to 06:00 is how somebody says
	// "not overnight".
	ActiveFrom patch.Nullable[time.Time] `json:"activeFrom"`
	// When to stop. Outside the window a delivery is held, not dropped: the inbox
	// line exists either way, and a discarded copy makes the badge and the mailbox
	// disagree.
	ActiveUntil patch.Nullable[time.Time] `json:"activeUntil"`
	// ISO weekdays, 1 being Monday. Empty means every day. An array rather than a
	// bitmask because [1,2,3,4,5] is legible in a settings payload and 62 is not.
	ActiveDays patch.Optional[[]int16] `json:"activeDays"`
}

// Normalize tidies the fields this request actually carries.
//
// It does not fill in the ones it does not: the repository writes exactly the
// columns that were sent, and filling them here would turn every update into a
// write of every column — so two requests changing different fields of one
// row would start overwriting each other instead of composing.
func (i *NotificationSettingUpdateInput) Normalize() {
	if v, ok := i.Kind.Get(); ok {
		v = strings.TrimSpace(v)
		i.Kind = patch.NewNullable(v)
	}
	if v, ok := i.Channel.Get(); ok {
		if parsed, ok := ParseNotificationChannel(string(v)); ok {
			v = parsed
		}
		i.Channel = patch.NewOptional(v)
	}
	if v, ok := i.Digest.Get(); ok {
		if parsed, ok := ParseNotificationDigest(string(v)); ok {
			v = parsed
		}
		i.Digest = patch.NewOptional(v)
	}
}

// Merged is the row as it will be once this update is applied.
//
// It is what validation runs against, and the reason it exists: a rule
// spanning two fields cannot be checked from a partial request. "Ends after
// starts" is unanswerable when only one of them was sent.
//
// It returns a copy. The input keeps its patches, so the repository still
// writes only the columns that were actually given.
func (i NotificationSettingUpdateInput) Merged(prev *NotificationSetting) NotificationSetting {
	out := *prev

	if v, ok := i.AccountID.Get(); ok {
		out.AccountID = v
	}
	if i.Kind.Touched() {
		out.Kind = i.Kind.Ptr()
	}
	if v, ok := i.Channel.Get(); ok {
		out.Channel = v
	}
	if v, ok := i.IsEnabled.Get(); ok {
		out.IsEnabled = v
	}
	if v, ok := i.Digest.Get(); ok {
		out.Digest = v
	}
	if i.ActiveFrom.Touched() {
		out.ActiveFrom = i.ActiveFrom.Ptr()
	}
	if i.ActiveUntil.Touched() {
		out.ActiveUntil = i.ActiveUntil.Ptr()
	}
	if v, ok := i.ActiveDays.Get(); ok {
		out.ActiveDays = v
	}

	return out
}

// NotificationSettingUpdateInputError says what was wrong with each field of a
// NotificationSettingUpdateInput.
//
// Its shape is the input's shape, so a client can attach every message to the
// field it is about without matching on strings. A member is nil when that
// field was fine, and the whole value is nil when the input was. It is what
// the 422 carries.
type NotificationSettingUpdateInputError struct {
	// Whose preference it is.
	AccountID *rigerr.FieldError `json:"accountId,omitempty"`
	// Null is the default for the channel, which is the second step of the
	// resolution.
	Kind *rigerr.FieldError `json:"kind,omitempty"`
	// Which channel it answers for.
	Channel *rigerr.FieldError `json:"channel,omitempty"`
	// False writes no delivery row at all. Not the same as digest Off, which is
	// about preferring to look rather than be told.
	IsEnabled *rigerr.FieldError `json:"isEnabled,omitempty"`
	// Immediate sends each on its own; anything else waits for the window and goes
	// out as one message.
	Digest *rigerr.FieldError `json:"digest,omitempty"`
	// When to start delivering, in the account time zone. Null means all day. A
	// window that wraps midnight is ordinary: 22:00 to 06:00 is how somebody says
	// "not overnight".
	ActiveFrom *rigerr.FieldError `json:"activeFrom,omitempty"`
	// When to stop. Outside the window a delivery is held, not dropped: the inbox
	// line exists either way, and a discarded copy makes the badge and the mailbox
	// disagree.
	ActiveUntil *rigerr.FieldError `json:"activeUntil,omitempty"`
	// ISO weekdays, 1 being Monday. Empty means every day. An array rather than a
	// bitmask because [1,2,3,4,5] is legible in a settings payload and 62 is not.
	ActiveDays *rigerr.FieldError `json:"activeDays,omitempty"`

	// Entity is a problem with the row as a whole rather than with one field: what
	// the Entity rule said.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// Empty reports whether anything went wrong. A validator that found nothing
// returns nil rather than one of these.
func (e *NotificationSettingUpdateInputError) Empty() bool {
	if e == nil {
		return true
	}

	return e.AccountID == nil && e.Kind == nil && e.Channel == nil && e.IsEnabled == nil && e.Digest == nil && e.ActiveFrom == nil && e.ActiveUntil == nil && e.ActiveDays == nil && e.Entity == nil
}

// Error implements error. The sentence is for logs and for a person; the
// structure above is what a client acts on.
func (e *NotificationSettingUpdateInputError) Error() string {
	var parts []string
	if e.AccountID != nil {
		parts = append(parts, "accountId "+e.AccountID.Error())
	}
	if e.Kind != nil {
		parts = append(parts, "kind "+e.Kind.Error())
	}
	if e.Channel != nil {
		parts = append(parts, "channel "+e.Channel.Error())
	}
	if e.IsEnabled != nil {
		parts = append(parts, "isEnabled "+e.IsEnabled.Error())
	}
	if e.Digest != nil {
		parts = append(parts, "digest "+e.Digest.Error())
	}
	if e.ActiveFrom != nil {
		parts = append(parts, "activeFrom "+e.ActiveFrom.Error())
	}
	if e.ActiveUntil != nil {
		parts = append(parts, "activeUntil "+e.ActiveUntil.Error())
	}
	if e.ActiveDays != nil {
		parts = append(parts, "activeDays "+e.ActiveDays.Error())
	}
	if e.Entity != nil {
		parts = append(parts, e.Entity.Error())
	}

	return "rig_notification_setting is not valid: " + strings.Join(parts, "; ")
}

// ErrorCode implements [rigerr.Coder]: the request was understood and its
// content is what is wrong, which is 422 and not 400.
func (e *NotificationSettingUpdateInputError) ErrorCode() rigerr.Code {
	return rigerr.CodeUnprocessableEntity
}

// ErrorFields implements [rigerr.FieldReporter], which is how the HTTP layer
// finds this and answers with it rather than with prose.
func (e *NotificationSettingUpdateInputError) ErrorFields() any { return e }

// Validate checks the row this update would produce.
//
// Against the merged state, not the request: a length rule on a field nobody
// sent still has to hold, and a rule about two fields needs both.
//
// What comes back is a *NotificationSettingUpdateInputError, shaped like the
// input itself.
func (i *NotificationSettingUpdateInput) Validate(prev *NotificationSetting) error {
	var failed NotificationSettingUpdateInputError

	merged := i.Merged(prev)

	if !merged.Channel.Valid() {
		failed.Channel = rigerr.NewFieldError(rigerr.FieldCodeInvalidValue, "%q is not one of the allowed values", merged.Channel)
	}
	if !merged.Digest.Valid() {
		failed.Digest = rigerr.NewFieldError(rigerr.FieldCodeInvalidValue, "%q is not one of the allowed values", merged.Digest)
	}

	if failed.Empty() {
		return nil
	}
	return &failed
}

// NotificationSettingDeleteInput is what deleting a NotificationSetting takes.
type NotificationSettingDeleteInput struct {
	// ID is the row to delete.
	ID uuid.UUID `json:"id"`
}

// NotificationSettingValidatorContext is what a rule sees.
//
// Values is the row as it will be if this goes through — merged from the
// previous state on an update, so every field is set whether or not the
// request mentioned it. That is the point: "ends after starts" cannot be
// answered from a request that only carried one of them.
type NotificationSettingValidatorContext struct {
	// Values is the intended end state.
	Values NotificationSetting

	// Claims are who is asking. They are a value rather than something to fetch
	// from the context because a rule that has to look them up is a rule that can
	// forget to, and because there is no case where they are absent: a write
	// without a caller is refused by the repository before any rule runs.
	Claims tenancy.Claims

	// previous is the row before this change, and is the zero value on a
	// create — there was nothing before.
	previous NotificationSetting
	isUpdate bool
	changed  map[string]bool
}

// IsUpdate reports whether there was a row before this.
func (c *NotificationSettingValidatorContext) IsUpdate() bool { return c.isUpdate }

// Previous is the row as it was, and the zero value on a create. Check
// IsUpdate before reading it.
func (c *NotificationSettingValidatorContext) Previous() NotificationSetting { return c.previous }

// Changed reports whether this request carried a new value for a column.
//
// It is what keeps an expensive rule from running on every update: a check
// that reaches another service to confirm a reference only needs to run when
// the reference actually moved. On a create everything is changed, because
// everything is new.
func (c *NotificationSettingValidatorContext) Changed(column string) bool { return c.changed[column] }

// AccountIDChanged reports whether this request set account_id.
func (c *NotificationSettingValidatorContext) AccountIDChanged() bool {
	return c.changed[ColumnNotificationSettingAccountID]
}

// KindChanged reports whether this request set kind.
func (c *NotificationSettingValidatorContext) KindChanged() bool {
	return c.changed[ColumnNotificationSettingKind]
}

// ChannelChanged reports whether this request set channel.
func (c *NotificationSettingValidatorContext) ChannelChanged() bool {
	return c.changed[ColumnNotificationSettingChannel]
}

// IsEnabledChanged reports whether this request set is_enabled.
func (c *NotificationSettingValidatorContext) IsEnabledChanged() bool {
	return c.changed[ColumnNotificationSettingIsEnabled]
}

// DigestChanged reports whether this request set digest.
func (c *NotificationSettingValidatorContext) DigestChanged() bool {
	return c.changed[ColumnNotificationSettingDigest]
}

// ActiveFromChanged reports whether this request set active_from.
func (c *NotificationSettingValidatorContext) ActiveFromChanged() bool {
	return c.changed[ColumnNotificationSettingActiveFrom]
}

// ActiveUntilChanged reports whether this request set active_until.
func (c *NotificationSettingValidatorContext) ActiveUntilChanged() bool {
	return c.changed[ColumnNotificationSettingActiveUntil]
}

// ActiveDaysChanged reports whether this request set active_days.
func (c *NotificationSettingValidatorContext) ActiveDaysChanged() bool {
	return c.changed[ColumnNotificationSettingActiveDays]
}

// NotificationSettingCreateValidator is the rules for bringing a
// NotificationSetting into existence: what the schema cannot express.
//
// One optional function per field this operation can set, so the set of fields
// is the set of rules that could apply — a column an update cannot touch has
// no hook here to write by mistake. A nil one is skipped. Every configured
// hook runs, because a rule that fails should not hide the next one, so a
// request reports everything wrong with it.
//
// A hook returns a FieldError to attach the message to a specific field, or
// any other error to fail the request outright.
type NotificationSettingCreateValidator struct {
	// Whose preference it is.
	AccountID func(ctx context.Context, c *NotificationSettingValidatorContext, value uuid.UUID) error
	// Null is the default for the channel, which is the second step of the
	// resolution.
	Kind func(ctx context.Context, c *NotificationSettingValidatorContext, value *string) error
	// Which channel it answers for.
	Channel func(ctx context.Context, c *NotificationSettingValidatorContext, value NotificationChannel) error
	// False writes no delivery row at all. Not the same as digest Off, which is
	// about preferring to look rather than be told.
	IsEnabled func(ctx context.Context, c *NotificationSettingValidatorContext, value bool) error
	// Immediate sends each on its own; anything else waits for the window and goes
	// out as one message.
	Digest func(ctx context.Context, c *NotificationSettingValidatorContext, value NotificationDigest) error
	// When to start delivering, in the account time zone. Null means all day. A
	// window that wraps midnight is ordinary: 22:00 to 06:00 is how somebody says
	// "not overnight".
	ActiveFrom func(ctx context.Context, c *NotificationSettingValidatorContext, value *time.Time) error
	// When to stop. Outside the window a delivery is held, not dropped: the inbox
	// line exists either way, and a discarded copy makes the badge and the mailbox
	// disagree.
	ActiveUntil func(ctx context.Context, c *NotificationSettingValidatorContext, value *time.Time) error
	// ISO weekdays, 1 being Monday. Empty means every day. An array rather than a
	// bitmask because [1,2,3,4,5] is legible in a settings payload and 62 is not.
	ActiveDays func(ctx context.Context, c *NotificationSettingValidatorContext, value []int16) error

	// Entity runs after the per-field hooks, for a rule that is about the row
	// rather than about one column.
	Entity func(ctx context.Context, c *NotificationSettingValidatorContext) error
}

// RunCreate implements [dbhook.CreateValidator]: it runs the service's rules
// against the row this input would produce.
//
// The generated checks are not repeated here. The repository runs Normalize
// and Validate first, so by the time a hook sees the input it is tidy and the
// schema is satisfied — which is what lets a hook be about the business
// rather than about NOT NULL.
func (v NotificationSettingCreateValidator) RunCreate(ctx context.Context, claims tenancy.Claims, i *NotificationSettingCreateInput) error {
	// Everything is new, so everything counts as changed.
	c := &NotificationSettingValidatorContext{Claims: claims, changed: map[string]bool{}}
	c.Values.AccountID = i.AccountID
	c.changed[ColumnNotificationSettingAccountID] = true
	c.Values.Kind = i.Kind
	c.changed[ColumnNotificationSettingKind] = true
	c.Values.Channel = i.Channel
	c.changed[ColumnNotificationSettingChannel] = true
	c.Values.IsEnabled = i.IsEnabled
	c.changed[ColumnNotificationSettingIsEnabled] = true
	c.Values.Digest = i.Digest
	c.changed[ColumnNotificationSettingDigest] = true
	c.Values.ActiveFrom = i.ActiveFrom
	c.changed[ColumnNotificationSettingActiveFrom] = true
	c.Values.ActiveUntil = i.ActiveUntil
	c.changed[ColumnNotificationSettingActiveUntil] = true
	c.Values.ActiveDays = i.ActiveDays
	c.changed[ColumnNotificationSettingActiveDays] = true

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// run calls every configured hook and puts what each one said under the field
// it was about.
//
// Two kinds of answer. A [rigerr.FieldError] is about the input: it lands on
// the field and the others still run, so one request reports everything wrong
// with it. Anything else is the rule itself failing — a lookup that could
// not reach another service — and there is nothing to tell the caller about
// their input, so it comes back wrapped with the rule that could not be run,
// keeping whatever code it carried and becoming Internal if it carried none.
func (v NotificationSettingCreateValidator) run(ctx context.Context, c *NotificationSettingValidatorContext) (*NotificationSettingCreateInputError, error) {
	var failed NotificationSettingCreateInputError

	if v.AccountID != nil {
		if err := v.AccountID(ctx, c, c.Values.AccountID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate account_id")
			}
			failed.AccountID = field
		}
	}
	if v.Kind != nil {
		if err := v.Kind(ctx, c, c.Values.Kind); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate kind")
			}
			failed.Kind = field
		}
	}
	if v.Channel != nil {
		if err := v.Channel(ctx, c, c.Values.Channel); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate channel")
			}
			failed.Channel = field
		}
	}
	if v.IsEnabled != nil {
		if err := v.IsEnabled(ctx, c, c.Values.IsEnabled); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate is_enabled")
			}
			failed.IsEnabled = field
		}
	}
	if v.Digest != nil {
		if err := v.Digest(ctx, c, c.Values.Digest); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate digest")
			}
			failed.Digest = field
		}
	}
	if v.ActiveFrom != nil {
		if err := v.ActiveFrom(ctx, c, c.Values.ActiveFrom); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate active_from")
			}
			failed.ActiveFrom = field
		}
	}
	if v.ActiveUntil != nil {
		if err := v.ActiveUntil(ctx, c, c.Values.ActiveUntil); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate active_until")
			}
			failed.ActiveUntil = field
		}
	}
	if v.ActiveDays != nil {
		if err := v.ActiveDays(ctx, c, c.Values.ActiveDays); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate active_days")
			}
			failed.ActiveDays = field
		}
	}

	if v.Entity != nil {
		if err := v.Entity(ctx, c); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate rig_notification_setting")
			}
			failed.Entity = field
		}
	}

	if failed.Empty() {
		return nil, nil
	}
	return &failed, nil
}

// NotificationSettingUpdateValidator is the rules for changing one that
// already exists: what the schema cannot express.
//
// One optional function per field this operation can set, so the set of fields
// is the set of rules that could apply — a column an update cannot touch has
// no hook here to write by mistake. A nil one is skipped. Every configured
// hook runs, because a rule that fails should not hide the next one, so a
// request reports everything wrong with it.
//
// A hook returns a FieldError to attach the message to a specific field, or
// any other error to fail the request outright.
type NotificationSettingUpdateValidator struct {
	// Whose preference it is.
	AccountID func(ctx context.Context, c *NotificationSettingValidatorContext, value uuid.UUID) error
	// Null is the default for the channel, which is the second step of the
	// resolution.
	Kind func(ctx context.Context, c *NotificationSettingValidatorContext, value *string) error
	// Which channel it answers for.
	Channel func(ctx context.Context, c *NotificationSettingValidatorContext, value NotificationChannel) error
	// False writes no delivery row at all. Not the same as digest Off, which is
	// about preferring to look rather than be told.
	IsEnabled func(ctx context.Context, c *NotificationSettingValidatorContext, value bool) error
	// Immediate sends each on its own; anything else waits for the window and goes
	// out as one message.
	Digest func(ctx context.Context, c *NotificationSettingValidatorContext, value NotificationDigest) error
	// When to start delivering, in the account time zone. Null means all day. A
	// window that wraps midnight is ordinary: 22:00 to 06:00 is how somebody says
	// "not overnight".
	ActiveFrom func(ctx context.Context, c *NotificationSettingValidatorContext, value *time.Time) error
	// When to stop. Outside the window a delivery is held, not dropped: the inbox
	// line exists either way, and a discarded copy makes the badge and the mailbox
	// disagree.
	ActiveUntil func(ctx context.Context, c *NotificationSettingValidatorContext, value *time.Time) error
	// ISO weekdays, 1 being Monday. Empty means every day. An array rather than a
	// bitmask because [1,2,3,4,5] is legible in a settings payload and 62 is not.
	ActiveDays func(ctx context.Context, c *NotificationSettingValidatorContext, value []int16) error

	// Entity runs after the per-field hooks, for a rule that is about the row
	// rather than about one column.
	Entity func(ctx context.Context, c *NotificationSettingValidatorContext) error
}

// RunUpdate implements [dbhook.UpdateValidator]: it runs the service's rules
// against the row this update would produce, with the row as it was available
// for a rule about the change itself.
func (v NotificationSettingUpdateValidator) RunUpdate(ctx context.Context, claims tenancy.Claims, i *NotificationSettingUpdateInput, prev *NotificationSetting) error {
	c := &NotificationSettingValidatorContext{
		Values:   i.Merged(prev),
		Claims:   claims,
		previous: *prev,
		isUpdate: true,
		changed:  map[string]bool{},
	}
	c.changed[ColumnNotificationSettingAccountID] = i.AccountID.IsSet()
	c.changed[ColumnNotificationSettingKind] = i.Kind.Touched()
	c.changed[ColumnNotificationSettingChannel] = i.Channel.IsSet()
	c.changed[ColumnNotificationSettingIsEnabled] = i.IsEnabled.IsSet()
	c.changed[ColumnNotificationSettingDigest] = i.Digest.IsSet()
	c.changed[ColumnNotificationSettingActiveFrom] = i.ActiveFrom.Touched()
	c.changed[ColumnNotificationSettingActiveUntil] = i.ActiveUntil.Touched()
	c.changed[ColumnNotificationSettingActiveDays] = i.ActiveDays.IsSet()

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// run calls every configured hook and puts what each one said under the field
// it was about.
//
// Two kinds of answer. A [rigerr.FieldError] is about the input: it lands on
// the field and the others still run, so one request reports everything wrong
// with it. Anything else is the rule itself failing — a lookup that could
// not reach another service — and there is nothing to tell the caller about
// their input, so it comes back wrapped with the rule that could not be run,
// keeping whatever code it carried and becoming Internal if it carried none.
func (v NotificationSettingUpdateValidator) run(ctx context.Context, c *NotificationSettingValidatorContext) (*NotificationSettingUpdateInputError, error) {
	var failed NotificationSettingUpdateInputError

	if v.AccountID != nil {
		if err := v.AccountID(ctx, c, c.Values.AccountID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate account_id")
			}
			failed.AccountID = field
		}
	}
	if v.Kind != nil {
		if err := v.Kind(ctx, c, c.Values.Kind); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate kind")
			}
			failed.Kind = field
		}
	}
	if v.Channel != nil {
		if err := v.Channel(ctx, c, c.Values.Channel); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate channel")
			}
			failed.Channel = field
		}
	}
	if v.IsEnabled != nil {
		if err := v.IsEnabled(ctx, c, c.Values.IsEnabled); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate is_enabled")
			}
			failed.IsEnabled = field
		}
	}
	if v.Digest != nil {
		if err := v.Digest(ctx, c, c.Values.Digest); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate digest")
			}
			failed.Digest = field
		}
	}
	if v.ActiveFrom != nil {
		if err := v.ActiveFrom(ctx, c, c.Values.ActiveFrom); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate active_from")
			}
			failed.ActiveFrom = field
		}
	}
	if v.ActiveUntil != nil {
		if err := v.ActiveUntil(ctx, c, c.Values.ActiveUntil); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate active_until")
			}
			failed.ActiveUntil = field
		}
	}
	if v.ActiveDays != nil {
		if err := v.ActiveDays(ctx, c, c.Values.ActiveDays); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate active_days")
			}
			failed.ActiveDays = field
		}
	}

	if v.Entity != nil {
		if err := v.Entity(ctx, c); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate rig_notification_setting")
			}
			failed.Entity = field
		}
	}

	if failed.Empty() {
		return nil, nil
	}
	return &failed, nil
}
