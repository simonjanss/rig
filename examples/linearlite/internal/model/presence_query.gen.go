// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on Presence fields.
type PresenceFilterEquals struct {
	// The row's identifier. Nothing references it: a client addresses its own
	// presence by session key, so this identifier never leaves the server except
	// on the stream.
	ID *uuid.UUID `json:"id,omitempty"`
	// Who is present. Read from the credential and never from a request body,
	// which is what makes "you may only write your own presence" a sentence a
	// client cannot phrase rather than a rule somebody enforces.
	AccountID *uuid.UUID `json:"accountId,omitempty"`
	// Which tab this is. The browser names itself, and the name points at nothing:
	// one sign-in can have several tabs, and each is present separately rather
	// than overwriting the others.
	SessionKey *string `json:"sessionKey,omitempty"`
	// When this tab first appeared. It deliberately does not move on a heartbeat,
	// so "joined four minutes ago" stays answerable.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// The last heartbeat. Not updated_at: this is not when the row was last
	// edited, it is the whole meaning of the row — whoever is reading decides
	// whether somebody is here by comparing it against the configured TTL.
	SeenAt *time.Time `json:"seenAt,omitempty"`
	// Which part of the application this presence is in, named by the application:
	// a board, a document, a tenant. It is what a subscriber narrows the stream
	// by, so it decides how much presence traffic one screen pays for.
	Scope *string `json:"scope,omitempty"`
	// The table the row being looked at is in, checked against this API's own
	// tables. Null means present in the scope without being on a particular row.
	TargetTable *string `json:"targetTable,omitempty"`
	// Which row. There is deliberately no foreign key, and this is the one place
	// rig accepts a polymorphic reference: nothing joins to a presence row,
	// nothing embeds one, and no client filters a list of them — so the
	// integrity a key would buy has no reader. A presence pointing at a row
	// somebody just deleted is not a bug, it is a row that will be gone before
	// anybody looks.
	TargetID *uuid.UUID `json:"targetId,omitempty"`
	// Which field, when the application tracks focus that finely. This is what
	// turns "Simon is on this issue" into "Simon is editing the title".
	TargetField *string `json:"targetField,omitempty"`
	// Whether they are looking or typing. Separate from target_field because a
	// client may know somebody is editing before it knows which control has focus.
	Activity *PresenceActivity `json:"activity,omitempty"`
}

// Ordering conditions on Presence fields that can be compared.
type PresenceFilterRange struct {
	// When this tab first appeared. It deliberately does not move on a heartbeat,
	// so "joined four minutes ago" stays answerable.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// The last heartbeat. Not updated_at: this is not when the row was last
	// edited, it is the whole meaning of the row — whoever is reading decides
	// whether somebody is here by comparing it against the configured TTL.
	SeenAt *time.Time `json:"seenAt,omitempty"`
}

// Set-membership conditions on Presence fields.
type PresenceFilterContains struct {
	// The row's identifier. Nothing references it: a client addresses its own
	// presence by session key, so this identifier never leaves the server except
	// on the stream.
	ID []uuid.UUID `json:"id,omitempty"`
	// Who is present. Read from the credential and never from a request body,
	// which is what makes "you may only write your own presence" a sentence a
	// client cannot phrase rather than a rule somebody enforces.
	AccountID []uuid.UUID `json:"accountId,omitempty"`
	// Which tab this is. The browser names itself, and the name points at nothing:
	// one sign-in can have several tabs, and each is present separately rather
	// than overwriting the others.
	SessionKey []string `json:"sessionKey,omitempty"`
	// When this tab first appeared. It deliberately does not move on a heartbeat,
	// so "joined four minutes ago" stays answerable.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// The last heartbeat. Not updated_at: this is not when the row was last
	// edited, it is the whole meaning of the row — whoever is reading decides
	// whether somebody is here by comparing it against the configured TTL.
	SeenAt []time.Time `json:"seenAt,omitempty"`
	// Which part of the application this presence is in, named by the application:
	// a board, a document, a tenant. It is what a subscriber narrows the stream
	// by, so it decides how much presence traffic one screen pays for.
	Scope []string `json:"scope,omitempty"`
	// The table the row being looked at is in, checked against this API's own
	// tables. Null means present in the scope without being on a particular row.
	TargetTable []string `json:"targetTable,omitempty"`
	// Which row. There is deliberately no foreign key, and this is the one place
	// rig accepts a polymorphic reference: nothing joins to a presence row,
	// nothing embeds one, and no client filters a list of them — so the
	// integrity a key would buy has no reader. A presence pointing at a row
	// somebody just deleted is not a bug, it is a row that will be gone before
	// anybody looks.
	TargetID []uuid.UUID `json:"targetId,omitempty"`
	// Which field, when the application tracks focus that finely. This is what
	// turns "Simon is on this issue" into "Simon is editing the title".
	TargetField []string `json:"targetField,omitempty"`
	// Whether they are looking or typing. Separate from target_field because a
	// client may know somebody is editing before it knows which control has focus.
	Activity []PresenceActivity `json:"activity,omitempty"`
}

// Pattern conditions on Presence text fields.
type PresenceFilterLike struct {
	// Which tab this is. The browser names itself, and the name points at nothing:
	// one sign-in can have several tabs, and each is present separately rather
	// than overwriting the others.
	SessionKey *string `json:"sessionKey,omitempty"`
	// Which part of the application this presence is in, named by the application:
	// a board, a document, a tenant. It is what a subscriber narrows the stream
	// by, so it decides how much presence traffic one screen pays for.
	Scope *string `json:"scope,omitempty"`
	// The table the row being looked at is in, checked against this API's own
	// tables. Null means present in the scope without being on a particular row.
	TargetTable *string `json:"targetTable,omitempty"`
	// Which field, when the application tracks focus that finely. This is what
	// turns "Simon is on this issue" into "Simon is editing the title".
	TargetField *string `json:"targetField,omitempty"`
}

// Presence conditions on nullable Presence fields.
type PresenceFilterNull struct {
	// The table the row being looked at is in, checked against this API's own
	// tables. Null means present in the scope without being on a particular row.
	TargetTable *bool `json:"targetTable,omitempty"`
	// Which row. There is deliberately no foreign key, and this is the one place
	// rig accepts a polymorphic reference: nothing joins to a presence row,
	// nothing embeds one, and no client filters a list of them — so the
	// integrity a key would buy has no reader. A presence pointing at a row
	// somebody just deleted is not a bug, it is a row that will be gone before
	// anybody looks.
	TargetID *bool `json:"targetId,omitempty"`
	// Which field, when the application tracks focus that finely. This is what
	// turns "Simon is on this issue" into "Simon is editing the title".
	TargetField *bool `json:"targetField,omitempty"`
}

// Conditions a Presence must satisfy to match a search.
type PresenceFilter struct {
	// Fields that must equal the given value.
	Equals *PresenceFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *PresenceFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *PresenceFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *PresenceFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *PresenceFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *PresenceFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *PresenceFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *PresenceFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *PresenceFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *PresenceFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *PresenceFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *PresenceFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []PresenceFilter `json:"nestedFilters,omitempty"`
}

// PresencePage says which slice of a result to return, and in what order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type PresencePage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []PresenceOrder
}

// PresenceOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type PresenceOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a Presence supports.
var (
	PresenceOrderIDAsc           = PresenceOrder{Column: "id"}
	PresenceOrderIDDesc          = PresenceOrder{Column: "id", Desc: true}
	PresenceOrderTenantIDAsc     = PresenceOrder{Column: "tenant_id"}
	PresenceOrderTenantIDDesc    = PresenceOrder{Column: "tenant_id", Desc: true}
	PresenceOrderAccountIDAsc    = PresenceOrder{Column: "account_id"}
	PresenceOrderAccountIDDesc   = PresenceOrder{Column: "account_id", Desc: true}
	PresenceOrderSessionKeyAsc   = PresenceOrder{Column: "session_key"}
	PresenceOrderSessionKeyDesc  = PresenceOrder{Column: "session_key", Desc: true}
	PresenceOrderCreatedAtAsc    = PresenceOrder{Column: "created_at"}
	PresenceOrderCreatedAtDesc   = PresenceOrder{Column: "created_at", Desc: true}
	PresenceOrderSeenAtAsc       = PresenceOrder{Column: "seen_at"}
	PresenceOrderSeenAtDesc      = PresenceOrder{Column: "seen_at", Desc: true}
	PresenceOrderScopeAsc        = PresenceOrder{Column: "scope"}
	PresenceOrderScopeDesc       = PresenceOrder{Column: "scope", Desc: true}
	PresenceOrderTargetTableAsc  = PresenceOrder{Column: "target_table"}
	PresenceOrderTargetTableDesc = PresenceOrder{Column: "target_table", Desc: true}
	PresenceOrderTargetIDAsc     = PresenceOrder{Column: "target_id"}
	PresenceOrderTargetIDDesc    = PresenceOrder{Column: "target_id", Desc: true}
	PresenceOrderTargetFieldAsc  = PresenceOrder{Column: "target_field"}
	PresenceOrderTargetFieldDesc = PresenceOrder{Column: "target_field", Desc: true}
	PresenceOrderActivityAsc     = PresenceOrder{Column: "activity"}
	PresenceOrderActivityDesc    = PresenceOrder{Column: "activity", Desc: true}
)

// NewPresenceFilterEquals builds an empty PresenceFilterEquals to fill in.
func NewPresenceFilterEquals() *PresenceFilterEquals { return &PresenceFilterEquals{} }

// NewPresenceFilterRange builds an empty PresenceFilterRange to fill in.
func NewPresenceFilterRange() *PresenceFilterRange { return &PresenceFilterRange{} }

// NewPresenceFilterContains builds an empty PresenceFilterContains to fill in.
func NewPresenceFilterContains() *PresenceFilterContains { return &PresenceFilterContains{} }

// NewPresenceFilterLike builds an empty PresenceFilterLike to fill in.
func NewPresenceFilterLike() *PresenceFilterLike { return &PresenceFilterLike{} }

// NewPresenceFilterNull builds an empty PresenceFilterNull to fill in.
func NewPresenceFilterNull() *PresenceFilterNull { return &PresenceFilterNull{} }

// NewPresenceFilter builds an empty filter, which matches every row the caller
// is allowed to see.
func NewPresenceFilter() PresenceFilter { return PresenceFilter{} }
