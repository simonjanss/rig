// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on RigNotificationDelivery fields.
type RigNotificationDeliveryFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// The inbox line this is a copy of. The line is the truth and this is one way
	// it was repeated.
	RecipientID *uuid.UUID `json:"recipientId,omitempty"`
	// Who it is for, denormalized off the line so a claim can group by it without
	// a join.
	AccountID *uuid.UUID `json:"accountId,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// Where it is going. One row per line per channel, which is what the unique
	// index below enforces.
	Channel *RigNotificationChannel `json:"channel,omitempty"`
	// Copied from the notification, so the settings resolution needs no join
	// either.
	Kind *string `json:"kind,omitempty"`
	// What the setting said when this row was written. Immediate rows are sent on
	// their own; the rest are batched per account and channel.
	Digest *RigNotificationDigest `json:"digest,omitempty"`
	// Skipped means a setting refused it, which is different from Failed and worth
	// telling apart in a report.
	State *RigNotificationDeliveryState `json:"state,omitempty"`
	// When this is due. A delivery held outside somebody hours moves to the next
	// opening; a digested one moves to its window close.
	DeliverAt *time.Time `json:"deliverAt,omitempty"`
	// When a channel accepted it, which is not the same as it arriving.
	SentAt *time.Time `json:"sentAt,omitempty"`
	// What the channel said last time. Kept on a retry as well as on a failure, so
	// a pattern is visible before the cap is reached.
	FailedReason *string `json:"failedReason,omitempty"`
	// How many times this has been claimed. Past notifications.max_attempts it is
	// Failed and stops being claimed.
	Attempts *int `json:"attempts,omitempty"`
	// When a dispatcher took it. Past notifications.claim_ttl another one may,
	// which is what makes a crashed process recoverable.
	ClaimedAt *time.Time `json:"claimedAt,omitempty"`
	// Which process holds the lease. A uuid generated once per process, with the
	// hostname beside it in the log line, so a stuck lease traces to a pod rather
	// than to a mystery.
	ClaimedBy *uuid.UUID `json:"claimedBy,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterEquals `json:"account,omitempty"`
}

// Ordering conditions on RigNotificationDelivery fields that can be compared.
type RigNotificationDeliveryFilterRange struct {
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// When this is due. A delivery held outside somebody hours moves to the next
	// opening; a digested one moves to its window close.
	DeliverAt *time.Time `json:"deliverAt,omitempty"`
	// When a channel accepted it, which is not the same as it arriving.
	SentAt *time.Time `json:"sentAt,omitempty"`
	// How many times this has been claimed. Past notifications.max_attempts it is
	// Failed and stops being claimed.
	Attempts *int `json:"attempts,omitempty"`
	// When a dispatcher took it. Past notifications.claim_ttl another one may,
	// which is what makes a crashed process recoverable.
	ClaimedAt *time.Time `json:"claimedAt,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterRange `json:"account,omitempty"`
}

// Set-membership conditions on RigNotificationDelivery fields.
type RigNotificationDeliveryFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// The inbox line this is a copy of. The line is the truth and this is one way
	// it was repeated.
	RecipientID []uuid.UUID `json:"recipientId,omitempty"`
	// Who it is for, denormalized off the line so a claim can group by it without
	// a join.
	AccountID []uuid.UUID `json:"accountId,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// Where it is going. One row per line per channel, which is what the unique
	// index below enforces.
	Channel []RigNotificationChannel `json:"channel,omitempty"`
	// Copied from the notification, so the settings resolution needs no join
	// either.
	Kind []string `json:"kind,omitempty"`
	// What the setting said when this row was written. Immediate rows are sent on
	// their own; the rest are batched per account and channel.
	Digest []RigNotificationDigest `json:"digest,omitempty"`
	// Skipped means a setting refused it, which is different from Failed and worth
	// telling apart in a report.
	State []RigNotificationDeliveryState `json:"state,omitempty"`
	// When this is due. A delivery held outside somebody hours moves to the next
	// opening; a digested one moves to its window close.
	DeliverAt []time.Time `json:"deliverAt,omitempty"`
	// When a channel accepted it, which is not the same as it arriving.
	SentAt []time.Time `json:"sentAt,omitempty"`
	// What the channel said last time. Kept on a retry as well as on a failure, so
	// a pattern is visible before the cap is reached.
	FailedReason []string `json:"failedReason,omitempty"`
	// How many times this has been claimed. Past notifications.max_attempts it is
	// Failed and stops being claimed.
	Attempts []int `json:"attempts,omitempty"`
	// When a dispatcher took it. Past notifications.claim_ttl another one may,
	// which is what makes a crashed process recoverable.
	ClaimedAt []time.Time `json:"claimedAt,omitempty"`
	// Which process holds the lease. A uuid generated once per process, with the
	// hostname beside it in the log line, so a stuck lease traces to a pod rather
	// than to a mystery.
	ClaimedBy []uuid.UUID `json:"claimedBy,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterContains `json:"account,omitempty"`
}

// Pattern conditions on RigNotificationDelivery text fields.
type RigNotificationDeliveryFilterLike struct {
	// Copied from the notification, so the settings resolution needs no join
	// either.
	Kind *string `json:"kind,omitempty"`
	// What the channel said last time. Kept on a retry as well as on a failure, so
	// a pattern is visible before the cap is reached.
	FailedReason *string `json:"failedReason,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterLike `json:"account,omitempty"`
}

// Presence conditions on nullable RigNotificationDelivery fields.
type RigNotificationDeliveryFilterNull struct {
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// When a channel accepted it, which is not the same as it arriving.
	SentAt *bool `json:"sentAt,omitempty"`
	// What the channel said last time. Kept on a retry as well as on a failure, so
	// a pattern is visible before the cap is reached.
	FailedReason *bool `json:"failedReason,omitempty"`
	// When a dispatcher took it. Past notifications.claim_ttl another one may,
	// which is what makes a crashed process recoverable.
	ClaimedAt *bool `json:"claimedAt,omitempty"`
	// Which process holds the lease. A uuid generated once per process, with the
	// hostname beside it in the log line, so a stuck lease traces to a pod rather
	// than to a mystery.
	ClaimedBy *bool `json:"claimedBy,omitempty"`
	// Conditions on this row's Account.
	//
	// A row with no Account never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the Account is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching Account, which a row with none satisfies — use
	// Without.Account.
	Account *RigAccountFilterNull `json:"account,omitempty"`
}

// Relations a RigNotificationDelivery must have no matching row for.
type RigNotificationDeliveryFilterWithout struct {
	// Match rows with no Account satisfying these conditions, including rows that
	// have none at all.
	//
	// This negates the existence of the related row rather than the comparison,
	// which is the whole difference from NotEquals.Account: that one needs the
	// Account to be there and to differ, so a row without one fails it and
	// satisfies this. An empty object asks for rows with no Account at all.
	Account *RigAccountFilter `json:"account,omitempty"`
}

// Conditions a RigNotificationDelivery must satisfy to match a search.
type RigNotificationDeliveryFilter struct {
	// Fields that must equal the given value.
	Equals *RigNotificationDeliveryFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *RigNotificationDeliveryFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *RigNotificationDeliveryFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *RigNotificationDeliveryFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *RigNotificationDeliveryFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *RigNotificationDeliveryFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *RigNotificationDeliveryFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *RigNotificationDeliveryFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *RigNotificationDeliveryFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *RigNotificationDeliveryFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *RigNotificationDeliveryFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *RigNotificationDeliveryFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []RigNotificationDeliveryFilter `json:"nestedFilters,omitempty"`
	// Relations with no row matching the given conditions. An empty one asks for
	// no related row at all.
	//
	// This is where a negation belongs when what you mean is "not": it negates
	// whether a matching related row exists, so a row with no related row at all
	// satisfies it. The negative operators above negate the comparison instead and
	// still require the related row to be there — "no related row called X" is
	// here, "a related row not called X" is NotEquals.
	Without *RigNotificationDeliveryFilterWithout `json:"without,omitempty"`
}

// RigNotificationDeliveryPage says which slice of a result to return, and in
// what order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type RigNotificationDeliveryPage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []RigNotificationDeliveryOrder
}

// RigNotificationDeliveryOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type RigNotificationDeliveryOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a RigNotificationDelivery supports.
var (
	RigNotificationDeliveryOrderIDAsc            = RigNotificationDeliveryOrder{Column: "id"}
	RigNotificationDeliveryOrderIDDesc           = RigNotificationDeliveryOrder{Column: "id", Desc: true}
	RigNotificationDeliveryOrderTenantIDAsc      = RigNotificationDeliveryOrder{Column: "tenant_id"}
	RigNotificationDeliveryOrderTenantIDDesc     = RigNotificationDeliveryOrder{Column: "tenant_id", Desc: true}
	RigNotificationDeliveryOrderRecipientIDAsc   = RigNotificationDeliveryOrder{Column: "recipient_id"}
	RigNotificationDeliveryOrderRecipientIDDesc  = RigNotificationDeliveryOrder{Column: "recipient_id", Desc: true}
	RigNotificationDeliveryOrderAccountIDAsc     = RigNotificationDeliveryOrder{Column: "account_id"}
	RigNotificationDeliveryOrderAccountIDDesc    = RigNotificationDeliveryOrder{Column: "account_id", Desc: true}
	RigNotificationDeliveryOrderCreatedAtAsc     = RigNotificationDeliveryOrder{Column: "created_at"}
	RigNotificationDeliveryOrderCreatedAtDesc    = RigNotificationDeliveryOrder{Column: "created_at", Desc: true}
	RigNotificationDeliveryOrderUpdatedAtAsc     = RigNotificationDeliveryOrder{Column: "updated_at"}
	RigNotificationDeliveryOrderUpdatedAtDesc    = RigNotificationDeliveryOrder{Column: "updated_at", Desc: true}
	RigNotificationDeliveryOrderChannelAsc       = RigNotificationDeliveryOrder{Column: "channel"}
	RigNotificationDeliveryOrderChannelDesc      = RigNotificationDeliveryOrder{Column: "channel", Desc: true}
	RigNotificationDeliveryOrderKindAsc          = RigNotificationDeliveryOrder{Column: "kind"}
	RigNotificationDeliveryOrderKindDesc         = RigNotificationDeliveryOrder{Column: "kind", Desc: true}
	RigNotificationDeliveryOrderDigestAsc        = RigNotificationDeliveryOrder{Column: "digest"}
	RigNotificationDeliveryOrderDigestDesc       = RigNotificationDeliveryOrder{Column: "digest", Desc: true}
	RigNotificationDeliveryOrderStateAsc         = RigNotificationDeliveryOrder{Column: "state"}
	RigNotificationDeliveryOrderStateDesc        = RigNotificationDeliveryOrder{Column: "state", Desc: true}
	RigNotificationDeliveryOrderDeliverAtAsc     = RigNotificationDeliveryOrder{Column: "deliver_at"}
	RigNotificationDeliveryOrderDeliverAtDesc    = RigNotificationDeliveryOrder{Column: "deliver_at", Desc: true}
	RigNotificationDeliveryOrderSentAtAsc        = RigNotificationDeliveryOrder{Column: "sent_at"}
	RigNotificationDeliveryOrderSentAtDesc       = RigNotificationDeliveryOrder{Column: "sent_at", Desc: true}
	RigNotificationDeliveryOrderFailedReasonAsc  = RigNotificationDeliveryOrder{Column: "failed_reason"}
	RigNotificationDeliveryOrderFailedReasonDesc = RigNotificationDeliveryOrder{Column: "failed_reason", Desc: true}
	RigNotificationDeliveryOrderAttemptsAsc      = RigNotificationDeliveryOrder{Column: "attempts"}
	RigNotificationDeliveryOrderAttemptsDesc     = RigNotificationDeliveryOrder{Column: "attempts", Desc: true}
	RigNotificationDeliveryOrderClaimedAtAsc     = RigNotificationDeliveryOrder{Column: "claimed_at"}
	RigNotificationDeliveryOrderClaimedAtDesc    = RigNotificationDeliveryOrder{Column: "claimed_at", Desc: true}
	RigNotificationDeliveryOrderClaimedByAsc     = RigNotificationDeliveryOrder{Column: "claimed_by"}
	RigNotificationDeliveryOrderClaimedByDesc    = RigNotificationDeliveryOrder{Column: "claimed_by", Desc: true}
)

// RigNotificationDeliveryOrderAccount orders a RigNotificationDelivery by a
// column of its Account.
//
// Take the term from RigAccount's own orderings:
//
//	RigNotificationDeliveryOrderAccount(RigAccountOrderIDDesc)
//
// The repository reaches it with a left join, so a RigNotificationDelivery
// with no Account sorts to one end rather than vanishing. Only RigAccount's
// own columns can be used; an ordering that itself reaches across a relation
// is refused, because one join is where this stops.
func RigNotificationDeliveryOrderAccount(o RigAccountOrder) RigNotificationDeliveryOrder {
	return RigNotificationDeliveryOrder{Relation: "Account", Column: o.Column, Desc: o.Desc}
}

// NewRigNotificationDeliveryFilterEquals builds an empty
// RigNotificationDeliveryFilterEquals to fill in.
func NewRigNotificationDeliveryFilterEquals() *RigNotificationDeliveryFilterEquals {
	return &RigNotificationDeliveryFilterEquals{}
}

// NewRigNotificationDeliveryFilterRange builds an empty
// RigNotificationDeliveryFilterRange to fill in.
func NewRigNotificationDeliveryFilterRange() *RigNotificationDeliveryFilterRange {
	return &RigNotificationDeliveryFilterRange{}
}

// NewRigNotificationDeliveryFilterContains builds an empty
// RigNotificationDeliveryFilterContains to fill in.
func NewRigNotificationDeliveryFilterContains() *RigNotificationDeliveryFilterContains {
	return &RigNotificationDeliveryFilterContains{}
}

// NewRigNotificationDeliveryFilterLike builds an empty
// RigNotificationDeliveryFilterLike to fill in.
func NewRigNotificationDeliveryFilterLike() *RigNotificationDeliveryFilterLike {
	return &RigNotificationDeliveryFilterLike{}
}

// NewRigNotificationDeliveryFilterNull builds an empty
// RigNotificationDeliveryFilterNull to fill in.
func NewRigNotificationDeliveryFilterNull() *RigNotificationDeliveryFilterNull {
	return &RigNotificationDeliveryFilterNull{}
}

// NewRigNotificationDeliveryFilterWithout builds an empty
// RigNotificationDeliveryFilterWithout to fill in.
func NewRigNotificationDeliveryFilterWithout() *RigNotificationDeliveryFilterWithout {
	return &RigNotificationDeliveryFilterWithout{}
}

// NewRigNotificationDeliveryFilter builds an empty filter, which matches every
// row the caller is allowed to see.
func NewRigNotificationDeliveryFilter() RigNotificationDeliveryFilter {
	return RigNotificationDeliveryFilter{}
}
