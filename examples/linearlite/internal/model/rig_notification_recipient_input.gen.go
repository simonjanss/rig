// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"context"
	"strings"
	"time"

	"github.com/google/uuid"
	"github.com/simonjanss/rig/runtime/patch"
	"github.com/simonjanss/rig/runtime/rigerr"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// RigNotificationRecipientCreateInput is what creating a
// RigNotificationRecipient takes.
//
// The identifier, the tenant, and the audit columns are absent: those are
// stamped by the repository from the request's claims.
type RigNotificationRecipientCreateInput struct {
	// What happened. The line is separate from it so that one person clearing
	// their inbox changes nothing anybody else sees.
	NotificationID uuid.UUID `json:"notificationId"`
	// Who this line is for. An account rather than an identity: an identity has no
	// tenant, so a row addressed to one would fall outside every generated query.
	AccountID uuid.UUID `json:"accountId"`
	// Copied from the notification, so the inbox and its live-sync shape never
	// touch a table holding rows for people who are not recipients.
	Kind string `json:"kind"`
	// What collapses several events into one line. Null opts out and every event
	// is its own row.
	GroupKey *string `json:"groupKey"`
	// How many events this line stands for. One unless a group key collapsed them.
	EventCount int `json:"eventCount"`
	// When the person read it. Null is unread, which is what the badge counts.
	ReadAt *time.Time `json:"readAt"`
}

// Normalize tidies what was given before anything checks it.
//
// It runs first so that validation sees the value that will actually be
// stored: a title with a trailing space and one without are the same title,
// and rejecting the second for a length rule the first passes would be
// indefensible.
func (i *RigNotificationRecipientCreateInput) Normalize() {
	i.Kind = strings.TrimSpace(i.Kind)
	if i.GroupKey != nil {
		*i.GroupKey = strings.TrimSpace(*i.GroupKey)
	}
	if i.EventCount == 0 {
		i.EventCount = 1
	}
}

// RigNotificationRecipientCreateInputError says what was wrong with each field
// of a RigNotificationRecipientCreateInput.
//
// Its shape is the input's shape, so a client can attach every message to the
// field it is about without matching on strings. A member is nil when that
// field was fine, and the whole value is nil when the input was. It is what
// the 422 carries.
type RigNotificationRecipientCreateInputError struct {
	// What happened. The line is separate from it so that one person clearing
	// their inbox changes nothing anybody else sees.
	NotificationID *rigerr.FieldError `json:"notificationId,omitempty"`
	// Who this line is for. An account rather than an identity: an identity has no
	// tenant, so a row addressed to one would fall outside every generated query.
	AccountID *rigerr.FieldError `json:"accountId,omitempty"`
	// Copied from the notification, so the inbox and its live-sync shape never
	// touch a table holding rows for people who are not recipients.
	Kind *rigerr.FieldError `json:"kind,omitempty"`
	// What collapses several events into one line. Null opts out and every event
	// is its own row.
	GroupKey *rigerr.FieldError `json:"groupKey,omitempty"`
	// How many events this line stands for. One unless a group key collapsed them.
	EventCount *rigerr.FieldError `json:"eventCount,omitempty"`
	// When the person read it. Null is unread, which is what the badge counts.
	ReadAt *rigerr.FieldError `json:"readAt,omitempty"`

	// Entity is a problem with the row as a whole rather than with one field: what
	// the Entity rule said.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// Empty reports whether anything went wrong. A validator that found nothing
// returns nil rather than one of these.
func (e *RigNotificationRecipientCreateInputError) Empty() bool {
	if e == nil {
		return true
	}

	return e.NotificationID == nil && e.AccountID == nil && e.Kind == nil && e.GroupKey == nil && e.EventCount == nil && e.ReadAt == nil && e.Entity == nil
}

// Error implements error. The sentence is for logs and for a person; the
// structure above is what a client acts on.
func (e *RigNotificationRecipientCreateInputError) Error() string {
	var parts []string
	if e.NotificationID != nil {
		parts = append(parts, "notificationId "+e.NotificationID.Error())
	}
	if e.AccountID != nil {
		parts = append(parts, "accountId "+e.AccountID.Error())
	}
	if e.Kind != nil {
		parts = append(parts, "kind "+e.Kind.Error())
	}
	if e.GroupKey != nil {
		parts = append(parts, "groupKey "+e.GroupKey.Error())
	}
	if e.EventCount != nil {
		parts = append(parts, "eventCount "+e.EventCount.Error())
	}
	if e.ReadAt != nil {
		parts = append(parts, "readAt "+e.ReadAt.Error())
	}
	if e.Entity != nil {
		parts = append(parts, e.Entity.Error())
	}

	return "rig_notification_recipient is not valid: " + strings.Join(parts, "; ")
}

// ErrorCode implements [rigerr.Coder]: the request was understood and its
// content is what is wrong, which is 422 and not 400.
func (e *RigNotificationRecipientCreateInputError) ErrorCode() rigerr.Code {
	return rigerr.CodeUnprocessableEntity
}

// ErrorFields implements [rigerr.FieldReporter], which is how the HTTP layer
// finds this and answers with it rather than with prose.
func (e *RigNotificationRecipientCreateInputError) ErrorFields() any { return e }

// Validate checks what the schema can decide on its own.
//
// Everything a column declares — NOT NULL, a length, an enumeration's values
// — is checked here, so a service only writes the rules that are actually
// about the business. Every field is checked before returning, because a form
// that reports one problem per round trip is a form people give up on.
//
// What comes back is a *RigNotificationRecipientCreateInputError, shaped like
// the input itself.
func (i *RigNotificationRecipientCreateInput) Validate() error {
	var failed RigNotificationRecipientCreateInputError

	if strings.TrimSpace(i.Kind) == "" {
		failed.Kind = rigerr.NewFieldError(rigerr.FieldCodeCannotBeEmpty, "cannot be empty")
	}

	if failed.Empty() {
		return nil
	}
	return &failed
}

// RigNotificationRecipientUpdateInput is what changing a
// RigNotificationRecipient takes.
//
// A field left out is untouched. A nullable field set to null is cleared —
// which is why the two wrappers differ: a column that cannot hold null has no
// way to be given one, so clearing it is a compile error rather than a
// rejection at runtime. Immutable fields are not here at all.
type RigNotificationRecipientUpdateInput struct {
	// What happened. The line is separate from it so that one person clearing
	// their inbox changes nothing anybody else sees.
	NotificationID patch.Optional[uuid.UUID] `json:"notificationId"`
	// Who this line is for. An account rather than an identity: an identity has no
	// tenant, so a row addressed to one would fall outside every generated query.
	AccountID patch.Optional[uuid.UUID] `json:"accountId"`
	// Copied from the notification, so the inbox and its live-sync shape never
	// touch a table holding rows for people who are not recipients.
	Kind patch.Optional[string] `json:"kind"`
	// What collapses several events into one line. Null opts out and every event
	// is its own row.
	GroupKey patch.Nullable[string] `json:"groupKey"`
	// How many events this line stands for. One unless a group key collapsed them.
	EventCount patch.Optional[int] `json:"eventCount"`
	// When the person read it. Null is unread, which is what the badge counts.
	ReadAt patch.Nullable[time.Time] `json:"readAt"`
}

// Normalize tidies the fields this request actually carries.
//
// It does not fill in the ones it does not: the repository writes exactly the
// columns that were sent, and filling them here would turn every update into a
// write of every column — so two requests changing different fields of one
// row would start overwriting each other instead of composing.
func (i *RigNotificationRecipientUpdateInput) Normalize() {
	if v, ok := i.Kind.Get(); ok {
		v = strings.TrimSpace(v)
		i.Kind = patch.NewOptional(v)
	}
	if v, ok := i.GroupKey.Get(); ok {
		v = strings.TrimSpace(v)
		i.GroupKey = patch.NewNullable(v)
	}
}

// Merged is the row as it will be once this update is applied.
//
// It is what validation runs against, and the reason it exists: a rule
// spanning two fields cannot be checked from a partial request. "Ends after
// starts" is unanswerable when only one of them was sent.
//
// It returns a copy. The input keeps its patches, so the repository still
// writes only the columns that were actually given.
func (i RigNotificationRecipientUpdateInput) Merged(prev *RigNotificationRecipient) RigNotificationRecipient {
	out := *prev

	if v, ok := i.NotificationID.Get(); ok {
		out.NotificationID = v
	}
	if v, ok := i.AccountID.Get(); ok {
		out.AccountID = v
	}
	if v, ok := i.Kind.Get(); ok {
		out.Kind = v
	}
	if i.GroupKey.Touched() {
		out.GroupKey = i.GroupKey.Ptr()
	}
	if v, ok := i.EventCount.Get(); ok {
		out.EventCount = v
	}
	if i.ReadAt.Touched() {
		out.ReadAt = i.ReadAt.Ptr()
	}

	return out
}

// RigNotificationRecipientUpdateInputError says what was wrong with each field
// of a RigNotificationRecipientUpdateInput.
//
// Its shape is the input's shape, so a client can attach every message to the
// field it is about without matching on strings. A member is nil when that
// field was fine, and the whole value is nil when the input was. It is what
// the 422 carries.
type RigNotificationRecipientUpdateInputError struct {
	// What happened. The line is separate from it so that one person clearing
	// their inbox changes nothing anybody else sees.
	NotificationID *rigerr.FieldError `json:"notificationId,omitempty"`
	// Who this line is for. An account rather than an identity: an identity has no
	// tenant, so a row addressed to one would fall outside every generated query.
	AccountID *rigerr.FieldError `json:"accountId,omitempty"`
	// Copied from the notification, so the inbox and its live-sync shape never
	// touch a table holding rows for people who are not recipients.
	Kind *rigerr.FieldError `json:"kind,omitempty"`
	// What collapses several events into one line. Null opts out and every event
	// is its own row.
	GroupKey *rigerr.FieldError `json:"groupKey,omitempty"`
	// How many events this line stands for. One unless a group key collapsed them.
	EventCount *rigerr.FieldError `json:"eventCount,omitempty"`
	// When the person read it. Null is unread, which is what the badge counts.
	ReadAt *rigerr.FieldError `json:"readAt,omitempty"`

	// Entity is a problem with the row as a whole rather than with one field: what
	// the Entity rule said.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// Empty reports whether anything went wrong. A validator that found nothing
// returns nil rather than one of these.
func (e *RigNotificationRecipientUpdateInputError) Empty() bool {
	if e == nil {
		return true
	}

	return e.NotificationID == nil && e.AccountID == nil && e.Kind == nil && e.GroupKey == nil && e.EventCount == nil && e.ReadAt == nil && e.Entity == nil
}

// Error implements error. The sentence is for logs and for a person; the
// structure above is what a client acts on.
func (e *RigNotificationRecipientUpdateInputError) Error() string {
	var parts []string
	if e.NotificationID != nil {
		parts = append(parts, "notificationId "+e.NotificationID.Error())
	}
	if e.AccountID != nil {
		parts = append(parts, "accountId "+e.AccountID.Error())
	}
	if e.Kind != nil {
		parts = append(parts, "kind "+e.Kind.Error())
	}
	if e.GroupKey != nil {
		parts = append(parts, "groupKey "+e.GroupKey.Error())
	}
	if e.EventCount != nil {
		parts = append(parts, "eventCount "+e.EventCount.Error())
	}
	if e.ReadAt != nil {
		parts = append(parts, "readAt "+e.ReadAt.Error())
	}
	if e.Entity != nil {
		parts = append(parts, e.Entity.Error())
	}

	return "rig_notification_recipient is not valid: " + strings.Join(parts, "; ")
}

// ErrorCode implements [rigerr.Coder]: the request was understood and its
// content is what is wrong, which is 422 and not 400.
func (e *RigNotificationRecipientUpdateInputError) ErrorCode() rigerr.Code {
	return rigerr.CodeUnprocessableEntity
}

// ErrorFields implements [rigerr.FieldReporter], which is how the HTTP layer
// finds this and answers with it rather than with prose.
func (e *RigNotificationRecipientUpdateInputError) ErrorFields() any { return e }

// Validate checks the row this update would produce.
//
// Against the merged state, not the request: a length rule on a field nobody
// sent still has to hold, and a rule about two fields needs both.
//
// What comes back is a *RigNotificationRecipientUpdateInputError, shaped like
// the input itself.
func (i *RigNotificationRecipientUpdateInput) Validate(prev *RigNotificationRecipient) error {
	var failed RigNotificationRecipientUpdateInputError

	merged := i.Merged(prev)

	if strings.TrimSpace(merged.Kind) == "" {
		failed.Kind = rigerr.NewFieldError(rigerr.FieldCodeCannotBeEmpty, "cannot be empty")
	}

	if failed.Empty() {
		return nil
	}
	return &failed
}

// RigNotificationRecipientDeleteInput is what deleting a
// RigNotificationRecipient takes.
type RigNotificationRecipientDeleteInput struct {
	// ID is the row to delete.
	ID uuid.UUID `json:"id"`
	// Hard removes the row outright instead of retiring it. A hard delete cannot
	// be undone and takes the row's snapshots with it.
	Hard bool `json:"hard,omitempty"`
}

// RigNotificationRecipientValidatorContext is what a rule sees.
//
// Values is the row as it will be if this goes through — merged from the
// previous state on an update, so every field is set whether or not the
// request mentioned it. That is the point: "ends after starts" cannot be
// answered from a request that only carried one of them.
type RigNotificationRecipientValidatorContext struct {
	// Values is the intended end state.
	Values RigNotificationRecipient

	// Claims are who is asking. They are a value rather than something to fetch
	// from the context because a rule that has to look them up is a rule that can
	// forget to, and because there is no case where they are absent: a write
	// without a caller is refused by the repository before any rule runs.
	Claims tenancy.Claims

	// previous is the row before this change, and is the zero value on a
	// create — there was nothing before.
	previous RigNotificationRecipient
	isUpdate bool
	changed  map[string]bool
}

// IsUpdate reports whether there was a row before this.
func (c *RigNotificationRecipientValidatorContext) IsUpdate() bool { return c.isUpdate }

// Previous is the row as it was, and the zero value on a create. Check
// IsUpdate before reading it.
func (c *RigNotificationRecipientValidatorContext) Previous() RigNotificationRecipient {
	return c.previous
}

// Changed reports whether this request carried a new value for a column.
//
// It is what keeps an expensive rule from running on every update: a check
// that reaches another service to confirm a reference only needs to run when
// the reference actually moved. On a create everything is changed, because
// everything is new.
func (c *RigNotificationRecipientValidatorContext) Changed(column string) bool {
	return c.changed[column]
}

// NotificationIDChanged reports whether this request set notification_id.
func (c *RigNotificationRecipientValidatorContext) NotificationIDChanged() bool {
	return c.changed[ColumnRigNotificationRecipientNotificationID]
}

// AccountIDChanged reports whether this request set account_id.
func (c *RigNotificationRecipientValidatorContext) AccountIDChanged() bool {
	return c.changed[ColumnRigNotificationRecipientAccountID]
}

// KindChanged reports whether this request set kind.
func (c *RigNotificationRecipientValidatorContext) KindChanged() bool {
	return c.changed[ColumnRigNotificationRecipientKind]
}

// GroupKeyChanged reports whether this request set group_key.
func (c *RigNotificationRecipientValidatorContext) GroupKeyChanged() bool {
	return c.changed[ColumnRigNotificationRecipientGroupKey]
}

// EventCountChanged reports whether this request set event_count.
func (c *RigNotificationRecipientValidatorContext) EventCountChanged() bool {
	return c.changed[ColumnRigNotificationRecipientEventCount]
}

// ReadAtChanged reports whether this request set read_at.
func (c *RigNotificationRecipientValidatorContext) ReadAtChanged() bool {
	return c.changed[ColumnRigNotificationRecipientReadAt]
}

// RigNotificationRecipientCreateValidator is the rules for bringing a
// RigNotificationRecipient into existence: what the schema cannot express.
//
// One optional function per field this operation can set, so the set of fields
// is the set of rules that could apply — a column an update cannot touch has
// no hook here to write by mistake. A nil one is skipped. Every configured
// hook runs, because a rule that fails should not hide the next one, so a
// request reports everything wrong with it.
//
// A hook returns a FieldError to attach the message to a specific field, or
// any other error to fail the request outright.
type RigNotificationRecipientCreateValidator struct {
	// What happened. The line is separate from it so that one person clearing
	// their inbox changes nothing anybody else sees.
	NotificationID func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value uuid.UUID) error
	// Who this line is for. An account rather than an identity: an identity has no
	// tenant, so a row addressed to one would fall outside every generated query.
	AccountID func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value uuid.UUID) error
	// Copied from the notification, so the inbox and its live-sync shape never
	// touch a table holding rows for people who are not recipients.
	Kind func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value string) error
	// What collapses several events into one line. Null opts out and every event
	// is its own row.
	GroupKey func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value *string) error
	// How many events this line stands for. One unless a group key collapsed them.
	EventCount func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value int) error
	// When the person read it. Null is unread, which is what the badge counts.
	ReadAt func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value *time.Time) error

	// Entity runs after the per-field hooks, for a rule that is about the row
	// rather than about one column.
	Entity func(ctx context.Context, c *RigNotificationRecipientValidatorContext) error
}

// RunCreate implements [dbhook.CreateValidator]: it runs the service's rules
// against the row this input would produce.
//
// The generated checks are not repeated here. The repository runs Normalize
// and Validate first, so by the time a hook sees the input it is tidy and the
// schema is satisfied — which is what lets a hook be about the business
// rather than about NOT NULL.
func (v RigNotificationRecipientCreateValidator) RunCreate(ctx context.Context, claims tenancy.Claims, i *RigNotificationRecipientCreateInput) error {
	// Everything is new, so everything counts as changed.
	c := &RigNotificationRecipientValidatorContext{Claims: claims, changed: map[string]bool{}}
	c.Values.NotificationID = i.NotificationID
	c.changed[ColumnRigNotificationRecipientNotificationID] = true
	c.Values.AccountID = i.AccountID
	c.changed[ColumnRigNotificationRecipientAccountID] = true
	c.Values.Kind = i.Kind
	c.changed[ColumnRigNotificationRecipientKind] = true
	c.Values.GroupKey = i.GroupKey
	c.changed[ColumnRigNotificationRecipientGroupKey] = true
	c.Values.EventCount = i.EventCount
	c.changed[ColumnRigNotificationRecipientEventCount] = true
	c.Values.ReadAt = i.ReadAt
	c.changed[ColumnRigNotificationRecipientReadAt] = true

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// run calls every configured hook and puts what each one said under the field
// it was about.
//
// Two kinds of answer. A [rigerr.FieldError] is about the input: it lands on
// the field and the others still run, so one request reports everything wrong
// with it. Anything else is the rule itself failing — a lookup that could
// not reach another service — and there is nothing to tell the caller about
// their input, so it comes back wrapped with the rule that could not be run,
// keeping whatever code it carried and becoming Internal if it carried none.
func (v RigNotificationRecipientCreateValidator) run(ctx context.Context, c *RigNotificationRecipientValidatorContext) (*RigNotificationRecipientCreateInputError, error) {
	var failed RigNotificationRecipientCreateInputError

	if v.NotificationID != nil {
		if err := v.NotificationID(ctx, c, c.Values.NotificationID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate notification_id")
			}
			failed.NotificationID = field
		}
	}
	if v.AccountID != nil {
		if err := v.AccountID(ctx, c, c.Values.AccountID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate account_id")
			}
			failed.AccountID = field
		}
	}
	if v.Kind != nil {
		if err := v.Kind(ctx, c, c.Values.Kind); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate kind")
			}
			failed.Kind = field
		}
	}
	if v.GroupKey != nil {
		if err := v.GroupKey(ctx, c, c.Values.GroupKey); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate group_key")
			}
			failed.GroupKey = field
		}
	}
	if v.EventCount != nil {
		if err := v.EventCount(ctx, c, c.Values.EventCount); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate event_count")
			}
			failed.EventCount = field
		}
	}
	if v.ReadAt != nil {
		if err := v.ReadAt(ctx, c, c.Values.ReadAt); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate read_at")
			}
			failed.ReadAt = field
		}
	}

	if v.Entity != nil {
		if err := v.Entity(ctx, c); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate rig_notification_recipient")
			}
			failed.Entity = field
		}
	}

	if failed.Empty() {
		return nil, nil
	}
	return &failed, nil
}

// RigNotificationRecipientUpdateValidator is the rules for changing one that
// already exists: what the schema cannot express.
//
// One optional function per field this operation can set, so the set of fields
// is the set of rules that could apply — a column an update cannot touch has
// no hook here to write by mistake. A nil one is skipped. Every configured
// hook runs, because a rule that fails should not hide the next one, so a
// request reports everything wrong with it.
//
// A hook returns a FieldError to attach the message to a specific field, or
// any other error to fail the request outright.
type RigNotificationRecipientUpdateValidator struct {
	// What happened. The line is separate from it so that one person clearing
	// their inbox changes nothing anybody else sees.
	NotificationID func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value uuid.UUID) error
	// Who this line is for. An account rather than an identity: an identity has no
	// tenant, so a row addressed to one would fall outside every generated query.
	AccountID func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value uuid.UUID) error
	// Copied from the notification, so the inbox and its live-sync shape never
	// touch a table holding rows for people who are not recipients.
	Kind func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value string) error
	// What collapses several events into one line. Null opts out and every event
	// is its own row.
	GroupKey func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value *string) error
	// How many events this line stands for. One unless a group key collapsed them.
	EventCount func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value int) error
	// When the person read it. Null is unread, which is what the badge counts.
	ReadAt func(ctx context.Context, c *RigNotificationRecipientValidatorContext, value *time.Time) error

	// Entity runs after the per-field hooks, for a rule that is about the row
	// rather than about one column.
	Entity func(ctx context.Context, c *RigNotificationRecipientValidatorContext) error
}

// RunUpdate implements [dbhook.UpdateValidator]: it runs the service's rules
// against the row this update would produce, with the row as it was available
// for a rule about the change itself.
func (v RigNotificationRecipientUpdateValidator) RunUpdate(ctx context.Context, claims tenancy.Claims, i *RigNotificationRecipientUpdateInput, prev *RigNotificationRecipient) error {
	c := &RigNotificationRecipientValidatorContext{
		Values:   i.Merged(prev),
		Claims:   claims,
		previous: *prev,
		isUpdate: true,
		changed:  map[string]bool{},
	}
	c.changed[ColumnRigNotificationRecipientNotificationID] = i.NotificationID.IsSet()
	c.changed[ColumnRigNotificationRecipientAccountID] = i.AccountID.IsSet()
	c.changed[ColumnRigNotificationRecipientKind] = i.Kind.IsSet()
	c.changed[ColumnRigNotificationRecipientGroupKey] = i.GroupKey.Touched()
	c.changed[ColumnRigNotificationRecipientEventCount] = i.EventCount.IsSet()
	c.changed[ColumnRigNotificationRecipientReadAt] = i.ReadAt.Touched()

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// RunRestore implements [dbhook.RestoreValidator]: it runs the service's rules
// against the row this restore would bring back.
//
// Every rule runs, not only the ones whose field the request mentioned. The
// row was not live, so nothing about it has been checked against the world it
// is returning to.
func (v RigNotificationRecipientUpdateValidator) RunRestore(ctx context.Context, claims tenancy.Claims, i *RigNotificationRecipientUpdateInput, prev *RigNotificationRecipient) error {
	c := &RigNotificationRecipientValidatorContext{
		Values:   i.Merged(prev),
		Claims:   claims,
		previous: *prev,
		isUpdate: true,
		changed:  map[string]bool{},
	}
	c.changed[ColumnRigNotificationRecipientNotificationID] = true
	c.changed[ColumnRigNotificationRecipientAccountID] = true
	c.changed[ColumnRigNotificationRecipientKind] = true
	c.changed[ColumnRigNotificationRecipientGroupKey] = true
	c.changed[ColumnRigNotificationRecipientEventCount] = true
	c.changed[ColumnRigNotificationRecipientReadAt] = true

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// run calls every configured hook and puts what each one said under the field
// it was about.
//
// Two kinds of answer. A [rigerr.FieldError] is about the input: it lands on
// the field and the others still run, so one request reports everything wrong
// with it. Anything else is the rule itself failing — a lookup that could
// not reach another service — and there is nothing to tell the caller about
// their input, so it comes back wrapped with the rule that could not be run,
// keeping whatever code it carried and becoming Internal if it carried none.
func (v RigNotificationRecipientUpdateValidator) run(ctx context.Context, c *RigNotificationRecipientValidatorContext) (*RigNotificationRecipientUpdateInputError, error) {
	var failed RigNotificationRecipientUpdateInputError

	if v.NotificationID != nil {
		if err := v.NotificationID(ctx, c, c.Values.NotificationID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate notification_id")
			}
			failed.NotificationID = field
		}
	}
	if v.AccountID != nil {
		if err := v.AccountID(ctx, c, c.Values.AccountID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate account_id")
			}
			failed.AccountID = field
		}
	}
	if v.Kind != nil {
		if err := v.Kind(ctx, c, c.Values.Kind); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate kind")
			}
			failed.Kind = field
		}
	}
	if v.GroupKey != nil {
		if err := v.GroupKey(ctx, c, c.Values.GroupKey); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate group_key")
			}
			failed.GroupKey = field
		}
	}
	if v.EventCount != nil {
		if err := v.EventCount(ctx, c, c.Values.EventCount); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate event_count")
			}
			failed.EventCount = field
		}
	}
	if v.ReadAt != nil {
		if err := v.ReadAt(ctx, c, c.Values.ReadAt); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate read_at")
			}
			failed.ReadAt = field
		}
	}

	if v.Entity != nil {
		if err := v.Entity(ctx, c); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate rig_notification_recipient")
			}
			failed.Entity = field
		}
	}

	if failed.Empty() {
		return nil, nil
	}
	return &failed, nil
}
