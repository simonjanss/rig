// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"strings"
)

// Whether an account is a person or a machine.
type RigAccountKind string

// The values of RigAccountKind.
const (
	// Somebody who signs in.
	RigAccountKindPerson RigAccountKind = "Person"
	// An account an integration's API key acts as. Nobody signs in to it.
	RigAccountKindService RigAccountKind = "Service"
)

// AllRigAccountKind is every value, in declaration order.
var AllRigAccountKind = []RigAccountKind{RigAccountKindPerson, RigAccountKindService}

// Valid reports whether the value is one the database will accept.
func (v RigAccountKind) Valid() bool {
	switch v {
	case RigAccountKindPerson, RigAccountKindService:
		return true
	default:
		return false
	}
}

// ParseRigAccountKind reads a value, accepting any casing and surrounding
// space.
//
// Normalization uses it, so "IN_PROGRESS" from one client and "in_progress"
// from another mean the same thing rather than one of them being a validation
// failure nobody can explain. The spelling still has to be the label’s: a
// value is a name the database knows, not a phrase.
func ParseRigAccountKind(s string) (RigAccountKind, bool) {
	switch strings.ToLower(strings.TrimSpace(s)) {
	case "person":
		return RigAccountKindPerson, true
	case "service":
		return RigAccountKindService, true
	default:
		return "", false
	}
}

// String implements fmt.Stringer.
func (v RigAccountKind) String() string { return string(v) }
