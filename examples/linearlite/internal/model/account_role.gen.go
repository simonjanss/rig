// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"github.com/google/uuid"
)

// Which roles an account holds.
//
// It is the one definition of a AccountRole: the repository scans into it and
// the API returns it, so there is no conversion between two shapes of the same
// thing and no field that can go missing from one of them.
type AccountRole struct {
	// Surrogate key; the pair below is what is unique.
	ID uuid.UUID `json:"id"`
	// The account holding the role.
	AccountID uuid.UUID `json:"accountId"`
	// The role it holds.
	RoleID uuid.UUID `json:"roleId"`
}

// TableAccountRole is the table this entity is stored in.
const TableAccountRole = "account_role"

// Column names for account_role, so nothing has to spell one out.
const (
	ColumnAccountRoleID        = "id"
	ColumnAccountRoleAccountID = "account_id"
	ColumnAccountRoleRoleID    = "role_id"
)

// AccountRoleColumns is every column, in the order the row is scanned.
var AccountRoleColumns = []string{"id", "account_id", "role_id"}
