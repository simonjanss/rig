// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"context"
	"strings"
	"time"

	"github.com/google/uuid"
	"github.com/simonjanss/rig/runtime/patch"
	"github.com/simonjanss/rig/runtime/rigerr"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// RigNotificationDeviceCreateInput is what creating a RigNotificationDevice
// takes.
//
// The identifier, the tenant, and the audit columns are absent: those are
// stamped by the repository from the request's claims.
type RigNotificationDeviceCreateInput struct {
	// Whose device it is.
	AccountID uuid.UUID `json:"accountId"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel RigNotificationChannel `json:"channel"`
	// What the provider was given to address this device. Opaque to rig.
	Token string `json:"token"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *string `json:"label"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *time.Time `json:"lastSeenAt"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *time.Time `json:"revokedAt"`
}

// Normalize tidies what was given before anything checks it.
//
// It runs first so that validation sees the value that will actually be
// stored: a title with a trailing space and one without are the same title,
// and rejecting the second for a length rule the first passes would be
// indefensible.
func (i *RigNotificationDeviceCreateInput) Normalize() {
	if v, ok := ParseRigNotificationChannel(string(i.Channel)); ok {
		i.Channel = v
	}
	i.Token = strings.TrimSpace(i.Token)
	if i.Label != nil {
		*i.Label = strings.TrimSpace(*i.Label)
	}
}

// RigNotificationDeviceCreateInputError says what was wrong with each field of
// a RigNotificationDeviceCreateInput.
//
// Its shape is the input's shape, so a client can attach every message to the
// field it is about without matching on strings. A member is nil when that
// field was fine, and the whole value is nil when the input was. It is what
// the 422 carries.
type RigNotificationDeviceCreateInputError struct {
	// Whose device it is.
	AccountID *rigerr.FieldError `json:"accountId,omitempty"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel *rigerr.FieldError `json:"channel,omitempty"`
	// What the provider was given to address this device. Opaque to rig.
	Token *rigerr.FieldError `json:"token,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *rigerr.FieldError `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *rigerr.FieldError `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *rigerr.FieldError `json:"revokedAt,omitempty"`

	// Entity is a problem with the row as a whole rather than with one field: what
	// the Entity rule said.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// Empty reports whether anything went wrong. A validator that found nothing
// returns nil rather than one of these.
func (e *RigNotificationDeviceCreateInputError) Empty() bool {
	if e == nil {
		return true
	}

	return e.AccountID == nil && e.Channel == nil && e.Token == nil && e.Label == nil && e.LastSeenAt == nil && e.RevokedAt == nil && e.Entity == nil
}

// Error implements error. The sentence is for logs and for a person; the
// structure above is what a client acts on.
func (e *RigNotificationDeviceCreateInputError) Error() string {
	var parts []string
	if e.AccountID != nil {
		parts = append(parts, "accountId "+e.AccountID.Error())
	}
	if e.Channel != nil {
		parts = append(parts, "channel "+e.Channel.Error())
	}
	if e.Token != nil {
		parts = append(parts, "token "+e.Token.Error())
	}
	if e.Label != nil {
		parts = append(parts, "label "+e.Label.Error())
	}
	if e.LastSeenAt != nil {
		parts = append(parts, "lastSeenAt "+e.LastSeenAt.Error())
	}
	if e.RevokedAt != nil {
		parts = append(parts, "revokedAt "+e.RevokedAt.Error())
	}
	if e.Entity != nil {
		parts = append(parts, e.Entity.Error())
	}

	return "rig_notification_device is not valid: " + strings.Join(parts, "; ")
}

// ErrorCode implements [rigerr.Coder]: the request was understood and its
// content is what is wrong, which is 422 and not 400.
func (e *RigNotificationDeviceCreateInputError) ErrorCode() rigerr.Code {
	return rigerr.CodeUnprocessableEntity
}

// ErrorFields implements [rigerr.FieldReporter], which is how the HTTP layer
// finds this and answers with it rather than with prose.
func (e *RigNotificationDeviceCreateInputError) ErrorFields() any { return e }

// Validate checks what the schema can decide on its own.
//
// Everything a column declares — NOT NULL, a length, an enumeration's values
// — is checked here, so a service only writes the rules that are actually
// about the business. Every field is checked before returning, because a form
// that reports one problem per round trip is a form people give up on.
//
// What comes back is a *RigNotificationDeviceCreateInputError, shaped like the
// input itself.
func (i *RigNotificationDeviceCreateInput) Validate() error {
	var failed RigNotificationDeviceCreateInputError

	if !i.Channel.Valid() {
		failed.Channel = rigerr.NewFieldError(rigerr.FieldCodeInvalidValue, "%q is not one of the allowed values", i.Channel)
	}
	if strings.TrimSpace(i.Token) == "" {
		failed.Token = rigerr.NewFieldError(rigerr.FieldCodeCannotBeEmpty, "cannot be empty")
	}

	if failed.Empty() {
		return nil
	}
	return &failed
}

// RigNotificationDeviceUpdateInput is what changing a RigNotificationDevice
// takes.
//
// A field left out is untouched. A nullable field set to null is cleared —
// which is why the two wrappers differ: a column that cannot hold null has no
// way to be given one, so clearing it is a compile error rather than a
// rejection at runtime. Immutable fields are not here at all.
type RigNotificationDeviceUpdateInput struct {
	// Whose device it is.
	AccountID patch.Optional[uuid.UUID] `json:"accountId"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel patch.Optional[RigNotificationChannel] `json:"channel"`
	// What the provider was given to address this device. Opaque to rig.
	Token patch.Optional[string] `json:"token"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label patch.Nullable[string] `json:"label"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt patch.Nullable[time.Time] `json:"lastSeenAt"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt patch.Nullable[time.Time] `json:"revokedAt"`
}

// Normalize tidies the fields this request actually carries.
//
// It does not fill in the ones it does not: the repository writes exactly the
// columns that were sent, and filling them here would turn every update into a
// write of every column — so two requests changing different fields of one
// row would start overwriting each other instead of composing.
func (i *RigNotificationDeviceUpdateInput) Normalize() {
	if v, ok := i.Channel.Get(); ok {
		if parsed, ok := ParseRigNotificationChannel(string(v)); ok {
			v = parsed
		}
		i.Channel = patch.NewOptional(v)
	}
	if v, ok := i.Token.Get(); ok {
		v = strings.TrimSpace(v)
		i.Token = patch.NewOptional(v)
	}
	if v, ok := i.Label.Get(); ok {
		v = strings.TrimSpace(v)
		i.Label = patch.NewNullable(v)
	}
}

// Merged is the row as it will be once this update is applied.
//
// It is what validation runs against, and the reason it exists: a rule
// spanning two fields cannot be checked from a partial request. "Ends after
// starts" is unanswerable when only one of them was sent.
//
// It returns a copy. The input keeps its patches, so the repository still
// writes only the columns that were actually given.
func (i RigNotificationDeviceUpdateInput) Merged(prev *RigNotificationDevice) RigNotificationDevice {
	out := *prev

	if v, ok := i.AccountID.Get(); ok {
		out.AccountID = v
	}
	if v, ok := i.Channel.Get(); ok {
		out.Channel = v
	}
	if v, ok := i.Token.Get(); ok {
		out.Token = v
	}
	if i.Label.Touched() {
		out.Label = i.Label.Ptr()
	}
	if i.LastSeenAt.Touched() {
		out.LastSeenAt = i.LastSeenAt.Ptr()
	}
	if i.RevokedAt.Touched() {
		out.RevokedAt = i.RevokedAt.Ptr()
	}

	return out
}

// RigNotificationDeviceUpdateInputError says what was wrong with each field of
// a RigNotificationDeviceUpdateInput.
//
// Its shape is the input's shape, so a client can attach every message to the
// field it is about without matching on strings. A member is nil when that
// field was fine, and the whole value is nil when the input was. It is what
// the 422 carries.
type RigNotificationDeviceUpdateInputError struct {
	// Whose device it is.
	AccountID *rigerr.FieldError `json:"accountId,omitempty"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel *rigerr.FieldError `json:"channel,omitempty"`
	// What the provider was given to address this device. Opaque to rig.
	Token *rigerr.FieldError `json:"token,omitempty"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *rigerr.FieldError `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *rigerr.FieldError `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *rigerr.FieldError `json:"revokedAt,omitempty"`

	// Entity is a problem with the row as a whole rather than with one field: what
	// the Entity rule said.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// Empty reports whether anything went wrong. A validator that found nothing
// returns nil rather than one of these.
func (e *RigNotificationDeviceUpdateInputError) Empty() bool {
	if e == nil {
		return true
	}

	return e.AccountID == nil && e.Channel == nil && e.Token == nil && e.Label == nil && e.LastSeenAt == nil && e.RevokedAt == nil && e.Entity == nil
}

// Error implements error. The sentence is for logs and for a person; the
// structure above is what a client acts on.
func (e *RigNotificationDeviceUpdateInputError) Error() string {
	var parts []string
	if e.AccountID != nil {
		parts = append(parts, "accountId "+e.AccountID.Error())
	}
	if e.Channel != nil {
		parts = append(parts, "channel "+e.Channel.Error())
	}
	if e.Token != nil {
		parts = append(parts, "token "+e.Token.Error())
	}
	if e.Label != nil {
		parts = append(parts, "label "+e.Label.Error())
	}
	if e.LastSeenAt != nil {
		parts = append(parts, "lastSeenAt "+e.LastSeenAt.Error())
	}
	if e.RevokedAt != nil {
		parts = append(parts, "revokedAt "+e.RevokedAt.Error())
	}
	if e.Entity != nil {
		parts = append(parts, e.Entity.Error())
	}

	return "rig_notification_device is not valid: " + strings.Join(parts, "; ")
}

// ErrorCode implements [rigerr.Coder]: the request was understood and its
// content is what is wrong, which is 422 and not 400.
func (e *RigNotificationDeviceUpdateInputError) ErrorCode() rigerr.Code {
	return rigerr.CodeUnprocessableEntity
}

// ErrorFields implements [rigerr.FieldReporter], which is how the HTTP layer
// finds this and answers with it rather than with prose.
func (e *RigNotificationDeviceUpdateInputError) ErrorFields() any { return e }

// Validate checks the row this update would produce.
//
// Against the merged state, not the request: a length rule on a field nobody
// sent still has to hold, and a rule about two fields needs both.
//
// What comes back is a *RigNotificationDeviceUpdateInputError, shaped like the
// input itself.
func (i *RigNotificationDeviceUpdateInput) Validate(prev *RigNotificationDevice) error {
	var failed RigNotificationDeviceUpdateInputError

	merged := i.Merged(prev)

	if !merged.Channel.Valid() {
		failed.Channel = rigerr.NewFieldError(rigerr.FieldCodeInvalidValue, "%q is not one of the allowed values", merged.Channel)
	}
	if strings.TrimSpace(merged.Token) == "" {
		failed.Token = rigerr.NewFieldError(rigerr.FieldCodeCannotBeEmpty, "cannot be empty")
	}

	if failed.Empty() {
		return nil
	}
	return &failed
}

// RigNotificationDeviceDeleteInput is what deleting a RigNotificationDevice
// takes.
type RigNotificationDeviceDeleteInput struct {
	// ID is the row to delete.
	ID uuid.UUID `json:"id"`
}

// RigNotificationDeviceValidatorContext is what a rule sees.
//
// Values is the row as it will be if this goes through — merged from the
// previous state on an update, so every field is set whether or not the
// request mentioned it. That is the point: "ends after starts" cannot be
// answered from a request that only carried one of them.
type RigNotificationDeviceValidatorContext struct {
	// Values is the intended end state.
	Values RigNotificationDevice

	// Claims are who is asking. They are a value rather than something to fetch
	// from the context because a rule that has to look them up is a rule that can
	// forget to, and because there is no case where they are absent: a write
	// without a caller is refused by the repository before any rule runs.
	Claims tenancy.Claims

	// previous is the row before this change, and is the zero value on a
	// create — there was nothing before.
	previous RigNotificationDevice
	isUpdate bool
	changed  map[string]bool
}

// IsUpdate reports whether there was a row before this.
func (c *RigNotificationDeviceValidatorContext) IsUpdate() bool { return c.isUpdate }

// Previous is the row as it was, and the zero value on a create. Check
// IsUpdate before reading it.
func (c *RigNotificationDeviceValidatorContext) Previous() RigNotificationDevice { return c.previous }

// Changed reports whether this request carried a new value for a column.
//
// It is what keeps an expensive rule from running on every update: a check
// that reaches another service to confirm a reference only needs to run when
// the reference actually moved. On a create everything is changed, because
// everything is new.
func (c *RigNotificationDeviceValidatorContext) Changed(column string) bool { return c.changed[column] }

// AccountIDChanged reports whether this request set account_id.
func (c *RigNotificationDeviceValidatorContext) AccountIDChanged() bool {
	return c.changed[ColumnRigNotificationDeviceAccountID]
}

// ChannelChanged reports whether this request set channel.
func (c *RigNotificationDeviceValidatorContext) ChannelChanged() bool {
	return c.changed[ColumnRigNotificationDeviceChannel]
}

// TokenChanged reports whether this request set token.
func (c *RigNotificationDeviceValidatorContext) TokenChanged() bool {
	return c.changed[ColumnRigNotificationDeviceToken]
}

// LabelChanged reports whether this request set label.
func (c *RigNotificationDeviceValidatorContext) LabelChanged() bool {
	return c.changed[ColumnRigNotificationDeviceLabel]
}

// LastSeenAtChanged reports whether this request set last_seen_at.
func (c *RigNotificationDeviceValidatorContext) LastSeenAtChanged() bool {
	return c.changed[ColumnRigNotificationDeviceLastSeenAt]
}

// RevokedAtChanged reports whether this request set revoked_at.
func (c *RigNotificationDeviceValidatorContext) RevokedAtChanged() bool {
	return c.changed[ColumnRigNotificationDeviceRevokedAt]
}

// RigNotificationDeviceCreateValidator is the rules for bringing a
// RigNotificationDevice into existence: what the schema cannot express.
//
// One optional function per field this operation can set, so the set of fields
// is the set of rules that could apply — a column an update cannot touch has
// no hook here to write by mistake. A nil one is skipped. Every configured
// hook runs, because a rule that fails should not hide the next one, so a
// request reports everything wrong with it.
//
// A hook returns a FieldError to attach the message to a specific field, or
// any other error to fail the request outright.
type RigNotificationDeviceCreateValidator struct {
	// Whose device it is.
	AccountID func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value uuid.UUID) error
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value RigNotificationChannel) error
	// What the provider was given to address this device. Opaque to rig.
	Token func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value string) error
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value *string) error
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value *time.Time) error
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value *time.Time) error

	// Entity runs after the per-field hooks, for a rule that is about the row
	// rather than about one column.
	Entity func(ctx context.Context, c *RigNotificationDeviceValidatorContext) error
}

// RunCreate implements [dbhook.CreateValidator]: it runs the service's rules
// against the row this input would produce.
//
// The generated checks are not repeated here. The repository runs Normalize
// and Validate first, so by the time a hook sees the input it is tidy and the
// schema is satisfied — which is what lets a hook be about the business
// rather than about NOT NULL.
func (v RigNotificationDeviceCreateValidator) RunCreate(ctx context.Context, claims tenancy.Claims, i *RigNotificationDeviceCreateInput) error {
	// Everything is new, so everything counts as changed.
	c := &RigNotificationDeviceValidatorContext{Claims: claims, changed: map[string]bool{}}
	c.Values.AccountID = i.AccountID
	c.changed[ColumnRigNotificationDeviceAccountID] = true
	c.Values.Channel = i.Channel
	c.changed[ColumnRigNotificationDeviceChannel] = true
	c.Values.Token = i.Token
	c.changed[ColumnRigNotificationDeviceToken] = true
	c.Values.Label = i.Label
	c.changed[ColumnRigNotificationDeviceLabel] = true
	c.Values.LastSeenAt = i.LastSeenAt
	c.changed[ColumnRigNotificationDeviceLastSeenAt] = true
	c.Values.RevokedAt = i.RevokedAt
	c.changed[ColumnRigNotificationDeviceRevokedAt] = true

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// run calls every configured hook and puts what each one said under the field
// it was about.
//
// Two kinds of answer. A [rigerr.FieldError] is about the input: it lands on
// the field and the others still run, so one request reports everything wrong
// with it. Anything else is the rule itself failing — a lookup that could
// not reach another service — and there is nothing to tell the caller about
// their input, so it comes back wrapped with the rule that could not be run,
// keeping whatever code it carried and becoming Internal if it carried none.
func (v RigNotificationDeviceCreateValidator) run(ctx context.Context, c *RigNotificationDeviceValidatorContext) (*RigNotificationDeviceCreateInputError, error) {
	var failed RigNotificationDeviceCreateInputError

	if v.AccountID != nil {
		if err := v.AccountID(ctx, c, c.Values.AccountID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate account_id")
			}
			failed.AccountID = field
		}
	}
	if v.Channel != nil {
		if err := v.Channel(ctx, c, c.Values.Channel); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate channel")
			}
			failed.Channel = field
		}
	}
	if v.Token != nil {
		if err := v.Token(ctx, c, c.Values.Token); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate token")
			}
			failed.Token = field
		}
	}
	if v.Label != nil {
		if err := v.Label(ctx, c, c.Values.Label); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate label")
			}
			failed.Label = field
		}
	}
	if v.LastSeenAt != nil {
		if err := v.LastSeenAt(ctx, c, c.Values.LastSeenAt); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate last_seen_at")
			}
			failed.LastSeenAt = field
		}
	}
	if v.RevokedAt != nil {
		if err := v.RevokedAt(ctx, c, c.Values.RevokedAt); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate revoked_at")
			}
			failed.RevokedAt = field
		}
	}

	if v.Entity != nil {
		if err := v.Entity(ctx, c); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate rig_notification_device")
			}
			failed.Entity = field
		}
	}

	if failed.Empty() {
		return nil, nil
	}
	return &failed, nil
}

// RigNotificationDeviceUpdateValidator is the rules for changing one that
// already exists: what the schema cannot express.
//
// One optional function per field this operation can set, so the set of fields
// is the set of rules that could apply — a column an update cannot touch has
// no hook here to write by mistake. A nil one is skipped. Every configured
// hook runs, because a rule that fails should not hide the next one, so a
// request reports everything wrong with it.
//
// A hook returns a FieldError to attach the message to a specific field, or
// any other error to fail the request outright.
type RigNotificationDeviceUpdateValidator struct {
	// Whose device it is.
	AccountID func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value uuid.UUID) error
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value RigNotificationChannel) error
	// What the provider was given to address this device. Opaque to rig.
	Token func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value string) error
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value *string) error
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value *time.Time) error
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt func(ctx context.Context, c *RigNotificationDeviceValidatorContext, value *time.Time) error

	// Entity runs after the per-field hooks, for a rule that is about the row
	// rather than about one column.
	Entity func(ctx context.Context, c *RigNotificationDeviceValidatorContext) error
}

// RunUpdate implements [dbhook.UpdateValidator]: it runs the service's rules
// against the row this update would produce, with the row as it was available
// for a rule about the change itself.
func (v RigNotificationDeviceUpdateValidator) RunUpdate(ctx context.Context, claims tenancy.Claims, i *RigNotificationDeviceUpdateInput, prev *RigNotificationDevice) error {
	c := &RigNotificationDeviceValidatorContext{
		Values:   i.Merged(prev),
		Claims:   claims,
		previous: *prev,
		isUpdate: true,
		changed:  map[string]bool{},
	}
	c.changed[ColumnRigNotificationDeviceAccountID] = i.AccountID.IsSet()
	c.changed[ColumnRigNotificationDeviceChannel] = i.Channel.IsSet()
	c.changed[ColumnRigNotificationDeviceToken] = i.Token.IsSet()
	c.changed[ColumnRigNotificationDeviceLabel] = i.Label.Touched()
	c.changed[ColumnRigNotificationDeviceLastSeenAt] = i.LastSeenAt.Touched()
	c.changed[ColumnRigNotificationDeviceRevokedAt] = i.RevokedAt.Touched()

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// run calls every configured hook and puts what each one said under the field
// it was about.
//
// Two kinds of answer. A [rigerr.FieldError] is about the input: it lands on
// the field and the others still run, so one request reports everything wrong
// with it. Anything else is the rule itself failing — a lookup that could
// not reach another service — and there is nothing to tell the caller about
// their input, so it comes back wrapped with the rule that could not be run,
// keeping whatever code it carried and becoming Internal if it carried none.
func (v RigNotificationDeviceUpdateValidator) run(ctx context.Context, c *RigNotificationDeviceValidatorContext) (*RigNotificationDeviceUpdateInputError, error) {
	var failed RigNotificationDeviceUpdateInputError

	if v.AccountID != nil {
		if err := v.AccountID(ctx, c, c.Values.AccountID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate account_id")
			}
			failed.AccountID = field
		}
	}
	if v.Channel != nil {
		if err := v.Channel(ctx, c, c.Values.Channel); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate channel")
			}
			failed.Channel = field
		}
	}
	if v.Token != nil {
		if err := v.Token(ctx, c, c.Values.Token); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate token")
			}
			failed.Token = field
		}
	}
	if v.Label != nil {
		if err := v.Label(ctx, c, c.Values.Label); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate label")
			}
			failed.Label = field
		}
	}
	if v.LastSeenAt != nil {
		if err := v.LastSeenAt(ctx, c, c.Values.LastSeenAt); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate last_seen_at")
			}
			failed.LastSeenAt = field
		}
	}
	if v.RevokedAt != nil {
		if err := v.RevokedAt(ctx, c, c.Values.RevokedAt); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate revoked_at")
			}
			failed.RevokedAt = field
		}
	}

	if v.Entity != nil {
		if err := v.Entity(ctx, c); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate rig_notification_device")
			}
			failed.Entity = field
		}
	}

	if failed.Empty() {
		return nil, nil
	}
	return &failed, nil
}
