// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"strings"
)

// The coarse level an account holds in its tenant.
type RigAccountRoleLevel string

// The values of RigAccountRoleLevel.
const (
	// Runs the tenant.
	RigAccountRoleLevelOwner RigAccountRoleLevel = "Owner"
	// Administers it without owning it.
	RigAccountRoleLevelAdmin RigAccountRoleLevel = "Admin"
	// An ordinary member. This is the default.
	RigAccountRoleLevelBasic RigAccountRoleLevel = "Basic"
)

// AllRigAccountRoleLevel is every value, in declaration order.
var AllRigAccountRoleLevel = []RigAccountRoleLevel{RigAccountRoleLevelOwner, RigAccountRoleLevelAdmin, RigAccountRoleLevelBasic}

// Valid reports whether the value is one the database will accept.
func (v RigAccountRoleLevel) Valid() bool {
	switch v {
	case RigAccountRoleLevelOwner, RigAccountRoleLevelAdmin, RigAccountRoleLevelBasic:
		return true
	default:
		return false
	}
}

// ParseRigAccountRoleLevel reads a value, accepting any casing and surrounding
// space.
//
// Normalization uses it, so "IN_PROGRESS" from one client and "in_progress"
// from another mean the same thing rather than one of them being a validation
// failure nobody can explain. The spelling still has to be the label’s: a
// value is a name the database knows, not a phrase.
func ParseRigAccountRoleLevel(s string) (RigAccountRoleLevel, bool) {
	switch strings.ToLower(strings.TrimSpace(s)) {
	case "owner":
		return RigAccountRoleLevelOwner, true
	case "admin":
		return RigAccountRoleLevelAdmin, true
	case "basic":
		return RigAccountRoleLevelBasic, true
	default:
		return "", false
	}
}

// String implements fmt.Stringer.
func (v RigAccountRoleLevel) String() string { return string(v) }
