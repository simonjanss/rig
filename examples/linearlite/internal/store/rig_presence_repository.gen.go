// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package store

import (
	"context"
	"fmt"
	"time"

	"github.com/google/uuid"
	pgx "github.com/jackc/pgx/v5"
	"github.com/simonjanss/rig/examples/linearlite/internal/model"
	"github.com/simonjanss/rig/observe"
	"github.com/simonjanss/rig/runtime/dbhook"
	"github.com/simonjanss/rig/runtime/dbx"
	"github.com/simonjanss/rig/runtime/query"
	"github.com/simonjanss/rig/runtime/readopt"
	"github.com/simonjanss/rig/runtime/rigerr"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// RigPresenceRepository reads and writes rig_presence rows.
//
// Every method scopes to the caller's tenant. There is no way to ask it not to
// from a request handler, which is the point.
type RigPresenceRepository interface {
	// Get returns one row by identifier.
	//
	// A lookup by primary key deliberately ignores the lifecycle filters: it
	// returns the row whether it is live, deleted, or a snapshot, because a caller
	// holding an identifier is usually asking about that exact row.
	Get(ctx context.Context, id uuid.UUID, opts ...readopt.Option) (*model.RigPresence, error)

	// List returns matching rows and the total ignoring pagination.
	//
	// The filter says which rows and the page says how many of them, in what
	// order. They are separate arguments because they arrive separately: the
	// filter is a request body, the page is two query parameters.
	List(ctx context.Context, f model.RigPresenceFilter, page model.RigPresencePage, opts ...readopt.Option) ([]*model.RigPresence, int64, error)

	// Create inserts a row, stamping the identifier, tenant and audit columns.
	//
	// It takes an envelope rather than the input alone: the rules that check the
	// input and the callbacks that run around the write belong to the same unit of
	// work as the write itself.
	Create(ctx context.Context, in dbhook.Create[model.RigPresenceCreateInput, model.RigPresence]) (*model.RigPresence, error)

	// Update changes the fields the input mentions and leaves the rest alone.
	Update(ctx context.Context, id uuid.UUID, in dbhook.Update[model.RigPresenceUpdateInput, model.RigPresence]) (*model.RigPresence, error)

	// Delete removes a row.
	Delete(ctx context.Context, in dbhook.Delete[model.RigPresenceDeleteInput, model.RigPresence]) error
}

// rigPresenceGroup turns a filter into the condition tree the runtime renders.
//
// The scope carries what a condition needs besides the filter itself: who is
// asking, which alias this level's columns belong to, and how deep the nesting
// has gone. It is a value rather than three parameters because every relation
// passes a changed copy of it down.
func rigPresenceGroup(f model.RigPresenceFilter, sc filterScope) (query.Group, error) {
	if err := sc.ok(); err != nil {
		return query.Group{}, err
	}

	g := query.Group{Or: f.OrCondition}

	if p := f.Equals; p != nil {
		if p.ID != nil {
			g.Add(sc.at(query.Eq("id", *p.ID)))
		}
		if p.AccountID != nil {
			g.Add(sc.at(query.Eq("account_id", *p.AccountID)))
		}
		if p.SessionKey != nil {
			g.Add(sc.at(query.Eq("session_key", *p.SessionKey)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Eq("created_at", *p.CreatedAt)))
		}
		if p.SeenAt != nil {
			g.Add(sc.at(query.Eq("seen_at", *p.SeenAt)))
		}
		if p.Scope != nil {
			g.Add(sc.at(query.Eq("scope", *p.Scope)))
		}
		if p.TargetTable != nil {
			g.Add(sc.at(query.Eq("target_table", p.TargetTable)))
		}
		if p.TargetID != nil {
			g.Add(sc.at(query.Eq("target_id", p.TargetID)))
		}
		if p.TargetField != nil {
			g.Add(sc.at(query.Eq("target_field", p.TargetField)))
		}
		if p.Activity != nil {
			g.Add(sc.at(query.Eq("activity", *p.Activity)))
		}
	}
	if p := f.NotEquals; p != nil {
		if p.ID != nil {
			g.Add(sc.at(query.Ne("id", *p.ID)))
		}
		if p.AccountID != nil {
			g.Add(sc.at(query.Ne("account_id", *p.AccountID)))
		}
		if p.SessionKey != nil {
			g.Add(sc.at(query.Ne("session_key", *p.SessionKey)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Ne("created_at", *p.CreatedAt)))
		}
		if p.SeenAt != nil {
			g.Add(sc.at(query.Ne("seen_at", *p.SeenAt)))
		}
		if p.Scope != nil {
			g.Add(sc.at(query.Ne("scope", *p.Scope)))
		}
		if p.TargetTable != nil {
			g.Add(sc.at(query.Ne("target_table", p.TargetTable)))
		}
		if p.TargetID != nil {
			g.Add(sc.at(query.Ne("target_id", p.TargetID)))
		}
		if p.TargetField != nil {
			g.Add(sc.at(query.Ne("target_field", p.TargetField)))
		}
		if p.Activity != nil {
			g.Add(sc.at(query.Ne("activity", *p.Activity)))
		}
	}
	if p := f.GreaterThan; p != nil {
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Gt("created_at", *p.CreatedAt)))
		}
		if p.SeenAt != nil {
			g.Add(sc.at(query.Gt("seen_at", *p.SeenAt)))
		}
	}
	if p := f.SmallerThan; p != nil {
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Lt("created_at", *p.CreatedAt)))
		}
		if p.SeenAt != nil {
			g.Add(sc.at(query.Lt("seen_at", *p.SeenAt)))
		}
	}
	if p := f.GreaterOrEqual; p != nil {
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Gte("created_at", *p.CreatedAt)))
		}
		if p.SeenAt != nil {
			g.Add(sc.at(query.Gte("seen_at", *p.SeenAt)))
		}
	}
	if p := f.SmallerOrEqual; p != nil {
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Lte("created_at", *p.CreatedAt)))
		}
		if p.SeenAt != nil {
			g.Add(sc.at(query.Lte("seen_at", *p.SeenAt)))
		}
	}
	if p := f.Contains; p != nil {
		if len(p.ID) > 0 {
			g.Add(sc.at(query.In("id", p.ID)))
		}
		if len(p.AccountID) > 0 {
			g.Add(sc.at(query.In("account_id", p.AccountID)))
		}
		if len(p.SessionKey) > 0 {
			g.Add(sc.at(query.In("session_key", p.SessionKey)))
		}
		if len(p.CreatedAt) > 0 {
			g.Add(sc.at(query.In("created_at", p.CreatedAt)))
		}
		if len(p.SeenAt) > 0 {
			g.Add(sc.at(query.In("seen_at", p.SeenAt)))
		}
		if len(p.Scope) > 0 {
			g.Add(sc.at(query.In("scope", p.Scope)))
		}
		if len(p.TargetTable) > 0 {
			g.Add(sc.at(query.In("target_table", p.TargetTable)))
		}
		if len(p.TargetID) > 0 {
			g.Add(sc.at(query.In("target_id", p.TargetID)))
		}
		if len(p.TargetField) > 0 {
			g.Add(sc.at(query.In("target_field", p.TargetField)))
		}
		if len(p.Activity) > 0 {
			g.Add(sc.at(query.In("activity", p.Activity)))
		}
	}
	if p := f.NotContains; p != nil {
		if len(p.ID) > 0 {
			g.Add(sc.at(query.NotIn("id", p.ID)))
		}
		if len(p.AccountID) > 0 {
			g.Add(sc.at(query.NotIn("account_id", p.AccountID)))
		}
		if len(p.SessionKey) > 0 {
			g.Add(sc.at(query.NotIn("session_key", p.SessionKey)))
		}
		if len(p.CreatedAt) > 0 {
			g.Add(sc.at(query.NotIn("created_at", p.CreatedAt)))
		}
		if len(p.SeenAt) > 0 {
			g.Add(sc.at(query.NotIn("seen_at", p.SeenAt)))
		}
		if len(p.Scope) > 0 {
			g.Add(sc.at(query.NotIn("scope", p.Scope)))
		}
		if len(p.TargetTable) > 0 {
			g.Add(sc.at(query.NotIn("target_table", p.TargetTable)))
		}
		if len(p.TargetID) > 0 {
			g.Add(sc.at(query.NotIn("target_id", p.TargetID)))
		}
		if len(p.TargetField) > 0 {
			g.Add(sc.at(query.NotIn("target_field", p.TargetField)))
		}
		if len(p.Activity) > 0 {
			g.Add(sc.at(query.NotIn("activity", p.Activity)))
		}
	}
	if p := f.Like; p != nil {
		if p.SessionKey != nil {
			g.Add(sc.at(query.Like("session_key", *p.SessionKey)))
		}
		if p.Scope != nil {
			g.Add(sc.at(query.Like("scope", *p.Scope)))
		}
		if p.TargetTable != nil {
			g.Add(sc.at(query.Like("target_table", *p.TargetTable)))
		}
		if p.TargetField != nil {
			g.Add(sc.at(query.Like("target_field", *p.TargetField)))
		}
	}
	if p := f.NotLike; p != nil {
		if p.SessionKey != nil {
			g.Add(sc.at(query.NotLike("session_key", *p.SessionKey)))
		}
		if p.Scope != nil {
			g.Add(sc.at(query.NotLike("scope", *p.Scope)))
		}
		if p.TargetTable != nil {
			g.Add(sc.at(query.NotLike("target_table", *p.TargetTable)))
		}
		if p.TargetField != nil {
			g.Add(sc.at(query.NotLike("target_field", *p.TargetField)))
		}
	}
	if p := f.Null; p != nil {
		if p.TargetTable != nil {
			if *p.TargetTable {
				g.Add(sc.at(query.IsNull("target_table")))
			} else {
				g.Add(sc.at(query.NotNull("target_table")))
			}
		}
		if p.TargetID != nil {
			if *p.TargetID {
				g.Add(sc.at(query.IsNull("target_id")))
			} else {
				g.Add(sc.at(query.NotNull("target_id")))
			}
		}
		if p.TargetField != nil {
			if *p.TargetField {
				g.Add(sc.at(query.IsNull("target_field")))
			} else {
				g.Add(sc.at(query.NotNull("target_field")))
			}
		}
	}
	if p := f.NotNull; p != nil {
		if p.TargetTable != nil {
			if *p.TargetTable {
				g.Add(sc.at(query.NotNull("target_table")))
			} else {
				g.Add(sc.at(query.IsNull("target_table")))
			}
		}
		if p.TargetID != nil {
			if *p.TargetID {
				g.Add(sc.at(query.NotNull("target_id")))
			} else {
				g.Add(sc.at(query.IsNull("target_id")))
			}
		}
		if p.TargetField != nil {
			if *p.TargetField {
				g.Add(sc.at(query.NotNull("target_field")))
			} else {
				g.Add(sc.at(query.IsNull("target_field")))
			}
		}
	}

	for _, n := range f.NestedFilters {
		nested, err := rigPresenceGroup(n, sc)
		if err != nil {
			return query.Group{}, err
		}
		g.Nest(nested)
	}
	return g, nil
}

// rigPresenceSortable reports whether a column can be ordered by.
func rigPresenceSortable(column string) bool {
	switch column {
	case "id", "tenant_id", "account_id", "session_key", "created_at", "seen_at", "scope", "target_table", "target_id", "target_field", "activity":
		return true
	}
	return false
}

// rigPresenceOrder converts the model's ordering terms into the runtime's.
func rigPresenceOrder(terms []model.RigPresenceOrder, sc filterScope) ([]query.Order, []query.Join, error) {
	if len(terms) == 0 {
		return nil, nil, nil
	}

	out := make([]query.Order, 0, len(terms))

	for _, t := range terms {
		// A column this table cannot be ordered by is the caller's mistake, not a
		// column name to paste into a statement.
		if !rigPresenceSortable(t.Column) {
			return nil, nil, rigerr.Invalid("a RigPresence cannot be ordered by %q", t.Column)
		}
		out = append(out, query.Order{Table: sc.as, Column: t.Column, Desc: t.Desc})
	}

	return out, nil, nil
}

type rigPresenceRepo struct {
	db *Store
}

var _ RigPresenceRepository = (*rigPresenceRepo)(nil)

// trace runs one stage of a write inside a span of its own.
//
// The stage is a callback rather than something bracketed by two calls,
// because that is what makes the span a function's: it is opened and ended in
// one place, and nothing at the call site is holding one.
func (r *rigPresenceRepo) trace(ctx context.Context, name string, f func(context.Context) error) error {
	return observe.Trace(ctx, r.db.tracer, name, f)
}

const rigPresenceRepoSelect = "rig_presence.id, rig_presence.tenant_id, rig_presence.account_id, rig_presence.session_key, rig_presence.created_at, rig_presence.seen_at, rig_presence.scope, rig_presence.target_table, rig_presence.target_id, rig_presence.target_field, rig_presence.activity"

// scanRigPresence reads one row in the order rigPresenceRepoSelect lists.
func scanRigPresence(row pgx.Row) (*model.RigPresence, error) {
	var m model.RigPresence
	if err := row.Scan(&m.ID, &m.TenantID, &m.AccountID, &m.SessionKey, &m.CreatedAt, &m.SeenAt, &m.Scope, &m.TargetTable, &m.TargetID, &m.TargetField, &m.Activity); err != nil {
		return nil, err
	}
	m.CreatedAt = dbx.UTC(m.CreatedAt)
	m.SeenAt = dbx.UTC(m.SeenAt)
	return &m, nil
}

// Get implements RigPresenceRepository.
func (r *rigPresenceRepo) Get(ctx context.Context, id uuid.UUID, opts ...readopt.Option) (*model.RigPresence, error) {
	ctx, span := r.db.tracer.Start(ctx, "repository.RigPresence.Get")
	defer span.End()

	cfg, err := readopt.Apply(opts)
	if err != nil {
		return nil, err
	}

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}

	args := []any{id}
	where := "id = $1"
	if !cfg.SkipTenantScope {
		args = append(args, claims.TenantID)
		where += " AND tenant_id = $2"
	}

	sql := fmt.Sprintf("SELECT %s FROM rig_presence WHERE %s", rigPresenceRepoSelect, where)
	m, err := scanRigPresence(r.db.connFor(ctx).QueryRow(ctx, sql, args...))
	if dbx.IsNoRows(err) {
		return nil, rigerr.NotFound("no RigPresence with id %s", id)
	}
	if err != nil {
		return nil, rigerr.Internal(err, "read rig_presence")
	}
	return m, nil
}

// List implements RigPresenceRepository.
func (r *rigPresenceRepo) List(ctx context.Context, f model.RigPresenceFilter, page model.RigPresencePage, opts ...readopt.Option) ([]*model.RigPresence, int64, error) {
	ctx, span := r.db.tracer.Start(ctx, "repository.RigPresence.List")
	defer span.End()

	return r.list(ctx, f, page, opts)
}

// list is the body of every read that takes a filter.
func (r *rigPresenceRepo) list(ctx context.Context, f model.RigPresenceFilter, page model.RigPresencePage, opts []readopt.Option) ([]*model.RigPresence, int64, error) {
	cfg, err := readopt.Apply(opts)
	if err != nil {
		return nil, 0, err
	}

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, 0, err
	}

	// One scope for the whole statement: it says who is asking, and it qualifies
	// every column with the table it belongs to. The qualification is not
	// decoration — an ordering that reaches a related table brings a second
	// tenant_id into scope, and an unqualified one is then ambiguous.
	sc := newFilterScope(claims, "rig_presence")

	scope := query.Group{}
	if !cfg.SkipTenantScope {
		scope.Add(sc.at(query.Eq("tenant_id", claims.TenantID)))
	}

	group, err := rigPresenceGroup(f, sc)
	if err != nil {
		return nil, 0, err
	}
	scope.Nest(group)

	// The count has its own arguments and no joins. A left join cannot change how
	// many rows there are, and a statement must be given exactly the parameters
	// its text refers to.
	countArgs := query.NewArgs()
	countWhere := scope.SQL(countArgs)
	if countWhere != "" {
		countWhere = " WHERE " + countWhere
	}
	countSQL := fmt.Sprintf("SELECT count(*) FROM rig_presence%s", countWhere)
	var total int64
	if err := r.db.connFor(ctx).QueryRow(ctx, countSQL, countArgs.Values()...).Scan(&total); err != nil {
		return nil, 0, rigerr.Internal(err, "count rig_presence")
	}

	order, joins, err := rigPresenceOrder(page.OrderBy, sc)
	if err != nil {
		return nil, 0, err
	}
	if len(order) == 0 {
		order = RigPresenceDefaultOrder
	}
	window := query.Page{Limit: page.Limit, Offset: page.Offset}.Clamp(DefaultLimit, MaxLimit)

	// The joins render first, because Postgres numbers placeholders in the order
	// the statement mentions them and a join stands before the conditions do.
	args := query.NewArgs()
	joinSQL := query.JoinSQL(joins, args)
	where := scope.SQL(args)
	if where != "" {
		where = " WHERE " + where
	}

	listSQL := fmt.Sprintf("SELECT %s FROM rig_presence%s%s%s%s", rigPresenceRepoSelect, joinSQL, where, query.OrderSQL(order), window.SQL(args))
	rows, err := r.db.connFor(ctx).Query(ctx, listSQL, args.Values()...)
	if err != nil {
		return nil, 0, rigerr.Internal(err, "list rig_presence")
	}
	defer rows.Close()

	out := make([]*model.RigPresence, 0, window.Limit)
	for rows.Next() {
		m, err := scanRigPresence(rows)
		if err != nil {
			return nil, 0, rigerr.Internal(err, "read rig_presence")
		}
		out = append(out, m)
	}
	if err := rows.Err(); err != nil {
		return nil, 0, rigerr.Internal(err, "list rig_presence")
	}
	return out, total, nil
}

// RigPresenceDefaultOrder is the ordering used when a query asks for none.
//
// It always ends with the primary key, so the order is total and a page
// boundary cannot repeat or skip a row.
var RigPresenceDefaultOrder = []query.Order{{Table: "rig_presence", Column: "created_at", Desc: true}, {Table: "rig_presence", Column: "id", Desc: false}}

// Create implements RigPresenceRepository.
func (r *rigPresenceRepo) Create(ctx context.Context, in dbhook.Create[model.RigPresenceCreateInput, model.RigPresence]) (*model.RigPresence, error) {
	ctx, span := r.db.tracer.Start(ctx, "repository.RigPresence.Create")
	defer span.End()

	// Who is asking, first of all. A write from a request carrying no identity is
	// refused here — before a rule runs, before a column notices — which is
	// what lets every hook below take the claims as a value rather than something
	// to look up and check.
	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}
	_ = claims

	// Normalize then validate, before anything is written. A service that forgets
	// to call these cannot write a row that would fail them, which is the only way
	// a rule the schema declares is actually a rule.
	in.Input.Normalize()
	if err := in.Input.Validate(); err != nil {
		return nil, err
	}

	// Then the rules the schema cannot express, if the service wrote any. They run
	// before the transaction opens: a rule that reaches another service should not
	// be holding a row lock while it waits.
	//
	// They run before the create hook rather than after it, which is the one place
	// the order goes this way: an update validates inside the transaction that
	// read the row, so its hook can run first and have what it sets judged. Here
	// there is no row to read and no transaction yet, and opening one to hold
	// across a rule that may call out to another service would be a worse trade
	// than the one it buys.
	if in.Hooks.Validator != nil {
		if err := r.trace(ctx, "repository.RigPresence.Create.Validator", func(ctx context.Context) error {
			return in.Hooks.Validator.RunCreate(ctx, claims, &in.Input)
		}); err != nil {
			return nil, err
		}
	}

	// A version 7 identifier sorts by creation time, so an index on the primary
	// key stays dense as rows are added.
	id, err := uuid.NewV7()
	if err != nil {
		return nil, rigerr.Internal(err, "generate an identifier")
	}
	now := time.Now().UTC()
	_ = now

	// A single INSERT is already atomic, so the transaction is opened only when a
	// hook needs to be able to undo it. Two round trips is not a price to pay for
	// nothing.
	needsTx := in.Hooks.Before != nil || in.Hooks.After != nil

	var m *model.RigPresence
	err = dbx.InTxIf(ctx, r.db.pool, r.db.connFor(ctx), needsTx, func(ctx context.Context, tx dbx.Conn) error {
		if in.Hooks.Before != nil {
			if err := r.trace(ctx, "repository.RigPresence.Create.Before", func(ctx context.Context) error {
				return in.Hooks.Before(ctx, claims, &in.Input)
			}); err != nil {
				return err
			}
		}

		columns := []string{"id", "tenant_id", "created_at", "account_id", "session_key", "seen_at", "scope", "target_table", "target_id", "target_field", "activity"}
		values := []any{id, claims.TenantID, now, in.Input.AccountID, in.Input.SessionKey, in.Input.SeenAt, in.Input.Scope, in.Input.TargetTable, in.Input.TargetID, in.Input.TargetField, in.Input.Activity}

		sql := fmt.Sprintf("INSERT INTO rig_presence (%s) VALUES (%s) RETURNING %s", joinColumns(columns), placeholders(len(values)), rigPresenceRepoSelect)

		created, err := scanRigPresence(tx.QueryRow(ctx, sql, values...))
		if err != nil {
			return writeError(err, "rig_presence")
		}
		m = created

		if in.Hooks.After != nil {
			if err := r.trace(ctx, "repository.RigPresence.Create.After", func(ctx context.Context) error {
				return in.Hooks.After(ctx, claims, m)
			}); err != nil {
				return err
			}
		}
		return nil
	})
	if err != nil {
		return nil, err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() {
			ctx, span := r.db.tracer.Start(ctx, "repository.RigPresence.Create.AfterCommit")
			defer span.End()

			done(ctx, who, m)
		})
	}

	return m, nil
}

// Update implements RigPresenceRepository.
func (r *rigPresenceRepo) Update(ctx context.Context, id uuid.UUID, in dbhook.Update[model.RigPresenceUpdateInput, model.RigPresence]) (*model.RigPresence, error) {
	ctx, span := r.db.tracer.Start(ctx, "repository.RigPresence.Update")
	defer span.End()

	in.Input.Normalize()

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}
	_ = claims

	var updated, prev *model.RigPresence
	err = dbx.InTx(ctx, r.db.pool, func(ctx context.Context, tx dbx.Conn) error {
		prev, err = r.Get(ctx, id)
		if err != nil {
			return err
		}

		// The hook shapes the input, so a field it sets is a field that gets written
		// — and one the rules below then judge. It runs first for exactly that
		// reason: a hook that ran after validation could write a value nothing had
		// checked.
		if in.Hooks.Before != nil {
			if err := r.trace(ctx, "repository.RigPresence.Update.Before", func(ctx context.Context) error {
				return in.Hooks.Before(ctx, claims, &in.Input, prev)
			}); err != nil {
				return err
			}
		}

		// Validation runs against the row this update would produce, not against the
		// request — a rule about two fields needs both, and only one of them may
		// have been sent. It happens inside the transaction that read prev, so nothing
		// can change underneath it.
		if err := in.Input.Validate(prev); err != nil {
			return err
		}

		if in.Hooks.Validator != nil {
			if err := r.trace(ctx, "repository.RigPresence.Update.Validator", func(ctx context.Context) error {
				return in.Hooks.Validator.RunUpdate(ctx, claims, &in.Input, prev)
			}); err != nil {
				return err
			}
		}

		columns := []string{}
		values := []any{}

		if v, ok := in.Input.AccountID.Get(); ok {
			columns = append(columns, "account_id")
			values = append(values, v)
		}
		if v, ok := in.Input.SessionKey.Get(); ok {
			columns = append(columns, "session_key")
			values = append(values, v)
		}
		if v, ok := in.Input.SeenAt.Get(); ok {
			columns = append(columns, "seen_at")
			values = append(values, v)
		}
		if v, ok := in.Input.Scope.Get(); ok {
			columns = append(columns, "scope")
			values = append(values, v)
		}
		if in.Input.TargetTable.Touched() {
			columns = append(columns, "target_table")
			values = append(values, in.Input.TargetTable.Ptr())
		}
		if in.Input.TargetID.Touched() {
			columns = append(columns, "target_id")
			values = append(values, in.Input.TargetID.Ptr())
		}
		if in.Input.TargetField.Touched() {
			columns = append(columns, "target_field")
			values = append(values, in.Input.TargetField.Ptr())
		}
		if v, ok := in.Input.Activity.Get(); ok {
			columns = append(columns, "activity")
			values = append(values, v)
		}

		if len(columns) == 0 {
			// Nothing was asked for, so nothing happens — no stamp, and no snapshot
			// either. An update that changes no column did not happen, and a history full
			// of copies of an unchanged row is a history nobody can read.
			updated = prev
			return nil
		}

		values = append(values, id)
		sql := fmt.Sprintf("UPDATE rig_presence SET %s WHERE id = $%d RETURNING %s", assignments(columns), len(values), rigPresenceRepoSelect)

		updated, err = scanRigPresence(tx.QueryRow(ctx, sql, values...))
		if err != nil {
			return writeError(err, "rig_presence")
		}

		if in.Hooks.After != nil {
			if err := r.trace(ctx, "repository.RigPresence.Update.After", func(ctx context.Context) error {
				return in.Hooks.After(ctx, claims, updated, prev)
			}); err != nil {
				return err
			}
		}
		return nil
	})
	if err != nil {
		return nil, err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() {
			ctx, span := r.db.tracer.Start(ctx, "repository.RigPresence.Update.AfterCommit")
			defer span.End()

			done(ctx, who, updated, prev)
		})
	}

	return updated, nil
}

// Delete implements RigPresenceRepository.
func (r *rigPresenceRepo) Delete(ctx context.Context, in dbhook.Delete[model.RigPresenceDeleteInput, model.RigPresence]) error {
	ctx, span := r.db.tracer.Start(ctx, "repository.RigPresence.Delete")
	defer span.End()

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return err
	}
	_ = claims

	var prev *model.RigPresence
	err = dbx.InTx(ctx, r.db.pool, func(ctx context.Context, tx dbx.Conn) error {
		prev, err = r.Get(ctx, in.Input.ID)
		if err != nil {
			return err
		}

		if in.Hooks.Before != nil {
			if err := r.trace(ctx, "repository.RigPresence.Delete.Before", func(ctx context.Context) error {
				return in.Hooks.Before(ctx, claims, &in.Input, prev)
			}); err != nil {
				return err
			}
		}

		if _, err := tx.Exec(ctx, "DELETE FROM rig_presence WHERE id = $1", in.Input.ID); err != nil {
			return writeError(err, "rig_presence")
		}
		if in.Hooks.After != nil {
			if err := r.trace(ctx, "repository.RigPresence.Delete.After", func(ctx context.Context) error {
				return in.Hooks.After(ctx, claims, prev)
			}); err != nil {
				return err
			}
		}
		return nil
	})
	if err != nil {
		return err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() {
			ctx, span := r.db.tracer.Start(ctx, "repository.RigPresence.Delete.AfterCommit")
			defer span.End()

			done(ctx, who, prev)
		})
	}

	return nil
}
