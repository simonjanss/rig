// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package client

import (
	"context"
	"iter"
	"net/http"
	"net/url"
	"strings"

	"github.com/google/uuid"
	"github.com/simonjanss/rig/rigclient"
)

// NotificationClient calls the Notification endpoints. It is reached as
// Client.Notifications rather than built directly.
type NotificationClient struct{ rt *rigclient.Runtime }

// List Notifications.
//
// GET /api/v1/notifications
//
// Operation listNotifications.
func (c *NotificationClient) List(ctx context.Context, q NotificationListQuery, opts ...rigclient.CallOption) (*NotificationListResponse, error) {
	query := url.Values{}
	rigclient.SetInt(query, "limit", q.Limit)
	rigclient.SetInt(query, "offset", q.Offset)

	op := rigclient.Op{
		Name:   "listNotifications",
		Method: http.MethodGet,
		Path:   "/notifications",
		Query:  query,
	}
	return rigclient.Do[NotificationListResponse](ctx, c.rt, op, opts...)
}

// Fetch one Notification by identifier.
//
// GET /api/v1/notifications/{id}
//
// Operation getNotification.
func (c *NotificationClient) Get(ctx context.Context, id uuid.UUID, opts ...rigclient.CallOption) (*Notification, error) {
	op := rigclient.Op{
		Name:   "getNotification",
		Method: http.MethodGet,
		Path:   strings.Replace("/notifications/{id}", "{id}", rigclient.PathValue(id.String()), 1),
	}
	return rigclient.Do[Notification](ctx, c.rt, op, opts...)
}

// All reads every Notification the query matches, a page at a time.
//
// The query's own limit is the page size, and its offset is where the walk
// starts. Iteration stops at the first failure, which arrives as the second
// value of the last pair — so a loop that ignores it is a loop that silently
// stops early.
func (c *NotificationClient) All(ctx context.Context, q NotificationListQuery, opts ...rigclient.CallOption) iter.Seq2[Notification, error] {
	start := 0
	if q.Offset != nil {
		start = *q.Offset
	}

	return rigclient.Paginate(ctx, start, func(ctx context.Context, offset int) (rigclient.Page[Notification], error) {
		q := q
		q.Offset = &offset

		res, err := c.List(ctx, q, opts...)
		if err != nil {
			return rigclient.Page[Notification]{}, err
		}
		if res == nil {
			return rigclient.Page[Notification]{}, nil
		}

		return rigclient.Page[Notification]{
			Items:  res.Data,
			Total:  res.Pagination.Total,
			Offset: res.Pagination.Offset,
		}, nil
	})
}
