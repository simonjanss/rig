// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type {
    TodoAttachmentFilter,
    TodoAttachmentFilterContains,
    TodoAttachmentFilterEquals,
    TodoAttachmentFilterLike,
    TodoAttachmentFilterNull,
    TodoAttachmentFilterRange,
} from "./todo_attachment_query.gen.js";
import type { TodoPriority } from "./todo_priority.gen.js";
import type { TodoStatus } from "./todo_status.gen.js";
import type { TodoVersionType } from "./todo_version_type.gen.js";

/** Conditions a Todo must satisfy to match a search. */
export type TodoFilter = {
    /** Fields that must equal the given value. */
    equals?: TodoFilterEquals | null;
    /** Fields that must not equal the given value. */
    notEquals?: TodoFilterEquals | null;
    /** Fields that must be greater than the given value. */
    greaterThan?: TodoFilterRange | null;
    /** Fields that must be less than the given value. */
    smallerThan?: TodoFilterRange | null;
    /** Fields that must be greater than or equal to the given value. */
    greaterOrEqual?: TodoFilterRange | null;
    /** Fields that must be less than or equal to the given value. */
    smallerOrEqual?: TodoFilterRange | null;
    /** Fields that must be one of the given values. */
    contains?: TodoFilterContains | null;
    /** Fields that must not be one of the given values. */
    notContains?: TodoFilterContains | null;
    /** Text fields that must match the given pattern. */
    like?: TodoFilterLike | null;
    /** Text fields that must not match the given pattern. */
    notLike?: TodoFilterLike | null;
    /** Fields that must be null. */
    null?: TodoFilterNull | null;
    /** Fields that must not be null. */
    notNull?: TodoFilterNull | null;
    /** Combine this filter's conditions with OR instead of AND. */
    orCondition: boolean;
    /**
     * Sub-filters combined with this one, so that AND and OR can be mixed to
     * any depth.
     */
    nestedFilters: TodoFilter[];
    /**
     * Relations with no row matching the given conditions. An empty one asks
     * for no related row at all.
     *
     * This is where a negation belongs when what you mean is "not": it negates
     * whether a matching related row exists, so a row with no related row at
     * all satisfies it. The negative operators above negate the comparison
     * instead and still require the related row to be there — "no related row
     * called X" is here, "a related row not called X" is NotEquals.
     */
    without?: TodoFilterWithout | null;
};

/** Set-membership conditions on Todo fields. */
export type TodoFilterContains = {
    /** Unique identifier for this row. */
    id: string[];
    /** What needs doing, in a few words. */
    title: string[];
    /** The longer story, or null while the title says it all. */
    description: string[];
    /** Which column of the board the item is in. */
    status: TodoStatus[];
    /** How urgently the item wants attention. */
    priority: TodoPriority[];
    /** Who has taken the item, or null while nobody has. */
    assigneeAccountId: string[];
    /** When this row was created. Set automatically and never changes. */
    createdAt: string[];
    /** Account that created this row, taken from the request's claims. */
    createdByAccountId: string[];
    /** When this row was last updated. Set automatically on every update. */
    updatedAt: string[];
    /** Account that last updated this row, taken from the request's claims. */
    updatedByAccountId: string[];
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt: string[];
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId: string[];
    /**
     * Whether this row is the live version or an immutable snapshot of a
     * previous one.
     */
    versionType: TodoVersionType[];
    /** The row this snapshot was copied from. Null on live rows. */
    snapshotFromTodoId: string[];
    /**
     * The source row's last-updated time at the moment this snapshot was taken.
     * This identifies the version captured, not when the copy was made.
     */
    snapshotFromTodoAt: string[];
    /**
     * Conditions on one of this row's TodoAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.TodoAttachments asks the second.
     */
    todoAttachments?: TodoAttachmentFilterContains | null;
};

/** Exact-match conditions on Todo fields. */
export type TodoFilterEquals = {
    /** Unique identifier for this row. */
    id?: string | null;
    /** What needs doing, in a few words. */
    title?: string | null;
    /** The longer story, or null while the title says it all. */
    description?: string | null;
    /** Which column of the board the item is in. */
    status?: TodoStatus | null;
    /** How urgently the item wants attention. */
    priority?: TodoPriority | null;
    /** Who has taken the item, or null while nobody has. */
    assigneeAccountId?: string | null;
    /** When this row was created. Set automatically and never changes. */
    createdAt?: string | null;
    /** Account that created this row, taken from the request's claims. */
    createdByAccountId?: string | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: string | null;
    /** Account that last updated this row, taken from the request's claims. */
    updatedByAccountId?: string | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: string | null;
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId?: string | null;
    /**
     * Whether this row is the live version or an immutable snapshot of a
     * previous one.
     */
    versionType?: TodoVersionType | null;
    /** The row this snapshot was copied from. Null on live rows. */
    snapshotFromTodoId?: string | null;
    /**
     * The source row's last-updated time at the moment this snapshot was taken.
     * This identifies the version captured, not when the copy was made.
     */
    snapshotFromTodoAt?: string | null;
    /**
     * Conditions on one of this row's TodoAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.TodoAttachments asks the second.
     */
    todoAttachments?: TodoAttachmentFilterEquals | null;
};

/** Pattern conditions on Todo text fields. */
export type TodoFilterLike = {
    /** What needs doing, in a few words. */
    title?: string | null;
    /** The longer story, or null while the title says it all. */
    description?: string | null;
    /**
     * Conditions on one of this row's TodoAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.TodoAttachments asks the second.
     */
    todoAttachments?: TodoAttachmentFilterLike | null;
};

/** Presence conditions on nullable Todo fields. */
export type TodoFilterNull = {
    /** The longer story, or null while the title says it all. */
    description?: boolean | null;
    /** Who has taken the item, or null while nobody has. */
    assigneeAccountId?: boolean | null;
    /** Account that created this row, taken from the request's claims. */
    createdByAccountId?: boolean | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: boolean | null;
    /** Account that last updated this row, taken from the request's claims. */
    updatedByAccountId?: boolean | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: boolean | null;
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId?: boolean | null;
    /** The row this snapshot was copied from. Null on live rows. */
    snapshotFromTodoId?: boolean | null;
    /**
     * The source row's last-updated time at the moment this snapshot was taken.
     * This identifies the version captured, not when the copy was made.
     */
    snapshotFromTodoAt?: boolean | null;
    /**
     * Conditions on one of this row's TodoAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.TodoAttachments asks the second.
     */
    todoAttachments?: TodoAttachmentFilterNull | null;
};

/** Ordering conditions on Todo fields that can be compared. */
export type TodoFilterRange = {
    /** When this row was created. Set automatically and never changes. */
    createdAt?: string | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: string | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: string | null;
    /**
     * The source row's last-updated time at the moment this snapshot was taken.
     * This identifies the version captured, not when the copy was made.
     */
    snapshotFromTodoAt?: string | null;
    /**
     * Conditions on one of this row's TodoAttachments.
     *
     * Every condition has to hold of the same one, not of the set between them.
     *
     * Under the negative operators this asks whether one of them differs, which
     * is not the same as none of them matching: a row can have one that matches
     * and one that does not, satisfying the first and failing the second.
     * Without.TodoAttachments asks the second.
     */
    todoAttachments?: TodoAttachmentFilterRange | null;
};

/** Relations a Todo must have no matching row for. */
export type TodoFilterWithout = {
    /**
     * Match rows with no TodoAttachments satisfying these conditions, including
     * rows with none at all.
     *
     * This negates the existence of a matching row rather than the comparison,
     * which is the whole difference from NotEquals.TodoAttachments: that one is
     * satisfied by one of them differing, and a row can have one that differs
     * and one that matches at the same time. An empty object asks for rows with
     * no TodoAttachments at all.
     */
    todoAttachments?: TodoAttachmentFilter | null;
};

