// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { NotificationState } from "./notification_state.gen.js";
import type { Pagination } from "./objects.gen.js";

/**
 * Something worth telling somebody about, and when it is due. Carries no
 * recipients: the audience is computed when it is sent.
 *
 * Every field the server returns, and no field it does not: this is the
 * readable projection of the row rather than the row.
 */
export type Notification = {
    /** Unique identifier for this row. */
    id: string;
    /** Tenant this row belongs to. Every query is scoped by it. */
    tenantId: string;
    /** When this row was created. Set automatically and never changes. */
    createdAt: string;
    /** Account that created this row, taken from the request's claims. */
    createdByAccountId?: string | null;
    /**
     * API key this row was created through, when it was an integration rather
     * than a person.
     */
    createdByApiKeyId?: string | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: string | null;
    /**
     * What happened, as the application names it. Narrow it to an enum of your
     * own to get a switch the compiler can see.
     */
    kind: string;
    /**
     * Resolved means the audience was determined and the inbox lines exist. It
     * does not mean anything was sent.
     */
    state: NotificationState;
    /**
     * When this is due. now() is the ordinary case; a scheduled notification is
     * the same row with a later time, which is the only difference between the
     * two.
     */
    deliverAt: string;
    /**
     * When the audience was computed. Null while the notification is still
     * pending or was cancelled.
     */
    resolvedAt?: string | null;
    /**
     * What a template needs beyond the linked row. Give it a Go type with the
     * go_type key if it has a shape.
     */
    payload: unknown;
    /**
     * What collapses several of these into one inbox line, decided when the
     * announcement was written and copied onto the line when the audience is
     * resolved.
     */
    groupKey?: string | null;
    /**
     * A recipient list captured at write time, and the one exception to
     * computing the audience late. Null is the ordinary case; a list here skips
     * the question entirely, for an audience that genuinely cannot be
     * re-derived.
     */
    accountIds?: string[] | null;
    /**
     * When a dispatcher took this to resolve. Past notifications.claim_ttl
     * another may, which is what makes a crashed process recoverable.
     */
    claimedAt?: string | null;
    /**
     * Which process holds the lease, so a stuck one traces to a pod rather than
     * to a mystery.
     */
    claimedBy?: string | null;
    /** How many times resolving this has been attempted. */
    attempts: number;
};

/** A page of Notifications. */
export type NotificationListResponse = {
    /** The rows in this page. */
    data: Notification[];
    /** Where this page sits in the full result set. */
    pagination: Pagination;
};

