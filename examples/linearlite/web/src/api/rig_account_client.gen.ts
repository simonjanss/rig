// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { RigAccount, RigAccountListResponse } from "./rig_account.gen.js";
import type {
    RigAccountListDeletedQuery,
    RigAccountListQuery,
} from "./rig_account_input.gen.js";
import type { CallOptions, Runtime } from "@rig/client";

import { pathValue, send, setParam } from "@rig/client";

/**
 * RigAccountClient calls the RigAccount endpoints. It is reached as
 * `client.rigAccounts` rather than built directly.
 */
export class RigAccountClient {
    readonly #rt: Runtime;

    constructor(rt: Runtime) {
        this.#rt = rt;
    }

    /**
     * List RigAccounts.
     *
     * GET /api/v1/rig-accounts
     *
     * Operation listRigAccounts.
     */
    list(query: RigAccountListQuery = {}, options?: CallOptions): Promise<RigAccountListResponse> {
        const search = new URLSearchParams();
        setParam(search, "limit", query.limit);
        setParam(search, "offset", query.offset);

        return send<RigAccountListResponse>(this.#rt, {
            name: "listRigAccounts",
            method: "GET",
            path: "/rig-accounts",
            query: search,
        }, options);
    }

    /**
     * A deletion stamps the row rather than removing it, so this is what was
     * deleted and can still be brought back. Only rows inside the 30-day
     * restore window appear: past it a row is gone as far as anyone is
     * concerned, so it is not in the trash either.
     *
     * GET /api/v1/rig-accounts/_deleted
     *
     * Operation listDeletedRigAccounts.
     */
    listDeleted(query: RigAccountListDeletedQuery = {}, options?: CallOptions): Promise<RigAccountListResponse> {
        const search = new URLSearchParams();
        setParam(search, "limit", query.limit);
        setParam(search, "offset", query.offset);

        return send<RigAccountListResponse>(this.#rt, {
            name: "listDeletedRigAccounts",
            method: "GET",
            path: "/rig-accounts/_deleted",
            query: search,
        }, options);
    }

    /**
     * Fetch one RigAccount by identifier.
     *
     * GET /api/v1/rig-accounts/{id}
     *
     * Operation getRigAccount.
     */
    get(iD: string, options?: CallOptions): Promise<RigAccount> {
        return send<RigAccount>(this.#rt, {
            name: "getRigAccount",
            method: "GET",
            path: `/rig-accounts/${pathValue(iD)}`,
        }, options);
    }
}
