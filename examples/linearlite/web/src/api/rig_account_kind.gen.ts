// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/** Whether an account is a person or a machine. */
export const RigAccountKind = {
    /** Somebody who signs in. */
    Person: "Person",
    /** An account an integration's API key acts as. Nobody signs in to it. */
    Service: "Service",
} as const;

/**
 * One of the values of RigAccountKind.
 *
 * The type and the object share a name on purpose: TypeScript keeps types and
 * values in separate namespaces, so `RigAccountKind.Person` and `:
 * RigAccountKind` both read the way somebody would write them.
 */
export type RigAccountKind = (typeof RigAccountKind)[keyof typeof RigAccountKind];

/**
 * Every value of RigAccountKind, in declaration order. It is what a picker is
 * drawn from.
 */
export const allRigAccountKind: readonly RigAccountKind[] = [RigAccountKind.Person, RigAccountKind.Service];
