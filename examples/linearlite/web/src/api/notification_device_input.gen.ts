// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { NotificationChannel } from "./notification_channel.gen.js";
import type { FieldError } from "@rig-ts/client";

/**
 * NotificationDeviceListQuery is the query string listNotificationDevices
 * takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type NotificationDeviceListQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
    /**
     * How wide to read: own for the notificationdevices the caller created,
     * which is the default, or all for every one in the tenant. Asking for all
     * requires the rig_notification_device.read.all permission and is refused
     * without it, rather than quietly answering with fewer rows.
     */
    scope?: string;
};

/**
 * NotificationDeviceCreateInput is what creating a NotificationDevice takes.
 *
 * A field the server fills in — the identifier, the tenant, the audit columns
 * — is not here: those are not a caller's to send.
 */
export type NotificationDeviceCreateInput = {
    /** Whose device it is. */
    accountId: string;
    /** Desktop or Mobile. Email is refused by the CHECK above. */
    channel: NotificationChannel;
    /** What the provider was given to address this device. Opaque to rig. */
    token: string;
    /**
     * What a person sees in a list of their devices, and the column that makes
     * revoking one possible for somebody who has four.
     */
    label?: string | null;
    /**
     * When this device last checked in, so a stale one can be told apart from a
     * revoked one.
     */
    lastSeenAt?: string | null;
    /**
     * When it stopped being a place to send to. Kept rather than deleted, so a
     * token that comes back can be recognised.
     */
    revokedAt?: string | null;
};

/**
 * NotificationDeviceCreateFields is what a validation failure says, shaped like
 * the body it is about — one member per member, so each message can be put
 * beside the control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type NotificationDeviceCreateFields = {
    accountId?: FieldError;
    channel?: FieldError;
    token?: FieldError;
    label?: FieldError;
    lastSeenAt?: FieldError;
    revokedAt?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

/**
 * NotificationDeviceGetQuery is the query string getNotificationDevice takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type NotificationDeviceGetQuery = {
    /**
     * How wide to read: own for the notificationdevices the caller created,
     * which is the default, or all for every one in the tenant. Asking for all
     * requires the rig_notification_device.read.all permission and is refused
     * without it, rather than quietly answering with fewer rows.
     */
    scope?: string;
};

