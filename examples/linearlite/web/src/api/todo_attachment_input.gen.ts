// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type { TodoAttachmentFilter } from "./todo_attachment_query.gen.js";
import type { FieldError, Upload } from "@rig-ts/client";

/**
 * TodoAttachmentCreateFiles is the files a create carries with it.
 *
 * A not-null file column is unreachable without this: the row would have to
 * exist before an upload had anywhere to go, and it cannot exist without one.
 * So a required column is a plain member here and a nullable one is optional,
 * and leaving out a file the schema insists on does not compile rather than
 * coming back a 422.
 */
export type TodoAttachmentCreateFiles = {
    attachmentFile: Upload;
};

/**
 * TodoAttachmentListQuery is the query string listTodoAttachments takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type TodoAttachmentListQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * TodoAttachmentCreateInput is what creating a TodoAttachment takes.
 *
 * A field the server fills in — the identifier, the tenant, the audit columns
 * — is not here: those are not a caller's to send.
 */
export type TodoAttachmentCreateInput = {
    /** The todo this is attached to. */
    todoId: string;
    /** The file itself. Required: an attachment with no file is nothing. */
    attachmentFileId: string;
    /** What the attachment is, in a few words. */
    caption?: string | null;
};

/**
 * TodoAttachmentCreateFields is what a validation failure says, shaped like the
 * body it is about — one member per member, so each message can be put beside
 * the control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type TodoAttachmentCreateFields = {
    todoId?: FieldError;
    attachmentFileId?: FieldError;
    caption?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

/** The body searchTodoAttachments sends. */
export type TodoAttachmentSearchBody = {
    /** Conditions rows must satisfy. */
    filter: TodoAttachmentFilter;
};

/**
 * TodoAttachmentSearchQuery is the query string searchTodoAttachments takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type TodoAttachmentSearchQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * TodoAttachmentListDeletedQuery is the query string listDeletedTodoAttachments
 * takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type TodoAttachmentListDeletedQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
};

/**
 * TodoAttachmentUpdateInput is what changing a TodoAttachment takes.
 *
 * A field left out is left alone. A nullable field set to `null` is cleared —
 * which is why only some members accept one: a column that cannot hold null has
 * no way to be given one.
 *
 * `undefined` reads as left out, because JSON.stringify drops the key either
 * way. It is `null` that is a request to clear.
 */
export type TodoAttachmentUpdateInput = {
    /** The todo this is attached to. */
    todoId?: string;
    /** The file itself. Required: an attachment with no file is nothing. */
    attachmentFileId?: string;
    /** What the attachment is, in a few words. */
    caption?: string | null;
};

/**
 * TodoAttachmentUpdateFields is what a validation failure says, shaped like the
 * body it is about — one member per member, so each message can be put beside
 * the control it belongs to.
 *
 * A member is absent when there was nothing wrong with that field. It arrives
 * as the `fields` of the error the call threw.
 */
export type TodoAttachmentUpdateFields = {
    todoId?: FieldError;
    attachmentFileId?: FieldError;
    caption?: FieldError;
    /**
     * What was wrong with the request as a whole rather than with any one
     * field.
     */
    entity?: FieldError;
};

