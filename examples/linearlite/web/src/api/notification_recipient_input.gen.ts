// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type {
    NotificationRecipientFilter,
} from "./notification_recipient_query.gen.js";

/**
 * NotificationRecipientListQuery is the query string listNotificationRecipients
 * takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type NotificationRecipientListQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
    /**
     * How wide to read: own for the notificationrecipients the caller created,
     * which is the default, or all for every one in the tenant. Asking for all
     * requires the rig_notification_recipient.read.all permission and is
     * refused without it, rather than quietly answering with fewer rows.
     */
    scope?: string;
};

/** The body searchNotificationRecipients sends. */
export type NotificationRecipientSearchBody = {
    /** Conditions rows must satisfy. */
    filter: NotificationRecipientFilter;
};

/**
 * NotificationRecipientSearchQuery is the query string
 * searchNotificationRecipients takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type NotificationRecipientSearchQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
    /**
     * How wide to read: own for the notificationrecipients the caller created,
     * which is the default, or all for every one in the tenant. Asking for all
     * requires the rig_notification_recipient.read.all permission and is
     * refused without it, rather than quietly answering with fewer rows.
     */
    scope?: string;
};

/**
 * NotificationRecipientListDeletedQuery is the query string
 * listDeletedNotificationRecipients takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type NotificationRecipientListDeletedQuery = {
    /** Maximum rows to return. */
    limit?: number;
    /** Rows to skip before the first returned row. */
    offset?: number;
    /**
     * How wide to read: own for the notificationrecipients the caller created,
     * which is the default, or all for every one in the tenant. Asking for all
     * requires the rig_notification_recipient.read.all permission and is
     * refused without it, rather than quietly answering with fewer rows.
     */
    scope?: string;
};

/**
 * NotificationRecipientGetQuery is the query string getNotificationRecipient
 * takes.
 *
 * Every member is optional. The server applies a parameter's default when the
 * parameter is absent, so leaving one out is how to ask for the default rather
 * than for zero.
 */
export type NotificationRecipientGetQuery = {
    /**
     * How wide to read: own for the notificationrecipients the caller created,
     * which is the default, or all for every one in the tenant. Asking for all
     * requires the rig_notification_recipient.read.all permission and is
     * refused without it, rather than quietly answering with fewer rows.
     */
    scope?: string;
};

