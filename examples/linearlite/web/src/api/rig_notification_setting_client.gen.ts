// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type {
    RigNotificationSetting,
    RigNotificationSettingListResponse,
} from "./rig_notification_setting.gen.js";
import type {
    RigNotificationSettingCreateFields,
    RigNotificationSettingCreateInput,
    RigNotificationSettingListQuery,
    RigNotificationSettingUpdateFields,
    RigNotificationSettingUpdateInput,
} from "./rig_notification_setting_input.gen.js";
import type { CallOptions, RigError, Runtime } from "@rig/client";

import {
    isInvalid,
    pathValue,
    send,
    sendNoContent,
    setParam,
} from "@rig/client";

/**
 * RigNotificationSettingClient calls the RigNotificationSetting endpoints. It
 * is reached as `client.rigNotificationSettings` rather than built directly.
 */
export class RigNotificationSettingClient {
    readonly #rt: Runtime;

    constructor(rt: Runtime) {
        this.#rt = rt;
    }

    /**
     * List RigNotificationSettings.
     *
     * GET /api/v1/rig-notification-settings
     *
     * Operation listRigNotificationSettings.
     */
    list(query: RigNotificationSettingListQuery = {}, options?: CallOptions): Promise<RigNotificationSettingListResponse> {
        const search = new URLSearchParams();
        setParam(search, "limit", query.limit);
        setParam(search, "offset", query.offset);
        setParam(search, "scope", query.scope);

        return send<RigNotificationSettingListResponse>(this.#rt, {
            name: "listRigNotificationSettings",
            method: "GET",
            path: "/rig-notification-settings",
            query: search,
        }, options);
    }

    /**
     * Create a RigNotificationSetting.
     *
     * POST /api/v1/rig-notification-settings
     *
     * Operation createRigNotificationSetting.
     *
     * A refusal is read back with `isRigNotificationSettingCreateError`, whose
     * `fields` say what was wrong with each member of the body.
     */
    create(input: RigNotificationSettingCreateInput, options?: CallOptions): Promise<RigNotificationSetting> {
        return send<RigNotificationSetting>(this.#rt, {
            name: "createRigNotificationSetting",
            method: "POST",
            path: "/rig-notification-settings",
            body: input,
        }, options);
    }

    /**
     * Delete a RigNotificationSetting.
     *
     * DELETE /api/v1/rig-notification-settings/{id}
     *
     * Operation deleteRigNotificationSetting.
     */
    delete(iD: string, options?: CallOptions): Promise<void> {
        return sendNoContent(this.#rt, {
            name: "deleteRigNotificationSetting",
            method: "DELETE",
            path: `/rig-notification-settings/${pathValue(iD)}`,
        }, options);
    }

    /**
     * Only the fields present in the body are changed. A field set to null is
     * cleared; a field left out is left alone.
     *
     * PATCH /api/v1/rig-notification-settings/{id}
     *
     * Operation updateRigNotificationSetting.
     *
     * A refusal is read back with `isRigNotificationSettingUpdateError`, whose
     * `fields` say what was wrong with each member of the body.
     */
    update(iD: string, input: RigNotificationSettingUpdateInput, options?: CallOptions): Promise<RigNotificationSetting> {
        return send<RigNotificationSetting>(this.#rt, {
            name: "updateRigNotificationSetting",
            method: "PATCH",
            path: `/rig-notification-settings/${pathValue(iD)}`,
            body: input,
        }, options);
    }
}

/**
 * isRigNotificationSettingCreateError reads back what
 * createRigNotificationSetting refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isRigNotificationSettingCreateError(err: unknown): err is RigError<RigNotificationSettingCreateFields> {
    return isInvalid(err);
}

/**
 * isRigNotificationSettingUpdateError reads back what
 * updateRigNotificationSetting refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isRigNotificationSettingUpdateError(err: unknown): err is RigError<RigNotificationSettingUpdateFields> {
    return isInvalid(err);
}
