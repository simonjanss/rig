// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/**
 * Where a copy of an inbox line can be sent. The inbox itself is not one of
 * them.
 */
export const NotificationChannel = {
    /** A browser or a desktop application, addressed by a device row. */
    Desktop: "Desktop",
    /** A phone, addressed by a device row. */
    Mobile: "Mobile",
    /**
     * The address on the account. No device row: there is nothing to register,
     * because the address is on the account already.
     */
    Email: "Email",
} as const;

/**
 * One of the values of NotificationChannel.
 *
 * The type and the object share a name on purpose: TypeScript keeps types and
 * values in separate namespaces, so `NotificationChannel.Desktop` and `:
 * NotificationChannel` both read the way somebody would write them.
 */
export type NotificationChannel = (typeof NotificationChannel)[keyof typeof NotificationChannel];

/**
 * Every value of NotificationChannel, in declaration order. It is what a picker
 * is drawn from.
 */
export const allNotificationChannel: readonly NotificationChannel[] = [NotificationChannel.Desktop, NotificationChannel.Mobile, NotificationChannel.Email];
