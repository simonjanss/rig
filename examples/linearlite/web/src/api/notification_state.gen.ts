// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/**
 * Where a notification is in its life. Nothing here says anything was sent:
 * that is the delivery table's business.
 */
export const NotificationState = {
    /**
     * The audience has not been computed yet. Every notification starts here,
     * and a scheduled one waits here until it is due.
     */
    Pending: "Pending",
    /**
     * The audience was computed and the inbox lines exist. It does not mean
     * anything was sent — a notification with an inbox line and no mail is a
     * working notification.
     */
    Resolved: "Resolved",
    /**
     * It never will be resolved: the row it was about was retired before its
     * time came.
     */
    Cancelled: "Cancelled",
} as const;

/**
 * One of the values of NotificationState.
 *
 * The type and the object share a name on purpose: TypeScript keeps types and
 * values in separate namespaces, so `NotificationState.Pending` and `:
 * NotificationState` both read the way somebody would write them.
 */
export type NotificationState = (typeof NotificationState)[keyof typeof NotificationState];

/**
 * Every value of NotificationState, in declaration order. It is what a picker
 * is drawn from.
 */
export const allNotificationState: readonly NotificationState[] = [NotificationState.Pending, NotificationState.Resolved, NotificationState.Cancelled];
