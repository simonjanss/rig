// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/** The coarse level an account holds in one tenant. */
export const AccountRoleLevel = {
    /** May do anything, including the things that end the tenant. */
    Owner: "Owner",
    /**
     * Administers the tenant without the decisions that are the owner's to
     * make.
     */
    Admin: "Admin",
    /** Gets on with the work. */
    Basic: "Basic",
} as const;

/**
 * One of the values of AccountRoleLevel.
 *
 * The type and the object share a name on purpose: TypeScript keeps types and
 * values in separate namespaces, so `AccountRoleLevel.Owner` and `:
 * AccountRoleLevel` both read the way somebody would write them.
 */
export type AccountRoleLevel = (typeof AccountRoleLevel)[keyof typeof AccountRoleLevel];

/**
 * Every value of AccountRoleLevel, in declaration order. It is what a picker is
 * drawn from.
 */
export const allAccountRoleLevel: readonly AccountRoleLevel[] = [AccountRoleLevel.Owner, AccountRoleLevel.Admin, AccountRoleLevel.Basic];
