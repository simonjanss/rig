// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/** What an account is. */
export const AccountKind = {
    /**
     * Somebody who signs in. They have an identity, and the identity has the
     * password.
     */
    Person: "Person",
    /**
     * What an integration's key acts as. It has no identity, so there is
     * nothing to sign in with.
     */
    Service: "Service",
} as const;

/**
 * One of the values of AccountKind.
 *
 * The type and the object share a name on purpose: TypeScript keeps types and
 * values in separate namespaces, so `AccountKind.Person` and `: AccountKind`
 * both read the way somebody would write them.
 */
export type AccountKind = (typeof AccountKind)[keyof typeof AccountKind];

/**
 * Every value of AccountKind, in declaration order. It is what a picker is
 * drawn from.
 */
export const allAccountKind: readonly AccountKind[] = [AccountKind.Person, AccountKind.Service];
