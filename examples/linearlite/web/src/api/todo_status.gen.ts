// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/** Which column of the board an item is in. */
export const TodoStatus = {
    /** Noted, and not yet planned. */
    Backlog: "backlog",
    /** Planned and waiting to be started. This is the default. */
    Todo: "todo",
    /** Somebody is working on it. */
    InProgress: "in_progress",
    /** Finished. */
    Done: "done",
    /** Decided against, which is not the same as finished. */
    Canceled: "canceled",
} as const;

/**
 * One of the values of TodoStatus.
 *
 * The type and the object share a name on purpose: TypeScript keeps types and
 * values in separate namespaces, so `TodoStatus.Backlog` and `: TodoStatus`
 * both read the way somebody would write them.
 */
export type TodoStatus = (typeof TodoStatus)[keyof typeof TodoStatus];

/**
 * Every value of TodoStatus, in declaration order. It is what a picker is drawn
 * from.
 */
export const allTodoStatus: readonly TodoStatus[] = [TodoStatus.Backlog, TodoStatus.Todo, TodoStatus.InProgress, TodoStatus.Done, TodoStatus.Canceled];
