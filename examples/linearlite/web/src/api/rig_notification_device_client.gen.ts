// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type {
    RigNotificationDevice,
    RigNotificationDeviceListResponse,
} from "./rig_notification_device.gen.js";
import type {
    RigNotificationDeviceCreateFields,
    RigNotificationDeviceCreateInput,
    RigNotificationDeviceListQuery,
} from "./rig_notification_device_input.gen.js";
import type { CallOptions, RigError, Runtime } from "@rig/client";

import {
    isInvalid,
    pathValue,
    send,
    sendNoContent,
    setParam,
} from "@rig/client";

/**
 * RigNotificationDeviceClient calls the RigNotificationDevice endpoints. It is
 * reached as `client.rigNotificationDevices` rather than built directly.
 */
export class RigNotificationDeviceClient {
    readonly #rt: Runtime;

    constructor(rt: Runtime) {
        this.#rt = rt;
    }

    /**
     * List RigNotificationDevices.
     *
     * GET /api/v1/rig-notification-devices
     *
     * Operation listRigNotificationDevices.
     */
    list(query: RigNotificationDeviceListQuery = {}, options?: CallOptions): Promise<RigNotificationDeviceListResponse> {
        const search = new URLSearchParams();
        setParam(search, "limit", query.limit);
        setParam(search, "offset", query.offset);
        setParam(search, "scope", query.scope);

        return send<RigNotificationDeviceListResponse>(this.#rt, {
            name: "listRigNotificationDevices",
            method: "GET",
            path: "/rig-notification-devices",
            query: search,
        }, options);
    }

    /**
     * Create a RigNotificationDevice.
     *
     * POST /api/v1/rig-notification-devices
     *
     * Operation createRigNotificationDevice.
     *
     * A refusal is read back with `isRigNotificationDeviceCreateError`, whose
     * `fields` say what was wrong with each member of the body.
     */
    create(input: RigNotificationDeviceCreateInput, options?: CallOptions): Promise<RigNotificationDevice> {
        return send<RigNotificationDevice>(this.#rt, {
            name: "createRigNotificationDevice",
            method: "POST",
            path: "/rig-notification-devices",
            body: input,
        }, options);
    }

    /**
     * Delete a RigNotificationDevice.
     *
     * DELETE /api/v1/rig-notification-devices/{id}
     *
     * Operation deleteRigNotificationDevice.
     */
    delete(iD: string, options?: CallOptions): Promise<void> {
        return sendNoContent(this.#rt, {
            name: "deleteRigNotificationDevice",
            method: "DELETE",
            path: `/rig-notification-devices/${pathValue(iD)}`,
        }, options);
    }
}

/**
 * isRigNotificationDeviceCreateError reads back what
 * createRigNotificationDevice refused.
 *
 * True only for a body the server refused field by field. Anything else — a
 * 404, a request that never arrived — has nothing to put beside a control, so
 * `fields` would be an object nobody complained about.
 */
export function isRigNotificationDeviceCreateError(err: unknown): err is RigError<RigNotificationDeviceCreateFields> {
    return isInvalid(err);
}
