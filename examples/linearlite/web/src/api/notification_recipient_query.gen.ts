// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/** Conditions a NotificationRecipient must satisfy to match a search. */
export type NotificationRecipientFilter = {
    /** Fields that must equal the given value. */
    equals?: NotificationRecipientFilterEquals | null;
    /** Fields that must not equal the given value. */
    notEquals?: NotificationRecipientFilterEquals | null;
    /** Fields that must be greater than the given value. */
    greaterThan?: NotificationRecipientFilterRange | null;
    /** Fields that must be less than the given value. */
    smallerThan?: NotificationRecipientFilterRange | null;
    /** Fields that must be greater than or equal to the given value. */
    greaterOrEqual?: NotificationRecipientFilterRange | null;
    /** Fields that must be less than or equal to the given value. */
    smallerOrEqual?: NotificationRecipientFilterRange | null;
    /** Fields that must be one of the given values. */
    contains?: NotificationRecipientFilterContains | null;
    /** Fields that must not be one of the given values. */
    notContains?: NotificationRecipientFilterContains | null;
    /** Text fields that must match the given pattern. */
    like?: NotificationRecipientFilterLike | null;
    /** Text fields that must not match the given pattern. */
    notLike?: NotificationRecipientFilterLike | null;
    /** Fields that must be null. */
    null?: NotificationRecipientFilterNull | null;
    /** Fields that must not be null. */
    notNull?: NotificationRecipientFilterNull | null;
    /** Combine this filter's conditions with OR instead of AND. */
    orCondition: boolean;
    /**
     * Sub-filters combined with this one, so that AND and OR can be mixed to
     * any depth.
     */
    nestedFilters: NotificationRecipientFilter[];
};

/** Set-membership conditions on NotificationRecipient fields. */
export type NotificationRecipientFilterContains = {
    /** Unique identifier for this row. */
    id: string[];
    /**
     * What happened. The line is separate from it so that one person clearing
     * their inbox changes nothing anybody else sees.
     */
    notificationId: string[];
    /**
     * Who this line is for. An account rather than an identity: an identity has
     * no tenant, so a row addressed to one would fall outside every generated
     * query.
     */
    accountId: string[];
    /** When this row was created. Set automatically and never changes. */
    createdAt: string[];
    /** When this row was last updated. Set automatically on every update. */
    updatedAt: string[];
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt: string[];
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId: string[];
    /**
     * Copied from the notification, so the inbox and its live-sync shape never
     * touch a table holding rows for people who are not recipients.
     */
    kind: string[];
    /**
     * What collapses several events into one line. Null opts out and every
     * event is its own row.
     */
    groupKey: string[];
    /**
     * How many events this line stands for. One unless a group key collapsed
     * them.
     */
    eventCount: number[];
    /** When the person read it. Null is unread, which is what the badge counts. */
    readAt: string[];
};

/** Exact-match conditions on NotificationRecipient fields. */
export type NotificationRecipientFilterEquals = {
    /** Unique identifier for this row. */
    id?: string | null;
    /**
     * What happened. The line is separate from it so that one person clearing
     * their inbox changes nothing anybody else sees.
     */
    notificationId?: string | null;
    /**
     * Who this line is for. An account rather than an identity: an identity has
     * no tenant, so a row addressed to one would fall outside every generated
     * query.
     */
    accountId?: string | null;
    /** When this row was created. Set automatically and never changes. */
    createdAt?: string | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: string | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: string | null;
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId?: string | null;
    /**
     * Copied from the notification, so the inbox and its live-sync shape never
     * touch a table holding rows for people who are not recipients.
     */
    kind?: string | null;
    /**
     * What collapses several events into one line. Null opts out and every
     * event is its own row.
     */
    groupKey?: string | null;
    /**
     * How many events this line stands for. One unless a group key collapsed
     * them.
     */
    eventCount?: number | null;
    /** When the person read it. Null is unread, which is what the badge counts. */
    readAt?: string | null;
};

/** Pattern conditions on NotificationRecipient text fields. */
export type NotificationRecipientFilterLike = {
    /**
     * Copied from the notification, so the inbox and its live-sync shape never
     * touch a table holding rows for people who are not recipients.
     */
    kind?: string | null;
    /**
     * What collapses several events into one line. Null opts out and every
     * event is its own row.
     */
    groupKey?: string | null;
};

/** Presence conditions on nullable NotificationRecipient fields. */
export type NotificationRecipientFilterNull = {
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: boolean | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: boolean | null;
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId?: boolean | null;
    /**
     * What collapses several events into one line. Null opts out and every
     * event is its own row.
     */
    groupKey?: boolean | null;
    /** When the person read it. Null is unread, which is what the badge counts. */
    readAt?: boolean | null;
};

/** Ordering conditions on NotificationRecipient fields that can be compared. */
export type NotificationRecipientFilterRange = {
    /** When this row was created. Set automatically and never changes. */
    createdAt?: string | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: string | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: string | null;
    /**
     * How many events this line stands for. One unless a group key collapsed
     * them.
     */
    eventCount?: number | null;
    /** When the person read it. Null is unread, which is what the badge counts. */
    readAt?: string | null;
};

