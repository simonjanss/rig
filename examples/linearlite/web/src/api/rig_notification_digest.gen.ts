// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/** How often somebody wants to be told, on one channel. */
export const RigNotificationDigest = {
    /** Each notification on its own, as soon as it is due. */
    Immediate: "Immediate",
    /** Whatever accumulated in an hour, as one message. */
    Hourly: "Hourly",
    /** The same, a day at a time. */
    Daily: "Daily",
    /**
     * The widest window, and the one notifications.retention has to outlive —
     * a weekly digest is assembled from rows a shorter retention would have
     * pruned.
     */
    Weekly: "Weekly",
    /**
     * Nothing on this channel, and the inbox line is written anyway: this is
     * somebody preferring to look rather than be told. is_enabled false is the
     * other thing, and says the channel is not available to them at all.
     */
    Off: "Off",
} as const;

/**
 * One of the values of RigNotificationDigest.
 *
 * The type and the object share a name on purpose: TypeScript keeps types and
 * values in separate namespaces, so `RigNotificationDigest.Immediate` and `:
 * RigNotificationDigest` both read the way somebody would write them.
 */
export type RigNotificationDigest = (typeof RigNotificationDigest)[keyof typeof RigNotificationDigest];

/**
 * Every value of RigNotificationDigest, in declaration order. It is what a
 * picker is drawn from.
 */
export const allRigNotificationDigest: readonly RigNotificationDigest[] = [RigNotificationDigest.Immediate, RigNotificationDigest.Hourly, RigNotificationDigest.Daily, RigNotificationDigest.Weekly, RigNotificationDigest.Off];
