// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"context"

	"github.com/google/uuid"
	"github.com/simonjanss/rig/examples/linearlite/internal/generated/model"
	"github.com/simonjanss/rig/examples/linearlite/internal/generated/store"
	"github.com/simonjanss/rig/runtime/dbhook"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// NotificationService is the interface your service layer implements.
//
// Embedding DefaultNotificationService satisfies all of it, so a resource with
// no business logic needs nothing but a constructor. Override a method to add
// a rule and delegate to the embedded default for the rest.
type NotificationService interface {
	// List Notifications.
	List(ctx context.Context, r Request[struct{}, NotificationListQuery, struct{}]) (*NotificationListResponse, error)

	// Fetch one Notification by identifier.
	Get(ctx context.Context, r Request[NotificationGetPath, struct{}, struct{}]) (*model.Notification, error)
}

// NotificationWriter writes a Notification with the service's rules attached.
//
// Every write rig generates goes through it, and so should every write a
// custom endpoint makes: reaching for the repository directly means passing
// the hooks by hand, and forgetting once is a second way into the table where
// the rules do not run.
type NotificationWriter struct {
	repo  store.NotificationRepository
	hooks NotificationHooks
}

// NewNotificationWriter pairs a repository with the rules that apply to it.
func NewNotificationWriter(repo store.NotificationRepository, hooks NotificationHooks) NotificationWriter {
	return NotificationWriter{repo: repo, hooks: hooks}
}

// Create inserts a row, running the create rules and hooks.
func (w NotificationWriter) Create(ctx context.Context, in model.NotificationCreateInput) (*model.Notification, error) {
	return w.repo.Create(ctx, dbhook.Create[model.NotificationCreateInput, model.Notification]{Input: in, Hooks: w.hooks.Create})
}

// Update changes a row, running the update rules and hooks.
func (w NotificationWriter) Update(ctx context.Context, id uuid.UUID, in model.NotificationUpdateInput) (*model.Notification, error) {
	return w.repo.Update(ctx, id, dbhook.Update[model.NotificationUpdateInput, model.Notification]{Input: in, Hooks: w.hooks.Update})
}

// Delete removes a row, running the delete hooks.
func (w NotificationWriter) Delete(ctx context.Context, in model.NotificationDeleteInput) error {
	return w.repo.Delete(ctx, dbhook.Delete[model.NotificationDeleteInput, model.Notification]{Input: in, Hooks: w.hooks.Delete})
}

// DefaultNotificationService implements every operation.
//
// The generated ones it answers itself, by calling the repository. A custom
// endpoint it hands to the contract, which is where the only implementation
// there could be lives.
type DefaultNotificationService struct {
	repo     store.NotificationRepository
	contract NotificationContract
	// write is the same writer a custom endpoint gets through Writer, so the
	// generated operations and the hand-written ones take one path.
	write NotificationWriter
}

// NotificationContract is what the service layer owes Notification: everything
// about it the schema cannot describe.
//
// The members are optional in the sense that an empty one runs nothing. None
// of them is optional in the sense of being skippable: the constructor takes
// this value, so a resource with no rules has an empty literal somebody can
// read rather than an absence nobody can see.
type NotificationContract struct {
	// Hooks is everything that happens around a write: the rules it is checked
	// against, and the callbacks that run with it. One set per operation, because
	// the rules for creating a row and for changing one are different questions
	// asked about different fields.
	Hooks NotificationHooks
}

// NotificationHooks are the callbacks that run around each generated write.
//
// Before and After run inside the write's transaction, so returning an error
// from either undoes the write. AfterCommit runs once it has landed, which is
// where anything outside the database belongs.
type NotificationHooks struct {
	// Read shapes what a read answers with. It is the one set that is not about a
	// write, and the only one that runs outside the repository — a write reads
	// the row it is about to change, and narrowing that read would have the update
	// judge a row that is not the stored one.
	Read dbhook.ReadHooks[model.NotificationFilter, model.Notification]

	Create dbhook.CreateHooks[model.NotificationCreateInput, model.Notification]
	Update dbhook.UpdateHooks[model.NotificationUpdateInput, model.Notification]
	Delete dbhook.DeleteHooks[model.NotificationDeleteInput, model.Notification]
}

// NotificationRules is everything the service layer supplies about
// Notification.
//
// Implement it on a type of your own and hand it to NewNotificationService. It
// is an interface rather than a struct so that the rules and the endpoints are
// one value: a resource whose configuration declares an endpoint cannot be
// wired up without an implementation of it, and the failure is at the call to
// the constructor rather than on the route.
type NotificationRules interface {
	// Hooks is everything that happens around a write, plus what a read answers
	// with. It is called once, during construction.
	Hooks() NotificationHooks

	// Bind receives the writer built from those hooks, so a custom endpoint can
	// write through the same path a generated operation does.
	//
	// It is in the interface rather than looked for, so a service that never
	// writes says so with an empty body instead of finding out at runtime that a
	// misspelled method left it without one. rig calls it once, before anything
	// can reach a hook.
	Bind(NotificationWriter)
}

// NewNotificationService is the front door.
//
// It asks the rules what they are, builds the writer from that, and hands the
// writer back. The service layer never has to hold a half-built value or name
// the type it is part of.
func NewNotificationService(repo store.NotificationRepository, rules NotificationRules) DefaultNotificationService {
	svc := NewDefaultNotificationService(repo, NotificationContract{Hooks: rules.Hooks()})
	rules.Bind(svc.Writer())
	return svc
}

// NewDefaultNotificationService builds the default implementation.
//
// The contract is a parameter rather than a field left to default, because a
// rule nobody attached is a rule that does not run, and nothing at the call
// site would have said so. An empty NotificationContract is still allowed —
// it is just a thing somebody wrote down.
func NewDefaultNotificationService(repo store.NotificationRepository, contract NotificationContract) DefaultNotificationService {
	return DefaultNotificationService{repo: repo, contract: contract, write: NewNotificationWriter(repo, contract.Hooks)}
}

// Writer is how a custom endpoint writes.
//
// It is the repository with this service's own rules already attached, so a
// hand-written endpoint takes the path a generated one takes. Reaching for the
// repository instead means passing the hooks by hand, and forgetting once is a
// second way into the table where the rules do not run.
//
// It is a method rather than something the service layer holds because there
// is then nothing to wire, and nothing to wire wrongly.
func (s DefaultNotificationService) Writer() NotificationWriter { return s.write }

// readFilter combines the caller's filter with whatever the read hook narrows
// to.
//
// Nested rather than merged, because the two are combined with AND: a caller
// whose own filter is an OR cannot widen its way out of the one the service
// added.
func (s DefaultNotificationService) readFilter(ctx context.Context, claims tenancy.Claims, asked model.NotificationFilter) (model.NotificationFilter, error) {
	if s.contract.Hooks.Read.Narrow == nil {
		return asked, nil
	}

	narrowed, err := s.contract.Hooks.Read.Narrow(ctx, caller(claims))
	if err != nil {
		return asked, err
	}
	if narrowed == nil {
		return asked, nil
	}

	return model.NotificationFilter{NestedFilters: []model.NotificationFilter{*narrowed, asked}}, nil
}

// readRows runs the read hook over what a read is about to answer with.
func (s DefaultNotificationService) readRows(ctx context.Context, claims tenancy.Claims, rows []*model.Notification) error {
	if s.contract.Hooks.Read.Rows == nil {
		return nil
	}
	return s.contract.Hooks.Read.Rows(ctx, caller(claims), rows)
}

// List implements NotificationService.
func (s DefaultNotificationService) List(ctx context.Context, r Request[struct{}, NotificationListQuery, struct{}]) (*NotificationListResponse, error) {
	page := model.NotificationPage{Limit: r.Query.Limit, Offset: r.Query.Offset}
	asked := model.NewNotificationFilter()
	filter, err := s.readFilter(ctx, r.Claims, asked)
	if err != nil {
		return nil, err
	}

	rows, total, err := s.repo.List(ctx, filter, page)
	if err != nil {
		return nil, err
	}
	if err := s.readRows(ctx, r.Claims, rows); err != nil {
		return nil, err
	}

	return &NotificationListResponse{
		Data:       rows,
		Pagination: Pagination{Offset: page.Offset, Limit: page.Limit, Total: total},
	}, nil
}

// Get implements NotificationService.
func (s DefaultNotificationService) Get(ctx context.Context, r Request[NotificationGetPath, struct{}, struct{}]) (*model.Notification, error) {
	row, err := s.repo.Get(ctx, r.Path.ID)
	if err != nil {
		return nil, err
	}

	// Not narrowed: Get fetches by primary key, and there is no filter to add a
	// condition to. A rule about whether this caller may have this row belongs in
	// Rows, which is handed it.
	if err := s.readRows(ctx, r.Claims, []*model.Notification{row}); err != nil {
		return nil, err
	}
	return row, nil
}
