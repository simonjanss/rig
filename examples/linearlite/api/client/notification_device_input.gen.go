// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package client

import (
	"time"

	"github.com/google/uuid"
	"github.com/simonjanss/rig/rigclient"
	"github.com/simonjanss/rig/runtime/rigerr"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// NotificationDeviceListQuery is the query for NotificationDevice.List.
//
// A nil member is left out of the request, and the server applies its own
// default. rigclient.P takes the address of a value, for filling one in.
type NotificationDeviceListQuery struct {
	// Maximum rows to return. Left out, the server uses 50.
	Limit *int `json:"limit"`
	// Rows to skip before the first returned row. Left out, the server uses 0.
	Offset *int `json:"offset"`
	// How wide to read: own for the notificationdevices the caller created, which
	// is the default, or all for every one in the tenant. Asking for all requires
	// the rig_notification_device.read.all permission and is refused without it,
	// rather than quietly answering with fewer rows. Left out, the server uses
	// own.
	Scope *tenancy.Scope `json:"scope"`
}

// NotificationDeviceCreateInput is what creating a NotificationDevice takes.
//
// A field the server fills in — the identifier, the tenant, the audit
// columns — is not here: those are not a caller's to send.
type NotificationDeviceCreateInput struct {
	// Whose device it is.
	AccountID uuid.UUID `json:"accountId"`
	// Desktop or Mobile. Email is refused by the CHECK above.
	Channel NotificationChannel `json:"channel"`
	// What the provider was given to address this device. Opaque to rig.
	Token string `json:"token"`
	// What a person sees in a list of their devices, and the column that makes
	// revoking one possible for somebody who has four.
	Label *string `json:"label,omitempty"`
	// When this device last checked in, so a stale one can be told apart from a
	// revoked one.
	LastSeenAt *time.Time `json:"lastSeenAt,omitempty"`
	// When it stopped being a place to send to. Kept rather than deleted, so a
	// token that comes back can be recognised.
	RevokedAt *time.Time `json:"revokedAt,omitempty"`
}

// NotificationDeviceCreateFields is what a validation failure says, shaped
// like the body it is about — one member per member, so each message can be
// put beside the control it belongs to.
//
// A member is nil when there was nothing wrong with that field. It arrives as
// the Fields of the error the call returns.
type NotificationDeviceCreateFields struct {
	AccountID  *rigerr.FieldError `json:"accountId,omitempty"`
	Channel    *rigerr.FieldError `json:"channel,omitempty"`
	Token      *rigerr.FieldError `json:"token,omitempty"`
	Label      *rigerr.FieldError `json:"label,omitempty"`
	LastSeenAt *rigerr.FieldError `json:"lastSeenAt,omitempty"`
	RevokedAt  *rigerr.FieldError `json:"revokedAt,omitempty"`
	// Entity carries what was wrong with the request as a whole rather than with
	// any one field.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// NotificationDeviceCreateError reads back what the server said about a
// refused NotificationDevices.Create: the envelope — Code, Message,
// RequestID, Status, RetryAfter — and, when the refusal was about the body,
// Fields shaped like the body that failed.
//
//	if refused, ok := NotificationDeviceCreateError(err); ok {
//		if refused.Fields != nil && refused.Fields.AccountID != nil {
//
// Fields is nil for every refusal but a 422: a 404 has a code and a message
// and nothing to put beside a control. The second value is false for anything
// that is not a refusal at all, which is where a request that never reached
// the server ends up.
func NotificationDeviceCreateError(err error) (*rigclient.Failure[NotificationDeviceCreateFields], bool) {
	return rigclient.As[NotificationDeviceCreateFields](err)
}

// NotificationDeviceGetQuery is the query for NotificationDevice.Get.
//
// A nil member is left out of the request, and the server applies its own
// default. rigclient.P takes the address of a value, for filling one in.
type NotificationDeviceGetQuery struct {
	// How wide to read: own for the notificationdevices the caller created, which
	// is the default, or all for every one in the tenant. Asking for all requires
	// the rig_notification_device.read.all permission and is refused without it,
	// rather than quietly answering with fewer rows. Left out, the server uses
	// own.
	Scope *tenancy.Scope `json:"scope"`
}
