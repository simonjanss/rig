// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package client

import (
	"github.com/simonjanss/rig/runtime/tenancy"
)

// NotificationRecipientListQuery is the query for NotificationRecipient.List.
//
// A nil member is left out of the request, and the server applies its own
// default. rigclient.P takes the address of a value, for filling one in.
type NotificationRecipientListQuery struct {
	// Maximum rows to return. Left out, the server uses 50.
	Limit *int `json:"limit"`
	// Rows to skip before the first returned row. Left out, the server uses 0.
	Offset *int `json:"offset"`
	// How wide to read: own for the notificationrecipients the caller created,
	// which is the default, or all for every one in the tenant. Asking for all
	// requires the rig_notification_recipient.read.all permission and is refused
	// without it, rather than quietly answering with fewer rows. Left out, the
	// server uses own.
	Scope *tenancy.Scope `json:"scope"`
}

// NotificationRecipientSearchBody is the request body for
// NotificationRecipient.Search.
type NotificationRecipientSearchBody struct {
	// Conditions rows must satisfy.
	Filter NotificationRecipientFilter `json:"filter"`
}

// NotificationRecipientSearchQuery is the query for
// NotificationRecipient.Search.
//
// A nil member is left out of the request, and the server applies its own
// default. rigclient.P takes the address of a value, for filling one in.
type NotificationRecipientSearchQuery struct {
	// Maximum rows to return. Left out, the server uses 50.
	Limit *int `json:"limit"`
	// Rows to skip before the first returned row. Left out, the server uses 0.
	Offset *int `json:"offset"`
	// How wide to read: own for the notificationrecipients the caller created,
	// which is the default, or all for every one in the tenant. Asking for all
	// requires the rig_notification_recipient.read.all permission and is refused
	// without it, rather than quietly answering with fewer rows. Left out, the
	// server uses own.
	Scope *tenancy.Scope `json:"scope"`
}

// NotificationRecipientListDeletedQuery is the query for
// NotificationRecipient.ListDeleted.
//
// A nil member is left out of the request, and the server applies its own
// default. rigclient.P takes the address of a value, for filling one in.
type NotificationRecipientListDeletedQuery struct {
	// Maximum rows to return. Left out, the server uses 50.
	Limit *int `json:"limit"`
	// Rows to skip before the first returned row. Left out, the server uses 0.
	Offset *int `json:"offset"`
	// How wide to read: own for the notificationrecipients the caller created,
	// which is the default, or all for every one in the tenant. Asking for all
	// requires the rig_notification_recipient.read.all permission and is refused
	// without it, rather than quietly answering with fewer rows. Left out, the
	// server uses own.
	Scope *tenancy.Scope `json:"scope"`
}

// NotificationRecipientGetQuery is the query for NotificationRecipient.Get.
//
// A nil member is left out of the request, and the server applies its own
// default. rigclient.P takes the address of a value, for filling one in.
type NotificationRecipientGetQuery struct {
	// How wide to read: own for the notificationrecipients the caller created,
	// which is the default, or all for every one in the tenant. Asking for all
	// requires the rig_notification_recipient.read.all permission and is refused
	// without it, rather than quietly answering with fewer rows. Left out, the
	// server uses own.
	Scope *tenancy.Scope `json:"scope"`
}
