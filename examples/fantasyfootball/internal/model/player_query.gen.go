// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on Player fields.
type PlayerFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// The name on the back of the shirt.
	FullName *string `json:"fullName,omitempty"`
	// Where on the pitch the player lines up.
	Position *PlayerPosition `json:"position,omitempty"`
	// The squad number, or null if unassigned.
	ShirtNumber *int `json:"shirtNumber,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *uuid.UUID `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID *uuid.UUID `json:"updatedByAccountId,omitempty"`
	// When this row was soft-deleted. Null while the row is live.
	DeletedAt *time.Time `json:"deletedAt,omitempty"`
	// Account that soft-deleted this row, taken from the request's claims.
	DeletedByAccountID *uuid.UUID `json:"deletedByAccountId,omitempty"`
	// Conditions on one of this row's Teams.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Teams asks the second.
	Teams *TeamFilterEquals `json:"teams,omitempty"`
}

// Ordering conditions on Player fields that can be compared.
type PlayerFilterRange struct {
	// The squad number, or null if unassigned.
	ShirtNumber *int `json:"shirtNumber,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// When this row was soft-deleted. Null while the row is live.
	DeletedAt *time.Time `json:"deletedAt,omitempty"`
	// Conditions on one of this row's Teams.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Teams asks the second.
	Teams *TeamFilterRange `json:"teams,omitempty"`
}

// Set-membership conditions on Player fields.
type PlayerFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// The name on the back of the shirt.
	FullName []string `json:"fullName,omitempty"`
	// Where on the pitch the player lines up.
	Position []PlayerPosition `json:"position,omitempty"`
	// The squad number, or null if unassigned.
	ShirtNumber []int `json:"shirtNumber,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID []uuid.UUID `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID []uuid.UUID `json:"updatedByAccountId,omitempty"`
	// When this row was soft-deleted. Null while the row is live.
	DeletedAt []time.Time `json:"deletedAt,omitempty"`
	// Account that soft-deleted this row, taken from the request's claims.
	DeletedByAccountID []uuid.UUID `json:"deletedByAccountId,omitempty"`
	// Conditions on one of this row's Teams.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Teams asks the second.
	Teams *TeamFilterContains `json:"teams,omitempty"`
}

// Pattern conditions on Player text fields.
type PlayerFilterLike struct {
	// The name on the back of the shirt.
	FullName *string `json:"fullName,omitempty"`
	// Conditions on one of this row's Teams.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Teams asks the second.
	Teams *TeamFilterLike `json:"teams,omitempty"`
}

// Presence conditions on nullable Player fields.
type PlayerFilterNull struct {
	// The squad number, or null if unassigned.
	ShirtNumber *bool `json:"shirtNumber,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *bool `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID *bool `json:"updatedByAccountId,omitempty"`
	// When this row was soft-deleted. Null while the row is live.
	DeletedAt *bool `json:"deletedAt,omitempty"`
	// Account that soft-deleted this row, taken from the request's claims.
	DeletedByAccountID *bool `json:"deletedByAccountId,omitempty"`
	// Conditions on one of this row's Teams.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Teams asks the second.
	Teams *TeamFilterNull `json:"teams,omitempty"`
}

// Relations a Player must have no matching row for.
type PlayerFilterWithout struct {
	// Match rows with no Teams satisfying these conditions, including rows with
	// none at all.
	//
	// This negates the existence of a matching row rather than the comparison,
	// which is the whole difference from NotEquals.Teams: that one is satisfied by
	// one of them differing, and a row can have one that differs and one that
	// matches at the same time. An empty object asks for rows with no Teams at
	// all.
	Teams *TeamFilter `json:"teams,omitempty"`
}

// Conditions a Player must satisfy to match a search.
type PlayerFilter struct {
	// Fields that must equal the given value.
	Equals *PlayerFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *PlayerFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *PlayerFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *PlayerFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *PlayerFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *PlayerFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *PlayerFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *PlayerFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *PlayerFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *PlayerFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *PlayerFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *PlayerFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []PlayerFilter `json:"nestedFilters,omitempty"`
	// Relations with no row matching the given conditions. An empty one asks for
	// no related row at all.
	//
	// This is where a negation belongs when what you mean is "not": it negates
	// whether a matching related row exists, so a row with no related row at all
	// satisfies it. The negative operators above negate the comparison instead and
	// still require the related row to be there — "no related row called X" is
	// here, "a related row not called X" is NotEquals.
	Without *PlayerFilterWithout `json:"without,omitempty"`
}

// PlayerPage says which slice of a result to return, and in what order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type PlayerPage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []PlayerOrder
}

// PlayerOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type PlayerOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a Player supports.
var (
	PlayerOrderIDAsc                  = PlayerOrder{Column: "id"}
	PlayerOrderIDDesc                 = PlayerOrder{Column: "id", Desc: true}
	PlayerOrderTenantIDAsc            = PlayerOrder{Column: "tenant_id"}
	PlayerOrderTenantIDDesc           = PlayerOrder{Column: "tenant_id", Desc: true}
	PlayerOrderFullNameAsc            = PlayerOrder{Column: "full_name"}
	PlayerOrderFullNameDesc           = PlayerOrder{Column: "full_name", Desc: true}
	PlayerOrderPositionAsc            = PlayerOrder{Column: "position"}
	PlayerOrderPositionDesc           = PlayerOrder{Column: "position", Desc: true}
	PlayerOrderShirtNumberAsc         = PlayerOrder{Column: "shirt_number"}
	PlayerOrderShirtNumberDesc        = PlayerOrder{Column: "shirt_number", Desc: true}
	PlayerOrderCreatedAtAsc           = PlayerOrder{Column: "created_at"}
	PlayerOrderCreatedAtDesc          = PlayerOrder{Column: "created_at", Desc: true}
	PlayerOrderCreatedByAccountIDAsc  = PlayerOrder{Column: "created_by_account_id"}
	PlayerOrderCreatedByAccountIDDesc = PlayerOrder{Column: "created_by_account_id", Desc: true}
	PlayerOrderUpdatedAtAsc           = PlayerOrder{Column: "updated_at"}
	PlayerOrderUpdatedAtDesc          = PlayerOrder{Column: "updated_at", Desc: true}
	PlayerOrderUpdatedByAccountIDAsc  = PlayerOrder{Column: "updated_by_account_id"}
	PlayerOrderUpdatedByAccountIDDesc = PlayerOrder{Column: "updated_by_account_id", Desc: true}
	PlayerOrderDeletedAtAsc           = PlayerOrder{Column: "deleted_at"}
	PlayerOrderDeletedAtDesc          = PlayerOrder{Column: "deleted_at", Desc: true}
	PlayerOrderDeletedByAccountIDAsc  = PlayerOrder{Column: "deleted_by_account_id"}
	PlayerOrderDeletedByAccountIDDesc = PlayerOrder{Column: "deleted_by_account_id", Desc: true}
)

// NewPlayerFilterEquals builds an empty PlayerFilterEquals to fill in.
func NewPlayerFilterEquals() *PlayerFilterEquals { return &PlayerFilterEquals{} }

// NewPlayerFilterRange builds an empty PlayerFilterRange to fill in.
func NewPlayerFilterRange() *PlayerFilterRange { return &PlayerFilterRange{} }

// NewPlayerFilterContains builds an empty PlayerFilterContains to fill in.
func NewPlayerFilterContains() *PlayerFilterContains { return &PlayerFilterContains{} }

// NewPlayerFilterLike builds an empty PlayerFilterLike to fill in.
func NewPlayerFilterLike() *PlayerFilterLike { return &PlayerFilterLike{} }

// NewPlayerFilterNull builds an empty PlayerFilterNull to fill in.
func NewPlayerFilterNull() *PlayerFilterNull { return &PlayerFilterNull{} }

// NewPlayerFilterWithout builds an empty PlayerFilterWithout to fill in.
func NewPlayerFilterWithout() *PlayerFilterWithout { return &PlayerFilterWithout{} }

// NewPlayerFilter builds an empty filter, which matches every row the caller
// is allowed to see.
func NewPlayerFilter() PlayerFilter { return PlayerFilter{} }
