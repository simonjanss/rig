// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on Team fields.
type TeamFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// What the manager calls their squad.
	Name *string `json:"name,omitempty"`
	// Whether the squad is playing this season.
	IsActive *bool `json:"isActive,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *uuid.UUID `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID *uuid.UUID `json:"updatedByAccountId,omitempty"`
	// When this row was soft-deleted. Null while the row is live.
	DeletedAt *time.Time `json:"deletedAt,omitempty"`
	// Account that soft-deleted this row, taken from the request's claims.
	DeletedByAccountID *uuid.UUID `json:"deletedByAccountId,omitempty"`
	// Conditions on one of this row's AwayFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.AwayFixtures asks the second.
	AwayFixtures *FixtureFilterEquals `json:"awayFixtures,omitempty"`
	// Conditions on one of this row's HomeFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.HomeFixtures asks the second.
	HomeFixtures *FixtureFilterEquals `json:"homeFixtures,omitempty"`
	// Conditions on one of this row's Players.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Players asks the second.
	Players *PlayerFilterEquals `json:"players,omitempty"`
}

// Ordering conditions on Team fields that can be compared.
type TeamFilterRange struct {
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// When this row was soft-deleted. Null while the row is live.
	DeletedAt *time.Time `json:"deletedAt,omitempty"`
	// Conditions on one of this row's AwayFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.AwayFixtures asks the second.
	AwayFixtures *FixtureFilterRange `json:"awayFixtures,omitempty"`
	// Conditions on one of this row's HomeFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.HomeFixtures asks the second.
	HomeFixtures *FixtureFilterRange `json:"homeFixtures,omitempty"`
	// Conditions on one of this row's Players.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Players asks the second.
	Players *PlayerFilterRange `json:"players,omitempty"`
}

// Set-membership conditions on Team fields.
type TeamFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// What the manager calls their squad.
	Name []string `json:"name,omitempty"`
	// Whether the squad is playing this season.
	IsActive []bool `json:"isActive,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID []uuid.UUID `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID []uuid.UUID `json:"updatedByAccountId,omitempty"`
	// When this row was soft-deleted. Null while the row is live.
	DeletedAt []time.Time `json:"deletedAt,omitempty"`
	// Account that soft-deleted this row, taken from the request's claims.
	DeletedByAccountID []uuid.UUID `json:"deletedByAccountId,omitempty"`
	// Conditions on one of this row's AwayFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.AwayFixtures asks the second.
	AwayFixtures *FixtureFilterContains `json:"awayFixtures,omitempty"`
	// Conditions on one of this row's HomeFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.HomeFixtures asks the second.
	HomeFixtures *FixtureFilterContains `json:"homeFixtures,omitempty"`
	// Conditions on one of this row's Players.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Players asks the second.
	Players *PlayerFilterContains `json:"players,omitempty"`
}

// Pattern conditions on Team text fields.
type TeamFilterLike struct {
	// What the manager calls their squad.
	Name *string `json:"name,omitempty"`
	// Conditions on one of this row's AwayFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.AwayFixtures asks the second.
	AwayFixtures *FixtureFilterLike `json:"awayFixtures,omitempty"`
	// Conditions on one of this row's HomeFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.HomeFixtures asks the second.
	HomeFixtures *FixtureFilterLike `json:"homeFixtures,omitempty"`
	// Conditions on one of this row's Players.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Players asks the second.
	Players *PlayerFilterLike `json:"players,omitempty"`
}

// Presence conditions on nullable Team fields.
type TeamFilterNull struct {
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *bool `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID *bool `json:"updatedByAccountId,omitempty"`
	// When this row was soft-deleted. Null while the row is live.
	DeletedAt *bool `json:"deletedAt,omitempty"`
	// Account that soft-deleted this row, taken from the request's claims.
	DeletedByAccountID *bool `json:"deletedByAccountId,omitempty"`
	// Conditions on one of this row's AwayFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.AwayFixtures asks the second.
	AwayFixtures *FixtureFilterNull `json:"awayFixtures,omitempty"`
	// Conditions on one of this row's HomeFixtures.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.HomeFixtures asks the second.
	HomeFixtures *FixtureFilterNull `json:"homeFixtures,omitempty"`
	// Conditions on one of this row's Players.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Players asks the second.
	Players *PlayerFilterNull `json:"players,omitempty"`
}

// Relations a Team must have no matching row for.
type TeamFilterWithout struct {
	// Match rows with no AwayFixtures satisfying these conditions, including rows
	// with none at all.
	//
	// This negates the existence of a matching row rather than the comparison,
	// which is the whole difference from NotEquals.AwayFixtures: that one is
	// satisfied by one of them differing, and a row can have one that differs and
	// one that matches at the same time. An empty object asks for rows with no
	// AwayFixtures at all.
	AwayFixtures *FixtureFilter `json:"awayFixtures,omitempty"`
	// Match rows with no HomeFixtures satisfying these conditions, including rows
	// with none at all.
	//
	// This negates the existence of a matching row rather than the comparison,
	// which is the whole difference from NotEquals.HomeFixtures: that one is
	// satisfied by one of them differing, and a row can have one that differs and
	// one that matches at the same time. An empty object asks for rows with no
	// HomeFixtures at all.
	HomeFixtures *FixtureFilter `json:"homeFixtures,omitempty"`
	// Match rows with no Players satisfying these conditions, including rows with
	// none at all.
	//
	// This negates the existence of a matching row rather than the comparison,
	// which is the whole difference from NotEquals.Players: that one is satisfied
	// by one of them differing, and a row can have one that differs and one that
	// matches at the same time. An empty object asks for rows with no Players at
	// all.
	Players *PlayerFilter `json:"players,omitempty"`
}

// Conditions a Team must satisfy to match a search.
type TeamFilter struct {
	// Fields that must equal the given value.
	Equals *TeamFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *TeamFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *TeamFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *TeamFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *TeamFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *TeamFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *TeamFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *TeamFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *TeamFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *TeamFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *TeamFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *TeamFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []TeamFilter `json:"nestedFilters,omitempty"`
	// Relations with no row matching the given conditions. An empty one asks for
	// no related row at all.
	//
	// This is where a negation belongs when what you mean is "not": it negates
	// whether a matching related row exists, so a row with no related row at all
	// satisfies it. The negative operators above negate the comparison instead and
	// still require the related row to be there — "no related row called X" is
	// here, "a related row not called X" is NotEquals.
	Without *TeamFilterWithout `json:"without,omitempty"`
}

// TeamPage says which slice of a result to return, and in what order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type TeamPage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []TeamOrder
}

// TeamOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type TeamOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a Team supports.
var (
	TeamOrderIDAsc                  = TeamOrder{Column: "id"}
	TeamOrderIDDesc                 = TeamOrder{Column: "id", Desc: true}
	TeamOrderTenantIDAsc            = TeamOrder{Column: "tenant_id"}
	TeamOrderTenantIDDesc           = TeamOrder{Column: "tenant_id", Desc: true}
	TeamOrderNameAsc                = TeamOrder{Column: "name"}
	TeamOrderNameDesc               = TeamOrder{Column: "name", Desc: true}
	TeamOrderIsActiveAsc            = TeamOrder{Column: "is_active"}
	TeamOrderIsActiveDesc           = TeamOrder{Column: "is_active", Desc: true}
	TeamOrderCreatedAtAsc           = TeamOrder{Column: "created_at"}
	TeamOrderCreatedAtDesc          = TeamOrder{Column: "created_at", Desc: true}
	TeamOrderCreatedByAccountIDAsc  = TeamOrder{Column: "created_by_account_id"}
	TeamOrderCreatedByAccountIDDesc = TeamOrder{Column: "created_by_account_id", Desc: true}
	TeamOrderUpdatedAtAsc           = TeamOrder{Column: "updated_at"}
	TeamOrderUpdatedAtDesc          = TeamOrder{Column: "updated_at", Desc: true}
	TeamOrderUpdatedByAccountIDAsc  = TeamOrder{Column: "updated_by_account_id"}
	TeamOrderUpdatedByAccountIDDesc = TeamOrder{Column: "updated_by_account_id", Desc: true}
	TeamOrderDeletedAtAsc           = TeamOrder{Column: "deleted_at"}
	TeamOrderDeletedAtDesc          = TeamOrder{Column: "deleted_at", Desc: true}
	TeamOrderDeletedByAccountIDAsc  = TeamOrder{Column: "deleted_by_account_id"}
	TeamOrderDeletedByAccountIDDesc = TeamOrder{Column: "deleted_by_account_id", Desc: true}
)

// NewTeamFilterEquals builds an empty TeamFilterEquals to fill in.
func NewTeamFilterEquals() *TeamFilterEquals { return &TeamFilterEquals{} }

// NewTeamFilterRange builds an empty TeamFilterRange to fill in.
func NewTeamFilterRange() *TeamFilterRange { return &TeamFilterRange{} }

// NewTeamFilterContains builds an empty TeamFilterContains to fill in.
func NewTeamFilterContains() *TeamFilterContains { return &TeamFilterContains{} }

// NewTeamFilterLike builds an empty TeamFilterLike to fill in.
func NewTeamFilterLike() *TeamFilterLike { return &TeamFilterLike{} }

// NewTeamFilterNull builds an empty TeamFilterNull to fill in.
func NewTeamFilterNull() *TeamFilterNull { return &TeamFilterNull{} }

// NewTeamFilterWithout builds an empty TeamFilterWithout to fill in.
func NewTeamFilterWithout() *TeamFilterWithout { return &TeamFilterWithout{} }

// NewTeamFilter builds an empty filter, which matches every row the caller is
// allowed to see.
func NewTeamFilter() TeamFilter { return TeamFilter{} }
