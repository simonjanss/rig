// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"strings"
)

// Where on the pitch a player lines up.
type PlayerPosition string

// The values of PlayerPosition.
const (
	// Keeps goal; the only one allowed to handle the ball.
	PlayerPositionGoalkeeper PlayerPosition = "goalkeeper"
	// Plays at the back.
	PlayerPositionDefender PlayerPosition = "defender"
	// Plays between the lines.
	PlayerPositionMidfielder PlayerPosition = "midfielder"
	// Plays up front and is expected to score.
	PlayerPositionForward PlayerPosition = "forward"
)

// AllPlayerPosition is every value, in declaration order.
var AllPlayerPosition = []PlayerPosition{PlayerPositionGoalkeeper, PlayerPositionDefender, PlayerPositionMidfielder, PlayerPositionForward}

// Valid reports whether the value is one the database will accept.
func (v PlayerPosition) Valid() bool {
	switch v {
	case PlayerPositionGoalkeeper, PlayerPositionDefender, PlayerPositionMidfielder, PlayerPositionForward:
		return true
	default:
		return false
	}
}

// ParsePlayerPosition reads a value, accepting any casing and surrounding
// space.
//
// Normalization uses it, so "IN_PROGRESS" from one client and "in_progress"
// from another mean the same thing rather than one of them being a validation
// failure nobody can explain. The spelling still has to be the label’s: a
// value is a name the database knows, not a phrase.
func ParsePlayerPosition(s string) (PlayerPosition, bool) {
	switch strings.ToLower(strings.TrimSpace(s)) {
	case "goalkeeper":
		return PlayerPositionGoalkeeper, true
	case "defender":
		return PlayerPositionDefender, true
	case "midfielder":
		return PlayerPositionMidfielder, true
	case "forward":
		return PlayerPositionForward, true
	default:
		return "", false
	}
}

// String implements fmt.Stringer.
func (v PlayerPosition) String() string { return string(v) }
