// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"context"
	"strings"
	"time"

	"github.com/google/uuid"
	"github.com/simonjanss/rig/runtime/patch"
	"github.com/simonjanss/rig/runtime/rigerr"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// FixtureCreateInput is what creating a Fixture takes.
//
// The identifier, the tenant, and the audit columns are absent: those are
// stamped by the repository from the request's claims.
type FixtureCreateInput struct {
	// The squad playing at home.
	HomeTeamID uuid.UUID `json:"homeTeamId"`
	// The squad playing away.
	AwayTeamID uuid.UUID `json:"awayTeamId"`
	// When the match starts.
	KickoffAt time.Time `json:"kickoffAt"`
}

// Normalize tidies what was given before anything checks it.
//
// It runs first so that validation sees the value that will actually be
// stored: a title with a trailing space and one without are the same title,
// and rejecting the second for a length rule the first passes would be
// indefensible.
func (i *FixtureCreateInput) Normalize() {
	// Nothing about this input needs tidying.
}

// FixtureCreateInputError says what was wrong with each field of a
// FixtureCreateInput.
//
// Its shape is the input's shape, so a client can attach every message to the
// field it is about without matching on strings. A member is nil when that
// field was fine, and the whole value is nil when the input was. It is what
// the 422 carries.
type FixtureCreateInputError struct {
	// The squad playing at home.
	HomeTeamID *rigerr.FieldError `json:"homeTeamId,omitempty"`
	// The squad playing away.
	AwayTeamID *rigerr.FieldError `json:"awayTeamId,omitempty"`
	// When the match starts.
	KickoffAt *rigerr.FieldError `json:"kickoffAt,omitempty"`

	// Entity is a problem with the row as a whole rather than with one field: what
	// the Entity rule said.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// Empty reports whether anything went wrong. A validator that found nothing
// returns nil rather than one of these.
func (e *FixtureCreateInputError) Empty() bool {
	if e == nil {
		return true
	}

	return e.HomeTeamID == nil && e.AwayTeamID == nil && e.KickoffAt == nil && e.Entity == nil
}

// Error implements error. The sentence is for logs and for a person; the
// structure above is what a client acts on.
func (e *FixtureCreateInputError) Error() string {
	var parts []string
	if e.HomeTeamID != nil {
		parts = append(parts, "homeTeamId "+e.HomeTeamID.Error())
	}
	if e.AwayTeamID != nil {
		parts = append(parts, "awayTeamId "+e.AwayTeamID.Error())
	}
	if e.KickoffAt != nil {
		parts = append(parts, "kickoffAt "+e.KickoffAt.Error())
	}
	if e.Entity != nil {
		parts = append(parts, e.Entity.Error())
	}

	return "fixture is not valid: " + strings.Join(parts, "; ")
}

// ErrorCode implements [rigerr.Coder]: the request was understood and its
// content is what is wrong, which is 422 and not 400.
func (e *FixtureCreateInputError) ErrorCode() rigerr.Code { return rigerr.CodeUnprocessableEntity }

// ErrorFields implements [rigerr.FieldReporter], which is how the HTTP layer
// finds this and answers with it rather than with prose.
func (e *FixtureCreateInputError) ErrorFields() any { return e }

// Validate checks what the schema can decide on its own.
//
// Everything a column declares — NOT NULL, a length, an enumeration's values
// — is checked here, so a service only writes the rules that are actually
// about the business. Every field is checked before returning, because a form
// that reports one problem per round trip is a form people give up on.
//
// What comes back is a *FixtureCreateInputError, shaped like the input itself.
func (i *FixtureCreateInput) Validate() error {
	var failed FixtureCreateInputError

	if failed.Empty() {
		return nil
	}
	return &failed
}

// FixtureUpdateInput is what changing a Fixture takes.
//
// A field left out is untouched. A nullable field set to null is cleared —
// which is why the two wrappers differ: a column that cannot hold null has no
// way to be given one, so clearing it is a compile error rather than a
// rejection at runtime. Immutable fields are not here at all.
type FixtureUpdateInput struct {
	// The squad playing at home.
	HomeTeamID patch.Optional[uuid.UUID] `json:"homeTeamId"`
	// The squad playing away.
	AwayTeamID patch.Optional[uuid.UUID] `json:"awayTeamId"`
	// When the match starts.
	KickoffAt patch.Optional[time.Time] `json:"kickoffAt"`
}

// Normalize tidies the fields this request actually carries.
//
// It does not fill in the ones it does not: the repository writes exactly the
// columns that were sent, and filling them here would turn every update into a
// write of every column — so two requests changing different fields of one
// row would start overwriting each other instead of composing.
func (i *FixtureUpdateInput) Normalize() {
	// Nothing about this input needs tidying.
}

// Merged is the row as it will be once this update is applied.
//
// It is what validation runs against, and the reason it exists: a rule
// spanning two fields cannot be checked from a partial request. "Ends after
// starts" is unanswerable when only one of them was sent.
//
// It returns a copy. The input keeps its patches, so the repository still
// writes only the columns that were actually given.
func (i FixtureUpdateInput) Merged(prev *Fixture) Fixture {
	out := *prev

	if v, ok := i.HomeTeamID.Get(); ok {
		out.HomeTeamID = v
	}
	if v, ok := i.AwayTeamID.Get(); ok {
		out.AwayTeamID = v
	}
	if v, ok := i.KickoffAt.Get(); ok {
		out.KickoffAt = v
	}

	return out
}

// FixtureUpdateInputError says what was wrong with each field of a
// FixtureUpdateInput.
//
// Its shape is the input's shape, so a client can attach every message to the
// field it is about without matching on strings. A member is nil when that
// field was fine, and the whole value is nil when the input was. It is what
// the 422 carries.
type FixtureUpdateInputError struct {
	// The squad playing at home.
	HomeTeamID *rigerr.FieldError `json:"homeTeamId,omitempty"`
	// The squad playing away.
	AwayTeamID *rigerr.FieldError `json:"awayTeamId,omitempty"`
	// When the match starts.
	KickoffAt *rigerr.FieldError `json:"kickoffAt,omitempty"`

	// Entity is a problem with the row as a whole rather than with one field: what
	// the Entity rule said.
	Entity *rigerr.FieldError `json:"entity,omitempty"`
}

// Empty reports whether anything went wrong. A validator that found nothing
// returns nil rather than one of these.
func (e *FixtureUpdateInputError) Empty() bool {
	if e == nil {
		return true
	}

	return e.HomeTeamID == nil && e.AwayTeamID == nil && e.KickoffAt == nil && e.Entity == nil
}

// Error implements error. The sentence is for logs and for a person; the
// structure above is what a client acts on.
func (e *FixtureUpdateInputError) Error() string {
	var parts []string
	if e.HomeTeamID != nil {
		parts = append(parts, "homeTeamId "+e.HomeTeamID.Error())
	}
	if e.AwayTeamID != nil {
		parts = append(parts, "awayTeamId "+e.AwayTeamID.Error())
	}
	if e.KickoffAt != nil {
		parts = append(parts, "kickoffAt "+e.KickoffAt.Error())
	}
	if e.Entity != nil {
		parts = append(parts, e.Entity.Error())
	}

	return "fixture is not valid: " + strings.Join(parts, "; ")
}

// ErrorCode implements [rigerr.Coder]: the request was understood and its
// content is what is wrong, which is 422 and not 400.
func (e *FixtureUpdateInputError) ErrorCode() rigerr.Code { return rigerr.CodeUnprocessableEntity }

// ErrorFields implements [rigerr.FieldReporter], which is how the HTTP layer
// finds this and answers with it rather than with prose.
func (e *FixtureUpdateInputError) ErrorFields() any { return e }

// Validate checks the row this update would produce.
//
// Against the merged state, not the request: a length rule on a field nobody
// sent still has to hold, and a rule about two fields needs both.
//
// What comes back is a *FixtureUpdateInputError, shaped like the input itself.
func (i *FixtureUpdateInput) Validate(prev *Fixture) error {
	var failed FixtureUpdateInputError

	if failed.Empty() {
		return nil
	}
	return &failed
}

// FixtureDeleteInput is what deleting a Fixture takes.
type FixtureDeleteInput struct {
	// ID is the row to delete.
	ID uuid.UUID `json:"id"`
}

// FixtureValidatorContext is what a rule sees.
//
// Values is the row as it will be if this goes through — merged from the
// previous state on an update, so every field is set whether or not the
// request mentioned it. That is the point: "ends after starts" cannot be
// answered from a request that only carried one of them.
type FixtureValidatorContext struct {
	// Values is the intended end state.
	Values Fixture

	// Claims are who is asking. They are a value rather than something to fetch
	// from the context because a rule that has to look them up is a rule that can
	// forget to, and because there is no case where they are absent: a write
	// without a caller is refused by the repository before any rule runs.
	Claims tenancy.Claims

	// previous is the row before this change, and is the zero value on a
	// create — there was nothing before.
	previous Fixture
	isUpdate bool
	changed  map[string]bool
}

// IsUpdate reports whether there was a row before this.
func (c *FixtureValidatorContext) IsUpdate() bool { return c.isUpdate }

// Previous is the row as it was, and the zero value on a create. Check
// IsUpdate before reading it.
func (c *FixtureValidatorContext) Previous() Fixture { return c.previous }

// Changed reports whether this request carried a new value for a column.
//
// It is what keeps an expensive rule from running on every update: a check
// that reaches another service to confirm a reference only needs to run when
// the reference actually moved. On a create everything is changed, because
// everything is new.
func (c *FixtureValidatorContext) Changed(column string) bool { return c.changed[column] }

// HomeTeamIDChanged reports whether this request set home_team_id.
func (c *FixtureValidatorContext) HomeTeamIDChanged() bool { return c.changed[ColumnFixtureHomeTeamID] }

// AwayTeamIDChanged reports whether this request set away_team_id.
func (c *FixtureValidatorContext) AwayTeamIDChanged() bool { return c.changed[ColumnFixtureAwayTeamID] }

// KickoffAtChanged reports whether this request set kickoff_at.
func (c *FixtureValidatorContext) KickoffAtChanged() bool { return c.changed[ColumnFixtureKickoffAt] }

// FixtureCreateValidator is the rules for bringing a Fixture into existence:
// what the schema cannot express.
//
// One optional function per field this operation can set, so the set of fields
// is the set of rules that could apply — a column an update cannot touch has
// no hook here to write by mistake. A nil one is skipped. Every configured
// hook runs, because a rule that fails should not hide the next one, so a
// request reports everything wrong with it.
//
// A hook returns a FieldError to attach the message to a specific field, or
// any other error to fail the request outright.
type FixtureCreateValidator struct {
	// The squad playing at home.
	HomeTeamID func(ctx context.Context, c *FixtureValidatorContext, value uuid.UUID) error
	// The squad playing away.
	AwayTeamID func(ctx context.Context, c *FixtureValidatorContext, value uuid.UUID) error
	// When the match starts.
	KickoffAt func(ctx context.Context, c *FixtureValidatorContext, value time.Time) error

	// Entity runs after the per-field hooks, for a rule that is about the row
	// rather than about one column.
	Entity func(ctx context.Context, c *FixtureValidatorContext) error
}

// RunCreate implements [dbhook.CreateValidator]: it runs the service's rules
// against the row this input would produce.
//
// The generated checks are not repeated here. The repository runs Normalize
// and Validate first, so by the time a hook sees the input it is tidy and the
// schema is satisfied — which is what lets a hook be about the business
// rather than about NOT NULL.
func (v FixtureCreateValidator) RunCreate(ctx context.Context, claims tenancy.Claims, i *FixtureCreateInput) error {
	// Everything is new, so everything counts as changed.
	c := &FixtureValidatorContext{Claims: claims, changed: map[string]bool{}}
	c.Values.HomeTeamID = i.HomeTeamID
	c.changed[ColumnFixtureHomeTeamID] = true
	c.Values.AwayTeamID = i.AwayTeamID
	c.changed[ColumnFixtureAwayTeamID] = true
	c.Values.KickoffAt = i.KickoffAt
	c.changed[ColumnFixtureKickoffAt] = true

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// run calls every configured hook and puts what each one said under the field
// it was about.
//
// Two kinds of answer. A [rigerr.FieldError] is about the input: it lands on
// the field and the others still run, so one request reports everything wrong
// with it. Anything else is the rule itself failing — a lookup that could
// not reach another service — and there is nothing to tell the caller about
// their input, so it comes back wrapped with the rule that could not be run,
// keeping whatever code it carried and becoming Internal if it carried none.
func (v FixtureCreateValidator) run(ctx context.Context, c *FixtureValidatorContext) (*FixtureCreateInputError, error) {
	var failed FixtureCreateInputError

	if v.HomeTeamID != nil {
		if err := v.HomeTeamID(ctx, c, c.Values.HomeTeamID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate home_team_id")
			}
			failed.HomeTeamID = field
		}
	}
	if v.AwayTeamID != nil {
		if err := v.AwayTeamID(ctx, c, c.Values.AwayTeamID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate away_team_id")
			}
			failed.AwayTeamID = field
		}
	}
	if v.KickoffAt != nil {
		if err := v.KickoffAt(ctx, c, c.Values.KickoffAt); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate kickoff_at")
			}
			failed.KickoffAt = field
		}
	}

	if v.Entity != nil {
		if err := v.Entity(ctx, c); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate fixture")
			}
			failed.Entity = field
		}
	}

	if failed.Empty() {
		return nil, nil
	}
	return &failed, nil
}

// FixtureUpdateValidator is the rules for changing one that already exists:
// what the schema cannot express.
//
// One optional function per field this operation can set, so the set of fields
// is the set of rules that could apply — a column an update cannot touch has
// no hook here to write by mistake. A nil one is skipped. Every configured
// hook runs, because a rule that fails should not hide the next one, so a
// request reports everything wrong with it.
//
// A hook returns a FieldError to attach the message to a specific field, or
// any other error to fail the request outright.
type FixtureUpdateValidator struct {
	// The squad playing at home.
	HomeTeamID func(ctx context.Context, c *FixtureValidatorContext, value uuid.UUID) error
	// The squad playing away.
	AwayTeamID func(ctx context.Context, c *FixtureValidatorContext, value uuid.UUID) error
	// When the match starts.
	KickoffAt func(ctx context.Context, c *FixtureValidatorContext, value time.Time) error

	// Entity runs after the per-field hooks, for a rule that is about the row
	// rather than about one column.
	Entity func(ctx context.Context, c *FixtureValidatorContext) error
}

// RunUpdate implements [dbhook.UpdateValidator]: it runs the service's rules
// against the row this update would produce, with the row as it was available
// for a rule about the change itself.
func (v FixtureUpdateValidator) RunUpdate(ctx context.Context, claims tenancy.Claims, i *FixtureUpdateInput, prev *Fixture) error {
	c := &FixtureValidatorContext{
		Values:   i.Merged(prev),
		Claims:   claims,
		previous: *prev,
		isUpdate: true,
		changed:  map[string]bool{},
	}
	c.changed[ColumnFixtureHomeTeamID] = i.HomeTeamID.IsSet()
	c.changed[ColumnFixtureAwayTeamID] = i.AwayTeamID.IsSet()
	c.changed[ColumnFixtureKickoffAt] = i.KickoffAt.IsSet()

	failed, err := v.run(ctx, c)
	if err != nil {
		return err
	}
	if failed != nil {
		return failed
	}
	return nil
}

// run calls every configured hook and puts what each one said under the field
// it was about.
//
// Two kinds of answer. A [rigerr.FieldError] is about the input: it lands on
// the field and the others still run, so one request reports everything wrong
// with it. Anything else is the rule itself failing — a lookup that could
// not reach another service — and there is nothing to tell the caller about
// their input, so it comes back wrapped with the rule that could not be run,
// keeping whatever code it carried and becoming Internal if it carried none.
func (v FixtureUpdateValidator) run(ctx context.Context, c *FixtureValidatorContext) (*FixtureUpdateInputError, error) {
	var failed FixtureUpdateInputError

	if v.HomeTeamID != nil {
		if err := v.HomeTeamID(ctx, c, c.Values.HomeTeamID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate home_team_id")
			}
			failed.HomeTeamID = field
		}
	}
	if v.AwayTeamID != nil {
		if err := v.AwayTeamID(ctx, c, c.Values.AwayTeamID); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate away_team_id")
			}
			failed.AwayTeamID = field
		}
	}
	if v.KickoffAt != nil {
		if err := v.KickoffAt(ctx, c, c.Values.KickoffAt); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate kickoff_at")
			}
			failed.KickoffAt = field
		}
	}

	if v.Entity != nil {
		if err := v.Entity(ctx, c); err != nil {
			field, ok := rigerr.AsFieldError(err)
			if !ok {
				return nil, rigerr.Wrap(err, "validate fixture")
			}
			failed.Entity = field
		}
	}

	if failed.Empty() {
		return nil, nil
	}
	return &failed, nil
}
