// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on Fixture fields.
type FixtureFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// The squad playing at home.
	HomeTeamID *uuid.UUID `json:"homeTeamId,omitempty"`
	// The squad playing away.
	AwayTeamID *uuid.UUID `json:"awayTeamId,omitempty"`
	// When the match starts.
	KickoffAt *time.Time `json:"kickoffAt,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *uuid.UUID `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID *uuid.UUID `json:"updatedByAccountId,omitempty"`
	// Conditions on this row's AwayTeam.
	//
	// A row with no AwayTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the AwayTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching AwayTeam, which a row with none satisfies — use
	// Without.AwayTeam.
	AwayTeam *TeamFilterEquals `json:"awayTeam,omitempty"`
	// Conditions on this row's HomeTeam.
	//
	// A row with no HomeTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the HomeTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching HomeTeam, which a row with none satisfies — use
	// Without.HomeTeam.
	HomeTeam *TeamFilterEquals `json:"homeTeam,omitempty"`
}

// Ordering conditions on Fixture fields that can be compared.
type FixtureFilterRange struct {
	// When the match starts.
	KickoffAt *time.Time `json:"kickoffAt,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// Conditions on this row's AwayTeam.
	//
	// A row with no AwayTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the AwayTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching AwayTeam, which a row with none satisfies — use
	// Without.AwayTeam.
	AwayTeam *TeamFilterRange `json:"awayTeam,omitempty"`
	// Conditions on this row's HomeTeam.
	//
	// A row with no HomeTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the HomeTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching HomeTeam, which a row with none satisfies — use
	// Without.HomeTeam.
	HomeTeam *TeamFilterRange `json:"homeTeam,omitempty"`
}

// Set-membership conditions on Fixture fields.
type FixtureFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// The squad playing at home.
	HomeTeamID []uuid.UUID `json:"homeTeamId,omitempty"`
	// The squad playing away.
	AwayTeamID []uuid.UUID `json:"awayTeamId,omitempty"`
	// When the match starts.
	KickoffAt []time.Time `json:"kickoffAt,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID []uuid.UUID `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID []uuid.UUID `json:"updatedByAccountId,omitempty"`
	// Conditions on this row's AwayTeam.
	//
	// A row with no AwayTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the AwayTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching AwayTeam, which a row with none satisfies — use
	// Without.AwayTeam.
	AwayTeam *TeamFilterContains `json:"awayTeam,omitempty"`
	// Conditions on this row's HomeTeam.
	//
	// A row with no HomeTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the HomeTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching HomeTeam, which a row with none satisfies — use
	// Without.HomeTeam.
	HomeTeam *TeamFilterContains `json:"homeTeam,omitempty"`
}

// Pattern conditions on Fixture text fields.
type FixtureFilterLike struct {
	// Conditions on this row's AwayTeam.
	//
	// A row with no AwayTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the AwayTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching AwayTeam, which a row with none satisfies — use
	// Without.AwayTeam.
	AwayTeam *TeamFilterLike `json:"awayTeam,omitempty"`
	// Conditions on this row's HomeTeam.
	//
	// A row with no HomeTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the HomeTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching HomeTeam, which a row with none satisfies — use
	// Without.HomeTeam.
	HomeTeam *TeamFilterLike `json:"homeTeam,omitempty"`
}

// Presence conditions on nullable Fixture fields.
type FixtureFilterNull struct {
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *bool `json:"createdByAccountId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// Account that last updated this row, taken from the request's claims.
	UpdatedByAccountID *bool `json:"updatedByAccountId,omitempty"`
	// Conditions on this row's AwayTeam.
	//
	// A row with no AwayTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the AwayTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching AwayTeam, which a row with none satisfies — use
	// Without.AwayTeam.
	AwayTeam *TeamFilterNull `json:"awayTeam,omitempty"`
	// Conditions on this row's HomeTeam.
	//
	// A row with no HomeTeam never matches, and that holds for the negative
	// operators too: under NotEquals this asks whether the HomeTeam is there and
	// differs, so a row without one fails that as well. To ask the other question
	// — no matching HomeTeam, which a row with none satisfies — use
	// Without.HomeTeam.
	HomeTeam *TeamFilterNull `json:"homeTeam,omitempty"`
}

// Relations a Fixture must have no matching row for.
type FixtureFilterWithout struct {
	// Match rows with no AwayTeam satisfying these conditions, including rows that
	// have none at all.
	//
	// This negates the existence of the related row rather than the comparison,
	// which is the whole difference from NotEquals.AwayTeam: that one needs the
	// AwayTeam to be there and to differ, so a row without one fails it and
	// satisfies this. An empty object asks for rows with no AwayTeam at all.
	AwayTeam *TeamFilter `json:"awayTeam,omitempty"`
	// Match rows with no HomeTeam satisfying these conditions, including rows that
	// have none at all.
	//
	// This negates the existence of the related row rather than the comparison,
	// which is the whole difference from NotEquals.HomeTeam: that one needs the
	// HomeTeam to be there and to differ, so a row without one fails it and
	// satisfies this. An empty object asks for rows with no HomeTeam at all.
	HomeTeam *TeamFilter `json:"homeTeam,omitempty"`
}

// Conditions a Fixture must satisfy to match a search.
type FixtureFilter struct {
	// Fields that must equal the given value.
	Equals *FixtureFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *FixtureFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *FixtureFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *FixtureFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *FixtureFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *FixtureFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *FixtureFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *FixtureFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *FixtureFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *FixtureFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *FixtureFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *FixtureFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []FixtureFilter `json:"nestedFilters,omitempty"`
	// Relations with no row matching the given conditions. An empty one asks for
	// no related row at all.
	//
	// This is where a negation belongs when what you mean is "not": it negates
	// whether a matching related row exists, so a row with no related row at all
	// satisfies it. The negative operators above negate the comparison instead and
	// still require the related row to be there — "no related row called X" is
	// here, "a related row not called X" is NotEquals.
	Without *FixtureFilterWithout `json:"without,omitempty"`
}

// FixturePage says which slice of a result to return, and in what order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type FixturePage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []FixtureOrder
}

// FixtureOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type FixtureOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a Fixture supports.
var (
	FixtureOrderIDAsc                  = FixtureOrder{Column: "id"}
	FixtureOrderIDDesc                 = FixtureOrder{Column: "id", Desc: true}
	FixtureOrderTenantIDAsc            = FixtureOrder{Column: "tenant_id"}
	FixtureOrderTenantIDDesc           = FixtureOrder{Column: "tenant_id", Desc: true}
	FixtureOrderHomeTeamIDAsc          = FixtureOrder{Column: "home_team_id"}
	FixtureOrderHomeTeamIDDesc         = FixtureOrder{Column: "home_team_id", Desc: true}
	FixtureOrderAwayTeamIDAsc          = FixtureOrder{Column: "away_team_id"}
	FixtureOrderAwayTeamIDDesc         = FixtureOrder{Column: "away_team_id", Desc: true}
	FixtureOrderKickoffAtAsc           = FixtureOrder{Column: "kickoff_at"}
	FixtureOrderKickoffAtDesc          = FixtureOrder{Column: "kickoff_at", Desc: true}
	FixtureOrderCreatedAtAsc           = FixtureOrder{Column: "created_at"}
	FixtureOrderCreatedAtDesc          = FixtureOrder{Column: "created_at", Desc: true}
	FixtureOrderCreatedByAccountIDAsc  = FixtureOrder{Column: "created_by_account_id"}
	FixtureOrderCreatedByAccountIDDesc = FixtureOrder{Column: "created_by_account_id", Desc: true}
	FixtureOrderUpdatedAtAsc           = FixtureOrder{Column: "updated_at"}
	FixtureOrderUpdatedAtDesc          = FixtureOrder{Column: "updated_at", Desc: true}
	FixtureOrderUpdatedByAccountIDAsc  = FixtureOrder{Column: "updated_by_account_id"}
	FixtureOrderUpdatedByAccountIDDesc = FixtureOrder{Column: "updated_by_account_id", Desc: true}
)

// FixtureOrderAwayTeam orders a Fixture by a column of its AwayTeam.
//
// Take the term from Team's own orderings:
//
//	FixtureOrderAwayTeam(TeamOrderIDDesc)
//
// The repository reaches it with a left join, so a Fixture with no AwayTeam
// sorts to one end rather than vanishing. Only Team's own columns can be used;
// an ordering that itself reaches across a relation is refused, because one
// join is where this stops.
func FixtureOrderAwayTeam(o TeamOrder) FixtureOrder {
	return FixtureOrder{Relation: "AwayTeam", Column: o.Column, Desc: o.Desc}
}

// FixtureOrderHomeTeam orders a Fixture by a column of its HomeTeam.
//
// Take the term from Team's own orderings:
//
//	FixtureOrderHomeTeam(TeamOrderIDDesc)
//
// The repository reaches it with a left join, so a Fixture with no HomeTeam
// sorts to one end rather than vanishing. Only Team's own columns can be used;
// an ordering that itself reaches across a relation is refused, because one
// join is where this stops.
func FixtureOrderHomeTeam(o TeamOrder) FixtureOrder {
	return FixtureOrder{Relation: "HomeTeam", Column: o.Column, Desc: o.Desc}
}

// NewFixtureFilterEquals builds an empty FixtureFilterEquals to fill in.
func NewFixtureFilterEquals() *FixtureFilterEquals { return &FixtureFilterEquals{} }

// NewFixtureFilterRange builds an empty FixtureFilterRange to fill in.
func NewFixtureFilterRange() *FixtureFilterRange { return &FixtureFilterRange{} }

// NewFixtureFilterContains builds an empty FixtureFilterContains to fill in.
func NewFixtureFilterContains() *FixtureFilterContains { return &FixtureFilterContains{} }

// NewFixtureFilterLike builds an empty FixtureFilterLike to fill in.
func NewFixtureFilterLike() *FixtureFilterLike { return &FixtureFilterLike{} }

// NewFixtureFilterNull builds an empty FixtureFilterNull to fill in.
func NewFixtureFilterNull() *FixtureFilterNull { return &FixtureFilterNull{} }

// NewFixtureFilterWithout builds an empty FixtureFilterWithout to fill in.
func NewFixtureFilterWithout() *FixtureFilterWithout { return &FixtureFilterWithout{} }

// NewFixtureFilter builds an empty filter, which matches every row the caller
// is allowed to see.
func NewFixtureFilter() FixtureFilter { return FixtureFilter{} }
