// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"github.com/google/uuid"
	"github.com/simonjanss/rig/examples/fantasyfootball/internal/model"
)

// A page of Players.
type PlayerListResponse struct {
	// The rows in this page.
	//
	// Pointers, because that is what the repository returns and copying every row
	// to change that would be work nobody asked for.
	Data []*model.Player `json:"data"`
	// Where this page sits in the full result set.
	Pagination Pagination `json:"pagination"`
}

// Query parameters for Player.List.
type PlayerListQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// Query parameters for Player.Search.
type PlayerSearchQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// The request body for Player.Search.
type PlayerSearchBody struct {
	// Conditions rows must satisfy.
	Filter model.PlayerFilter `json:"filter"`
}

// Query parameters for Player.ListDeleted.
type PlayerListDeletedQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// Path parameters for Player.Delete.
type PlayerDeletePath struct {
	// Identifier of the Player.
	ID uuid.UUID `json:"id"`
}

// Path parameters for Player.Get.
type PlayerGetPath struct {
	// Identifier of the Player.
	ID uuid.UUID `json:"id"`
}

// Path parameters for Player.Update.
type PlayerUpdatePath struct {
	// Identifier of the Player.
	ID uuid.UUID `json:"id"`
}

// Path parameters for Player.Restore.
type PlayerRestorePath struct {
	// Identifier of the Player.
	ID uuid.UUID `json:"id"`
}
