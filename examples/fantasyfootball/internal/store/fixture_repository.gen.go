// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package store

import (
	"context"
	"fmt"
	"strconv"
	"time"

	"github.com/google/uuid"
	pgx "github.com/jackc/pgx/v5"
	"github.com/simonjanss/rig/examples/fantasyfootball/internal/model"
	"github.com/simonjanss/rig/observe"
	"github.com/simonjanss/rig/runtime/dbhook"
	"github.com/simonjanss/rig/runtime/dbx"
	"github.com/simonjanss/rig/runtime/query"
	"github.com/simonjanss/rig/runtime/readopt"
	"github.com/simonjanss/rig/runtime/rigerr"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// FixtureRepository reads and writes fixture rows.
//
// Every method scopes to the caller's tenant. There is no way to ask it not to
// from a request handler, which is the point.
type FixtureRepository interface {
	// Get returns one row by identifier.
	//
	// A lookup by primary key deliberately ignores the lifecycle filters: it
	// returns the row whether it is live, deleted, or a snapshot, because a caller
	// holding an identifier is usually asking about that exact row.
	Get(ctx context.Context, id uuid.UUID, opts ...readopt.Option) (*model.Fixture, error)

	// List returns matching rows and the total ignoring pagination.
	//
	// The filter says which rows and the page says how many of them, in what
	// order. They are separate arguments because they arrive separately: the
	// filter is a request body, the page is two query parameters.
	List(ctx context.Context, f model.FixtureFilter, page model.FixturePage, opts ...readopt.Option) ([]*model.Fixture, int64, error)

	// Create inserts a row, stamping the identifier, tenant and audit columns.
	//
	// It takes an envelope rather than the input alone: the rules that check the
	// input and the callbacks that run around the write belong to the same unit of
	// work as the write itself.
	Create(ctx context.Context, in dbhook.Create[model.FixtureCreateInput, model.Fixture]) (*model.Fixture, error)

	// Update changes the fields the input mentions and leaves the rest alone.
	Update(ctx context.Context, id uuid.UUID, in dbhook.Update[model.FixtureUpdateInput, model.Fixture]) (*model.Fixture, error)

	// Delete removes a row.
	Delete(ctx context.Context, in dbhook.Delete[model.FixtureDeleteInput, model.Fixture]) error
}

// fixtureAwayTeamFilter collects the conditions written on a Fixture's
// AwayTeam.
//
// The bool is whether there were any: a relation nobody mentioned is not a
// condition that everything satisfies, it is no condition at all.
func fixtureAwayTeamFilter(f model.FixtureFilter) (model.TeamFilter, bool) {
	var (
		sub   model.TeamFilter
		asked bool
	)

	if p := f.Equals; p != nil && p.AwayTeam != nil {
		sub.Equals, asked = p.AwayTeam, true
	}
	if p := f.NotEquals; p != nil && p.AwayTeam != nil {
		sub.NotEquals, asked = p.AwayTeam, true
	}
	if p := f.GreaterThan; p != nil && p.AwayTeam != nil {
		sub.GreaterThan, asked = p.AwayTeam, true
	}
	if p := f.SmallerThan; p != nil && p.AwayTeam != nil {
		sub.SmallerThan, asked = p.AwayTeam, true
	}
	if p := f.GreaterOrEqual; p != nil && p.AwayTeam != nil {
		sub.GreaterOrEqual, asked = p.AwayTeam, true
	}
	if p := f.SmallerOrEqual; p != nil && p.AwayTeam != nil {
		sub.SmallerOrEqual, asked = p.AwayTeam, true
	}
	if p := f.Contains; p != nil && p.AwayTeam != nil {
		sub.Contains, asked = p.AwayTeam, true
	}
	if p := f.NotContains; p != nil && p.AwayTeam != nil {
		sub.NotContains, asked = p.AwayTeam, true
	}
	if p := f.Like; p != nil && p.AwayTeam != nil {
		sub.Like, asked = p.AwayTeam, true
	}
	if p := f.NotLike; p != nil && p.AwayTeam != nil {
		sub.NotLike, asked = p.AwayTeam, true
	}
	if p := f.Null; p != nil && p.AwayTeam != nil {
		sub.Null, asked = p.AwayTeam, true
	}
	if p := f.NotNull; p != nil && p.AwayTeam != nil {
		sub.NotNull, asked = p.AwayTeam, true
	}

	if !asked {
		return sub, false
	}

	// The connective comes down with them, and it has to: under OR the caller
	// asked for a related row satisfying either condition, and one subquery whose
	// inside is a disjunction is exactly that. Under AND it is the same row
	// satisfying both, which is the part a subquery per operator could not say.
	sub.OrCondition = f.OrCondition
	return sub, true
}

// fixtureHomeTeamFilter collects the conditions written on a Fixture's
// HomeTeam.
//
// The bool is whether there were any: a relation nobody mentioned is not a
// condition that everything satisfies, it is no condition at all.
func fixtureHomeTeamFilter(f model.FixtureFilter) (model.TeamFilter, bool) {
	var (
		sub   model.TeamFilter
		asked bool
	)

	if p := f.Equals; p != nil && p.HomeTeam != nil {
		sub.Equals, asked = p.HomeTeam, true
	}
	if p := f.NotEquals; p != nil && p.HomeTeam != nil {
		sub.NotEquals, asked = p.HomeTeam, true
	}
	if p := f.GreaterThan; p != nil && p.HomeTeam != nil {
		sub.GreaterThan, asked = p.HomeTeam, true
	}
	if p := f.SmallerThan; p != nil && p.HomeTeam != nil {
		sub.SmallerThan, asked = p.HomeTeam, true
	}
	if p := f.GreaterOrEqual; p != nil && p.HomeTeam != nil {
		sub.GreaterOrEqual, asked = p.HomeTeam, true
	}
	if p := f.SmallerOrEqual; p != nil && p.HomeTeam != nil {
		sub.SmallerOrEqual, asked = p.HomeTeam, true
	}
	if p := f.Contains; p != nil && p.HomeTeam != nil {
		sub.Contains, asked = p.HomeTeam, true
	}
	if p := f.NotContains; p != nil && p.HomeTeam != nil {
		sub.NotContains, asked = p.HomeTeam, true
	}
	if p := f.Like; p != nil && p.HomeTeam != nil {
		sub.Like, asked = p.HomeTeam, true
	}
	if p := f.NotLike; p != nil && p.HomeTeam != nil {
		sub.NotLike, asked = p.HomeTeam, true
	}
	if p := f.Null; p != nil && p.HomeTeam != nil {
		sub.Null, asked = p.HomeTeam, true
	}
	if p := f.NotNull; p != nil && p.HomeTeam != nil {
		sub.NotNull, asked = p.HomeTeam, true
	}

	if !asked {
		return sub, false
	}

	// The connective comes down with them, and it has to: under OR the caller
	// asked for a related row satisfying either condition, and one subquery whose
	// inside is a disjunction is exactly that. Under AND it is the same row
	// satisfying both, which is the part a subquery per operator could not say.
	sub.OrCondition = f.OrCondition
	return sub, true
}

// fixtureGroup turns a filter into the condition tree the runtime renders.
//
// The scope carries what a condition needs besides the filter itself: who is
// asking, which alias this level's columns belong to, and how deep the nesting
// has gone. It is a value rather than three parameters because every relation
// passes a changed copy of it down.
func fixtureGroup(f model.FixtureFilter, sc filterScope) (query.Group, error) {
	if err := sc.ok(); err != nil {
		return query.Group{}, err
	}

	g := query.Group{Or: f.OrCondition}

	if p := f.Equals; p != nil {
		if p.ID != nil {
			g.Add(sc.at(query.Eq("id", *p.ID)))
		}
		if p.HomeTeamID != nil {
			g.Add(sc.at(query.Eq("home_team_id", *p.HomeTeamID)))
		}
		if p.AwayTeamID != nil {
			g.Add(sc.at(query.Eq("away_team_id", *p.AwayTeamID)))
		}
		if p.KickoffAt != nil {
			g.Add(sc.at(query.Eq("kickoff_at", *p.KickoffAt)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Eq("created_at", *p.CreatedAt)))
		}
		if p.CreatedByAccountID != nil {
			g.Add(sc.at(query.Eq("created_by_account_id", p.CreatedByAccountID)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Eq("updated_at", p.UpdatedAt)))
		}
		if p.UpdatedByAccountID != nil {
			g.Add(sc.at(query.Eq("updated_by_account_id", p.UpdatedByAccountID)))
		}
	}
	if p := f.NotEquals; p != nil {
		if p.ID != nil {
			g.Add(sc.at(query.Ne("id", *p.ID)))
		}
		if p.HomeTeamID != nil {
			g.Add(sc.at(query.Ne("home_team_id", *p.HomeTeamID)))
		}
		if p.AwayTeamID != nil {
			g.Add(sc.at(query.Ne("away_team_id", *p.AwayTeamID)))
		}
		if p.KickoffAt != nil {
			g.Add(sc.at(query.Ne("kickoff_at", *p.KickoffAt)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Ne("created_at", *p.CreatedAt)))
		}
		if p.CreatedByAccountID != nil {
			g.Add(sc.at(query.Ne("created_by_account_id", p.CreatedByAccountID)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Ne("updated_at", p.UpdatedAt)))
		}
		if p.UpdatedByAccountID != nil {
			g.Add(sc.at(query.Ne("updated_by_account_id", p.UpdatedByAccountID)))
		}
	}
	if p := f.GreaterThan; p != nil {
		if p.KickoffAt != nil {
			g.Add(sc.at(query.Gt("kickoff_at", *p.KickoffAt)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Gt("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Gt("updated_at", p.UpdatedAt)))
		}
	}
	if p := f.SmallerThan; p != nil {
		if p.KickoffAt != nil {
			g.Add(sc.at(query.Lt("kickoff_at", *p.KickoffAt)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Lt("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Lt("updated_at", p.UpdatedAt)))
		}
	}
	if p := f.GreaterOrEqual; p != nil {
		if p.KickoffAt != nil {
			g.Add(sc.at(query.Gte("kickoff_at", *p.KickoffAt)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Gte("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Gte("updated_at", p.UpdatedAt)))
		}
	}
	if p := f.SmallerOrEqual; p != nil {
		if p.KickoffAt != nil {
			g.Add(sc.at(query.Lte("kickoff_at", *p.KickoffAt)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Lte("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Lte("updated_at", p.UpdatedAt)))
		}
	}
	if p := f.Contains; p != nil {
		if len(p.ID) > 0 {
			g.Add(sc.at(query.In("id", p.ID)))
		}
		if len(p.HomeTeamID) > 0 {
			g.Add(sc.at(query.In("home_team_id", p.HomeTeamID)))
		}
		if len(p.AwayTeamID) > 0 {
			g.Add(sc.at(query.In("away_team_id", p.AwayTeamID)))
		}
		if len(p.KickoffAt) > 0 {
			g.Add(sc.at(query.In("kickoff_at", p.KickoffAt)))
		}
		if len(p.CreatedAt) > 0 {
			g.Add(sc.at(query.In("created_at", p.CreatedAt)))
		}
		if len(p.CreatedByAccountID) > 0 {
			g.Add(sc.at(query.In("created_by_account_id", p.CreatedByAccountID)))
		}
		if len(p.UpdatedAt) > 0 {
			g.Add(sc.at(query.In("updated_at", p.UpdatedAt)))
		}
		if len(p.UpdatedByAccountID) > 0 {
			g.Add(sc.at(query.In("updated_by_account_id", p.UpdatedByAccountID)))
		}
	}
	if p := f.NotContains; p != nil {
		if len(p.ID) > 0 {
			g.Add(sc.at(query.NotIn("id", p.ID)))
		}
		if len(p.HomeTeamID) > 0 {
			g.Add(sc.at(query.NotIn("home_team_id", p.HomeTeamID)))
		}
		if len(p.AwayTeamID) > 0 {
			g.Add(sc.at(query.NotIn("away_team_id", p.AwayTeamID)))
		}
		if len(p.KickoffAt) > 0 {
			g.Add(sc.at(query.NotIn("kickoff_at", p.KickoffAt)))
		}
		if len(p.CreatedAt) > 0 {
			g.Add(sc.at(query.NotIn("created_at", p.CreatedAt)))
		}
		if len(p.CreatedByAccountID) > 0 {
			g.Add(sc.at(query.NotIn("created_by_account_id", p.CreatedByAccountID)))
		}
		if len(p.UpdatedAt) > 0 {
			g.Add(sc.at(query.NotIn("updated_at", p.UpdatedAt)))
		}
		if len(p.UpdatedByAccountID) > 0 {
			g.Add(sc.at(query.NotIn("updated_by_account_id", p.UpdatedByAccountID)))
		}
	}
	if p := f.Null; p != nil {
		if p.CreatedByAccountID != nil {
			if *p.CreatedByAccountID {
				g.Add(sc.at(query.IsNull("created_by_account_id")))
			} else {
				g.Add(sc.at(query.NotNull("created_by_account_id")))
			}
		}
		if p.UpdatedAt != nil {
			if *p.UpdatedAt {
				g.Add(sc.at(query.IsNull("updated_at")))
			} else {
				g.Add(sc.at(query.NotNull("updated_at")))
			}
		}
		if p.UpdatedByAccountID != nil {
			if *p.UpdatedByAccountID {
				g.Add(sc.at(query.IsNull("updated_by_account_id")))
			} else {
				g.Add(sc.at(query.NotNull("updated_by_account_id")))
			}
		}
	}
	if p := f.NotNull; p != nil {
		if p.CreatedByAccountID != nil {
			if *p.CreatedByAccountID {
				g.Add(sc.at(query.NotNull("created_by_account_id")))
			} else {
				g.Add(sc.at(query.IsNull("created_by_account_id")))
			}
		}
		if p.UpdatedAt != nil {
			if *p.UpdatedAt {
				g.Add(sc.at(query.NotNull("updated_at")))
			} else {
				g.Add(sc.at(query.IsNull("updated_at")))
			}
		}
		if p.UpdatedByAccountID != nil {
			if *p.UpdatedByAccountID {
				g.Add(sc.at(query.NotNull("updated_by_account_id")))
			} else {
				g.Add(sc.at(query.IsNull("updated_by_account_id")))
			}
		}
	}

	if sub, ok := fixtureAwayTeamFilter(f); ok {
		inner, from, on := sc.belongsTo("team", "id", "away_team_id")
		where, err := teamGroup(sub, inner)
		if err != nil {
			return query.Group{}, err
		}

		// The far side is scoped whatever the read asked for. A read option widens
		// what this query returns, not what it may look through to decide.
		where.Add(inner.tenant("tenant_id"))
		where.Add(inner.live("deleted_at"))

		g.Add(query.Related(query.Exists{From: from, On: on, Where: where}))
	}

	if sub, ok := fixtureHomeTeamFilter(f); ok {
		inner, from, on := sc.belongsTo("team", "id", "home_team_id")
		where, err := teamGroup(sub, inner)
		if err != nil {
			return query.Group{}, err
		}

		// The far side is scoped whatever the read asked for. A read option widens
		// what this query returns, not what it may look through to decide.
		where.Add(inner.tenant("tenant_id"))
		where.Add(inner.live("deleted_at"))

		g.Add(query.Related(query.Exists{From: from, On: on, Where: where}))
	}

	if p := f.Without; p != nil && p.AwayTeam != nil {
		inner, from, on := sc.belongsTo("team", "id", "away_team_id")
		where, err := teamGroup(*p.AwayTeam, inner)
		if err != nil {
			return query.Group{}, err
		}

		// The far side is scoped whatever the read asked for. A read option widens
		// what this query returns, not what it may look through to decide.
		where.Add(inner.tenant("tenant_id"))
		where.Add(inner.live("deleted_at"))

		g.Add(query.Related(query.Exists{From: from, On: on, Where: where, Not: true}))
	}

	if p := f.Without; p != nil && p.HomeTeam != nil {
		inner, from, on := sc.belongsTo("team", "id", "home_team_id")
		where, err := teamGroup(*p.HomeTeam, inner)
		if err != nil {
			return query.Group{}, err
		}

		// The far side is scoped whatever the read asked for. A read option widens
		// what this query returns, not what it may look through to decide.
		where.Add(inner.tenant("tenant_id"))
		where.Add(inner.live("deleted_at"))

		g.Add(query.Related(query.Exists{From: from, On: on, Where: where, Not: true}))
	}

	for _, n := range f.NestedFilters {
		nested, err := fixtureGroup(n, sc)
		if err != nil {
			return query.Group{}, err
		}
		g.Nest(nested)
	}
	return g, nil
}

// fixtureSortable reports whether a column can be ordered by.
func fixtureSortable(column string) bool {
	switch column {
	case "id", "tenant_id", "home_team_id", "away_team_id", "kickoff_at", "created_at", "created_by_account_id", "updated_at", "updated_by_account_id":
		return true
	}
	return false
}

// fixtureOrderColumn reports whether an ordering names a column of a relation
// that can be ordered by.
func fixtureOrderColumn(relation, column string) error {
	switch relation {
	case "AwayTeam":
		if !teamSortable(column) {
			return rigerr.Invalid("a Fixture has no AwayTeam that can be ordered by %q", column)
		}
		return nil
	case "HomeTeam":
		if !teamSortable(column) {
			return rigerr.Invalid("a Fixture has no HomeTeam that can be ordered by %q", column)
		}
		return nil
	}
	return rigerr.Invalid("a Fixture has no relation named %q to order by", relation)
}

// fixtureOrderJoin builds the left join an ordering across a relation needs.
//
// The scope predicates go into the join's own condition rather than the
// statement's WHERE. In WHERE they would be false for a row that matched
// nothing and would discard it, which is an inner join wearing a left join's
// clothes.
func fixtureOrderJoin(relation, column, alias string, sc filterScope) (query.Join, error) {
	if err := fixtureOrderColumn(relation, column); err != nil {
		return query.Join{}, err
	}

	switch relation {
	case "AwayTeam":
		far, j := sc.orderJoin("team", alias, "id", "away_team_id")
		j.Where.Add(far.tenant("tenant_id"))
		j.Where.Add(far.live("deleted_at"))
		return j, nil
	case "HomeTeam":
		far, j := sc.orderJoin("team", alias, "id", "home_team_id")
		j.Where.Add(far.tenant("tenant_id"))
		j.Where.Add(far.live("deleted_at"))
		return j, nil
	}
	return query.Join{}, rigerr.Invalid("a Fixture has no relation named %q to order by", relation)
}

// fixtureOrder converts the model's ordering terms into the runtime's.
func fixtureOrder(terms []model.FixtureOrder, sc filterScope) ([]query.Order, []query.Join, error) {
	if len(terms) == 0 {
		return nil, nil, nil
	}

	out := make([]query.Order, 0, len(terms))
	var joins []query.Join
	// One join per relation however many of its columns are named.
	aliases := make(map[string]string)

	for _, t := range terms {
		if t.Relation != "" {
			alias, ok := aliases[t.Relation]
			if !ok {
				alias = strconv.Itoa(len(joins) + 1)
				alias = "o" + alias
				join, err := fixtureOrderJoin(t.Relation, t.Column, alias, sc)
				if err != nil {
					return nil, nil, err
				}
				joins = append(joins, join)
				aliases[t.Relation] = alias
			} else if err := fixtureOrderColumn(t.Relation, t.Column); err != nil {
				return nil, nil, err
			}
			out = append(out, query.Order{Table: alias, Column: t.Column, Desc: t.Desc})
			continue
		}

		// A column this table cannot be ordered by is the caller's mistake, not a
		// column name to paste into a statement.
		if !fixtureSortable(t.Column) {
			return nil, nil, rigerr.Invalid("a Fixture cannot be ordered by %q", t.Column)
		}
		out = append(out, query.Order{Table: sc.as, Column: t.Column, Desc: t.Desc})
	}

	return out, joins, nil
}

type fixtureRepo struct {
	db *Store
}

var _ FixtureRepository = (*fixtureRepo)(nil)

// trace runs one stage of a write inside a span of its own.
//
// The stage is a callback rather than something bracketed by two calls,
// because that is what makes the span a function's: it is opened and ended in
// one place, and nothing at the call site is holding one.
func (r *fixtureRepo) trace(ctx context.Context, name string, f func(context.Context) error) error {
	return observe.Trace(ctx, r.db.tracer, name, f)
}

const fixtureRepoSelect = "fixture.id, fixture.tenant_id, fixture.home_team_id, fixture.away_team_id, fixture.kickoff_at, fixture.created_at, fixture.created_by_account_id, fixture.updated_at, fixture.updated_by_account_id"

// scanFixture reads one row in the order fixtureRepoSelect lists.
func scanFixture(row pgx.Row) (*model.Fixture, error) {
	var m model.Fixture
	if err := row.Scan(&m.ID, &m.TenantID, &m.HomeTeamID, &m.AwayTeamID, &m.KickoffAt, &m.CreatedAt, &m.CreatedByAccountID, &m.UpdatedAt, &m.UpdatedByAccountID); err != nil {
		return nil, err
	}
	m.KickoffAt = dbx.UTC(m.KickoffAt)
	m.CreatedAt = dbx.UTC(m.CreatedAt)
	m.UpdatedAt = dbx.UTCPtr(m.UpdatedAt)
	return &m, nil
}

// Get implements FixtureRepository.
func (r *fixtureRepo) Get(ctx context.Context, id uuid.UUID, opts ...readopt.Option) (*model.Fixture, error) {
	ctx, span := r.db.tracer.Start(ctx, "repository.Fixture.Get")
	defer span.End()

	cfg, err := readopt.Apply(opts)
	if err != nil {
		return nil, err
	}

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}

	args := []any{id}
	where := "id = $1"
	if !cfg.SkipTenantScope {
		args = append(args, claims.TenantID)
		where += " AND tenant_id = $2"
	}

	sql := fmt.Sprintf("SELECT %s FROM fixture WHERE %s", fixtureRepoSelect, where)
	m, err := scanFixture(r.db.connFor(ctx).QueryRow(ctx, sql, args...))
	if dbx.IsNoRows(err) {
		return nil, rigerr.NotFound("no Fixture with id %s", id)
	}
	if err != nil {
		return nil, rigerr.Internal(err, "read fixture")
	}
	return m, nil
}

// List implements FixtureRepository.
func (r *fixtureRepo) List(ctx context.Context, f model.FixtureFilter, page model.FixturePage, opts ...readopt.Option) ([]*model.Fixture, int64, error) {
	ctx, span := r.db.tracer.Start(ctx, "repository.Fixture.List")
	defer span.End()

	return r.list(ctx, f, page, opts)
}

// list is the body of every read that takes a filter.
func (r *fixtureRepo) list(ctx context.Context, f model.FixtureFilter, page model.FixturePage, opts []readopt.Option) ([]*model.Fixture, int64, error) {
	cfg, err := readopt.Apply(opts)
	if err != nil {
		return nil, 0, err
	}

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, 0, err
	}

	// One scope for the whole statement: it says who is asking, and it qualifies
	// every column with the table it belongs to. The qualification is not
	// decoration — an ordering that reaches a related table brings a second
	// tenant_id into scope, and an unqualified one is then ambiguous.
	sc := newFilterScope(claims, "fixture")

	scope := query.Group{}
	if !cfg.SkipTenantScope {
		scope.Add(sc.at(query.Eq("tenant_id", claims.TenantID)))
	}

	group, err := fixtureGroup(f, sc)
	if err != nil {
		return nil, 0, err
	}
	scope.Nest(group)

	// The count has its own arguments and no joins. A left join cannot change how
	// many rows there are, and a statement must be given exactly the parameters
	// its text refers to.
	countArgs := query.NewArgs()
	countWhere := scope.SQL(countArgs)
	if countWhere != "" {
		countWhere = " WHERE " + countWhere
	}
	countSQL := fmt.Sprintf("SELECT count(*) FROM fixture%s", countWhere)
	var total int64
	if err := r.db.connFor(ctx).QueryRow(ctx, countSQL, countArgs.Values()...).Scan(&total); err != nil {
		return nil, 0, rigerr.Internal(err, "count fixture")
	}

	order, joins, err := fixtureOrder(page.OrderBy, sc)
	if err != nil {
		return nil, 0, err
	}
	if len(order) == 0 {
		order = FixtureDefaultOrder
	}
	window := query.Page{Limit: page.Limit, Offset: page.Offset}.Clamp(DefaultLimit, MaxLimit)

	// The joins render first, because Postgres numbers placeholders in the order
	// the statement mentions them and a join stands before the conditions do.
	args := query.NewArgs()
	joinSQL := query.JoinSQL(joins, args)
	where := scope.SQL(args)
	if where != "" {
		where = " WHERE " + where
	}

	listSQL := fmt.Sprintf("SELECT %s FROM fixture%s%s%s%s", fixtureRepoSelect, joinSQL, where, query.OrderSQL(order), window.SQL(args))
	rows, err := r.db.connFor(ctx).Query(ctx, listSQL, args.Values()...)
	if err != nil {
		return nil, 0, rigerr.Internal(err, "list fixture")
	}
	defer rows.Close()

	out := make([]*model.Fixture, 0, window.Limit)
	for rows.Next() {
		m, err := scanFixture(rows)
		if err != nil {
			return nil, 0, rigerr.Internal(err, "read fixture")
		}
		out = append(out, m)
	}
	if err := rows.Err(); err != nil {
		return nil, 0, rigerr.Internal(err, "list fixture")
	}
	return out, total, nil
}

// FixtureDefaultOrder is the ordering used when a query asks for none.
//
// It always ends with the primary key, so the order is total and a page
// boundary cannot repeat or skip a row.
var FixtureDefaultOrder = []query.Order{{Table: "fixture", Column: "created_at", Desc: true}, {Table: "fixture", Column: "id", Desc: false}}

// Create implements FixtureRepository.
func (r *fixtureRepo) Create(ctx context.Context, in dbhook.Create[model.FixtureCreateInput, model.Fixture]) (*model.Fixture, error) {
	ctx, span := r.db.tracer.Start(ctx, "repository.Fixture.Create")
	defer span.End()

	// Who is asking, first of all. A write from a request carrying no identity is
	// refused here — before a rule runs, before a column notices — which is
	// what lets every hook below take the claims as a value rather than something
	// to look up and check.
	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}
	_ = claims

	// Normalize then validate, before anything is written. A service that forgets
	// to call these cannot write a row that would fail them, which is the only way
	// a rule the schema declares is actually a rule.
	in.Input.Normalize()
	if err := in.Input.Validate(); err != nil {
		return nil, err
	}

	// Then the rules the schema cannot express, if the service wrote any. They run
	// before the transaction opens: a rule that reaches another service should not
	// be holding a row lock while it waits.
	//
	// They run before the create hook rather than after it, which is the one place
	// the order goes this way: an update validates inside the transaction that
	// read the row, so its hook can run first and have what it sets judged. Here
	// there is no row to read and no transaction yet, and opening one to hold
	// across a rule that may call out to another service would be a worse trade
	// than the one it buys.
	if in.Hooks.Validator != nil {
		if err := r.trace(ctx, "repository.Fixture.Create.Validator", func(ctx context.Context) error {
			return in.Hooks.Validator.RunCreate(ctx, claims, &in.Input)
		}); err != nil {
			return nil, err
		}
	}

	// A version 7 identifier sorts by creation time, so an index on the primary
	// key stays dense as rows are added.
	id, err := uuid.NewV7()
	if err != nil {
		return nil, rigerr.Internal(err, "generate an identifier")
	}
	now := time.Now().UTC()
	_ = now

	// A single INSERT is already atomic, so the transaction is opened only when a
	// hook needs to be able to undo it — or, here, when a reference has to be
	// checked first. A check on one connection and an insert on another leaves a
	// window where the row it approved is deleted before the row that points at it
	// lands.
	needsTx := true

	var m *model.Fixture
	err = dbx.InTxIf(ctx, r.db.pool, r.db.connFor(ctx), needsTx, func(ctx context.Context, tx dbx.Conn) error {
		if in.Hooks.Before != nil {
			if err := r.trace(ctx, "repository.Fixture.Create.Before", func(ctx context.Context) error {
				return in.Hooks.Before(ctx, claims, &in.Input)
			}); err != nil {
				return err
			}
		}

		// Every identifier this row would store has to name a row the caller could
		// have read. One indexed lookup per key, inside the transaction that is about
		// to do the write.
		{
			ok, err := visibleTeam(ctx, tx, claims, in.Input.AwayTeamID)
			if err != nil {
				return err
			}
			if !ok {
				return &model.FixtureCreateInputError{AwayTeamID: rigerr.NewFieldError(rigerr.FieldCodeNotFound, "no Team with id %s", in.Input.AwayTeamID)}
			}
		}

		{
			ok, err := visibleTeam(ctx, tx, claims, in.Input.HomeTeamID)
			if err != nil {
				return err
			}
			if !ok {
				return &model.FixtureCreateInputError{HomeTeamID: rigerr.NewFieldError(rigerr.FieldCodeNotFound, "no Team with id %s", in.Input.HomeTeamID)}
			}
		}

		columns := []string{"id", "tenant_id", "created_at", "created_by_account_id", "home_team_id", "away_team_id", "kickoff_at"}
		values := []any{id, claims.TenantID, now, claims.Actor(), in.Input.HomeTeamID, in.Input.AwayTeamID, in.Input.KickoffAt}

		sql := fmt.Sprintf("INSERT INTO fixture (%s) VALUES (%s) RETURNING %s", joinColumns(columns), placeholders(len(values)), fixtureRepoSelect)

		created, err := scanFixture(tx.QueryRow(ctx, sql, values...))
		if err != nil {
			return writeError(err, "fixture")
		}
		m = created

		if in.Hooks.After != nil {
			if err := r.trace(ctx, "repository.Fixture.Create.After", func(ctx context.Context) error {
				return in.Hooks.After(ctx, claims, m)
			}); err != nil {
				return err
			}
		}
		return nil
	})
	if err != nil {
		return nil, err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() {
			ctx, span := r.db.tracer.Start(ctx, "repository.Fixture.Create.AfterCommit")
			defer span.End()

			done(ctx, who, m)
		})
	}

	return m, nil
}

// Update implements FixtureRepository.
func (r *fixtureRepo) Update(ctx context.Context, id uuid.UUID, in dbhook.Update[model.FixtureUpdateInput, model.Fixture]) (*model.Fixture, error) {
	ctx, span := r.db.tracer.Start(ctx, "repository.Fixture.Update")
	defer span.End()

	in.Input.Normalize()

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}
	_ = claims

	var updated, prev *model.Fixture
	err = dbx.InTx(ctx, r.db.pool, func(ctx context.Context, tx dbx.Conn) error {
		prev, err = r.Get(ctx, id)
		if err != nil {
			return err
		}

		// The hook shapes the input, so a field it sets is a field that gets written
		// — and one the rules below then judge. It runs first for exactly that
		// reason: a hook that ran after validation could write a value nothing had
		// checked.
		if in.Hooks.Before != nil {
			if err := r.trace(ctx, "repository.Fixture.Update.Before", func(ctx context.Context) error {
				return in.Hooks.Before(ctx, claims, &in.Input, prev)
			}); err != nil {
				return err
			}
		}

		// Validation runs against the row this update would produce, not against the
		// request — a rule about two fields needs both, and only one of them may
		// have been sent. It happens inside the transaction that read prev, so nothing
		// can change underneath it.
		if err := in.Input.Validate(prev); err != nil {
			return err
		}

		if in.Hooks.Validator != nil {
			if err := r.trace(ctx, "repository.Fixture.Update.Validator", func(ctx context.Context) error {
				return in.Hooks.Validator.RunUpdate(ctx, claims, &in.Input, prev)
			}); err != nil {
				return err
			}
		}

		// Every identifier this update would store has to name a row the caller could
		// have read — the ones it actually sends, and no others.
		if v, sent := in.Input.AwayTeamID.Get(); sent {
			ok, err := visibleTeam(ctx, tx, claims, v)
			if err != nil {
				return err
			}
			if !ok {
				return &model.FixtureUpdateInputError{AwayTeamID: rigerr.NewFieldError(rigerr.FieldCodeNotFound, "no Team with id %s", v)}
			}
		}

		if v, sent := in.Input.HomeTeamID.Get(); sent {
			ok, err := visibleTeam(ctx, tx, claims, v)
			if err != nil {
				return err
			}
			if !ok {
				return &model.FixtureUpdateInputError{HomeTeamID: rigerr.NewFieldError(rigerr.FieldCodeNotFound, "no Team with id %s", v)}
			}
		}

		columns := []string{}
		values := []any{}

		if v, ok := in.Input.HomeTeamID.Get(); ok {
			columns = append(columns, "home_team_id")
			values = append(values, v)
		}
		if v, ok := in.Input.AwayTeamID.Get(); ok {
			columns = append(columns, "away_team_id")
			values = append(values, v)
		}
		if v, ok := in.Input.KickoffAt.Get(); ok {
			columns = append(columns, "kickoff_at")
			values = append(values, v)
		}

		if len(columns) == 0 {
			// Nothing was asked for, so nothing happens — no stamp, and no snapshot
			// either. An update that changes no column did not happen, and a history full
			// of copies of an unchanged row is a history nobody can read.
			updated = prev
			return nil
		}

		columns = append(columns, "updated_at")
		values = append(values, time.Now().UTC())
		columns = append(columns, "updated_by_account_id")
		values = append(values, claims.Actor())

		values = append(values, id)
		sql := fmt.Sprintf("UPDATE fixture SET %s WHERE id = $%d RETURNING %s", assignments(columns), len(values), fixtureRepoSelect)

		updated, err = scanFixture(tx.QueryRow(ctx, sql, values...))
		if err != nil {
			return writeError(err, "fixture")
		}

		if in.Hooks.After != nil {
			if err := r.trace(ctx, "repository.Fixture.Update.After", func(ctx context.Context) error {
				return in.Hooks.After(ctx, claims, updated, prev)
			}); err != nil {
				return err
			}
		}
		return nil
	})
	if err != nil {
		return nil, err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() {
			ctx, span := r.db.tracer.Start(ctx, "repository.Fixture.Update.AfterCommit")
			defer span.End()

			done(ctx, who, updated, prev)
		})
	}

	return updated, nil
}

// Delete implements FixtureRepository.
func (r *fixtureRepo) Delete(ctx context.Context, in dbhook.Delete[model.FixtureDeleteInput, model.Fixture]) error {
	ctx, span := r.db.tracer.Start(ctx, "repository.Fixture.Delete")
	defer span.End()

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return err
	}
	_ = claims

	var prev *model.Fixture
	err = dbx.InTx(ctx, r.db.pool, func(ctx context.Context, tx dbx.Conn) error {
		prev, err = r.Get(ctx, in.Input.ID)
		if err != nil {
			return err
		}

		if in.Hooks.Before != nil {
			if err := r.trace(ctx, "repository.Fixture.Delete.Before", func(ctx context.Context) error {
				return in.Hooks.Before(ctx, claims, &in.Input, prev)
			}); err != nil {
				return err
			}
		}

		if _, err := tx.Exec(ctx, "DELETE FROM fixture WHERE id = $1", in.Input.ID); err != nil {
			return writeError(err, "fixture")
		}
		if in.Hooks.After != nil {
			if err := r.trace(ctx, "repository.Fixture.Delete.After", func(ctx context.Context) error {
				return in.Hooks.After(ctx, claims, prev)
			}); err != nil {
				return err
			}
		}
		return nil
	})
	if err != nil {
		return err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() {
			ctx, span := r.db.tracer.Start(ctx, "repository.Fixture.Delete.AfterCommit")
			defer span.End()

			done(ctx, who, prev)
		})
	}

	return nil
}

var _ = time.Now
var _ = fmt.Sprintf
var _ = rigerr.NotFound
