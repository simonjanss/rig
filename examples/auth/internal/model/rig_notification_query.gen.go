// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"encoding/json"
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on RigNotification fields.
type RigNotificationFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *uuid.UUID `json:"createdByAccountId,omitempty"`
	// API key this row was created through, when it was an integration rather than
	// a person.
	CreatedByAPIKeyID *uuid.UUID `json:"createdByApiKeyId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// What happened, as the application names it. Narrow it to an enum of your own
	// to get a switch the compiler can see.
	Kind *string `json:"kind,omitempty"`
	// Resolved means the audience was determined and the inbox lines exist. It
	// does not mean anything was sent.
	State *RigNotificationState `json:"state,omitempty"`
	// When this is due. now() is the ordinary case; a scheduled notification is
	// the same row with a later time, which is the only difference between the
	// two.
	DeliverAt *time.Time `json:"deliverAt,omitempty"`
	// When the audience was computed. Null while the notification is still pending
	// or was cancelled.
	ResolvedAt *time.Time `json:"resolvedAt,omitempty"`
	// What a template needs beyond the linked row. Give it a Go type with the
	// go_type key if it has a shape.
	Payload *json.RawMessage `json:"payload,omitempty"`
	// What collapses several of these into one inbox line, decided when the
	// announcement was written and copied onto the line when the audience is
	// resolved.
	GroupKey *string `json:"groupKey,omitempty"`
	// A recipient list captured at write time, and the one exception to computing
	// the audience late. Null is the ordinary case; a list here skips the question
	// entirely, for an audience that genuinely cannot be re-derived.
	AccountIds []uuid.UUID `json:"accountIds,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterEquals `json:"notes,omitempty"`
}

// Ordering conditions on RigNotification fields that can be compared.
type RigNotificationFilterRange struct {
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// When this is due. now() is the ordinary case; a scheduled notification is
	// the same row with a later time, which is the only difference between the
	// two.
	DeliverAt *time.Time `json:"deliverAt,omitempty"`
	// When the audience was computed. Null while the notification is still pending
	// or was cancelled.
	ResolvedAt *time.Time `json:"resolvedAt,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterRange `json:"notes,omitempty"`
}

// Set-membership conditions on RigNotification fields.
type RigNotificationFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID []uuid.UUID `json:"createdByAccountId,omitempty"`
	// API key this row was created through, when it was an integration rather than
	// a person.
	CreatedByAPIKeyID []uuid.UUID `json:"createdByApiKeyId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// What happened, as the application names it. Narrow it to an enum of your own
	// to get a switch the compiler can see.
	Kind []string `json:"kind,omitempty"`
	// Resolved means the audience was determined and the inbox lines exist. It
	// does not mean anything was sent.
	State []RigNotificationState `json:"state,omitempty"`
	// When this is due. now() is the ordinary case; a scheduled notification is
	// the same row with a later time, which is the only difference between the
	// two.
	DeliverAt []time.Time `json:"deliverAt,omitempty"`
	// When the audience was computed. Null while the notification is still pending
	// or was cancelled.
	ResolvedAt []time.Time `json:"resolvedAt,omitempty"`
	// What a template needs beyond the linked row. Give it a Go type with the
	// go_type key if it has a shape.
	Payload []json.RawMessage `json:"payload,omitempty"`
	// What collapses several of these into one inbox line, decided when the
	// announcement was written and copied onto the line when the audience is
	// resolved.
	GroupKey []string `json:"groupKey,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterContains `json:"notes,omitempty"`
}

// Pattern conditions on RigNotification text fields.
type RigNotificationFilterLike struct {
	// What happened, as the application names it. Narrow it to an enum of your own
	// to get a switch the compiler can see.
	Kind *string `json:"kind,omitempty"`
	// What collapses several of these into one inbox line, decided when the
	// announcement was written and copied onto the line when the audience is
	// resolved.
	GroupKey *string `json:"groupKey,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterLike `json:"notes,omitempty"`
}

// Presence conditions on nullable RigNotification fields.
type RigNotificationFilterNull struct {
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *bool `json:"createdByAccountId,omitempty"`
	// API key this row was created through, when it was an integration rather than
	// a person.
	CreatedByAPIKeyID *bool `json:"createdByApiKeyId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// When the audience was computed. Null while the notification is still pending
	// or was cancelled.
	ResolvedAt *bool `json:"resolvedAt,omitempty"`
	// What collapses several of these into one inbox line, decided when the
	// announcement was written and copied onto the line when the audience is
	// resolved.
	GroupKey *bool `json:"groupKey,omitempty"`
	// A recipient list captured at write time, and the one exception to computing
	// the audience late. Null is the ordinary case; a list here skips the question
	// entirely, for an audience that genuinely cannot be re-derived.
	AccountIds *bool `json:"accountIds,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterNull `json:"notes,omitempty"`
}

// Relations a RigNotification must have no matching row for.
type RigNotificationFilterWithout struct {
	// Match rows with no Notes satisfying these conditions, including rows with
	// none at all.
	//
	// This negates the existence of a matching row rather than the comparison,
	// which is the whole difference from NotEquals.Notes: that one is satisfied by
	// one of them differing, and a row can have one that differs and one that
	// matches at the same time. An empty object asks for rows with no Notes at
	// all.
	Notes *NoteFilter `json:"notes,omitempty"`
}

// Conditions a RigNotification must satisfy to match a search.
type RigNotificationFilter struct {
	// Fields that must equal the given value.
	Equals *RigNotificationFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *RigNotificationFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *RigNotificationFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *RigNotificationFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *RigNotificationFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *RigNotificationFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *RigNotificationFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *RigNotificationFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *RigNotificationFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *RigNotificationFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *RigNotificationFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *RigNotificationFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []RigNotificationFilter `json:"nestedFilters,omitempty"`
	// Relations with no row matching the given conditions. An empty one asks for
	// no related row at all.
	//
	// This is where a negation belongs when what you mean is "not": it negates
	// whether a matching related row exists, so a row with no related row at all
	// satisfies it. The negative operators above negate the comparison instead and
	// still require the related row to be there — "no related row called X" is
	// here, "a related row not called X" is NotEquals.
	Without *RigNotificationFilterWithout `json:"without,omitempty"`
}

// RigNotificationPage says which slice of a result to return, and in what
// order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type RigNotificationPage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []RigNotificationOrder
}

// RigNotificationOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type RigNotificationOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a RigNotification supports.
var (
	RigNotificationOrderIDAsc                  = RigNotificationOrder{Column: "id"}
	RigNotificationOrderIDDesc                 = RigNotificationOrder{Column: "id", Desc: true}
	RigNotificationOrderTenantIDAsc            = RigNotificationOrder{Column: "tenant_id"}
	RigNotificationOrderTenantIDDesc           = RigNotificationOrder{Column: "tenant_id", Desc: true}
	RigNotificationOrderCreatedAtAsc           = RigNotificationOrder{Column: "created_at"}
	RigNotificationOrderCreatedAtDesc          = RigNotificationOrder{Column: "created_at", Desc: true}
	RigNotificationOrderCreatedByAccountIDAsc  = RigNotificationOrder{Column: "created_by_account_id"}
	RigNotificationOrderCreatedByAccountIDDesc = RigNotificationOrder{Column: "created_by_account_id", Desc: true}
	RigNotificationOrderCreatedByAPIKeyIDAsc   = RigNotificationOrder{Column: "created_by_api_key_id"}
	RigNotificationOrderCreatedByAPIKeyIDDesc  = RigNotificationOrder{Column: "created_by_api_key_id", Desc: true}
	RigNotificationOrderUpdatedAtAsc           = RigNotificationOrder{Column: "updated_at"}
	RigNotificationOrderUpdatedAtDesc          = RigNotificationOrder{Column: "updated_at", Desc: true}
	RigNotificationOrderKindAsc                = RigNotificationOrder{Column: "kind"}
	RigNotificationOrderKindDesc               = RigNotificationOrder{Column: "kind", Desc: true}
	RigNotificationOrderStateAsc               = RigNotificationOrder{Column: "state"}
	RigNotificationOrderStateDesc              = RigNotificationOrder{Column: "state", Desc: true}
	RigNotificationOrderDeliverAtAsc           = RigNotificationOrder{Column: "deliver_at"}
	RigNotificationOrderDeliverAtDesc          = RigNotificationOrder{Column: "deliver_at", Desc: true}
	RigNotificationOrderResolvedAtAsc          = RigNotificationOrder{Column: "resolved_at"}
	RigNotificationOrderResolvedAtDesc         = RigNotificationOrder{Column: "resolved_at", Desc: true}
	RigNotificationOrderPayloadAsc             = RigNotificationOrder{Column: "payload"}
	RigNotificationOrderPayloadDesc            = RigNotificationOrder{Column: "payload", Desc: true}
	RigNotificationOrderGroupKeyAsc            = RigNotificationOrder{Column: "group_key"}
	RigNotificationOrderGroupKeyDesc           = RigNotificationOrder{Column: "group_key", Desc: true}
)

// NewRigNotificationFilterEquals builds an empty RigNotificationFilterEquals
// to fill in.
func NewRigNotificationFilterEquals() *RigNotificationFilterEquals {
	return &RigNotificationFilterEquals{}
}

// NewRigNotificationFilterRange builds an empty RigNotificationFilterRange to
// fill in.
func NewRigNotificationFilterRange() *RigNotificationFilterRange {
	return &RigNotificationFilterRange{}
}

// NewRigNotificationFilterContains builds an empty
// RigNotificationFilterContains to fill in.
func NewRigNotificationFilterContains() *RigNotificationFilterContains {
	return &RigNotificationFilterContains{}
}

// NewRigNotificationFilterLike builds an empty RigNotificationFilterLike to
// fill in.
func NewRigNotificationFilterLike() *RigNotificationFilterLike { return &RigNotificationFilterLike{} }

// NewRigNotificationFilterNull builds an empty RigNotificationFilterNull to
// fill in.
func NewRigNotificationFilterNull() *RigNotificationFilterNull { return &RigNotificationFilterNull{} }

// NewRigNotificationFilterWithout builds an empty RigNotificationFilterWithout
// to fill in.
func NewRigNotificationFilterWithout() *RigNotificationFilterWithout {
	return &RigNotificationFilterWithout{}
}

// NewRigNotificationFilter builds an empty filter, which matches every row the
// caller is allowed to see.
func NewRigNotificationFilter() RigNotificationFilter { return RigNotificationFilter{} }
