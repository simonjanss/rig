// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"encoding/json"
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on Notification fields.
type NotificationFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *uuid.UUID `json:"createdByAccountId,omitempty"`
	// API key this row was created through, when it was an integration rather than
	// a person.
	CreatedByAPIKeyID *uuid.UUID `json:"createdByApiKeyId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// What happened, as the application names it. Narrow it to an enum of your own
	// to get a switch the compiler can see.
	Kind *string `json:"kind,omitempty"`
	// Resolved means the audience was determined and the inbox lines exist. It
	// does not mean anything was sent.
	State *NotificationState `json:"state,omitempty"`
	// When this is due. now() is the ordinary case; a scheduled notification is
	// the same row with a later time, which is the only difference between the
	// two.
	DeliverAt *time.Time `json:"deliverAt,omitempty"`
	// When the audience was computed. Null while the notification is still pending
	// or was cancelled.
	ResolvedAt *time.Time `json:"resolvedAt,omitempty"`
	// What a template needs beyond the linked row. Give it a Go type with the
	// go_type key if it has a shape.
	Payload *json.RawMessage `json:"payload,omitempty"`
	// What collapses several of these into one inbox line, decided when the
	// announcement was written and copied onto the line when the audience is
	// resolved.
	GroupKey *string `json:"groupKey,omitempty"`
	// A recipient list captured at write time, and the one exception to computing
	// the audience late. Null is the ordinary case; a list here skips the question
	// entirely, for an audience that genuinely cannot be re-derived.
	AccountIds []uuid.UUID `json:"accountIds,omitempty"`
	// When a dispatcher took this to resolve. Past notifications.claim_ttl another
	// may, which is what makes a crashed process recoverable.
	ClaimedAt *time.Time `json:"claimedAt,omitempty"`
	// Which process holds the lease, so a stuck one traces to a pod rather than to
	// a mystery.
	ClaimedBy *uuid.UUID `json:"claimedBy,omitempty"`
	// How many times resolving this has been attempted.
	Attempts *int `json:"attempts,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterEquals `json:"notes,omitempty"`
}

// Ordering conditions on Notification fields that can be compared.
type NotificationFilterRange struct {
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// When this is due. now() is the ordinary case; a scheduled notification is
	// the same row with a later time, which is the only difference between the
	// two.
	DeliverAt *time.Time `json:"deliverAt,omitempty"`
	// When the audience was computed. Null while the notification is still pending
	// or was cancelled.
	ResolvedAt *time.Time `json:"resolvedAt,omitempty"`
	// When a dispatcher took this to resolve. Past notifications.claim_ttl another
	// may, which is what makes a crashed process recoverable.
	ClaimedAt *time.Time `json:"claimedAt,omitempty"`
	// How many times resolving this has been attempted.
	Attempts *int `json:"attempts,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterRange `json:"notes,omitempty"`
}

// Set-membership conditions on Notification fields.
type NotificationFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID []uuid.UUID `json:"createdByAccountId,omitempty"`
	// API key this row was created through, when it was an integration rather than
	// a person.
	CreatedByAPIKeyID []uuid.UUID `json:"createdByApiKeyId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// What happened, as the application names it. Narrow it to an enum of your own
	// to get a switch the compiler can see.
	Kind []string `json:"kind,omitempty"`
	// Resolved means the audience was determined and the inbox lines exist. It
	// does not mean anything was sent.
	State []NotificationState `json:"state,omitempty"`
	// When this is due. now() is the ordinary case; a scheduled notification is
	// the same row with a later time, which is the only difference between the
	// two.
	DeliverAt []time.Time `json:"deliverAt,omitempty"`
	// When the audience was computed. Null while the notification is still pending
	// or was cancelled.
	ResolvedAt []time.Time `json:"resolvedAt,omitempty"`
	// What a template needs beyond the linked row. Give it a Go type with the
	// go_type key if it has a shape.
	Payload []json.RawMessage `json:"payload,omitempty"`
	// What collapses several of these into one inbox line, decided when the
	// announcement was written and copied onto the line when the audience is
	// resolved.
	GroupKey []string `json:"groupKey,omitempty"`
	// When a dispatcher took this to resolve. Past notifications.claim_ttl another
	// may, which is what makes a crashed process recoverable.
	ClaimedAt []time.Time `json:"claimedAt,omitempty"`
	// Which process holds the lease, so a stuck one traces to a pod rather than to
	// a mystery.
	ClaimedBy []uuid.UUID `json:"claimedBy,omitempty"`
	// How many times resolving this has been attempted.
	Attempts []int `json:"attempts,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterContains `json:"notes,omitempty"`
}

// Pattern conditions on Notification text fields.
type NotificationFilterLike struct {
	// What happened, as the application names it. Narrow it to an enum of your own
	// to get a switch the compiler can see.
	Kind *string `json:"kind,omitempty"`
	// What collapses several of these into one inbox line, decided when the
	// announcement was written and copied onto the line when the audience is
	// resolved.
	GroupKey *string `json:"groupKey,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterLike `json:"notes,omitempty"`
}

// Presence conditions on nullable Notification fields.
type NotificationFilterNull struct {
	// Account that created this row, taken from the request's claims.
	CreatedByAccountID *bool `json:"createdByAccountId,omitempty"`
	// API key this row was created through, when it was an integration rather than
	// a person.
	CreatedByAPIKeyID *bool `json:"createdByApiKeyId,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// When the audience was computed. Null while the notification is still pending
	// or was cancelled.
	ResolvedAt *bool `json:"resolvedAt,omitempty"`
	// What collapses several of these into one inbox line, decided when the
	// announcement was written and copied onto the line when the audience is
	// resolved.
	GroupKey *bool `json:"groupKey,omitempty"`
	// A recipient list captured at write time, and the one exception to computing
	// the audience late. Null is the ordinary case; a list here skips the question
	// entirely, for an audience that genuinely cannot be re-derived.
	AccountIds *bool `json:"accountIds,omitempty"`
	// When a dispatcher took this to resolve. Past notifications.claim_ttl another
	// may, which is what makes a crashed process recoverable.
	ClaimedAt *bool `json:"claimedAt,omitempty"`
	// Which process holds the lease, so a stuck one traces to a pod rather than to
	// a mystery.
	ClaimedBy *bool `json:"claimedBy,omitempty"`
	// Conditions on one of this row's Notes.
	//
	// Every condition has to hold of the same one, not of the set between them.
	//
	// Under the negative operators this asks whether one of them differs, which is
	// not the same as none of them matching: a row can have one that matches and
	// one that does not, satisfying the first and failing the second.
	// Without.Notes asks the second.
	Notes *NoteFilterNull `json:"notes,omitempty"`
}

// Relations a Notification must have no matching row for.
type NotificationFilterWithout struct {
	// Match rows with no Notes satisfying these conditions, including rows with
	// none at all.
	//
	// This negates the existence of a matching row rather than the comparison,
	// which is the whole difference from NotEquals.Notes: that one is satisfied by
	// one of them differing, and a row can have one that differs and one that
	// matches at the same time. An empty object asks for rows with no Notes at
	// all.
	Notes *NoteFilter `json:"notes,omitempty"`
}

// Conditions a Notification must satisfy to match a search.
type NotificationFilter struct {
	// Fields that must equal the given value.
	Equals *NotificationFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *NotificationFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *NotificationFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *NotificationFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *NotificationFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *NotificationFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *NotificationFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *NotificationFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *NotificationFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *NotificationFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *NotificationFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *NotificationFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []NotificationFilter `json:"nestedFilters,omitempty"`
	// Relations with no row matching the given conditions. An empty one asks for
	// no related row at all.
	//
	// This is where a negation belongs when what you mean is "not": it negates
	// whether a matching related row exists, so a row with no related row at all
	// satisfies it. The negative operators above negate the comparison instead and
	// still require the related row to be there — "no related row called X" is
	// here, "a related row not called X" is NotEquals.
	Without *NotificationFilterWithout `json:"without,omitempty"`
}

// NotificationPage says which slice of a result to return, and in what order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type NotificationPage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []NotificationOrder
}

// NotificationOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type NotificationOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a Notification supports.
var (
	NotificationOrderIDAsc                  = NotificationOrder{Column: "id"}
	NotificationOrderIDDesc                 = NotificationOrder{Column: "id", Desc: true}
	NotificationOrderTenantIDAsc            = NotificationOrder{Column: "tenant_id"}
	NotificationOrderTenantIDDesc           = NotificationOrder{Column: "tenant_id", Desc: true}
	NotificationOrderCreatedAtAsc           = NotificationOrder{Column: "created_at"}
	NotificationOrderCreatedAtDesc          = NotificationOrder{Column: "created_at", Desc: true}
	NotificationOrderCreatedByAccountIDAsc  = NotificationOrder{Column: "created_by_account_id"}
	NotificationOrderCreatedByAccountIDDesc = NotificationOrder{Column: "created_by_account_id", Desc: true}
	NotificationOrderCreatedByAPIKeyIDAsc   = NotificationOrder{Column: "created_by_api_key_id"}
	NotificationOrderCreatedByAPIKeyIDDesc  = NotificationOrder{Column: "created_by_api_key_id", Desc: true}
	NotificationOrderUpdatedAtAsc           = NotificationOrder{Column: "updated_at"}
	NotificationOrderUpdatedAtDesc          = NotificationOrder{Column: "updated_at", Desc: true}
	NotificationOrderKindAsc                = NotificationOrder{Column: "kind"}
	NotificationOrderKindDesc               = NotificationOrder{Column: "kind", Desc: true}
	NotificationOrderStateAsc               = NotificationOrder{Column: "state"}
	NotificationOrderStateDesc              = NotificationOrder{Column: "state", Desc: true}
	NotificationOrderDeliverAtAsc           = NotificationOrder{Column: "deliver_at"}
	NotificationOrderDeliverAtDesc          = NotificationOrder{Column: "deliver_at", Desc: true}
	NotificationOrderResolvedAtAsc          = NotificationOrder{Column: "resolved_at"}
	NotificationOrderResolvedAtDesc         = NotificationOrder{Column: "resolved_at", Desc: true}
	NotificationOrderPayloadAsc             = NotificationOrder{Column: "payload"}
	NotificationOrderPayloadDesc            = NotificationOrder{Column: "payload", Desc: true}
	NotificationOrderGroupKeyAsc            = NotificationOrder{Column: "group_key"}
	NotificationOrderGroupKeyDesc           = NotificationOrder{Column: "group_key", Desc: true}
	NotificationOrderClaimedAtAsc           = NotificationOrder{Column: "claimed_at"}
	NotificationOrderClaimedAtDesc          = NotificationOrder{Column: "claimed_at", Desc: true}
	NotificationOrderClaimedByAsc           = NotificationOrder{Column: "claimed_by"}
	NotificationOrderClaimedByDesc          = NotificationOrder{Column: "claimed_by", Desc: true}
	NotificationOrderAttemptsAsc            = NotificationOrder{Column: "attempts"}
	NotificationOrderAttemptsDesc           = NotificationOrder{Column: "attempts", Desc: true}
)

// NewNotificationFilterEquals builds an empty NotificationFilterEquals to fill
// in.
func NewNotificationFilterEquals() *NotificationFilterEquals { return &NotificationFilterEquals{} }

// NewNotificationFilterRange builds an empty NotificationFilterRange to fill
// in.
func NewNotificationFilterRange() *NotificationFilterRange { return &NotificationFilterRange{} }

// NewNotificationFilterContains builds an empty NotificationFilterContains to
// fill in.
func NewNotificationFilterContains() *NotificationFilterContains {
	return &NotificationFilterContains{}
}

// NewNotificationFilterLike builds an empty NotificationFilterLike to fill in.
func NewNotificationFilterLike() *NotificationFilterLike { return &NotificationFilterLike{} }

// NewNotificationFilterNull builds an empty NotificationFilterNull to fill in.
func NewNotificationFilterNull() *NotificationFilterNull { return &NotificationFilterNull{} }

// NewNotificationFilterWithout builds an empty NotificationFilterWithout to
// fill in.
func NewNotificationFilterWithout() *NotificationFilterWithout { return &NotificationFilterWithout{} }

// NewNotificationFilter builds an empty filter, which matches every row the
// caller is allowed to see.
func NewNotificationFilter() NotificationFilter { return NotificationFilter{} }
