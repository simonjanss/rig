// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on Permission fields.
type PermissionFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// What code checks for, such as lesson.publish.
	Key *string `json:"key,omitempty"`
	// A short label for an administration screen.
	Name *string `json:"name,omitempty"`
	// What holding this permission lets somebody do.
	Description *string `json:"description,omitempty"`
}

// Ordering conditions on Permission fields that can be compared.
type PermissionFilterRange struct {
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
}

// Set-membership conditions on Permission fields.
type PermissionFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// What code checks for, such as lesson.publish.
	Key []string `json:"key,omitempty"`
	// A short label for an administration screen.
	Name []string `json:"name,omitempty"`
	// What holding this permission lets somebody do.
	Description []string `json:"description,omitempty"`
}

// Pattern conditions on Permission text fields.
type PermissionFilterLike struct {
	// What code checks for, such as lesson.publish.
	Key *string `json:"key,omitempty"`
	// A short label for an administration screen.
	Name *string `json:"name,omitempty"`
	// What holding this permission lets somebody do.
	Description *string `json:"description,omitempty"`
}

// Presence conditions on nullable Permission fields.
type PermissionFilterNull struct {
}

// Conditions a Permission must satisfy to match a search.
type PermissionFilter struct {
	// Fields that must equal the given value.
	Equals *PermissionFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *PermissionFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *PermissionFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *PermissionFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *PermissionFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *PermissionFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *PermissionFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *PermissionFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *PermissionFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *PermissionFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *PermissionFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *PermissionFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []PermissionFilter `json:"nestedFilters,omitempty"`
}

// PermissionPage says which slice of a result to return, and in what order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type PermissionPage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []PermissionOrder
}

// PermissionOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type PermissionOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a Permission supports.
var (
	PermissionOrderIDAsc           = PermissionOrder{Column: "id"}
	PermissionOrderIDDesc          = PermissionOrder{Column: "id", Desc: true}
	PermissionOrderCreatedAtAsc    = PermissionOrder{Column: "created_at"}
	PermissionOrderCreatedAtDesc   = PermissionOrder{Column: "created_at", Desc: true}
	PermissionOrderKeyAsc          = PermissionOrder{Column: "key"}
	PermissionOrderKeyDesc         = PermissionOrder{Column: "key", Desc: true}
	PermissionOrderNameAsc         = PermissionOrder{Column: "name"}
	PermissionOrderNameDesc        = PermissionOrder{Column: "name", Desc: true}
	PermissionOrderDescriptionAsc  = PermissionOrder{Column: "description"}
	PermissionOrderDescriptionDesc = PermissionOrder{Column: "description", Desc: true}
)

// NewPermissionFilterEquals builds an empty PermissionFilterEquals to fill in.
func NewPermissionFilterEquals() *PermissionFilterEquals { return &PermissionFilterEquals{} }

// NewPermissionFilterRange builds an empty PermissionFilterRange to fill in.
func NewPermissionFilterRange() *PermissionFilterRange { return &PermissionFilterRange{} }

// NewPermissionFilterContains builds an empty PermissionFilterContains to fill
// in.
func NewPermissionFilterContains() *PermissionFilterContains { return &PermissionFilterContains{} }

// NewPermissionFilterLike builds an empty PermissionFilterLike to fill in.
func NewPermissionFilterLike() *PermissionFilterLike { return &PermissionFilterLike{} }

// NewPermissionFilterNull builds an empty PermissionFilterNull to fill in.
func NewPermissionFilterNull() *PermissionFilterNull { return &PermissionFilterNull{} }

// NewPermissionFilter builds an empty filter, which matches every row the
// caller is allowed to see.
func NewPermissionFilter() PermissionFilter { return PermissionFilter{} }
