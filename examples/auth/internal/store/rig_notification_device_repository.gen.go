// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package store

import (
	"context"
	"fmt"
	"time"

	"github.com/google/uuid"
	pgx "github.com/jackc/pgx/v5"
	"github.com/simonjanss/rig/examples/auth/internal/model"
	"github.com/simonjanss/rig/runtime/dbhook"
	"github.com/simonjanss/rig/runtime/dbx"
	"github.com/simonjanss/rig/runtime/query"
	"github.com/simonjanss/rig/runtime/readopt"
	"github.com/simonjanss/rig/runtime/rigerr"
	"github.com/simonjanss/rig/runtime/tenancy"
)

// RigNotificationDeviceRepository reads and writes rig_notification_device
// rows.
//
// Every method scopes to the caller's tenant. There is no way to ask it not to
// from a request handler, which is the point.
type RigNotificationDeviceRepository interface {
	// Get returns one row by identifier.
	//
	// A lookup by primary key deliberately ignores the lifecycle filters: it
	// returns the row whether it is live, deleted, or a snapshot, because a caller
	// holding an identifier is usually asking about that exact row.
	Get(ctx context.Context, id uuid.UUID, opts ...readopt.Option) (*model.RigNotificationDevice, error)

	// List returns matching rows and the total ignoring pagination.
	//
	// The filter says which rows and the page says how many of them, in what
	// order. They are separate arguments because they arrive separately: the
	// filter is a request body, the page is two query parameters.
	List(ctx context.Context, f model.RigNotificationDeviceFilter, page model.RigNotificationDevicePage, opts ...readopt.Option) ([]*model.RigNotificationDevice, int64, error)

	// Create inserts a row, stamping the identifier, tenant and audit columns.
	//
	// It takes an envelope rather than the input alone: the rules that check the
	// input and the callbacks that run around the write belong to the same unit of
	// work as the write itself.
	Create(ctx context.Context, in dbhook.Create[model.RigNotificationDeviceCreateInput, model.RigNotificationDevice]) (*model.RigNotificationDevice, error)

	// Update changes the fields the input mentions and leaves the rest alone.
	Update(ctx context.Context, id uuid.UUID, in dbhook.Update[model.RigNotificationDeviceUpdateInput, model.RigNotificationDevice]) (*model.RigNotificationDevice, error)

	// Delete removes a row.
	Delete(ctx context.Context, in dbhook.Delete[model.RigNotificationDeviceDeleteInput, model.RigNotificationDevice]) error
}

// rigNotificationDeviceGroup turns a filter into the condition tree the
// runtime renders.
//
// The scope carries what a condition needs besides the filter itself: who is
// asking, which alias this level's columns belong to, and how deep the nesting
// has gone. It is a value rather than three parameters because every relation
// passes a changed copy of it down.
func rigNotificationDeviceGroup(f model.RigNotificationDeviceFilter, sc filterScope) (query.Group, error) {
	if err := sc.ok(); err != nil {
		return query.Group{}, err
	}

	g := query.Group{Or: f.OrCondition}

	if p := f.Equals; p != nil {
		if p.ID != nil {
			g.Add(sc.at(query.Eq("id", *p.ID)))
		}
		if p.AccountID != nil {
			g.Add(sc.at(query.Eq("account_id", *p.AccountID)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Eq("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Eq("updated_at", p.UpdatedAt)))
		}
		if p.Channel != nil {
			g.Add(sc.at(query.Eq("channel", *p.Channel)))
		}
		if p.Token != nil {
			g.Add(sc.at(query.Eq("token", *p.Token)))
		}
		if p.Label != nil {
			g.Add(sc.at(query.Eq("label", p.Label)))
		}
		if p.LastSeenAt != nil {
			g.Add(sc.at(query.Eq("last_seen_at", p.LastSeenAt)))
		}
		if p.RevokedAt != nil {
			g.Add(sc.at(query.Eq("revoked_at", p.RevokedAt)))
		}
	}
	if p := f.NotEquals; p != nil {
		if p.ID != nil {
			g.Add(sc.at(query.Ne("id", *p.ID)))
		}
		if p.AccountID != nil {
			g.Add(sc.at(query.Ne("account_id", *p.AccountID)))
		}
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Ne("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Ne("updated_at", p.UpdatedAt)))
		}
		if p.Channel != nil {
			g.Add(sc.at(query.Ne("channel", *p.Channel)))
		}
		if p.Token != nil {
			g.Add(sc.at(query.Ne("token", *p.Token)))
		}
		if p.Label != nil {
			g.Add(sc.at(query.Ne("label", p.Label)))
		}
		if p.LastSeenAt != nil {
			g.Add(sc.at(query.Ne("last_seen_at", p.LastSeenAt)))
		}
		if p.RevokedAt != nil {
			g.Add(sc.at(query.Ne("revoked_at", p.RevokedAt)))
		}
	}
	if p := f.GreaterThan; p != nil {
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Gt("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Gt("updated_at", p.UpdatedAt)))
		}
		if p.LastSeenAt != nil {
			g.Add(sc.at(query.Gt("last_seen_at", p.LastSeenAt)))
		}
		if p.RevokedAt != nil {
			g.Add(sc.at(query.Gt("revoked_at", p.RevokedAt)))
		}
	}
	if p := f.SmallerThan; p != nil {
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Lt("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Lt("updated_at", p.UpdatedAt)))
		}
		if p.LastSeenAt != nil {
			g.Add(sc.at(query.Lt("last_seen_at", p.LastSeenAt)))
		}
		if p.RevokedAt != nil {
			g.Add(sc.at(query.Lt("revoked_at", p.RevokedAt)))
		}
	}
	if p := f.GreaterOrEqual; p != nil {
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Gte("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Gte("updated_at", p.UpdatedAt)))
		}
		if p.LastSeenAt != nil {
			g.Add(sc.at(query.Gte("last_seen_at", p.LastSeenAt)))
		}
		if p.RevokedAt != nil {
			g.Add(sc.at(query.Gte("revoked_at", p.RevokedAt)))
		}
	}
	if p := f.SmallerOrEqual; p != nil {
		if p.CreatedAt != nil {
			g.Add(sc.at(query.Lte("created_at", *p.CreatedAt)))
		}
		if p.UpdatedAt != nil {
			g.Add(sc.at(query.Lte("updated_at", p.UpdatedAt)))
		}
		if p.LastSeenAt != nil {
			g.Add(sc.at(query.Lte("last_seen_at", p.LastSeenAt)))
		}
		if p.RevokedAt != nil {
			g.Add(sc.at(query.Lte("revoked_at", p.RevokedAt)))
		}
	}
	if p := f.Contains; p != nil {
		if len(p.ID) > 0 {
			g.Add(sc.at(query.In("id", p.ID)))
		}
		if len(p.AccountID) > 0 {
			g.Add(sc.at(query.In("account_id", p.AccountID)))
		}
		if len(p.CreatedAt) > 0 {
			g.Add(sc.at(query.In("created_at", p.CreatedAt)))
		}
		if len(p.UpdatedAt) > 0 {
			g.Add(sc.at(query.In("updated_at", p.UpdatedAt)))
		}
		if len(p.Channel) > 0 {
			g.Add(sc.at(query.In("channel", p.Channel)))
		}
		if len(p.Token) > 0 {
			g.Add(sc.at(query.In("token", p.Token)))
		}
		if len(p.Label) > 0 {
			g.Add(sc.at(query.In("label", p.Label)))
		}
		if len(p.LastSeenAt) > 0 {
			g.Add(sc.at(query.In("last_seen_at", p.LastSeenAt)))
		}
		if len(p.RevokedAt) > 0 {
			g.Add(sc.at(query.In("revoked_at", p.RevokedAt)))
		}
	}
	if p := f.NotContains; p != nil {
		if len(p.ID) > 0 {
			g.Add(sc.at(query.NotIn("id", p.ID)))
		}
		if len(p.AccountID) > 0 {
			g.Add(sc.at(query.NotIn("account_id", p.AccountID)))
		}
		if len(p.CreatedAt) > 0 {
			g.Add(sc.at(query.NotIn("created_at", p.CreatedAt)))
		}
		if len(p.UpdatedAt) > 0 {
			g.Add(sc.at(query.NotIn("updated_at", p.UpdatedAt)))
		}
		if len(p.Channel) > 0 {
			g.Add(sc.at(query.NotIn("channel", p.Channel)))
		}
		if len(p.Token) > 0 {
			g.Add(sc.at(query.NotIn("token", p.Token)))
		}
		if len(p.Label) > 0 {
			g.Add(sc.at(query.NotIn("label", p.Label)))
		}
		if len(p.LastSeenAt) > 0 {
			g.Add(sc.at(query.NotIn("last_seen_at", p.LastSeenAt)))
		}
		if len(p.RevokedAt) > 0 {
			g.Add(sc.at(query.NotIn("revoked_at", p.RevokedAt)))
		}
	}
	if p := f.Like; p != nil {
		if p.Token != nil {
			g.Add(sc.at(query.Like("token", *p.Token)))
		}
		if p.Label != nil {
			g.Add(sc.at(query.Like("label", *p.Label)))
		}
	}
	if p := f.NotLike; p != nil {
		if p.Token != nil {
			g.Add(sc.at(query.NotLike("token", *p.Token)))
		}
		if p.Label != nil {
			g.Add(sc.at(query.NotLike("label", *p.Label)))
		}
	}
	if p := f.Null; p != nil {
		if p.UpdatedAt != nil {
			if *p.UpdatedAt {
				g.Add(sc.at(query.IsNull("updated_at")))
			} else {
				g.Add(sc.at(query.NotNull("updated_at")))
			}
		}
		if p.Label != nil {
			if *p.Label {
				g.Add(sc.at(query.IsNull("label")))
			} else {
				g.Add(sc.at(query.NotNull("label")))
			}
		}
		if p.LastSeenAt != nil {
			if *p.LastSeenAt {
				g.Add(sc.at(query.IsNull("last_seen_at")))
			} else {
				g.Add(sc.at(query.NotNull("last_seen_at")))
			}
		}
		if p.RevokedAt != nil {
			if *p.RevokedAt {
				g.Add(sc.at(query.IsNull("revoked_at")))
			} else {
				g.Add(sc.at(query.NotNull("revoked_at")))
			}
		}
	}
	if p := f.NotNull; p != nil {
		if p.UpdatedAt != nil {
			if *p.UpdatedAt {
				g.Add(sc.at(query.NotNull("updated_at")))
			} else {
				g.Add(sc.at(query.IsNull("updated_at")))
			}
		}
		if p.Label != nil {
			if *p.Label {
				g.Add(sc.at(query.NotNull("label")))
			} else {
				g.Add(sc.at(query.IsNull("label")))
			}
		}
		if p.LastSeenAt != nil {
			if *p.LastSeenAt {
				g.Add(sc.at(query.NotNull("last_seen_at")))
			} else {
				g.Add(sc.at(query.IsNull("last_seen_at")))
			}
		}
		if p.RevokedAt != nil {
			if *p.RevokedAt {
				g.Add(sc.at(query.NotNull("revoked_at")))
			} else {
				g.Add(sc.at(query.IsNull("revoked_at")))
			}
		}
	}

	for _, n := range f.NestedFilters {
		nested, err := rigNotificationDeviceGroup(n, sc)
		if err != nil {
			return query.Group{}, err
		}
		g.Nest(nested)
	}
	return g, nil
}

// rigNotificationDeviceSortable reports whether a column can be ordered by.
func rigNotificationDeviceSortable(column string) bool {
	switch column {
	case "id", "tenant_id", "account_id", "created_at", "updated_at", "channel", "token", "label", "last_seen_at", "revoked_at":
		return true
	}
	return false
}

// rigNotificationDeviceOrder converts the model's ordering terms into the
// runtime's.
func rigNotificationDeviceOrder(terms []model.RigNotificationDeviceOrder, sc filterScope) ([]query.Order, []query.Join, error) {
	if len(terms) == 0 {
		return nil, nil, nil
	}

	out := make([]query.Order, 0, len(terms))

	for _, t := range terms {
		// A column this table cannot be ordered by is the caller's mistake, not a
		// column name to paste into a statement.
		if !rigNotificationDeviceSortable(t.Column) {
			return nil, nil, rigerr.Invalid("a RigNotificationDevice cannot be ordered by %q", t.Column)
		}
		out = append(out, query.Order{Table: sc.as, Column: t.Column, Desc: t.Desc})
	}

	return out, nil, nil
}

type rigNotificationDeviceRepo struct {
	db *Store
}

var _ RigNotificationDeviceRepository = (*rigNotificationDeviceRepo)(nil)

const rigNotificationDeviceRepoSelect = "rig_notification_device.id, rig_notification_device.tenant_id, rig_notification_device.account_id, rig_notification_device.created_at, rig_notification_device.updated_at, rig_notification_device.channel, rig_notification_device.token, rig_notification_device.label, rig_notification_device.last_seen_at, rig_notification_device.revoked_at"

// scanRigNotificationDevice reads one row in the order
// rigNotificationDeviceRepoSelect lists.
func scanRigNotificationDevice(row pgx.Row) (*model.RigNotificationDevice, error) {
	var m model.RigNotificationDevice
	if err := row.Scan(&m.ID, &m.TenantID, &m.AccountID, &m.CreatedAt, &m.UpdatedAt, &m.Channel, &m.Token, &m.Label, &m.LastSeenAt, &m.RevokedAt); err != nil {
		return nil, err
	}
	m.CreatedAt = dbx.UTC(m.CreatedAt)
	m.UpdatedAt = dbx.UTCPtr(m.UpdatedAt)
	m.LastSeenAt = dbx.UTCPtr(m.LastSeenAt)
	m.RevokedAt = dbx.UTCPtr(m.RevokedAt)
	return &m, nil
}

// Get implements RigNotificationDeviceRepository.
func (r *rigNotificationDeviceRepo) Get(ctx context.Context, id uuid.UUID, opts ...readopt.Option) (*model.RigNotificationDevice, error) {
	cfg, err := readopt.Apply(opts)
	if err != nil {
		return nil, err
	}

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}

	args := []any{id}
	where := "id = $1"
	if !cfg.SkipTenantScope {
		args = append(args, claims.TenantID)
		where += " AND tenant_id = $2"
	}

	sql := fmt.Sprintf("SELECT %s FROM rig_notification_device WHERE %s", rigNotificationDeviceRepoSelect, where)
	m, err := scanRigNotificationDevice(r.db.conn().QueryRow(ctx, sql, args...))
	if dbx.IsNoRows(err) {
		return nil, rigerr.NotFound("no RigNotificationDevice with id %s", id)
	}
	if err != nil {
		return nil, rigerr.Internal(err, "read rig_notification_device")
	}
	return m, nil
}

// List implements RigNotificationDeviceRepository.
func (r *rigNotificationDeviceRepo) List(ctx context.Context, f model.RigNotificationDeviceFilter, page model.RigNotificationDevicePage, opts ...readopt.Option) ([]*model.RigNotificationDevice, int64, error) {
	return r.list(ctx, f, page, opts)
}

// list is the body of every read that takes a filter.
func (r *rigNotificationDeviceRepo) list(ctx context.Context, f model.RigNotificationDeviceFilter, page model.RigNotificationDevicePage, opts []readopt.Option) ([]*model.RigNotificationDevice, int64, error) {
	cfg, err := readopt.Apply(opts)
	if err != nil {
		return nil, 0, err
	}

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, 0, err
	}

	// One scope for the whole statement: it says who is asking, and it qualifies
	// every column with the table it belongs to. The qualification is not
	// decoration — an ordering that reaches a related table brings a second
	// tenant_id into scope, and an unqualified one is then ambiguous.
	sc := newFilterScope(claims, "rig_notification_device")

	scope := query.Group{}
	if !cfg.SkipTenantScope {
		scope.Add(sc.at(query.Eq("tenant_id", claims.TenantID)))
	}

	group, err := rigNotificationDeviceGroup(f, sc)
	if err != nil {
		return nil, 0, err
	}
	scope.Nest(group)

	// The count has its own arguments and no joins. A left join cannot change how
	// many rows there are, and a statement must be given exactly the parameters
	// its text refers to.
	countArgs := query.NewArgs()
	countWhere := scope.SQL(countArgs)
	if countWhere != "" {
		countWhere = " WHERE " + countWhere
	}
	countSQL := fmt.Sprintf("SELECT count(*) FROM rig_notification_device%s", countWhere)
	var total int64
	if err := r.db.conn().QueryRow(ctx, countSQL, countArgs.Values()...).Scan(&total); err != nil {
		return nil, 0, rigerr.Internal(err, "count rig_notification_device")
	}

	order, joins, err := rigNotificationDeviceOrder(page.OrderBy, sc)
	if err != nil {
		return nil, 0, err
	}
	if len(order) == 0 {
		order = RigNotificationDeviceDefaultOrder
	}
	window := query.Page{Limit: page.Limit, Offset: page.Offset}.Clamp(DefaultLimit, MaxLimit)

	// The joins render first, because Postgres numbers placeholders in the order
	// the statement mentions them and a join stands before the conditions do.
	args := query.NewArgs()
	joinSQL := query.JoinSQL(joins, args)
	where := scope.SQL(args)
	if where != "" {
		where = " WHERE " + where
	}

	listSQL := fmt.Sprintf("SELECT %s FROM rig_notification_device%s%s%s%s", rigNotificationDeviceRepoSelect, joinSQL, where, query.OrderSQL(order), window.SQL(args))
	rows, err := r.db.conn().Query(ctx, listSQL, args.Values()...)
	if err != nil {
		return nil, 0, rigerr.Internal(err, "list rig_notification_device")
	}
	defer rows.Close()

	out := make([]*model.RigNotificationDevice, 0, window.Limit)
	for rows.Next() {
		m, err := scanRigNotificationDevice(rows)
		if err != nil {
			return nil, 0, rigerr.Internal(err, "read rig_notification_device")
		}
		out = append(out, m)
	}
	if err := rows.Err(); err != nil {
		return nil, 0, rigerr.Internal(err, "list rig_notification_device")
	}
	return out, total, nil
}

// RigNotificationDeviceDefaultOrder is the ordering used when a query asks for
// none.
//
// It always ends with the primary key, so the order is total and a page
// boundary cannot repeat or skip a row.
var RigNotificationDeviceDefaultOrder = []query.Order{{Table: "rig_notification_device", Column: "created_at", Desc: true}, {Table: "rig_notification_device", Column: "id", Desc: false}}

// Create implements RigNotificationDeviceRepository.
func (r *rigNotificationDeviceRepo) Create(ctx context.Context, in dbhook.Create[model.RigNotificationDeviceCreateInput, model.RigNotificationDevice]) (*model.RigNotificationDevice, error) {
	// Who is asking, first of all. A write from a request carrying no identity is
	// refused here — before a rule runs, before a column notices — which is
	// what lets every hook below take the claims as a value rather than something
	// to look up and check.
	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}
	_ = claims

	// Normalize then validate, before anything is written. A service that forgets
	// to call these cannot write a row that would fail them, which is the only way
	// a rule the schema declares is actually a rule.
	in.Input.Normalize()
	if err := in.Input.Validate(); err != nil {
		return nil, err
	}

	// Then the rules the schema cannot express, if the service wrote any. They run
	// before the transaction opens: a rule that reaches another service should not
	// be holding a row lock while it waits.
	//
	// They run before the create hook rather than after it, which is the one place
	// the order goes this way: an update validates inside the transaction that
	// read the row, so its hook can run first and have what it sets judged. Here
	// there is no row to read and no transaction yet, and opening one to hold
	// across a rule that may call out to another service would be a worse trade
	// than the one it buys.
	if in.Hooks.Validator != nil {
		if err := in.Hooks.Validator.RunCreate(ctx, claims, &in.Input); err != nil {
			return nil, err
		}
	}

	// A version 7 identifier sorts by creation time, so an index on the primary
	// key stays dense as rows are added.
	id, err := uuid.NewV7()
	if err != nil {
		return nil, rigerr.Internal(err, "generate an identifier")
	}
	now := time.Now().UTC()
	_ = now

	// A single INSERT is already atomic, so the transaction is opened only when a
	// hook needs to be able to undo it. Two round trips is not a price to pay for
	// nothing.
	needsTx := in.Hooks.Before != nil || in.Hooks.After != nil

	var m *model.RigNotificationDevice
	err = dbx.InTxIf(ctx, r.db.pool, r.db.conn(), needsTx, func(ctx context.Context, tx dbx.Conn) error {
		if in.Hooks.Before != nil {
			if err := in.Hooks.Before(ctx, claims, &in.Input); err != nil {
				return err
			}
		}

		columns := []string{"id", "tenant_id", "created_at", "account_id", "channel", "token", "label", "last_seen_at", "revoked_at"}
		values := []any{id, claims.TenantID, now, in.Input.AccountID, in.Input.Channel, in.Input.Token, in.Input.Label, in.Input.LastSeenAt, in.Input.RevokedAt}

		sql := fmt.Sprintf("INSERT INTO rig_notification_device (%s) VALUES (%s) RETURNING %s", joinColumns(columns), placeholders(len(values)), rigNotificationDeviceRepoSelect)

		created, err := scanRigNotificationDevice(tx.QueryRow(ctx, sql, values...))
		if err != nil {
			return writeError(err, "rig_notification_device")
		}
		m = created

		if in.Hooks.After != nil {
			return in.Hooks.After(ctx, claims, m)
		}
		return nil
	})
	if err != nil {
		return nil, err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() { done(ctx, who, m) })
	}

	return m, nil
}

// Update implements RigNotificationDeviceRepository.
func (r *rigNotificationDeviceRepo) Update(ctx context.Context, id uuid.UUID, in dbhook.Update[model.RigNotificationDeviceUpdateInput, model.RigNotificationDevice]) (*model.RigNotificationDevice, error) {
	in.Input.Normalize()

	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return nil, err
	}
	_ = claims

	var updated, prev *model.RigNotificationDevice
	err = dbx.InTx(ctx, r.db.pool, func(ctx context.Context, tx dbx.Conn) error {
		prev, err = r.Get(ctx, id)
		if err != nil {
			return err
		}

		// The hook shapes the input, so a field it sets is a field that gets written
		// — and one the rules below then judge. It runs first for exactly that
		// reason: a hook that ran after validation could write a value nothing had
		// checked.
		if in.Hooks.Before != nil {
			if err := in.Hooks.Before(ctx, claims, &in.Input, prev); err != nil {
				return err
			}
		}

		// Validation runs against the row this update would produce, not against the
		// request — a rule about two fields needs both, and only one of them may
		// have been sent. It happens inside the transaction that read prev, so nothing
		// can change underneath it.
		if err := in.Input.Validate(prev); err != nil {
			return err
		}

		if in.Hooks.Validator != nil {
			if err := in.Hooks.Validator.RunUpdate(ctx, claims, &in.Input, prev); err != nil {
				return err
			}
		}

		columns := []string{}
		values := []any{}

		if v, ok := in.Input.AccountID.Get(); ok {
			columns = append(columns, "account_id")
			values = append(values, v)
		}
		if v, ok := in.Input.Channel.Get(); ok {
			columns = append(columns, "channel")
			values = append(values, v)
		}
		if v, ok := in.Input.Token.Get(); ok {
			columns = append(columns, "token")
			values = append(values, v)
		}
		if in.Input.Label.Touched() {
			columns = append(columns, "label")
			values = append(values, in.Input.Label.Ptr())
		}
		if in.Input.LastSeenAt.Touched() {
			columns = append(columns, "last_seen_at")
			values = append(values, in.Input.LastSeenAt.Ptr())
		}
		if in.Input.RevokedAt.Touched() {
			columns = append(columns, "revoked_at")
			values = append(values, in.Input.RevokedAt.Ptr())
		}

		if len(columns) == 0 {
			// Nothing was asked for, so nothing happens — no stamp, and no snapshot
			// either. An update that changes no column did not happen, and a history full
			// of copies of an unchanged row is a history nobody can read.
			updated = prev
			return nil
		}

		columns = append(columns, "updated_at")
		values = append(values, time.Now().UTC())

		values = append(values, id)
		sql := fmt.Sprintf("UPDATE rig_notification_device SET %s WHERE id = $%d RETURNING %s", assignments(columns), len(values), rigNotificationDeviceRepoSelect)

		updated, err = scanRigNotificationDevice(tx.QueryRow(ctx, sql, values...))
		if err != nil {
			return writeError(err, "rig_notification_device")
		}

		if in.Hooks.After != nil {
			return in.Hooks.After(ctx, claims, updated, prev)
		}
		return nil
	})
	if err != nil {
		return nil, err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() { done(ctx, who, updated, prev) })
	}

	return updated, nil
}

// Delete implements RigNotificationDeviceRepository.
func (r *rigNotificationDeviceRepo) Delete(ctx context.Context, in dbhook.Delete[model.RigNotificationDeviceDeleteInput, model.RigNotificationDevice]) error {
	claims, err := tenancy.FromContext(ctx)
	if err != nil {
		return err
	}
	_ = claims

	var prev *model.RigNotificationDevice
	err = dbx.InTx(ctx, r.db.pool, func(ctx context.Context, tx dbx.Conn) error {
		prev, err = r.Get(ctx, in.Input.ID)
		if err != nil {
			return err
		}

		if in.Hooks.Before != nil {
			if err := in.Hooks.Before(ctx, claims, &in.Input, prev); err != nil {
				return err
			}
		}

		if _, err := tx.Exec(ctx, "DELETE FROM rig_notification_device WHERE id = $1", in.Input.ID); err != nil {
			return writeError(err, "rig_notification_device")
		}
		if in.Hooks.After != nil {
			if err := in.Hooks.After(ctx, claims, prev); err != nil {
				return err
			}
		}
		return nil
	})
	if err != nil {
		return err
	}

	if in.Hooks.AfterCommit != nil {
		// The claims are captured with the rest: the request may be over by the time
		// this runs, and reaching into a context for them then is reaching into one
		// that has been cancelled.
		done, who := in.Hooks.AfterCommit, claims
		dbx.AfterCommit(ctx, func() { done(ctx, who, prev) })
	}

	return nil
}

var _ = time.Now
var _ = fmt.Sprintf
var _ = rigerr.NotFound
