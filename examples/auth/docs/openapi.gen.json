{
  "openapi": "3.1.0",
  "info": {
    "title": "auth",
    "description": "The auth HTTP API.\n\nThe authentication endpoints under `/auth` are served by a hand-written module and are not described here.\n\nThe notification inbox endpoints are served by a hand-written module and are not described here.",
    "version": "2026-08-30"
  },
  "servers": [
    {
      "url": "http://localhost:8082",
      "description": "This example, running on your machine."
    }
  ],
  "paths": {
    "/api/v1/notes": {
      "get": {
        "tags": [
          "Notes"
        ],
        "summary": "List Notes.",
        "description": "List Notes.\n\nRequires the `note.read` permission.",
        "operationId": "listNotes",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Maximum rows to return.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50
            }
          },
          {
            "name": "offset",
            "in": "query",
            "description": "Rows to skip before the first returned row.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 0
            }
          },
          {
            "name": "scope",
            "in": "query",
            "description": "How wide to read: own for the notes the caller created, which is the default, or all for every one in the tenant. Asking for all requires the note.read.all permission and is refused without it, rather than quietly answering with fewer rows.",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "own",
                "all"
              ],
              "default": "own"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          }
        ],
        "responses": {
          "200": {
            "description": "A page of Notes.",
            "headers": {
              "API-Revision": {
                "$ref": "#/components/headers/ApiRevision"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NoteListResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "429": {
            "$ref": "#/components/responses/RateLimited"
          },
          "500": {
            "$ref": "#/components/responses/Internal"
          }
        },
        "x-rig-permission": "note.read",
        "x-rig-wide-permission": "note.read.all"
      },
      "post": {
        "tags": [
          "Notes"
        ],
        "summary": "Create a Note.",
        "description": "Create a Note.\n\nRequires the `note.write` permission.",
        "operationId": "createNote",
        "parameters": [
          {
            "$ref": "#/components/parameters/ApiRevision"
          },
          {
            "$ref": "#/components/parameters/IdempotencyKey"
          }
        ],
        "requestBody": {
          "description": "The fields a new Note is created with. A field the database cannot supply for itself is required.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NoteCreateInput"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "The created Note.",
            "headers": {
              "API-Revision": {
                "$ref": "#/components/headers/ApiRevision"
              },
              "Idempotency-Replayed": {
                "$ref": "#/components/headers/IdempotencyReplayed"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Note"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "422": {
            "$ref": "#/components/responses/UnprocessableEntity"
          },
          "429": {
            "$ref": "#/components/responses/RateLimited"
          },
          "500": {
            "$ref": "#/components/responses/Internal"
          }
        },
        "x-rig-permission": "note.write"
      }
    },
    "/api/v1/notes/_deleted": {
      "get": {
        "tags": [
          "Notes"
        ],
        "summary": "List retired Notes.",
        "description": "A deletion stamps the row rather than removing it, so this is what was deleted and can still be brought back. Only rows inside the 30-day restore window appear: past it a row is gone as far as anyone is concerned, so it is not in the trash either.\n\nRequires the `note.read` permission.",
        "operationId": "listDeletedNotes",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Maximum rows to return.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50
            }
          },
          {
            "name": "offset",
            "in": "query",
            "description": "Rows to skip before the first returned row.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 0
            }
          },
          {
            "name": "scope",
            "in": "query",
            "description": "How wide to read: own for the notes the caller created, which is the default, or all for every one in the tenant. Asking for all requires the note.read.all permission and is refused without it, rather than quietly answering with fewer rows.",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "own",
                "all"
              ],
              "default": "own"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          }
        ],
        "responses": {
          "200": {
            "description": "A page of retired Notes.",
            "headers": {
              "API-Revision": {
                "$ref": "#/components/headers/ApiRevision"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NoteListResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "429": {
            "$ref": "#/components/responses/RateLimited"
          },
          "500": {
            "$ref": "#/components/responses/Internal"
          }
        },
        "x-rig-permission": "note.read",
        "x-rig-wide-permission": "note.read.all"
      }
    },
    "/api/v1/notes/_search": {
      "post": {
        "tags": [
          "Notes"
        ],
        "summary": "Search Notes with filters.",
        "description": "Search Notes with filters.\n\nThe primary form of this operation is `QUERY /api/v1/notes` — a read that carries a body, and so safe and idempotent in a way POST is not. OpenAPI 3.1 cannot describe an operation on the QUERY method, so it is documented here as the POST alias that exists for intermediaries which reject unfamiliar methods. One handler serves both routes and they answer identically.\n\nRequires the `note.read` permission.",
        "operationId": "searchNotes",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Maximum rows to return.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50
            }
          },
          {
            "name": "offset",
            "in": "query",
            "description": "Rows to skip before the first returned row.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 0
            }
          },
          {
            "name": "scope",
            "in": "query",
            "description": "How wide to read: own for the notes the caller created, which is the default, or all for every one in the tenant. Asking for all requires the note.read.all permission and is refused without it, rather than quietly answering with fewer rows.",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "own",
                "all"
              ],
              "default": "own"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          }
        ],
        "requestBody": {
          "description": "The request body for Note.Search.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NoteSearchBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "A page of matching Notes.",
            "headers": {
              "API-Revision": {
                "$ref": "#/components/headers/ApiRevision"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NoteListResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "422": {
            "$ref": "#/components/responses/UnprocessableEntity"
          },
          "429": {
            "$ref": "#/components/responses/RateLimited"
          },
          "500": {
            "$ref": "#/components/responses/Internal"
          }
        },
        "x-rig-permission": "note.read",
        "x-rig-wide-permission": "note.read.all"
      }
    },
    "/api/v1/notes/{id}": {
      "get": {
        "tags": [
          "Notes"
        ],
        "summary": "Fetch one Note by identifier.",
        "description": "Fetch one Note by identifier.\n\nRequires the `note.read` permission.",
        "operationId": "getNote",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Identifier of the Note.",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "scope",
            "in": "query",
            "description": "How wide to read: own for the notes the caller created, which is the default, or all for every one in the tenant. Asking for all requires the note.read.all permission and is refused without it, rather than quietly answering with fewer rows.",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "own",
                "all"
              ],
              "default": "own"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          }
        ],
        "responses": {
          "200": {
            "description": "The requested Note.",
            "headers": {
              "API-Revision": {
                "$ref": "#/components/headers/ApiRevision"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Note"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "429": {
            "$ref": "#/components/responses/RateLimited"
          },
          "500": {
            "$ref": "#/components/responses/Internal"
          }
        },
        "x-rig-permission": "note.read",
        "x-rig-wide-permission": "note.read.all"
      },
      "delete": {
        "tags": [
          "Notes"
        ],
        "summary": "Delete a Note.",
        "description": "The row is retired by stamping a deletion time; it stops appearing in reads but can be restored within the retention window.\n\nRequires the `note.delete` permission.",
        "operationId": "deleteNote",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Identifier of the Note.",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          }
        ],
        "responses": {
          "204": {
            "description": "The Note was deleted.",
            "headers": {
              "API-Revision": {
                "$ref": "#/components/headers/ApiRevision"
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "429": {
            "$ref": "#/components/responses/RateLimited"
          },
          "500": {
            "$ref": "#/components/responses/Internal"
          }
        },
        "x-rig-permission": "note.delete"
      },
      "patch": {
        "tags": [
          "Notes"
        ],
        "summary": "Update a Note.",
        "description": "Only the fields present in the body are changed. A field set to null is cleared; a field left out is left alone.\n\nRequires the `note.write` permission.",
        "operationId": "updateNote",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Identifier of the Note.",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          },
          {
            "$ref": "#/components/parameters/IdempotencyKey"
          }
        ],
        "requestBody": {
          "description": "The fields of a Note to change. Every member is optional: one left out is left alone, and an explicit null clears it.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NoteUpdateInput"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The updated Note.",
            "headers": {
              "API-Revision": {
                "$ref": "#/components/headers/ApiRevision"
              },
              "Idempotency-Replayed": {
                "$ref": "#/components/headers/IdempotencyReplayed"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Note"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "422": {
            "$ref": "#/components/responses/UnprocessableEntity"
          },
          "429": {
            "$ref": "#/components/responses/RateLimited"
          },
          "500": {
            "$ref": "#/components/responses/Internal"
          }
        },
        "x-rig-permission": "note.write"
      }
    },
    "/api/v1/notes/{id}/_restore": {
      "post": {
        "tags": [
          "Notes"
        ],
        "summary": "Bring a retired Note back.",
        "description": "The deletion stamp is cleared and the row appears in reads again. It works for 30 days after the deletion; past that the row is no longer eligible and this answers 409.\n\nRestoring a row that was never deleted is not an error. It is already in the state the caller asked for, and answering otherwise would make a retry of a request whose response went missing look like a failure.\n\nThe request carries no fields. What to do when the world has moved on while the row was retired — a unique value taken by something created since — is the application's decision, and it is made in the restore hook: refuse, or change the row so that it fits.\n\nEvery rule then runs against whatever the hook settled on, not only the ones for fields it touched. The row was not live, so nothing about it has been checked against the world it is returning to.\n\nRequires the `note.write` permission.",
        "operationId": "restoreNote",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Identifier of the Note.",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          },
          {
            "$ref": "#/components/parameters/IdempotencyKey"
          }
        ],
        "responses": {
          "200": {
            "description": "The restored Note.",
            "headers": {
              "API-Revision": {
                "$ref": "#/components/headers/ApiRevision"
              },
              "Idempotency-Replayed": {
                "$ref": "#/components/headers/IdempotencyReplayed"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Note"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "422": {
            "$ref": "#/components/responses/UnprocessableEntity"
          },
          "429": {
            "$ref": "#/components/responses/RateLimited"
          },
          "500": {
            "$ref": "#/components/responses/Internal"
          }
        },
        "x-rig-permission": "note.write"
      }
    },
    "/api/v1/rig_notification_recipient/_deleted/_stream": {
      "get": {
        "tags": [
          "NotificationRecipients"
        ],
        "summary": "Subscribe to retired NotificationRecipients.",
        "description": "The rows a delete stamped rather than removed, kept up to date.\n\nA shape is a filtered view of one table that a client subscribes to. This route is a proxy in front of the sync service: which rows exist in the shape — the tenant, the lifecycle predicate, and the application's own scoping — is decided by the server and cannot be influenced from here. Where the client is in the stream is forwarded untouched.\n\nWith `live=true` the request is a long poll: it does not answer until something changes, so a client timeout set for an ordinary request will cut it short.\n\nThe response body is the sync protocol's own and is not described here. The `electric-*` response headers carry the cursor: send `handle` and `offset` back on the next request, or the subscription starts over.\n\nRequires a session scoped to a tenant, and the shape is filtered to it.",
        "operationId": "streamDeletedNotificationRecipients",
        "parameters": [
          {
            "name": "offset",
            "in": "query",
            "description": "Where in the stream to resume, from the previous response's electric-offset.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "handle",
            "in": "query",
            "description": "The shape handle from a previous response. Omit it to start a new subscription.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "live",
            "in": "query",
            "description": "Long-poll until something changes, rather than answering with what is there now.",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "The sync service's own cache-busting cursor, echoed back from the last response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "replica",
            "in": "query",
            "description": "Whether updates carry the whole row or only the columns that changed.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          }
        ],
        "responses": {
          "200": {
            "description": "A chunk of the shape, in the sync protocol's own format.",
            "headers": {
              "electric-handle": {
                "description": "The shape handle. Send it back to continue this subscription.",
                "schema": {
                  "type": "string"
                }
              },
              "electric-offset": {
                "description": "Where this chunk ended. Send it back to resume from here.",
                "schema": {
                  "type": "string"
                }
              },
              "electric-schema": {
                "description": "The shape's column types.",
                "schema": {
                  "type": "string"
                }
              },
              "electric-cursor": {
                "description": "The cursor to echo on the next request.",
                "schema": {
                  "type": "string"
                }
              },
              "electric-up-to-date": {
                "description": "Present when the client has caught up with the shape.",
                "schema": {
                  "type": "string"
                }
              },
              "X-Rig-Sync-Fallback": {
                "description": "Present when this response is a snapshot served because the sync service could not be reached, rather than a chunk of a live subscription. The rows are correct as of the moment they were read and are not updated until the subscription resumes.",
                "schema": {
                  "type": "string"
                }
              }
            },
            "content": {
              "application/json": {}
            }
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "502": {
            "description": "The sync service could not be reached, and this shape had nothing to answer with instead: no database was configured, this shape asked not to be answered from one, or the read failed or was larger than a snapshot may be.",
            "content": {
              "text/plain": {}
            }
          },
          "503": {
            "description": "The sync service could not be reached, and this subscription already holds a snapshot. Keep it and retry after Retry-After.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before polling again.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "text/plain": {}
            }
          }
        }
      }
    },
    "/api/v1/rig_notification_recipient/_stream": {
      "get": {
        "tags": [
          "NotificationRecipients"
        ],
        "summary": "Subscribe to the live NotificationRecipient shape.",
        "description": "Every NotificationRecipient the caller may read, kept up to date.\n\nA shape is a filtered view of one table that a client subscribes to. This route is a proxy in front of the sync service: which rows exist in the shape — the tenant, the lifecycle predicate, and the application's own scoping — is decided by the server and cannot be influenced from here. Where the client is in the stream is forwarded untouched.\n\nWith `live=true` the request is a long poll: it does not answer until something changes, so a client timeout set for an ordinary request will cut it short.\n\nThe response body is the sync protocol's own and is not described here. The `electric-*` response headers carry the cursor: send `handle` and `offset` back on the next request, or the subscription starts over.\n\nRequires a session scoped to a tenant, and the shape is filtered to it.",
        "operationId": "streamNotificationRecipients",
        "parameters": [
          {
            "name": "offset",
            "in": "query",
            "description": "Where in the stream to resume, from the previous response's electric-offset.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "handle",
            "in": "query",
            "description": "The shape handle from a previous response. Omit it to start a new subscription.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "live",
            "in": "query",
            "description": "Long-poll until something changes, rather than answering with what is there now.",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "The sync service's own cache-busting cursor, echoed back from the last response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "replica",
            "in": "query",
            "description": "Whether updates carry the whole row or only the columns that changed.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "$ref": "#/components/parameters/ApiRevision"
          }
        ],
        "responses": {
          "200": {
            "description": "A chunk of the shape, in the sync protocol's own format.",
            "headers": {
              "electric-handle": {
                "description": "The shape handle. Send it back to continue this subscription.",
                "schema": {
                  "type": "string"
                }
              },
              "electric-offset": {
                "description": "Where this chunk ended. Send it back to resume from here.",
                "schema": {
                  "type": "string"
                }
              },
              "electric-schema": {
                "description": "The shape's column types.",
                "schema": {
                  "type": "string"
                }
              },
              "electric-cursor": {
                "description": "The cursor to echo on the next request.",
                "schema": {
                  "type": "string"
                }
              },
              "electric-up-to-date": {
                "description": "Present when the client has caught up with the shape.",
                "schema": {
                  "type": "string"
                }
              },
              "X-Rig-Sync-Fallback": {
                "description": "Present when this response is a snapshot served because the sync service could not be reached, rather than a chunk of a live subscription. The rows are correct as of the moment they were read and are not updated until the subscription resumes.",
                "schema": {
                  "type": "string"
                }
              }
            },
            "content": {
              "application/json": {}
            }
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "403": {
            "$ref": "#/components/responses/Forbidden"
          },
          "502": {
            "description": "The sync service could not be reached, and this shape had nothing to answer with instead: no database was configured, this shape asked not to be answered from one, or the read failed or was larger than a snapshot may be.",
            "content": {
              "text/plain": {}
            }
          },
          "503": {
            "description": "The sync service could not be reached, and this subscription already holds a snapshot. Keep it and retry after Retry-After.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before polling again.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "text/plain": {}
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Error": {
        "type": "object",
        "properties": {
          "code": {
            "$ref": "#/components/schemas/ErrorCode",
            "description": "Machine-readable reason the request failed."
          },
          "message": {
            "type": "string",
            "description": "Human-readable explanation. Not intended for clients to parse."
          },
          "requestId": {
            "type": [
              "string",
              "null"
            ],
            "description": "Identifier of this request, for correlating with server logs."
          },
          "fields": {
            "description": "Present when the failure was validation. It is shaped like the request body that failed — one member per field, holding the problem with that field — so a client can put each message beside the control it belongs to."
          }
        },
        "description": "Returned whenever a request fails."
      },
      "ErrorCode": {
        "type": "string",
        "enum": [
          "BadRequest",
          "Unauthorized",
          "Forbidden",
          "NotFound",
          "Conflict",
          "UnprocessableEntity",
          "RateLimited",
          "TooLarge",
          "UnsupportedMediaType",
          "UpgradeRequired",
          "Internal"
        ],
        "description": "Machine-readable reason a request failed.",
        "x-enum-varnames": [
          "BadRequest",
          "Unauthorized",
          "Forbidden",
          "NotFound",
          "Conflict",
          "UnprocessableEntity",
          "RateLimited",
          "TooLarge",
          "UnsupportedMediaType",
          "UpgradeRequired",
          "Internal"
        ]
      },
      "Note": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "Unique identifier for this row.",
            "readOnly": true
          },
          "tenantId": {
            "type": "string",
            "format": "uuid",
            "description": "Tenant this row belongs to. Every query is scoped by it.",
            "readOnly": true
          },
          "title": {
            "type": "string",
            "description": "What the note is about, in a few words."
          },
          "body": {
            "type": [
              "string",
              "null"
            ],
            "description": "The note itself, or null while it is just a title."
          },
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "When this row was created. Set automatically and never changes.",
            "readOnly": true
          },
          "createdByAccountId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Account that created this row, taken from the request's claims.",
            "readOnly": true
          },
          "updatedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When this row was last updated. Set automatically on every update.",
            "readOnly": true
          },
          "updatedByAccountId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Account that last updated this row, taken from the request's claims.",
            "readOnly": true
          },
          "deletedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When this row was soft-deleted. Null while the row is live.",
            "readOnly": true
          },
          "deletedByAccountId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Account that soft-deleted this row, taken from the request's claims.",
            "readOnly": true
          },
          "createdByApiKeyId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "API key this row was created through, when it was an integration rather than a person.",
            "readOnly": true
          },
          "updatedByApiKeyId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "API key this row was last updated through, when it was an integration rather than a person.",
            "readOnly": true
          },
          "deletedByApiKeyId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "API key this row was soft-deleted through, when it was an integration rather than a person.",
            "readOnly": true
          },
          "publishAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When the note goes live, or null while it is a draft. Notifications about it are due then."
          }
        },
        "description": "Something somebody wrote down."
      },
      "NoteCreateInput": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "What the note is about, in a few words."
          },
          "body": {
            "type": [
              "string",
              "null"
            ],
            "description": "The note itself, or null while it is just a title."
          },
          "publishAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When the note goes live, or null while it is a draft. Notifications about it are due then."
          }
        },
        "required": [
          "title"
        ],
        "description": "The fields a new Note is created with. A field the database cannot supply for itself is required."
      },
      "NoteFilter": {
        "type": "object",
        "properties": {
          "equals": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterEquals"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must equal the given value."
          },
          "notEquals": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterEquals"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must not equal the given value."
          },
          "greaterThan": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterRange"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must be greater than the given value."
          },
          "smallerThan": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterRange"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must be less than the given value."
          },
          "greaterOrEqual": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterRange"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must be greater than or equal to the given value."
          },
          "smallerOrEqual": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterRange"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must be less than or equal to the given value."
          },
          "contains": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterContains"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must be one of the given values."
          },
          "notContains": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterContains"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must not be one of the given values."
          },
          "like": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterLike"
              },
              {
                "type": "null"
              }
            ],
            "description": "Text fields that must match the given pattern."
          },
          "notLike": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterLike"
              },
              {
                "type": "null"
              }
            ],
            "description": "Text fields that must not match the given pattern."
          },
          "null": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterNull"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must be null."
          },
          "notNull": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/NoteFilterNull"
              },
              {
                "type": "null"
              }
            ],
            "description": "Fields that must not be null."
          },
          "orCondition": {
            "type": "boolean",
            "description": "Combine this filter's conditions with OR instead of AND."
          },
          "nestedFilters": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NoteFilter"
            },
            "description": "Sub-filters combined with this one, so that AND and OR can be mixed to any depth."
          }
        },
        "description": "Conditions a Note must satisfy to match a search."
      },
      "NoteFilterContains": {
        "type": "object",
        "properties": {
          "id": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Unique identifier for this row."
          },
          "title": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "What the note is about, in a few words."
          },
          "body": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The note itself, or null while it is just a title."
          },
          "createdAt": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "date-time"
            },
            "description": "When this row was created. Set automatically and never changes."
          },
          "createdByAccountId": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Account that created this row, taken from the request's claims."
          },
          "updatedAt": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "date-time"
            },
            "description": "When this row was last updated. Set automatically on every update."
          },
          "updatedByAccountId": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Account that last updated this row, taken from the request's claims."
          },
          "deletedAt": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "date-time"
            },
            "description": "When this row was soft-deleted. Null while the row is live."
          },
          "deletedByAccountId": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Account that soft-deleted this row, taken from the request's claims."
          },
          "createdByApiKeyId": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "API key this row was created through, when it was an integration rather than a person."
          },
          "updatedByApiKeyId": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "API key this row was last updated through, when it was an integration rather than a person."
          },
          "deletedByApiKeyId": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "API key this row was soft-deleted through, when it was an integration rather than a person."
          },
          "publishAt": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "date-time"
            },
            "description": "When the note goes live, or null while it is a draft. Notifications about it are due then."
          }
        },
        "description": "Set-membership conditions on Note fields."
      },
      "NoteFilterEquals": {
        "type": "object",
        "properties": {
          "id": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Unique identifier for this row."
          },
          "title": {
            "type": [
              "string",
              "null"
            ],
            "description": "What the note is about, in a few words."
          },
          "body": {
            "type": [
              "string",
              "null"
            ],
            "description": "The note itself, or null while it is just a title."
          },
          "createdAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When this row was created. Set automatically and never changes."
          },
          "createdByAccountId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Account that created this row, taken from the request's claims."
          },
          "updatedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When this row was last updated. Set automatically on every update."
          },
          "updatedByAccountId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Account that last updated this row, taken from the request's claims."
          },
          "deletedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When this row was soft-deleted. Null while the row is live."
          },
          "deletedByAccountId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Account that soft-deleted this row, taken from the request's claims."
          },
          "createdByApiKeyId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "API key this row was created through, when it was an integration rather than a person."
          },
          "updatedByApiKeyId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "API key this row was last updated through, when it was an integration rather than a person."
          },
          "deletedByApiKeyId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "API key this row was soft-deleted through, when it was an integration rather than a person."
          },
          "publishAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When the note goes live, or null while it is a draft. Notifications about it are due then."
          }
        },
        "description": "Exact-match conditions on Note fields."
      },
      "NoteFilterLike": {
        "type": "object",
        "properties": {
          "title": {
            "type": [
              "string",
              "null"
            ],
            "description": "What the note is about, in a few words."
          },
          "body": {
            "type": [
              "string",
              "null"
            ],
            "description": "The note itself, or null while it is just a title."
          }
        },
        "description": "Pattern conditions on Note text fields."
      },
      "NoteFilterNull": {
        "type": "object",
        "properties": {
          "body": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "The note itself, or null while it is just a title."
          },
          "createdByAccountId": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Account that created this row, taken from the request's claims."
          },
          "updatedAt": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "When this row was last updated. Set automatically on every update."
          },
          "updatedByAccountId": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Account that last updated this row, taken from the request's claims."
          },
          "deletedAt": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "When this row was soft-deleted. Null while the row is live."
          },
          "deletedByAccountId": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Account that soft-deleted this row, taken from the request's claims."
          },
          "createdByApiKeyId": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "API key this row was created through, when it was an integration rather than a person."
          },
          "updatedByApiKeyId": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "API key this row was last updated through, when it was an integration rather than a person."
          },
          "deletedByApiKeyId": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "API key this row was soft-deleted through, when it was an integration rather than a person."
          },
          "publishAt": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "When the note goes live, or null while it is a draft. Notifications about it are due then."
          }
        },
        "description": "Presence conditions on nullable Note fields."
      },
      "NoteFilterRange": {
        "type": "object",
        "properties": {
          "createdAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When this row was created. Set automatically and never changes."
          },
          "updatedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When this row was last updated. Set automatically on every update."
          },
          "deletedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When this row was soft-deleted. Null while the row is live."
          },
          "publishAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When the note goes live, or null while it is a draft. Notifications about it are due then."
          }
        },
        "description": "Ordering conditions on Note fields that can be compared."
      },
      "NoteListResponse": {
        "type": "object",
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Note"
            },
            "description": "The rows in this page."
          },
          "pagination": {
            "$ref": "#/components/schemas/Pagination",
            "description": "Where this page sits in the full result set."
          }
        },
        "description": "A page of Notes."
      },
      "NoteSearchBody": {
        "type": "object",
        "properties": {
          "filter": {
            "$ref": "#/components/schemas/NoteFilter",
            "description": "Conditions rows must satisfy."
          }
        },
        "required": [
          "filter"
        ],
        "description": "The request body for Note.Search."
      },
      "NoteUpdateInput": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "What the note is about, in a few words."
          },
          "body": {
            "type": [
              "string",
              "null"
            ],
            "description": "The note itself, or null while it is just a title."
          },
          "publishAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When the note goes live, or null while it is a draft. Notifications about it are due then."
          }
        },
        "description": "The fields of a Note to change. Every member is optional: one left out is left alone, and an explicit null clears it."
      },
      "Pagination": {
        "type": "object",
        "properties": {
          "offset": {
            "type": "integer",
            "format": "int32",
            "description": "Number of rows skipped before this page."
          },
          "limit": {
            "type": "integer",
            "format": "int32",
            "description": "Maximum number of rows in this page."
          },
          "total": {
            "type": "integer",
            "format": "int64",
            "description": "Total number of rows matching the query, ignoring pagination."
          }
        },
        "description": "Where the returned page sits within the full result set."
      }
    },
    "responses": {
      "BadRequest": {
        "description": "The request was malformed or a parameter could not be parsed.\n\nThe body's `code` is `BadRequest`.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Error"
            }
          }
        }
      },
      "Unauthorized": {
        "description": "No valid session or API key was presented.\n\nThe body's `code` is `Unauthorized`.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Error"
            }
          }
        }
      },
      "Forbidden": {
        "description": "The caller is known but not permitted to do this.\n\nThe body's `code` is `Forbidden`.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Error"
            }
          }
        }
      },
      "NotFound": {
        "description": "No such resource, or it belongs to another tenant.\n\nThe body's `code` is `NotFound`.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Error"
            }
          }
        }
      },
      "Conflict": {
        "description": "The request conflicts with the current state of the resource.\n\nThe body's `code` is `Conflict`.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Error"
            }
          }
        }
      },
      "UnprocessableEntity": {
        "description": "The request was well-formed but failed validation.\n\nThe body's `code` is `UnprocessableEntity`.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Error"
            }
          }
        }
      },
      "RateLimited": {
        "description": "Too many requests. Retry after the interval in the Retry-After header.\n\nThe body's `code` is `RateLimited`.",
        "headers": {
          "Retry-After": {
            "$ref": "#/components/headers/RetryAfter"
          },
          "RateLimit-Limit": {
            "$ref": "#/components/headers/RateLimitLimit"
          },
          "RateLimit-Remaining": {
            "$ref": "#/components/headers/RateLimitRemaining"
          },
          "RateLimit-Reset": {
            "$ref": "#/components/headers/RateLimitReset"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Error"
            }
          }
        }
      },
      "Internal": {
        "description": "Something went wrong on the server.\n\nThe body's `code` is `Internal`.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Error"
            }
          }
        }
      }
    },
    "parameters": {
      "ApiRevision": {
        "name": "API-Revision",
        "in": "header",
        "description": "The API revision this client was built against. A server generated from an older document than the caller expects answers 426 rather than guessing.",
        "required": false,
        "schema": {
          "type": "string"
        }
      },
      "IdempotencyKey": {
        "name": "Idempotency-Key",
        "in": "header",
        "description": "A key of the caller's choosing, so a retried write happens once. Sending the same key with the same body replays the first answer and sets Idempotency-Replayed; with a different body it is refused with 422, and while the first is still in flight, with 409.",
        "required": false,
        "schema": {
          "type": "string"
        }
      }
    },
    "headers": {
      "ApiRevision": {
        "description": "The revision this server was generated from.",
        "schema": {
          "type": "string"
        }
      },
      "IdempotencyReplayed": {
        "description": "Present and true when this answer is a replay of an earlier request carrying the same Idempotency-Key, rather than work done again.",
        "schema": {
          "type": "boolean"
        }
      },
      "RateLimitLimit": {
        "description": "Requests permitted in the current window.",
        "schema": {
          "type": "integer"
        }
      },
      "RateLimitRemaining": {
        "description": "Requests left in the current window.",
        "schema": {
          "type": "integer"
        }
      },
      "RateLimitReset": {
        "description": "Seconds until the current window resets.",
        "schema": {
          "type": "integer"
        }
      },
      "RetryAfter": {
        "description": "Seconds to wait before retrying.",
        "schema": {
          "type": "integer"
        }
      }
    },
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "description": "A credential in `Authorization: Bearer \u003ctoken\u003e`.\n\nTwo kinds are accepted and are told apart by a prefix on the token itself. A session access token travels on every request and is good for 10m, renewed against the session endpoints before it expires. An API key does not expire: an integration key's scopes are its permissions outright, and a personal key carries what its owner carries and no more.\n\nThe endpoints that issue and renew these are mounted under `/auth` and are not described in this document.",
        "scheme": "bearer",
        "bearerFormat": "opaque"
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ],
  "tags": [
    {
      "name": "Notes",
      "description": "Something somebody wrote down."
    },
    {
      "name": "NotificationRecipients",
      "description": "One inbox line: a notification, an account, and whether it has been read."
    }
  ]
}
