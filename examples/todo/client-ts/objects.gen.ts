// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

/** Where the returned page sits within the full result set. */
export type Pagination = {
    /** Number of rows skipped before this page. */
    offset: number;
    /** Maximum number of rows in this page. */
    limit: number;
    /** Total number of rows matching the query, ignoring pagination. */
    total: number;
};

/** One uploaded file. Metadata only; the bytes are fetched from Url. */
export type RigFile = {
    /** Identifier of the file. */
    id: string;
    /**
     * Where the file is served from. Stable and unsigned, so it is safe to keep
     * and to sync and grants nothing on its own: the endpoint behind it still
     * checks the caller. Null until the upload is finished.
     */
    url?: string | null;
    /**
     * What the file was called when it arrived. It is for the save dialog, and
     * it is not a path: if you write the file somewhere, you decide where and
     * you sanitize what.
     */
    fileName: string;
    /**
     * What the bytes were sniffed to be, which is what a download announces. It
     * is not necessarily what the client claimed on the way up.
     */
    contentType: string;
    /** How large the file is, in bytes. */
    sizeBytes: number;
};

