// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the code that calls it.

import type {
    TodoFilter,
    TodoFilterContains,
    TodoFilterEquals,
    TodoFilterLike,
    TodoFilterNull,
    TodoFilterRange,
} from "./todo_query.gen.js";

/** Conditions a TodoAttachment must satisfy to match a search. */
export type TodoAttachmentFilter = {
    /** Fields that must equal the given value. */
    equals?: TodoAttachmentFilterEquals | null;
    /** Fields that must not equal the given value. */
    notEquals?: TodoAttachmentFilterEquals | null;
    /** Fields that must be greater than the given value. */
    greaterThan?: TodoAttachmentFilterRange | null;
    /** Fields that must be less than the given value. */
    smallerThan?: TodoAttachmentFilterRange | null;
    /** Fields that must be greater than or equal to the given value. */
    greaterOrEqual?: TodoAttachmentFilterRange | null;
    /** Fields that must be less than or equal to the given value. */
    smallerOrEqual?: TodoAttachmentFilterRange | null;
    /** Fields that must be one of the given values. */
    contains?: TodoAttachmentFilterContains | null;
    /** Fields that must not be one of the given values. */
    notContains?: TodoAttachmentFilterContains | null;
    /** Text fields that must match the given pattern. */
    like?: TodoAttachmentFilterLike | null;
    /** Text fields that must not match the given pattern. */
    notLike?: TodoAttachmentFilterLike | null;
    /** Fields that must be null. */
    null?: TodoAttachmentFilterNull | null;
    /** Fields that must not be null. */
    notNull?: TodoAttachmentFilterNull | null;
    /** Combine this filter's conditions with OR instead of AND. */
    orCondition: boolean;
    /**
     * Sub-filters combined with this one, so that AND and OR can be mixed to
     * any depth.
     */
    nestedFilters: TodoAttachmentFilter[];
    /**
     * Relations with no row matching the given conditions. An empty one asks
     * for no related row at all.
     *
     * This is where a negation belongs when what you mean is "not": it negates
     * whether a matching related row exists, so a row with no related row at
     * all satisfies it. The negative operators above negate the comparison
     * instead and still require the related row to be there — "no related row
     * called X" is here, "a related row not called X" is NotEquals.
     */
    without?: TodoAttachmentFilterWithout | null;
};

/** Set-membership conditions on TodoAttachment fields. */
export type TodoAttachmentFilterContains = {
    /** Unique identifier for this row. */
    id: string[];
    /** The todo this is attached to. */
    todoId: string[];
    /** The file itself. Required: an attachment with no file is nothing. */
    attachmentFileId: string[];
    /** What the attachment is, in a few words. */
    caption: string[];
    /** Where this sits in the todo's list of attachments. */
    position: number[];
    /** When this row was created. Set automatically and never changes. */
    createdAt: string[];
    /** Account that created this row, taken from the request's claims. */
    createdByAccountId: string[];
    /** When this row was last updated. Set automatically on every update. */
    updatedAt: string[];
    /** Account that last updated this row, taken from the request's claims. */
    updatedByAccountId: string[];
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt: string[];
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId: string[];
    /**
     * Conditions on this row's Todo.
     *
     * A row with no Todo never matches, and that holds for the negative
     * operators too: under NotEquals this asks whether the Todo is there and
     * differs, so a row without one fails that as well. To ask the other
     * question — no matching Todo, which a row with none satisfies — use
     * Without.Todo.
     */
    todo?: TodoFilterContains | null;
};

/** Exact-match conditions on TodoAttachment fields. */
export type TodoAttachmentFilterEquals = {
    /** Unique identifier for this row. */
    id?: string | null;
    /** The todo this is attached to. */
    todoId?: string | null;
    /** The file itself. Required: an attachment with no file is nothing. */
    attachmentFileId?: string | null;
    /** What the attachment is, in a few words. */
    caption?: string | null;
    /** Where this sits in the todo's list of attachments. */
    position?: number | null;
    /** When this row was created. Set automatically and never changes. */
    createdAt?: string | null;
    /** Account that created this row, taken from the request's claims. */
    createdByAccountId?: string | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: string | null;
    /** Account that last updated this row, taken from the request's claims. */
    updatedByAccountId?: string | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: string | null;
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId?: string | null;
    /**
     * Conditions on this row's Todo.
     *
     * A row with no Todo never matches, and that holds for the negative
     * operators too: under NotEquals this asks whether the Todo is there and
     * differs, so a row without one fails that as well. To ask the other
     * question — no matching Todo, which a row with none satisfies — use
     * Without.Todo.
     */
    todo?: TodoFilterEquals | null;
};

/** Pattern conditions on TodoAttachment text fields. */
export type TodoAttachmentFilterLike = {
    /** What the attachment is, in a few words. */
    caption?: string | null;
    /**
     * Conditions on this row's Todo.
     *
     * A row with no Todo never matches, and that holds for the negative
     * operators too: under NotEquals this asks whether the Todo is there and
     * differs, so a row without one fails that as well. To ask the other
     * question — no matching Todo, which a row with none satisfies — use
     * Without.Todo.
     */
    todo?: TodoFilterLike | null;
};

/** Presence conditions on nullable TodoAttachment fields. */
export type TodoAttachmentFilterNull = {
    /** What the attachment is, in a few words. */
    caption?: boolean | null;
    /** Account that created this row, taken from the request's claims. */
    createdByAccountId?: boolean | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: boolean | null;
    /** Account that last updated this row, taken from the request's claims. */
    updatedByAccountId?: boolean | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: boolean | null;
    /** Account that soft-deleted this row, taken from the request's claims. */
    deletedByAccountId?: boolean | null;
    /**
     * Conditions on this row's Todo.
     *
     * A row with no Todo never matches, and that holds for the negative
     * operators too: under NotEquals this asks whether the Todo is there and
     * differs, so a row without one fails that as well. To ask the other
     * question — no matching Todo, which a row with none satisfies — use
     * Without.Todo.
     */
    todo?: TodoFilterNull | null;
};

/** Ordering conditions on TodoAttachment fields that can be compared. */
export type TodoAttachmentFilterRange = {
    /** Where this sits in the todo's list of attachments. */
    position?: number | null;
    /** When this row was created. Set automatically and never changes. */
    createdAt?: string | null;
    /** When this row was last updated. Set automatically on every update. */
    updatedAt?: string | null;
    /** When this row was soft-deleted. Null while the row is live. */
    deletedAt?: string | null;
    /**
     * Conditions on this row's Todo.
     *
     * A row with no Todo never matches, and that holds for the negative
     * operators too: under NotEquals this asks whether the Todo is there and
     * differs, so a row without one fails that as well. To ask the other
     * question — no matching Todo, which a row with none satisfies — use
     * Without.Todo.
     */
    todo?: TodoFilterRange | null;
};

/** Relations a TodoAttachment must have no matching row for. */
export type TodoAttachmentFilterWithout = {
    /**
     * Match rows with no Todo satisfying these conditions, including rows that
     * have none at all.
     *
     * This negates the existence of the related row rather than the comparison,
     * which is the whole difference from NotEquals.Todo: that one needs the
     * Todo to be there and to differ, so a row without one fails it and
     * satisfies this. An empty object asks for rows with no Todo at all.
     */
    todo?: TodoFilter | null;
};

