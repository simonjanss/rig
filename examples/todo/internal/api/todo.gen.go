// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"github.com/google/uuid"
	"github.com/simonjanss/rig/examples/todo/internal/model"
)

// A page of Todos.
type TodoListResponse struct {
	// The rows in this page.
	//
	// Pointers, because that is what the repository returns and copying every row
	// to change that would be work nobody asked for.
	Data []*model.Todo `json:"data"`
	// Where this page sits in the full result set.
	Pagination Pagination `json:"pagination"`
}

// Query parameters for Todo.List.
type TodoListQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// Query parameters for Todo.Search.
type TodoSearchQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// The request body for Todo.Search.
type TodoSearchBody struct {
	// Conditions rows must satisfy.
	Filter model.TodoFilter `json:"filter"`
}

// Query parameters for Todo.ListDeleted.
type TodoListDeletedQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// Path parameters for Todo.Delete.
type TodoDeletePath struct {
	// Identifier of the Todo.
	ID uuid.UUID `json:"id"`
}

// Path parameters for Todo.Get.
type TodoGetPath struct {
	// Identifier of the Todo.
	ID uuid.UUID `json:"id"`
}

// Path parameters for Todo.Update.
type TodoUpdatePath struct {
	// Identifier of the Todo.
	ID uuid.UUID `json:"id"`
}

// Path parameters for Todo.Complete.
type TodoCompletePath struct {
	// The task to complete.
	ID uuid.UUID `json:"id"`
}

// The request body for Todo.Complete.
type TodoCompleteBody struct {
	// What was done, appended to the task's notes.
	Note *string `json:"note,omitempty"`
}

// Path parameters for Todo.Restore.
type TodoRestorePath struct {
	// Identifier of the Todo.
	ID uuid.UUID `json:"id"`
}

// Path parameters for Todo.Revert.
type TodoRevertPath struct {
	// Identifier of the Todo.
	ID uuid.UUID `json:"id"`
}

// The request body for Todo.Revert.
type TodoRevertBody struct {
	// The version to put back, from the Todo's history.
	VersionID uuid.UUID `json:"versionId"`
}

// Path parameters for Todo.Versions.
type TodoVersionsPath struct {
	// Identifier of the Todo.
	ID uuid.UUID `json:"id"`
}
