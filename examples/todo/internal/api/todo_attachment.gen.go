// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package api

import (
	"github.com/google/uuid"
	"github.com/simonjanss/rig/examples/todo/internal/model"
)

// A page of TodoAttachments.
type TodoAttachmentListResponse struct {
	// The rows in this page.
	//
	// Pointers, because that is what the repository returns and copying every row
	// to change that would be work nobody asked for.
	Data []*model.TodoAttachment `json:"data"`
	// Where this page sits in the full result set.
	Pagination Pagination `json:"pagination"`
}

// Query parameters for TodoAttachment.List.
type TodoAttachmentListQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// Query parameters for TodoAttachment.Search.
type TodoAttachmentSearchQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// The request body for TodoAttachment.Search.
type TodoAttachmentSearchBody struct {
	// Conditions rows must satisfy.
	Filter model.TodoAttachmentFilter `json:"filter"`
}

// Query parameters for TodoAttachment.ListDeleted.
type TodoAttachmentListDeletedQuery struct {
	// Maximum rows to return.
	Limit int `json:"limit"`
	// Rows to skip before the first returned row.
	Offset int `json:"offset"`
}

// Path parameters for TodoAttachment.Delete.
type TodoAttachmentDeletePath struct {
	// Identifier of the TodoAttachment.
	ID uuid.UUID `json:"id"`
}

// Path parameters for TodoAttachment.Get.
type TodoAttachmentGetPath struct {
	// Identifier of the TodoAttachment.
	ID uuid.UUID `json:"id"`
}

// Path parameters for TodoAttachment.Update.
type TodoAttachmentUpdatePath struct {
	// Identifier of the TodoAttachment.
	ID uuid.UUID `json:"id"`
}

// Path parameters for TodoAttachment.Restore.
type TodoAttachmentRestorePath struct {
	// Identifier of the TodoAttachment.
	ID uuid.UUID `json:"id"`
}

// Path parameters for TodoAttachment.UploadAttachmentFile.
type TodoAttachmentUploadAttachmentFilePath struct {
	// Identifier of the TodoAttachment.
	ID uuid.UUID `json:"id"`
}

// Path parameters for TodoAttachment.DownloadAttachmentFile.
type TodoAttachmentDownloadAttachmentFilePath struct {
	// Identifier of the TodoAttachment.
	ID uuid.UUID `json:"id"`
	// Identifier of the file, from the row's attachment field.
	FileID uuid.UUID `json:"fileId"`
	// The file's own name. It has to match the stored one, and it is what the
	// browser offers to save the download as.
	Filename string `json:"filename"`
}
