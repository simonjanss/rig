// Code generated by rig. DO NOT EDIT.
//
// This file is rewritten on every run. Put changes in the service layer.

package model

import (
	"time"

	"github.com/google/uuid"
)

// Exact-match conditions on NotificationDelivery fields.
type NotificationDeliveryFilterEquals struct {
	// Unique identifier for this row.
	ID *uuid.UUID `json:"id,omitempty"`
	// The inbox line this is a copy of. The line is the truth and this is one way
	// it was repeated.
	RecipientID *uuid.UUID `json:"recipientId,omitempty"`
	// Who it is for, denormalized off the line so a claim can group by it without
	// a join.
	AccountID *uuid.UUID `json:"accountId,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// Where it is going. One row per line per channel, which is what the unique
	// index below enforces.
	Channel *NotificationChannel `json:"channel,omitempty"`
	// Copied from the notification, so the settings resolution needs no join
	// either.
	Kind *string `json:"kind,omitempty"`
	// What the setting said when this row was written. Immediate rows are sent on
	// their own; the rest are batched per account and channel.
	Digest *NotificationDigest `json:"digest,omitempty"`
	// Skipped means a setting refused it, which is different from Failed and worth
	// telling apart in a report.
	State *NotificationDeliveryState `json:"state,omitempty"`
	// When this is due. A delivery held outside somebody hours moves to the next
	// opening; a digested one moves to its window close.
	DeliverAt *time.Time `json:"deliverAt,omitempty"`
	// When a channel accepted it, which is not the same as it arriving.
	SentAt *time.Time `json:"sentAt,omitempty"`
	// What the channel said last time. Kept on a retry as well as on a failure, so
	// a pattern is visible before the cap is reached.
	FailedReason *string `json:"failedReason,omitempty"`
	// How many times this has been claimed. Past notifications.max_attempts it is
	// Failed and stops being claimed.
	Attempts *int `json:"attempts,omitempty"`
	// When a dispatcher took it. Past notifications.claim_ttl another one may,
	// which is what makes a crashed process recoverable.
	ClaimedAt *time.Time `json:"claimedAt,omitempty"`
	// Which process holds the lease. A uuid generated once per process, with the
	// hostname beside it in the log line, so a stuck lease traces to a pod rather
	// than to a mystery.
	ClaimedBy *uuid.UUID `json:"claimedBy,omitempty"`
}

// Ordering conditions on NotificationDelivery fields that can be compared.
type NotificationDeliveryFilterRange struct {
	// When this row was created. Set automatically and never changes.
	CreatedAt *time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
	// When this is due. A delivery held outside somebody hours moves to the next
	// opening; a digested one moves to its window close.
	DeliverAt *time.Time `json:"deliverAt,omitempty"`
	// When a channel accepted it, which is not the same as it arriving.
	SentAt *time.Time `json:"sentAt,omitempty"`
	// How many times this has been claimed. Past notifications.max_attempts it is
	// Failed and stops being claimed.
	Attempts *int `json:"attempts,omitempty"`
	// When a dispatcher took it. Past notifications.claim_ttl another one may,
	// which is what makes a crashed process recoverable.
	ClaimedAt *time.Time `json:"claimedAt,omitempty"`
}

// Set-membership conditions on NotificationDelivery fields.
type NotificationDeliveryFilterContains struct {
	// Unique identifier for this row.
	ID []uuid.UUID `json:"id,omitempty"`
	// The inbox line this is a copy of. The line is the truth and this is one way
	// it was repeated.
	RecipientID []uuid.UUID `json:"recipientId,omitempty"`
	// Who it is for, denormalized off the line so a claim can group by it without
	// a join.
	AccountID []uuid.UUID `json:"accountId,omitempty"`
	// When this row was created. Set automatically and never changes.
	CreatedAt []time.Time `json:"createdAt,omitempty"`
	// When this row was last updated. Set automatically on every update.
	UpdatedAt []time.Time `json:"updatedAt,omitempty"`
	// Where it is going. One row per line per channel, which is what the unique
	// index below enforces.
	Channel []NotificationChannel `json:"channel,omitempty"`
	// Copied from the notification, so the settings resolution needs no join
	// either.
	Kind []string `json:"kind,omitempty"`
	// What the setting said when this row was written. Immediate rows are sent on
	// their own; the rest are batched per account and channel.
	Digest []NotificationDigest `json:"digest,omitempty"`
	// Skipped means a setting refused it, which is different from Failed and worth
	// telling apart in a report.
	State []NotificationDeliveryState `json:"state,omitempty"`
	// When this is due. A delivery held outside somebody hours moves to the next
	// opening; a digested one moves to its window close.
	DeliverAt []time.Time `json:"deliverAt,omitempty"`
	// When a channel accepted it, which is not the same as it arriving.
	SentAt []time.Time `json:"sentAt,omitempty"`
	// What the channel said last time. Kept on a retry as well as on a failure, so
	// a pattern is visible before the cap is reached.
	FailedReason []string `json:"failedReason,omitempty"`
	// How many times this has been claimed. Past notifications.max_attempts it is
	// Failed and stops being claimed.
	Attempts []int `json:"attempts,omitempty"`
	// When a dispatcher took it. Past notifications.claim_ttl another one may,
	// which is what makes a crashed process recoverable.
	ClaimedAt []time.Time `json:"claimedAt,omitempty"`
	// Which process holds the lease. A uuid generated once per process, with the
	// hostname beside it in the log line, so a stuck lease traces to a pod rather
	// than to a mystery.
	ClaimedBy []uuid.UUID `json:"claimedBy,omitempty"`
}

// Pattern conditions on NotificationDelivery text fields.
type NotificationDeliveryFilterLike struct {
	// Copied from the notification, so the settings resolution needs no join
	// either.
	Kind *string `json:"kind,omitempty"`
	// What the channel said last time. Kept on a retry as well as on a failure, so
	// a pattern is visible before the cap is reached.
	FailedReason *string `json:"failedReason,omitempty"`
}

// Presence conditions on nullable NotificationDelivery fields.
type NotificationDeliveryFilterNull struct {
	// When this row was last updated. Set automatically on every update.
	UpdatedAt *bool `json:"updatedAt,omitempty"`
	// When a channel accepted it, which is not the same as it arriving.
	SentAt *bool `json:"sentAt,omitempty"`
	// What the channel said last time. Kept on a retry as well as on a failure, so
	// a pattern is visible before the cap is reached.
	FailedReason *bool `json:"failedReason,omitempty"`
	// When a dispatcher took it. Past notifications.claim_ttl another one may,
	// which is what makes a crashed process recoverable.
	ClaimedAt *bool `json:"claimedAt,omitempty"`
	// Which process holds the lease. A uuid generated once per process, with the
	// hostname beside it in the log line, so a stuck lease traces to a pod rather
	// than to a mystery.
	ClaimedBy *bool `json:"claimedBy,omitempty"`
}

// Conditions a NotificationDelivery must satisfy to match a search.
type NotificationDeliveryFilter struct {
	// Fields that must equal the given value.
	Equals *NotificationDeliveryFilterEquals `json:"equals,omitempty"`
	// Fields that must not equal the given value.
	NotEquals *NotificationDeliveryFilterEquals `json:"notEquals,omitempty"`
	// Fields that must be greater than the given value.
	GreaterThan *NotificationDeliveryFilterRange `json:"greaterThan,omitempty"`
	// Fields that must be less than the given value.
	SmallerThan *NotificationDeliveryFilterRange `json:"smallerThan,omitempty"`
	// Fields that must be greater than or equal to the given value.
	GreaterOrEqual *NotificationDeliveryFilterRange `json:"greaterOrEqual,omitempty"`
	// Fields that must be less than or equal to the given value.
	SmallerOrEqual *NotificationDeliveryFilterRange `json:"smallerOrEqual,omitempty"`
	// Fields that must be one of the given values.
	Contains *NotificationDeliveryFilterContains `json:"contains,omitempty"`
	// Fields that must not be one of the given values.
	NotContains *NotificationDeliveryFilterContains `json:"notContains,omitempty"`
	// Text fields that must match the given pattern.
	Like *NotificationDeliveryFilterLike `json:"like,omitempty"`
	// Text fields that must not match the given pattern.
	NotLike *NotificationDeliveryFilterLike `json:"notLike,omitempty"`
	// Fields that must be null.
	Null *NotificationDeliveryFilterNull `json:"null,omitempty"`
	// Fields that must not be null.
	NotNull *NotificationDeliveryFilterNull `json:"notNull,omitempty"`
	// Combine this filter's conditions with OR instead of AND.
	OrCondition bool `json:"orCondition,omitempty"`
	// Sub-filters combined with this one, so that AND and OR can be mixed to any
	// depth.
	NestedFilters []NotificationDeliveryFilter `json:"nestedFilters,omitempty"`
}

// NotificationDeliveryPage says which slice of a result to return, and in what
// order.
//
// The zero value is the first page at the repository's default size, in the
// table's configured order, which is what a caller who has not thought about
// it wants.
type NotificationDeliveryPage struct {
	Limit  int
	Offset int

	// OrderBy replaces the table's default ordering.
	OrderBy []NotificationDeliveryOrder
}

// NotificationDeliveryOrder is one clause of an ordering.
//
// Relation names the relation the column belongs to, and is empty for this
// table's own columns. The repository turns it into a left join, so a row
// whose related row is missing still appears — at one end of the order
// rather than not at all.
type NotificationDeliveryOrder struct {
	Relation string
	Column   string
	Desc     bool
}

// The orderings a NotificationDelivery supports.
var (
	NotificationDeliveryOrderIDAsc            = NotificationDeliveryOrder{Column: "id"}
	NotificationDeliveryOrderIDDesc           = NotificationDeliveryOrder{Column: "id", Desc: true}
	NotificationDeliveryOrderTenantIDAsc      = NotificationDeliveryOrder{Column: "tenant_id"}
	NotificationDeliveryOrderTenantIDDesc     = NotificationDeliveryOrder{Column: "tenant_id", Desc: true}
	NotificationDeliveryOrderRecipientIDAsc   = NotificationDeliveryOrder{Column: "recipient_id"}
	NotificationDeliveryOrderRecipientIDDesc  = NotificationDeliveryOrder{Column: "recipient_id", Desc: true}
	NotificationDeliveryOrderAccountIDAsc     = NotificationDeliveryOrder{Column: "account_id"}
	NotificationDeliveryOrderAccountIDDesc    = NotificationDeliveryOrder{Column: "account_id", Desc: true}
	NotificationDeliveryOrderCreatedAtAsc     = NotificationDeliveryOrder{Column: "created_at"}
	NotificationDeliveryOrderCreatedAtDesc    = NotificationDeliveryOrder{Column: "created_at", Desc: true}
	NotificationDeliveryOrderUpdatedAtAsc     = NotificationDeliveryOrder{Column: "updated_at"}
	NotificationDeliveryOrderUpdatedAtDesc    = NotificationDeliveryOrder{Column: "updated_at", Desc: true}
	NotificationDeliveryOrderChannelAsc       = NotificationDeliveryOrder{Column: "channel"}
	NotificationDeliveryOrderChannelDesc      = NotificationDeliveryOrder{Column: "channel", Desc: true}
	NotificationDeliveryOrderKindAsc          = NotificationDeliveryOrder{Column: "kind"}
	NotificationDeliveryOrderKindDesc         = NotificationDeliveryOrder{Column: "kind", Desc: true}
	NotificationDeliveryOrderDigestAsc        = NotificationDeliveryOrder{Column: "digest"}
	NotificationDeliveryOrderDigestDesc       = NotificationDeliveryOrder{Column: "digest", Desc: true}
	NotificationDeliveryOrderStateAsc         = NotificationDeliveryOrder{Column: "state"}
	NotificationDeliveryOrderStateDesc        = NotificationDeliveryOrder{Column: "state", Desc: true}
	NotificationDeliveryOrderDeliverAtAsc     = NotificationDeliveryOrder{Column: "deliver_at"}
	NotificationDeliveryOrderDeliverAtDesc    = NotificationDeliveryOrder{Column: "deliver_at", Desc: true}
	NotificationDeliveryOrderSentAtAsc        = NotificationDeliveryOrder{Column: "sent_at"}
	NotificationDeliveryOrderSentAtDesc       = NotificationDeliveryOrder{Column: "sent_at", Desc: true}
	NotificationDeliveryOrderFailedReasonAsc  = NotificationDeliveryOrder{Column: "failed_reason"}
	NotificationDeliveryOrderFailedReasonDesc = NotificationDeliveryOrder{Column: "failed_reason", Desc: true}
	NotificationDeliveryOrderAttemptsAsc      = NotificationDeliveryOrder{Column: "attempts"}
	NotificationDeliveryOrderAttemptsDesc     = NotificationDeliveryOrder{Column: "attempts", Desc: true}
	NotificationDeliveryOrderClaimedAtAsc     = NotificationDeliveryOrder{Column: "claimed_at"}
	NotificationDeliveryOrderClaimedAtDesc    = NotificationDeliveryOrder{Column: "claimed_at", Desc: true}
	NotificationDeliveryOrderClaimedByAsc     = NotificationDeliveryOrder{Column: "claimed_by"}
	NotificationDeliveryOrderClaimedByDesc    = NotificationDeliveryOrder{Column: "claimed_by", Desc: true}
)

// NewNotificationDeliveryFilterEquals builds an empty
// NotificationDeliveryFilterEquals to fill in.
func NewNotificationDeliveryFilterEquals() *NotificationDeliveryFilterEquals {
	return &NotificationDeliveryFilterEquals{}
}

// NewNotificationDeliveryFilterRange builds an empty
// NotificationDeliveryFilterRange to fill in.
func NewNotificationDeliveryFilterRange() *NotificationDeliveryFilterRange {
	return &NotificationDeliveryFilterRange{}
}

// NewNotificationDeliveryFilterContains builds an empty
// NotificationDeliveryFilterContains to fill in.
func NewNotificationDeliveryFilterContains() *NotificationDeliveryFilterContains {
	return &NotificationDeliveryFilterContains{}
}

// NewNotificationDeliveryFilterLike builds an empty
// NotificationDeliveryFilterLike to fill in.
func NewNotificationDeliveryFilterLike() *NotificationDeliveryFilterLike {
	return &NotificationDeliveryFilterLike{}
}

// NewNotificationDeliveryFilterNull builds an empty
// NotificationDeliveryFilterNull to fill in.
func NewNotificationDeliveryFilterNull() *NotificationDeliveryFilterNull {
	return &NotificationDeliveryFilterNull{}
}

// NewNotificationDeliveryFilter builds an empty filter, which matches every
// row the caller is allowed to see.
func NewNotificationDeliveryFilter() NotificationDeliveryFilter { return NotificationDeliveryFilter{} }
